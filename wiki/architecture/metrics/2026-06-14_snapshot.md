# 2026-06-14 Metrics Snapshot

METRICS-2026-06-14:
  Stage: 1 (carry-forward). No Stage-2-relevant event today — automated-only day; DECISION-054 Round 2 (M7/M8) still pending-dyad, unchanged.
  PRS triplets (network): 269 (carry-forward; extraction freeze continues; no tradition-content work today). OPEN-081 reconciliation still open.
  Cross-program connections: 90 (carry-forward; no new tradition cross-connections today).
  Cross-connections by type: carry-forward.
  Connection density: carry-forward.
  Approval rate (cumulative): UNCHANGED. 0 APPROVE / 0 DENY; pending proposal queue: 10 (carry from 06-12; review pass overdue).

  Assumptions:
    Total: 317 (+1 today: ASSUMPTION-317 staleness-clock/marking interaction [LOW-MED]). Prior max 316.
    Routed today: 1 QUEUED cycle 0 (2026-06-14).
    Type distribution (today): methodological = 317.
  Presumptions:
    Total: 348 (+2 today: PRESUMPTION-347 pinned-model-identifier durability [Medium-High]; 348 scheduled-task liveness self-announcing [Medium]). Prior max 346.
    Risk levels (today): Medium-High = 347; Medium = 348.
    Routed today: 2 QUEUED cycle 0 (2026-06-14).
  Self-awareness registry (total): 665 items (317 + 348); +3 today (1 + 2). Ratio 2:1 presumption:assumption — characteristic of an automated-only day: little new stated rationale, but operational failures expose unstated infrastructure premises. All 3 of today's items cluster on pipeline legibility/liveness (ASSUMPTION-317 marking legibility; 347/348 scheduled-task durability/observability), adjacent to the existing trace-vs-substance and queue-health families.

  Lit search queue:
    QUEUED this EOD batch (cycle 0: 2026-06-14): 3 new items (ASSUMPTION-317; PRESUMPTION-347, 348).
    Prior backlog: ~33 queued from before, 0 searched today (no 15-pipeline run today).
  Validated premises: 62 (carry-forward; cumulative through PREMISE-062; no INCORPORATE today).
  Decisions registry: 56 (no new today; DECISION-054 Round 2 still open).
  Open questions: 82 total (no new today). OPEN-082 (parser/linker a/b/c) now ~3 days open and load-bearing on BOTH Summa pipelines; OPEN-079/080/081 carry.

  Agents (total): 20 defined + watch/janitor/periodic (unchanged). 14a/14b ran on cadence (this run). 15a/15b/15c did NOT run today (no autonomous lit-pipeline run). Summa daily batch + QC sweep + commentary reviewer ran autonomously. Morning chat scrape FAILED (model error); evening cowork→chat sync produced summary but delivery FAILED (logged out).

  Operational flags (fail-loud):
    (1) Sync channel degraded ~3 days (06-12 → 06-14): morning scrape failed on a pinned-model error (claude-fable-5 unavailable → use Opus 4.8); evening delivery failed on claude.ai logout (same mode as 06-12); 06-13 summaries missing entirely. Surfaced PRESUMPTION-347 and 348. Needs Tom: check scheduled-task model config + re-sign into claude.ai in Chrome.
    (2) OPEN-082 now blocks BOTH Summa pipelines every run, not just QC marking — 65 bottom-frontmatter files reviewed-but-unmarkable behind the 06-11 auto-linker parser regression; the divergence grows daily. Highest-leverage decision outstanding. Both pipelines independently re-confirmed these are false positives (a re-confirmation of the trace-vs-substance presumption family — no new ID issued; logged here).
    (3) QC sweep marking policy made explicit and captured as ASSUMPTION-317 (synthesis halves deliberately left unmarked to avoid resetting the staleness clock).
    (4) Carries: extraction freeze; proposal queue 10 (review pass overdue); REVISE-111 (HIGH, pre-register falsifiers) outstanding; ANTHROPIC_API_KEY blocks Ch. IX/X ingest; stale index.lock blocks Physics Explorer push; AWAITING-REVIEW backlog 65.
  Notes: An automated-only Sunday with no attended design session. The day's epistemic yield is entirely self-referential infrastructure: the self-awareness pipeline's own scheduled siblings (sync, scrape) failed in ways that exposed two previously-invisible durability presumptions about the automation substrate itself. Trajectory: the binding human decision (OPEN-082) and the binding human action (re-auth + model-config fix) are both now ~3 days stale; tomorrow's leverage is unblocking those, not new content.

PROVENANCE:
  Origin: 14a
  Chain: [14a]
  Item type: METRICS (daily snapshot)
  Transform at each step:
    14a: Computed from registry state (assumptions/presumptions/decisions/open_questions/for_lit_search/validated-premises footer) + the 2026-06-14 evening cowork summary and session transcripts (evening cowork→chat sync; failed morning scrape). Carry-forwards retained where no tradition-content work occurred.
  Current status: COMPLETE
