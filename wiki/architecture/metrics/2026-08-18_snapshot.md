# METRICS-2026-08-18

METRICS-2026-08-18:
  Stage: 1 (network assembled and operating; no intra-tradition consensus mechanism live)
  PRS triplets (total): **636** [as stated by the daily run, unchanged from 08-17]
    - not re-measured this run; the 636 / 642 divergence between the daily run's network figure and the
      08-16 `prs_3d.html` measurement is unchanged and **no register records which build a triplet
      figure refers to** (PRESUMPTION-822, still open, fourth consecutive night)
  PRS triplets (per tradition): not re-derived (no agent is authorised to run in-house measurements —
    **15th ask**, continuing ASSUMPTION-1118)
  Cross-connections (total): **103** [measured — `cross_program_index.md` tops out at CROSS-103;
    **exact** against the evening summary's stated "90+ CROSS entries"]
  Cross-connections by type:
    Surface analogies / Structural homologies / Explanatory bridges / Paradigm-shift candidates:
      not separately tracked
    Findings: 69 · L2 signals: 1152 · pairs: 82 [as stated; L2 → 2026-08-10, stale_days=8, no WARN]
    **Caveat carried, and one counter-instance entered.** The standing caveat — that an unknown fraction
    of the 103 may be lexical rather than substantive — was restated tonight as a formal risk flag:
    **G4 (HIGH), "shared vocabulary read as structural identity, bearing on 90+ CROSS entries. Rated
    HIGH because the artefact rate is unmeasured, not because it's known to be large."** The proposed
    remedy is stated as cheap: "The recommended blind re-detection audit is cheap and would either clear
    or indict the corpus." **Against it, the first recorded counter-instance:** the tradition-agent run
    filed a cross-signal *as a disanalogy* rather than a convergence — "Monty's non-veridicality is
    selective and geometry-anchored, Hoffman's is total — **a disanalogy, not a convergence**" — and a
    second as sharpening an open question rather than bridging: "**sharpens open Q12, not a bridge.**"
    Two correct negative classifications on one day is not a rate, but it is the first evidence in the
    record that the detector is capable of the distinction G4 says is unmeasured.
  Connection density (connections per tradition): 103/14 = 7.36 [measured]
  Approval rate (cumulative): 301 approved / 302 decided = **99.7%** [measured — approved 301, denied 1,
    needs_review 1; all three unchanged since 08-16]
    - **pending 54** [measured — **exact** against the daily run's stated 54], up from 53 on 08-17
    - trajectory as stated by the daily run: **10 → 27 → 35 → 37 → 47 → 53 → 54**
    - disposition gap: **11 days** (last batch 2026-08-07). **[measured]** — and note that two live
      instruments disagreed about it today: Agent 16 reported "**Review gap is now 10 days**", the daily
      run "**11 days silent since 2026-08-07**". **Eleven is correct; Agent 16's figure is stale by one
      day and was reported as current.**
    - "**The queue is now deeper than any batch you've ever cleared. 54 pending vs. a 47-card drain on
      08-07.**" Still the only flag deteriorating monotonically. The 08-11 review-page ID fix is unpushed
      on **day seventeen**: "That delay is self-reinforcing."
    - `2026-08-18_review.html` **677,501 bytes** at 04:35 [measured — **exact** against the stated
      677,501 B]. Independently integrity-checked by Agent 16: "card IDs match the `proposal_id` of every
      file in `pending/` exactly (both difference sets empty)". **Auto-open failed for the ninth
      consecutive run**; seven review pages exist and six are unused.
  Assumptions: **1,157 total** | 0 tested this run | supported n/a | challenged n/a | novel n/a
    - 17 added tonight (ASSUMPTION-1142 … 1158)
    - **[measured] 1,157 unique ids, max 1158, offset exactly 1 — the long-known ASSUMPTION-459 gap,
      unchanged and stated here as known, not as a finding** (see the correction at ASSUMPTION-1123)
  Presumptions: **845 total** | 0 tested this run
    - 9 added tonight (PRESUMPTION-837 … 845): 3 Critical-risk, 5 High, 1 Medium
    - by type: epistemic 4 · methodological 2 · structural 2 · normative 1
    - **[measured] 845 unique ids, max 845 — no gap.** One duplicate line-start id, PRESUMPTION-295, is
      the known 2026-06-03 reinforcement note, not a collision.
  Validated premises: **max PREMISE-178** [measured]. Three minted tonight — **176** (reversibility, not
    review, is the control for irreversible operations), **177** (a recorded root cause is an untested
    inference that persists), **178** (existence + label checks are silent on citation polarity) —
    **exact** against the lit pipeline's stated mint. Two INCORPOREs were deliberately *not* minted:
    "PRESUMPTION-829 and 834 restate active PREMISE-100 and PREMISE-098, so their new evidence is
    attached by notice instead of a fourth entry." **Refuted: PREMISE-175 now exists.** Last night's
    register carried the line "PREMISE-175 REMAINS UNALLOCATED" and **no run today reported minting it.**
  Lit search dispositions (from tonight's 15a/15b/15c run):
    stated **5 INCORPORATE · 7 MONITOR · 11 REVISE** over 23 items
    - **[measured] `monitor_queue.md` holds MONITOR-535..541 — seven, exact.**
    - **[measured, refuted] `revision_flags.md` holds REVISE-347..358 — twelve, against the stated
      eleven.** 5 + 7 + 12 = 24, not 23. Both this and the PREMISE-175 discrepancy run in the direction
      of *more* register activity than any agent claimed, and **neither is detectable from any agent's
      own report.**
    - **REVISE-350 (Critical, reflexive)** is the cohort's and the night's largest result: "Kohli 2026
      finds nine models from seven families supply ~2 independent votes; correlation *rises* with
      capability … **This invalidates concordance-weighted confidence across the system — including this
      run's own 15a/15b concordance**, which is why every disposition cites sources rather than
      agreement." The pipeline changed method mid-run rather than filing and continuing.
    - **REVISE-352**, named the cheapest high-value action in the cohort: add a `WITHDRAWN` terminal
      state to `provenance_protocol.md`. "**This pipeline may not make that change itself under
      PREMISE-096.**" This is PRESUMPTION-833's remedy, raised 08-17, now formally flagged and still
      unexecutable by any agent.
    - **REVISE-355**: "FLAG-019's conclusion is right and its citation is wrong." Both search directions
      independently traced the "Fredrickson N=73 cohort" to "a pair of solitary fMRI loneliness studies
      sharing one cohort — no dyads, no shared affect, no community outcome." Verdict: "Keep the flag,
      re-ground it" on Páez et al. (2015).
    - self-audit: "An independent verifier caught **five 15a files missing the `Current status`
      provenance field** — repaired in-run, all 46 re-checked."
    - citation audit clean: "**20 sampled citations verified against Crossref, PubMed, arXiv and
      OpenAlex, zero fabrications.**"
    - **new backlog shape:** "10 items from last night's intake, plus **13 that had been sitting
      unsearched since an earlier intake — they live in a one-line-per-item format the queue's own parser
      misses**"; "**Three different parses of the queue file give three different backlog figures**";
      "**26 literature-intake items from 2026-07-21 remain unsearched.**"
  Lit search queue: **11 new items queued tonight**, all `[QUEUED]` with provenance headers
  Decisions: **DECISION-001..078, unchanged** [measured]. **Forty-fourth consecutive day with no new
    decision** — with the caveat entered at OPEN-158 that a contract *was* materially narrowed today by
    an agent, and that no register treats this as a decision.
  Open questions: **max OPEN-159** [measured], +3 tonight
  Intra-tradition consensus rate: N/A until Stage 2
  Cross-tradition survival rate: N/A until Stage 3
  Health ratio r: N/A until both rates available

## Fleet reliability

  Sessions today: **27** [boundary: every session above `local_8725efed`, the 08-17 self-awareness run]
  Attended sessions: **0** — **seventeenth consecutive unattended day; eighth consecutive day with zero
    identifiable attended sessions.** Every `[user]` turn across all 27 transcripts is a
    `<scheduled-task>` wrapper or the system string `[Request interrupted by user]`.
  **Mute runs: 4 / 27 = 14.8%** (08-17: 24.1%; 08-16: 11.4%). **The rate fell by a third and the mute
    set barely changed.** Three of the four were mute last night as well, and all four were:
    - `morning-system-health` — **third consecutive day.** Died on its second
      `request_cowork_directory` having said only "I'll run the daily system health check." Its own spec
      names the condition it could not evaluate: "A missing file, or a newest line that is not from
      today, **is itself a FAIL** — it means the 05:45 launchd agent did not run, so nothing is watching."
    - `openstory-agents-telemetry-refresh` — **fifth consecutive stall.** It reached further than last
      night: found the mount gone, routed via Desktop Commander, read the DB fresh, reported "Status
      file's last entry is 2026-08-13 — **four days missing**", then died at `start_process`. The gap is
      now **five days.**
    - `morning-project-status` — second consecutive day. Died at `start_process` with **zero assistant
      prose ever emitted**; the eyes-free voice summary was never composed.
    - `morning-walk-cowork-handoff` — cut after "**No self-sent walk notes found. The wiki paths are
      outside the connected folder — trying an alternate reader.**" No briefing written.
    **All four terminate on `[Request interrupted by user]` with no user present. Three of the four died
    at the same tool, `Desktop_Commander__start_process`; the fourth at `request_cowork_directory`.**
    Every one was reaching for host filesystem access it did not have (ASSUMPTION-1142,
    PRESUMPTION-838, OPEN-157).
  Partial-crash (not counted as mute): 1 — `summa-qc-sweep` (`local_cff5519a`) landed edits to Day 271
    and QC-log rows and then terminated on "**API Error: 500 Internal server error**", leaving the vault
    partially edited with no record of what completed. Counting it, 5/27 = 18.5%.
  Loud failures (completed, but the task failed): 2 — and **this is where the improvement came from.**
    - `metabolism-regen-daily` — met the **identical** mount absence as the four nulls, did **not** reach
      for the workaround, and completed: "**FAIL — the regen did not run. Nothing was regenerated.** …
      `EXIT_CODE=1` … **This is a mount-scope failure, not a script or database fault.**" It also
      corrected the standing cause — "**This is a regression in the environment, not a longstanding
      block** … Something narrowed the mount set since" — and refused to let an unreached gate be scored:
      "The freshness gate was never reached, so it **neither passed nor failed — it is simply
      unevaluated.**"
    - `c2a2-morning-chat-scrape` — "**The scrape failed — no Chat content was retrieved today.**"
      Extension "not connected" on both attempts *and* the claude.ai profile signed out: "`/recents`
      redirected to `/login?from=logout` … **I won't enter credentials in an automated run**". It wrote a
      dated failure record deliberately: "documents the failure explicitly rather than leaving a
      false-empty summary." **Third consecutive day, both directions.**
  Scheduler health: **79 OK / 1 WARN / 4 FAIL** (08-17: 78/1/5; 08-16: 77/1/6; 08-05 baseline: 78/4/0)
    - the register **again logged OK and FAIL for the same run** — `run_stall`: "OK
      c282-wiki-agent-daily-run: run of 2026-08-18 08:34Z completed"; `commit_check`: "FAIL
      c282-wiki-agent-daily-run: ran 2026-08-18 08:34Z (1.2h ago) and **committed nothing**". **Third
      consecutive night.**
    - FAIL: `com.c2a2.metabolism-publish` (exit 1, now **`runs = 1` — it fired and failed**, having been
      a WARN awaiting first proof last night), `com.tloughran.summa-vault-sync` (exit 1, runs = 4),
      `metabolism-regen-daily`, and the git-debris check
    - WARN: `execution-assistant` — enabled, no cron, never run
    - **git debris grew**: "**4 stale lock(s) (HEAD.lock, ORIG_HEAD.lock, index.lock, main.lock)**,
      2026-08-15 04:40 .. 2026-08-17 04:46 … also **248 stranded tmp_obj**" (08-17: 3 locks, 248
      tmp_obj). From inside the daily run, independently: "`.git/index.lock` is a **0-byte file stamped
      2026-08-17 04:46 — left by yesterday's run**", and "`commit_daily_run.sh` at 05:45 Mac-side has
      probably been failing on the same lock since."
    - **Fourth consecutive day with zero commits** — "newest commit matching '^C2A2 daily run' is
      2026-08-14 01:00Z." Phase 5 also failed ("mount refused rm and mv") and Phase 6 was blocked.
    - the scheduler's own root-cause claim — "The git debris is the root blocker" — is a causal
      inference its skill forbids ("a partial check reported as a full one is what broke this task in the
      first place"), and is filed here as a claim, not a measurement (PREMISE-177 applies).
  Budget compliance: **0 of ~14 substantive runs**, and for the first time the counters were compared.
    Six runs reported six different consecutive-breach counts for one condition — **third, fourth,
    fifth, sixth, seventh, eleventh** — each correct within its own lineage, none of them the fleet's
    number, which nothing computes (PRESUMPTION-839). Largest: the lit pipeline at "**roughly 25× the
    declared 30,000-token session budget**", filed inside the disposition of PRESUMPTION-764, "which is
    the item predicting exactly this." One breach was pure waste: "`report --max 99` **emitting a full
    25-line Python traceback per day — ~24 near-identical `FileNotFoundError` blocks that are pure
    noise.**" And the sharpest self-assessment in the record: "**Disclosure isn't compliance — on
    Vaughan's account it's what the drift looks like from inside.**" Stated flatly by one reviewer: "**Six
    pairs at this depth and the 30k session budget remain not simultaneously satisfiable.**"
  Channels: chat sync down in both directions for the **third** consecutive day. Gmail walk-notes channel
    empty and the fallback never reached. The evening summary was written but not delivered: "**Chat
    delivery failed** — Chrome isn't running, the Claude-in-Chrome extension MCP is not connected, and
    the profile was already signed out of claude.ai this morning." **Three independent channels for Tom's
    intent produced nothing, for the second consecutive day.**
  Tradition-level yield: **1 proposal** — PROP-2026-08-18-001 (Hawkins), pending 53 → 54. Its own run
    filed the weakness first: "**this is not a new source.** The underlying paper was already ingested
    twice, and the headline FLOPs figures are already verbatim in PRS-16." What justified it: "four
    empirical results in that paper **were never extracted**", and "**Monty's emergent shape bias is the
    Hawkins node's first *behavioral* contact with human cognition.**" Confidence deliberately held
    down: "I marked it Medium, not High — the explainer asserts the mirroring and cites no psychophysics
    benchmark." Hoffman returned zero and the run declined to dress it up: "**nothing passed the filter,
    and that is the honest result, not a search failure.**" **Thirteen of fourteen traditions were not
    swept at all** (ASSUMPTION-1147, OPEN-158).
  Vault verification: **307 synthesis files verified**, Days 001–307 contiguous, all paired; **35 drift
    items**, all in the known unpadded two-digit class, "zero PRS and zero tradition-wiki path failures";
    "**No synthesis or transcript file was modified.**"

## Reconciliations attempted this run

  Ten attempted; **eight exact, two refuted.**
  - `inbox/proposals/pending` **54** vs the daily run's stated 54 — **exact**
  - `2026-08-18_review.html` **677,501 bytes** vs stated 677,501 B — **exact**
  - approved **301** · denied **1** · needs_review **1**, all unchanged — **exact**
  - `monitor_queue.md` holds **MONITOR-535..541**, seven, vs stated seven — **exact**
  - `validated_premises.md` holds **PREMISE-176, 177, 178** vs stated three — **exact**
  - `deferred/watch_list.md` **452,560 bytes = 442.0 KB** vs stated "~442 KB" — **exact**
  - `cross_program_index.md` max **CROSS-103** vs stated "90+ CROSS entries" — **exact**
  - `assumptions.md` at intake **1,140 unique / max 1141 / offset 1** — **exact**, and stated as the
    known ASSUMPTION-459 gap rather than as a finding
  - **Refuted: `revision_flags.md` holds REVISE-347..358 — twelve — against the stated eleven.**
  - **Refuted: PREMISE-175 exists**, against last night's explicit "PREMISE-175 REMAINS UNALLOCATED" and
    no claim by any run today.
  **Two further instrument disagreements, neither refutable from disk:**
  - the review gap, reported as **10** by Agent 16 and **11** by the daily run within hours of each
    other. Eleven is correct by the 08-07 date both cite.
  - `metabolism/metabolism_data.json` is **dated 2026-08-13 21:00 on disk** against the scheduler's
    "generated 2026-08-12 09:59Z, **6.0 days ago**". **This is the same disagreement refuted last night,
    carried forward a full day by both instruments without either amending or acknowledging it.**
  **Recorded as unknown rather than as absent:** `~/Documents/Claude/Reports/` is not mounted here, so
  whether any host-side briefing landed today is unknown; and the four null runs wrote nothing this
  sandbox can see, so their partial work is unknown rather than absent — with one exception,
  `local_cff5519a`, **known** to have landed partial edits before dying, because it narrated them.

## Notes

  The night's central finding inverts last night's. **Mount absence did not kill a single run. The
  workaround for it killed four.** Three of the four nulls died at `Desktop_Commander__start_process`
  and the fourth at `request_cowork_directory`, each one reaching for host filesystem access after the
  sandbox mount came up empty — while `metabolism-regen-daily`, which met the identical absence and
  declined the substitution, is the only one of the five that produced anything. Last night's
  PRESUMPTION-834, "mount availability is the dividing line", was INCORPORATED by 15c today on the
  08-17 evidence. It is not withdrawn — the correlation was real — but tonight's data shows the mount
  was not the mechanism, and an item already promoted should not silently absorb a correction. That
  notice is filed to 15c in the intake (PRESUMPTION-838, OPEN-157). Sharper still: **the two runs that
  hit the same missing `~/Documents` reached opposite verdicts on whether the data was reachable** — one
  read the database through Desktop Commander and reported it fresh, the other declared it unreachable
  and never tried — with no awareness of each other anywhere in either transcript.

  The second theme is that corrections ran the dangerous way. **Four instruments retracted their own
  confident, specific findings within one day**, and one of those retractions is the only reason correct
  work survived: "Had that reading been carried forward, it would have licensed reversing the correct
  Day 268/269 repairs on the grounds their target doesn't exist. **One grep prevented it.**" A near-miss
  ran the same way on id padding — "I nearly broke a correct citation … here the *padded* form is what
  would produce a false 'no such id'" — and a live memory file written yesterday currently instructs a
  sweep to "**withdraw on sight**" ids the wiki holds, which "**would delete live citations**", with the
  finding run barred from repairing it. **Third consecutive day a run has found a false claim in a live
  memory file and been unable to fix it.** The lifecycle has five terminal states for a claim that fails
  verification, none for a withdrawn flag, and none at all for a retraction later shown to be wrong.
  REVISE-352 flags exactly this and cannot execute it (OPEN-159, PRESUMPTION-842).

  Against those, the largest epistemic result the lit pipeline has produced, and it is about this
  system's own evidence: **agreement among model-generated judgements is evidence about the model, not
  about the world.** Nine frontier models from seven families supply roughly two independent votes.
  15a/15b concordance is therefore not corroboration, the pipeline said so about itself, changed method
  mid-run to cite sources rather than agreement — **and then completed its cohort, minted three
  premises, and raised twelve revision flags.** Its own remedy, "your adjudication is the only
  decorrelated stream", routes to a gate that has been silent eleven days behind a 54-card queue
  (PRESUMPTION-845).

  What improved: the mute rate fell by a third, and it fell because two runs that would have died
  silently failed loud instead. What did not: **the mute set is not a rotating sample. It is the same
  three observation-tasks, on their third, fifth and second consecutive day, and it is counted nowhere
  but here.** Six runs reported six different consecutive-breach counters tonight, all correct, none of
  them the fleet's. **The measured reliability of this fleet remains an upper bound on its actual
  reliability, and the reason is now specific: every counter in the system is scoped to one lineage,
  and nothing aggregates them** (PRESUMPTION-839, OPEN-154).

  **This run's own breaches, surfaced per Rules 6 and 12.** It exceeded the 30,000-token session budget
  by roughly an order of magnitude, delegating 27 transcript reads to five subagents at ~230k subagent
  tokens. And **its own scheduled-task file gives the wiki path as `.../Projects/RC Karpathy Wiki
  Project/Wiki`, capital W, against the real lowercase `wiki`** — the same contract-path defect two
  other runs reported today and last night, present in this pipeline's own contract, surviving only
  because the host filesystem is case-insensitive. It is recorded here rather than repaired, because
  this pipeline may not amend its own task file.
