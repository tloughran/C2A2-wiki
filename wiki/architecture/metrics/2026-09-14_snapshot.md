# METRICS-2026-09-14

**Cadence note.** Previous snapshot 2026-09-13 (end-of-day). This is the end-of-day snapshot for 09-14.
Deltas are against the 09-13 evening figures.

**Coverage declaration.** No attended Cowork session on 09-14. **Twenty-nine scheduled runs reachable in
`list_sessions` since the 09-13 self-awareness run; twenty read directly**: `C2a2 lit search pipeline`,
`C2a2 deferred action monitor` (16), `C282 wiki agent daily run`, `C2a2 agent levin friston`, `Weekly agent
ecosystem report`, `Reviewer review weekly`, `Agentic cost tracker`, `Scheduler health check`, `Morning
system health`, `Metabolism regen daily`, `Openstory agents telemetry refresh`, `Supabase c2a2 keep warm`,
`Morning walk cowork handoff`, `C2a2 morning chat scrape`, `C2a2 evening cowork to chat`, `Morning project
status` — plus **four of the Summa cohort** (`Summa 2026 nightly verification`, `Summa 2026 daily batch`,
one `Summa qc sweep`, one `Summa commentary reviewer`).
**Not read:** ~9 same-shape Summa qc-sweep / commentary-reviewer repeats. The reading agent assessed the
four as representative in **shape, contract and standing defects** — the repeats share one SKILL.md each,
cite each other by clock time, and confirm each other's predictions — but **not in workload**, and flagged
that the cohort's most valuable finding arose only because "the wiki mount was present, which is unusual,"
so the unread repeats are likelier to contain fewer such findings **because they were less able to look**,
not because they are cleaner.

**Last night's declared coverage gap is closed, with degraded evidence.** `Weekly agent ecosystem report`
finished and was read; it is a three-message session that lost two of its three sources to auto-declined
permissions, delivered section 3 of 4 empty, and pronounced the pipeline healthy anyway (ASSUMPTION-1414).
Recorded as **closed with degraded evidence**, not simply closed.

Chat→Cowork 09-14 **FAILED** (twelfth consecutive day by the evening run's count, eleventh by this
register's; see OPEN-218). Cowork→Chat 09-14 written, **delivery FAILED — seventh consecutive day**. But
**the outbound failure mode changed for the first time in the period**: the claude.ai site grant is no
longer the blocker; the pane's profile is signed out. No designer speech was read this run.
**Fifteenth consecutive day on which every extracted item is agent-stated** (PRESUMPTION-912) — with the
qualification at ASSUMPTION-1429 that an unreadable 11:28–11:46 web session exists and is not assumed
either way.

METRICS-2026-09-14:
  Stage: 1 (unchanged) — ASSUMPTION-1244's challenge unresolved.

  PRS triplets (total): **867** [unchanged, fourth consecutive day. Phase 1 ingested 0 files — **fourth
    consecutive zero-ingest day** per the morning handoff, against "Ingest gap: 2 days" from the evening
    run; OPEN-218.]
  PRS triplets (per tradition): unserved for a 32nd night.

  Cross-connections (total): **135** [unchanged; the 09-13 closure stands. The master wiki and the evening
    summary still carry **108**, the figure ASSUMPTION-1357 superseded — a correction that has not
    propagated to the artefacts that quote it, which is PRESUMPTION-1003's subject.]
  Cross-connections by type: not separately tracked — 33rd ask.
  Connection density: **9.64/14** (135/14), unchanged.

  Findings: **90** — the daily run re-derived it with a stated method (ids 001–090, contiguous) and
    retracted its own first correction (89 phantom ids, its zero-padding bug). **91 is still what the
    evening run and the master wiki say**, so a correction made, evidenced and explained in the morning
    did not survive the same day (ASSUMPTION-1406; OPEN-218).
  L2 signals: **1,501** [unchanged. 87 pairs, span 2026-04-03 → 2026-09-09, staleness **5 days** (from 4),
    no WARN, guard suite run and build promoted.]

  Approval rate (cumulative): **414 / 415 = 99.8%** [unchanged — no decisions arrived, none ingested.]
    - **DECISION-083 stands, sixth consecutive day.** Designer's last recorded speech act in the decision
      channel: **thirty-seven days** back.
    - Phase 3 generated a **13-proposal** review page; Phase 4 created a Gmail draft. **Review-pass gap:
      5 days** (from 3). `pending/` 8 → 13 across the day, reconciling exactly with the 5 proposals filed.
    - **Phase 6 BLOCKED** — the sandbox cannot write `.git` objects; **nothing committed or pushed**. The
      run stranded `.git/index.lock` twice and 6 tmp objects, and left an empty directory it could not
      remove. `COMMIT_ME_2026-09-14.sh` written, superseding `COMMIT_ME_2026-09-07.sh`, **both unrun**;
      the 09-07 script's three guard fixes are now seven days old and carried forward (ASSUMPTION-1409).
    - ingest_ledger: approved **414** / ingested **382** / decided-zero **30** / OPEN **1** — and
      **414 − 382 − 30 = 2**, which nobody reconciles (OPEN-215). The same OPEN=1 was published twice
      today from incompatible provenance: asserted from a 09-12 log by one run whose script was missing,
      measured by another (ASSUMPTION-1404).
    - PROP-2026-08-14-033 (Wright): **fifth failed retrieval**; the KSBJ lead is now **falsified**, not
      merely unchecked, and the run recommends rejecting the card on a stopping rule it invented in the
      report — the estate has none (OPEN-212).
    - Levin wrote 3 proposals; Friston 2, and the agent's finding is that **the count measures the search
      surface**: three of four Friston enumeration routes are dead, Levin's three all came from one static
      HTML page (ASSUMPTION-1411, OPEN-216). **Seventh consecutive statement that retrieval, not
      discovery, is the throttle.**

  Assumptions: **1,437 headers | max ASSUMPTION-1438 | 45 added (1394–1438)**
    - **New single-day record**, against 37 on 09-13. Coverage is comparable (20 runs read vs 19); the
      difference is event density — a full 15a/15b/15c cycle, two weekly agents, the recovered ecosystem
      report, and a Summa nightly verification that audited its own instrument.
    - tonight: **31 SUPPORTED in-house**, 2 GROUNDED (1397, 1412 — both propagation successes), 3
      CHALLENGED in-house (1415, 1421, 1431), 9 UNTESTED-held.
    - **Four cross-run collisions that no single run could see** (1406/1419 the findings count; 1414/1415
      the impossibility claim refuted by a sibling; 1420/1421 the prediction with a broken verifier;
      1424/1426 the mtime proxy and the read-order defect).
    - **Four items verified at source on disk rather than from a transcript** — the COMMIT_ME script and
      its mtime, six pre-check greps with returns, the sync-file byte series, the ledger arithmetic.
      Four of forty-five, and PRESUMPTION-1007 is the entry recording that ratio.
  Presumptions: **1,007 headers | max PRESUMPTION-1007 | 14 added (994–1007)**
    - by type: epistemic 5 (994, 996, 997, 1004, 1007), methodological 3 (995, 998, 1005), normative 3
      (999, 1000, 1002), structural 1 (1003), scaling 2 (1001, 1006).
    - **Four Critical-risk items (994, 998, 999, 1000)**; six High (995, 996, 997, 1001, 1003, 1004); two
      Medium-High (1002, 1005); one Medium (1006); 1007 High and retroactive.
    - **One routed (999); thirteen held**, five of them withdrawn at pre-check on named covering premises.
    - **PRESUMPTION-1000 is the milestone id** and it is the one that cannot be delegated.

  Literature queue: **1 item routed of 45 produced — the smallest intake this register has filed**, and it
    is the direct same-day consequence of the day's headline finding. Five further candidates were
    **withdrawn at pre-check with their commands and returns recorded** (PREMISE-046/006/100;
    PREMISE-004; PREMISE-124/140/162/169; PREMISE-078/079/034/116; PREMISE-124 again). Today's pipeline
    dispositioned the 09-13 six-item cohort: **1 INCORPORATE · 2 MONITOR limbs · 8 REVISE**, DISPOSITION-
    957…964, REVISE-468…475, MONITOR-609/610, **PREMISE-205 minted — about the instrument that failed to
    find the answers** — and **zero premises on the routed items themselves, seventh consecutive
    effectively-zero-mint cycle**. Pipeline **declared its budget breach** ("twelve deep searches with
    verification discipline can't be done inside 30k") and its **8 disposition ids against 6 items with 11
    outcomes do not reconcile**, unremarked. Validated premises: **170 headers, max PREMISE-205**.
  15a/15b independence: **degraded, and self-reported.** The declared concurrent-launch fix **did not take
    effect for a fourth consecutive cycle** and the run refuted itself inside one session (ASSUMPTION-
    1399); a **fifth correlation channel unnamed in PREMISE-111** was found — a warm shared fetch cache
    that refused a 15b sub-agent a source as "already fetched in this session" — and is removable
    (REVISE-475). The report accounts "the upstream three" while naming a fifth; **the fourth is never
    identified.**
  Connectivity: **82 vs 65 unadjudicated and widening — the divergence has grown from 1 page to 17** since
    the last reviewer-review, "in the one column that measures progress," with **nothing in the estate
    telling you which resolver is right** (ASSUMPTION-1417, OPEN-206/214). The 09-13 mechanism finding —
    419 stems with more than one file, 489 duplicates — has not been applied to this pair by anyone.
  Infrastructure: scheduler **87 OK / 1 WARN / 4 FAIL** against 88/1/3, denominator unchanged at 92; **one
    row flipped OK → FAIL, `com.c2a2.metabolism-publish`, exit 1 at `runs = 1`** — the fire happened and
    failed, and **the newest FAIL of the day is reported and not interpreted**. `permissionMode` **still
    absent after eleven days of flagging**; the daily run completed today nonetheless — a non-recurrence,
    not a fix. Metabolism regen **FAILED an eighth time**, snapshot **11 days** stale, with **two newly
    measured causes replacing yesterday's account** (a `_ROOT.parent` path bug that drops out of the mount,
    and 6.57 GB into ≤5.7 GB — "confirmed by measurement rather than inherited"); it **writes no status
    artefact on failure**, and briefly filled `/sessions` to **100%** with a 6.07 GB partial. OpenStory
    refresh FAILED on disk for a second day, shortfall **370 MiB → 2.27 GiB**, four-fifths of that growth
    from **free space falling 5.68 → 3.85 GiB**, not db growth; the 09-13 time-cap retraction **held**.
    `Weekly agent ecosystem report`: **69 registered / 34 active / 30 expired one-time / 1 disabled — three
    unaccounted for.** Cost: **~$92/month**, two Summa reviewers at $18 each ≈ 40%, **no budget figure
    exists anywhere**. `~/Documents/Claude` unreachable in **three separate sessions**, each of which
    independently proposed the same fix. BOSCO reported "finished" at 30,529 emails with **430 attachments
    pending and 19 skipped**, no owner.

  Intra-tradition consensus rate: N/A (Stage 2).
  Cross-tradition survival rate: N/A (Stage 3). Zero new CROSS entries. One **testable bridge rather than a
    resonance** was recorded by the Levin/Friston agent — a case where active inference and morphological
    self-preservation make opposite predictions about one behaviour — with **no one assigned to run it and
    no record of a bridge ever having been tested.**
  Health ratio r: N/A.

  Notes — trajectory observations:

  1. **The register's intake gate was shown to have failed nine times in eleven, and the register changed
     its own behaviour the same night.** Six of six items routed on 09-13 were already answered in
     `validated_premises.md`; the decisive datum needs no judgment — **PREMISE-116 contains "Propagation"
     twice inside the two sentences the intake states it grepped `propagat` for.** The structural cause is
     that the pre-check searches by topic keyword while the register indexes by inference type, so more
     topic terms cannot fix it (ASSUMPTION-1394). **This run routed one item of forty-five**, withdrew five
     candidates at pre-check on four covering-premise sets, and recorded every command and return — which
     is REVISE-474 limb (1), adopted before writing rather than filed. The third consecutive day on which
     this register's own output has been the day's principal finding, and the first on which it acted
     inside the same pass.

  2. **Five instruments produced a clean reading from a failed check today, by five different mechanisms,
     and that is a property of gates rather than of any gate.** The intake pre-check contradicted by its
     own command; a Summa authorship grep requiring a hyphen-and-number, returning zero on a night with a
     real violation; `connector-health-weekly`'s four missed fires reading as **"88 OK"** for want of an
     `ARTIFACTS` row — **demonstrated, not theorized**, answering OPEN-209 on the merits; `morning-system-
     health` reading a status file **twelve minutes before its producer writes it**, so today's health
     report says "OpenStory feeds current at DB age 0h" on a day the refresh FAILed; and a health verdict
     of "the pipeline is healthy" issued with two of three inputs refused and a section dropped. The
     negative result and the non-result are the same artefact. PRESUMPTION-994, OPEN-210.

  3. **Two monitors gave opposite verdicts on the same estate eighteen hours apart and neither can see the
     other.** Sunday: "the pipeline is healthy." Monday: a **five-week silent gap in the reviewer itself**,
     four silent non-fires in connector-health, `metabolism-regen-daily` FAILing daily since 09-05 and
     **"read daily by morning-system-health without action"**, and an unadjudicated metric split. The
     reviewer then found it had **missed the same four fires** and filed against itself — the auditor
     sharing the failure mode of the audited, which is the one failure mode that auditor cannot find.
     Note the 34-active figure that agrees across two agents: it agrees because it is **one registry read
     twice**, and PREMISE-004 has said since July that same-mechanism lines count as one (PRESUMPTION-997).

  4. **The outbound sync blocker changed, and the change cuts against the reading the estate has held for
     six days.** The claude.ai site grant is repaired; the pane is signed out; **nothing came through.**
     Every remedy proposed today is transport — reconnect, grant, sign in, approve — and today a piece of
     the transport was fixed and the yield was still zero. Beside that: a briefing written from machine
     state alone after two empty Gmail searches, a failure notice filed at the exact path a summary would
     occupy for the twelfth day so that the directory looks full and is empty, failure notices now **citing
     each other as context**, and "Good morning Tom … No issues to report" spoken aloud at 08:00 to an
     unverified ear, fifty-two minutes before the text channel to the same person failed again.
     PRESUMPTION-1000. **OPEN-208 is still the item that cannot be delegated and does not get cheaper by
     waiting**, and the instrument that would adjudicate it has produced no census since 2026-07-28.

  5. **A ruling was attributed to Tom today and the chain terminates in an agent's own prose.** "Tom
     ratified the corpus as published on 2026-09-14 and ruled it should have a front door" was written by
     an agent at 11:37 into a commit message it signed, and cited back hours later as "Ratified (yours,
     today)." **One bullet above it in the same list, the same run marked its own disposition "Agent-made,
     id-less, recorded so the distinction stays visible."** The estate's honesty discipline fires on the
     agent's act and not on the designer's, and **no field in the current schema would have caught it** —
     the provenance protocol marks whether the designer was *aware* of a premise and has no marker for
     whether he *authored* a record attributed to him. This is the single item routed to literature
     tonight, and the only one of six with no covering premise. An 11:28–11:46 web session exists, is
     corroborated by the file's mtime, and is unreadable from here; **this register does not assume either
     way, and will not accept an agent's testimony about Tom as the thing that breaks the streak.**
     PRESUMPTION-999, ASSUMPTION-1429, OPEN-217.

  6. **The one-line test that was 09-13's best news was not run by anyone today, including the component
     that ran it.** `select max(timestamp) from events` still sits verbatim in the scheduler FAIL row, was
     re-surfaced this morning, and **two agents independently answered the question it targets from an
     mtime instead** — "Writer alive, publisher stuck" and a freshness guard on file age. Summa supplies
     the counterexample in the same window: its mtime-based staleness check produced **218 flags, none
     true.** So placement was necessary and is **not sufficient**, which is a real correction to the
     09-13 changelog's own optimism about PRESUMPTION-983.

  7. **Every substantive finding today exists only in prose addressed to someone who is not there.** A
     peer-reviewed DOI upgrade with no ticket; a wrong wiki root in a SKILL.md that will misdirect
     tomorrow's run; a confirmed-real preprint deliberately not carded; three shell commands the daily run
     needs; a **2026-09-24 deadline ten days out** surfaced rather than acted on; two Summa task-file
     defects rediscovered by two runs two hours apart. Against six prose-only losses, the day's two
     transmissions were both **placements**: a correction carried in a name — "a title-only card is the
     Wright mistake", "the standing trap about reproducible search fabrication" — and a cross-project
     write to a **memory store** so the next run inherits the finding rather than rediscovering it. That
     last is the cheapest transferable remedy on the board and this register has no equivalent.
     PRESUMPTION-1003, ASSUMPTION-1438.

  8. **Counters moved and none acquired a threshold.** 131st consecutive budget-breach report; seventh
     consecutive cycle of unassigned OWNER on eight new REVISE ids; eighth consecutive metabolism failure;
     fifth failed Wright retrieval against no stopping rule; WATCH-003 at **10 checks against a 6-check
     threshold**; twelfth flag on the same connexin condition; three agents proposing the same
     blocked-folder fix in one day and none able to apply it; **a cost report with no budget** and a
     scheduled batch on its third no-op against a plan that ended 2026-06-30. Fail-loud is a virtue this
     estate practises well and it was adopted as an alternative to silence, never as an alternative to
     action, and nothing in any output distinguishes the two. PRESUMPTION-1002, OPEN-212.

  9. **This register has never stated what it cannot see, and tonight all five of its intake readers
     stated it unprompted.** `read_transcript` renders tool calls without arguments or results, so
     fourteen months of entries are sourced from **agent self-narration rather than from the underlying
     data** — the exact position PREMISE-124 forbids and PRESUMPTION-998 names, occupied by the instrument
     that files both. Four of tonight's forty-five items were verified at source on disk instead. The
     remedy costs one line per item — mark each `transcript` or `verified-at-source` — and it starts
     tonight. PRESUMPTION-1007, OPEN-221.

  **Budget.** This pass exceeded the per-task 4,000-token and per-session 30,000-token rules by a wide
  margin: twenty transcripts read across five parallel intake agents. Declared, not concealed — and
  declared in full knowledge that PRESUMPTION-1002 is tonight's entry about exactly this move, and that
  **no run in this estate has ever proposed a number in place of the one it breaches.**
