# METRICS-2026-09-19

**Cadence note.** Previous snapshot 2026-09-18 (one day). This is the end-of-day snapshot for 09-19;
deltas are against the 09-18 evening figures. Run began ~23:30 EDT.

**Coverage declaration.** No interactive Cowork session on 09-19 — **twentieth consecutive day**
(ASSUMPTION-1554). **Nine sessions read first-hand** by this pass: `C2a2 evening cowork to chat`,
`C282 wiki agent daily run`, `Scheduler health check`, `Morning system health`, `Openstory agents
telemetry refresh`, `Metabolism regen daily`, `C2a2 morning chat scrape`, `Morning project status`,
`C2a2 agent wolfram`, `C2a2 deferred action monitor` — plus a **live probe** of `C2a2 lit search
pipeline` and a **full `find -newermt` mtime sweep of the mounted wiki**. **Not read:** the ~10
same-shape Summa qc-sweep / commentary-reviewer repeats and the Supabase keep-warm.

**The daily run left a transcript today** — the first in four days. That closes ASSUMPTION-1521's
coverage gap for tonight: the estate's largest daily change producer was read first-hand rather than
through the evening sync. The scheduler's run-stall FAIL row nonetheless still reports "newest transcript
starts 2026-09-15", so the row and the transcript disagree; the row was not re-derived tonight.

**First-hand / second-hand ratio (PRESUMPTION-1041, second measurement).** Of twenty-eight assumptions,
**~24 first-hand** (read in the originating transcript or measured at the mount) and **~4 second-hand**
(reaching this pass only through the evening sync). Sharply better than 09-18's ~11/~7, and for one
reason: the daily run wrote a transcript. **Caveat added tonight (PRESUMPTION-1049):** first-hand counts
hands, not roots. The Rohr gap is stated by two runs but descends from one reading of one archive index,
so "two independent statements" overstates it. A root-source count is the next refinement.

Chat→Cowork **FAILED** and Cowork→Chat **delivery FAILED**, both blocked by a **deliberate** claude.ai
logout (`from=logout&reauth=1`, not an expiry — ASSUMPTION-1547). **Seventeenth consecutive day both
directions dead.** No designer speech in nine of nine — **twentieth consecutive day** (PRESUMPTION-912).

METRICS-2026-09-19:
  Stage: 1 (unchanged) — ASSUMPTION-1244's challenge unresolved.

  PRS triplets (total): **867** [unchanged, **ninth** consecutive day; verified at source:
    `cat traditions/*/prs_triplets.md | grep -cE '^PRS-[0-9]+:'` → 867. Phase 1 ingested 0 —
    **ninth consecutive zero-ingest day** (ASSUMPTION-1549).]
  PRS triplets (per tradition): unchanged; not re-served as the total is unchanged and nothing ingested.

  Cross-connections (total): **135** [verified at source with an **anchored** pattern:
    `grep -oE 'CROSS-[0-9]{3}' master/cross_program_index.md | sort -u | wc -l` → 135. The unanchored
    form overcounts by one (ASSUMPTION-1548).]
  Cross-connections by type: not separately tracked — **38th ask**.
  Connection density: **9.64/14** (135/14), unchanged.
  Conditional, not entered: **CROSS-017 / CROSS-024** — Wolfram hypergraph ↔ Arkani-Hamed positive
    geometry, via a proposed "Wolfram Institute Pure Math Project". Held CONDITIONAL until the 09-18
    recording is heard (ASSUMPTION-1546). This is the correct handling and is recorded as such.

  Findings: **90** [unchanged; not re-counted tonight.]
  L2 signals: **1,501** [unchanged; 87 pairs; coverage gate **PASS 379/379**; staleness **10 days**
    (from 9), no WARN — threshold 21. Agent-stated via the daily run, read first-hand.]

  Approval rate (cumulative): **414 / 415 = 99.8%** [unchanged — no decisions arrived.]
    - **DECISION-083 stands, 23 days** (attended, 08-27). `decisions.md` unmodified by this pass.
    - `pending/` **24** (+3), `approved/` **414** — both verified at source. Three cards filed today:
      PROP-2026-09-19-001 (Wolfram, return AMA / Pure Math Project), -002 and -003 (Rohr, from the CAC
      week-37 gap). Review page `review/2026-09-19_review.html` built, **265,580 bytes**, 24 cards
      (verified at source, 04:37 EDT); auto-open failed — no macOS `open` in the sandbox, Chrome not
      connected. Two old review pages retired to `_superseded/`.
    - Review log: 6,369,414 bytes; cards **479** (+3); address-clean.
    - ingest_ledger: **414 / 382 / 30 / OPEN 1** — transcribed, not re-measured (ASSUMPTION-1404
      continued, ~7 weeks).
    - **Queue age: ten days, 24 cards, no decision email since 09-09.** This remains the single
      human-gated unblock.
    - Wright PROP-2026-08-14-033: **eighth attempt — and the first that produced new information**
      (ASSUMPTION-1542). Attempt count disputed tonight, seventh vs eighth (ASSUMPTION-1543).
    - Mark-as-read: **not re-attempted** — a run retired its own futile retry (ASSUMPTION-1550). First
      instance of this in the register.
    - Leakage count **holds at fifteen**. Agent 16: 0 due / 0 run / 0 resolved / 0 added; WATCH-003 next
      09-22; first Channel 2/3 hit outside `deferred/`+`agents/`, adjudicated a non-deferral
      (ASSUMPTION-1552). **Ruling deadline 2026-09-24 — five days.**

  Assumptions: **1,553 headers | max ASSUMPTION-1554 | 28 added (1527–1554)**.
    Tested: 0 returned from 15a/15b this pass. Supported 9 · Challenged 3 · Reconciled 1 · Untested 15.
    **Three corrections to claims made today or yesterday** (1528, 1532, 1536); **one consolidation** of
    two failures previously carried as independent into a single cause (1531); **two positives** (1542,
    1550).
  Presumptions: **1,055 headers | max PRESUMPTION-1051 | 9 added (1043–1051)**.
    epistemic 3 · structural 2 · methodological 2 · normative 2. **No Critical-risk item — fifth
    consecutive night.** High 3 (1043, 1044, 1048). **Four self-referential** (1045, 1046, 1050, 1051).
  Open questions: **max OPEN-244 | 4 added (241–244)**. Three are commands; **OPEN-244 needs Tom** and is
    the first architectural question this pass has raised in some weeks.
  Lit-search queue: **3 routed tonight** (PRESUMPTION-1043, 1047, 1048) — highest in a fortnight; 0
    assumptions routed, with a stated reason. Raw `QUEUED` line count 2,598, reported as a line count
    with its caveat, not as an item count (PRESUMPTION-1050).

  Intra-tradition consensus rate: N/A (Stage 2).
  Cross-tradition survival rate: N/A (Stage 3).
  Health ratio r: N/A.

  Infrastructure:
    Scheduler: **87 OK / 3 WARN / 5 FAIL** (09-18: 88/3/4).
    Voice-shell suite: **RED**, 364 rows / 353 passed / **11 failed** on `main@f9e2f83` (09-18: 363/355/8
      on `c0eda6e`). Reported twice in one report with two exit codes, 1 and 3 (ASSUMPTION-1529).
      Whether this is decay or a changed suite is **OPEN-241** — the row count moved too.
    OpenStory: **ingest alive** (db mtime 04:39 EDT), **regen dead**. `metabolism_data.json` 16 days
      stale; publish gate correctly red and unread for two weeks.
    **Common cause, newly identified:** OpenStory telemetry refresh and metabolism regen both died on a
      **6.94 GB database against a ≤5.9 GB scratch ceiling** (ASSUMPTION-1531). Two FAIL rows, one wall,
      neither run naming the other. → PRESUMPTION-1043, PRESUMPTION-1044, OPEN-244.
    Unreported debt found incidentally: the **2026-09-03 metabolism publish push was rejected
      non-fast-forward and that commit is still local** — 16 days, surfaced by no daily instrument
      (ASSUMPTION-1535).
    `permissionMode` absent on the daily run: **16 days**.
    git debris: 6 stranded `tmp_obj` files, 09-15 22:00 .. 09-16 22:00 — **the same clock position as the
      restamp window**, which is tonight's first evidence pointing at git rather than rsync.
    Restamp: **one window tonight, not two** — 134 files between 22:00:02.32 and 22:00:04.94 EDT,
      including a closed day's changelog, a closed day's metrics snapshot and ~70 Summa synthesis files
      (ASSUMPTION-1538, 1539). **Fifth day observed; first day the windows merged.** → OPEN-242, which
      now carries a one-line hash test.
    watch_list.md: **707,191 bytes / 5,942 lines** (verified at source). **Fifteenth** consecutive
      archival-split recommendation.

  Notes:
    1. **Tonight's principal finding is a consolidation, not a discovery.** Two failures the estate has
       carried separately for two weeks are one failure. The instruments that reported them were each
       correct and individually careful; what none of them could see is that they were looking at the
       same wall. That is a property of a per-task failure taxonomy, not of any run.
    2. **Two of today's runs did something the register should record as good practice**, because the
       register mostly records decay: the Wright card's eighth attempt failed *differently* and thereby
       produced a mechanism rather than an eighth null, and the daily run stopped re-attempting an
       approval-gated call it had learned could not succeed unattended. Both are the shape of behaviour
       the fail-loud debt items (PRESUMPTION-1037) have been waiting for.
    3. **The lit-search pipeline is alive** — refuting yesterday's inference (ASSUMPTION-1536) — and has
       delivered nothing for four days. The narrower claim is the worse one.
    4. **Trajectory.** Ninth zero-ingest day, tenth day of an undecided queue, twentieth day without
       designer speech, seventeenth with both sync directions dead. Register intake +28/+9/+4 against a
       throughput of 0 decisions and 0 lit-search dispositions. PRESUMPTION-1042's arithmetic is
       unchanged and PRESUMPTION-1051 now gives it a second instance in a second rule.

*Budget note (Rule 6): this pass exceeded the 4,000-token per-task budget by roughly an order of
magnitude — ten transcripts, a live session probe, a full mount-wide mtime sweep, five registers and four
re-derived counts. Surfaced, not hidden; and see PRESUMPTION-1051, which is about exactly this sentence
appearing every night. The cheap version of tonight is: read the evening sync only, and accept that the
common-cause finding (1531), the restamp merge (1538), the live-pipeline correction (1536) and both
positives (1542, 1550) would not have been found — the evening sync reports none of them.*
