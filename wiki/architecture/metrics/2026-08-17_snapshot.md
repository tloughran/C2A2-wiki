# METRICS-2026-08-17

METRICS-2026-08-17:
  Stage: 1 (network assembled and operating; no intra-tradition consensus mechanism live)
  PRS triplets (total): **636** [as stated by the daily run]
    - 642 was measured directly in `prs_3d.html` on 08-16 against the connectome's stated 642; tonight's
      636 is the daily run's figure for the network state and was **not** re-measured this run
    - the two figures refer to different builds and **no register records which build a triplet figure
      refers to** (PRESUMPTION-822, still open, third consecutive night)
  PRS triplets (per tradition): not re-derived this run (no agent is authorised to run in-house
    measurements — 14th ask, continuing ASSUMPTION-1118)
  Cross-connections (total): 103 [as stated, unchanged from 08-16]
  Cross-connections by type:
    Surface analogies / Structural homologies / Explanatory bridges / Paradigm-shift candidates:
      not separately tracked
    Findings: 69 · L2 signals: 1152 [as stated]
    **Caveat carried and amended.** The 08-16 caveat — that an unknown fraction of the 103 may be
    lexical rather than substantive — is now itself under challenge from two directions. 15c found
    ASSUMPTION-1116 **half-unattested** ("The Thomist conjunct was never checked"; no occurrence of
    "analogia", "univoc" or "Scotus" anywhere in `traditions/stump/`), and found PRESUMPTION-827 to be
    "a **register contradiction, not a gap**" against PREMISE-042/043, which hold the *opposite* error
    profile at High confidence. **A second and independent caveat is entered tonight:** four ids were
    cited today for claims their own bodies argue against, so a match can be substantive and still be
    disagreement (ASSUMPTION-1131, PRESUMPTION-831, OPEN-156). The schema has no field for either
    distinction.
  Connection density (connections per tradition): 103/14 = 7.36 [as stated]
  Approval rate (cumulative): 301 approved / 302 decided = **99.7%** [measured — approved 301, denied 1,
    needs_review 1; all three unchanged since 08-16]
    - **pending 53** [measured — **exact** against the daily run's stated 53], up from 47 on 08-16
    - the chain 47 → 51 → 53 is arithmetically consistent (4 tradition-agent, 2 daily-run proposals) but
      each figure was reported as a standalone measurement by an agent that had caused part of the
      movement (PRESUMPTION-836)
    - disposition gap: **10 days** (last batch 2026-08-07). **The queue is now deeper than the last
      drain** — 53 against the 47-card batch the 08-07 reply cleared. Still the only flag deteriorating
      with time. The 08-11 review-page ID fix is unpushed on day sixteen.
    - `2026-08-17_review.html` **664,184 bytes** at 04:43 [measured — **exact** against the stated 664 KB].
      The 04:43 producer ran fine; every failure today was downstream of it.
  Assumptions: **1,140 total** | 0 tested this run | supported n/a | challenged n/a | novel n/a
    - 18 added tonight (ASSUMPTION-1124 … 1141)
    - **[measured] 1,140 unique ids, max 1141, offset exactly 1 — the long-known ASSUMPTION-459 gap,
      unchanged and stated here as known, not as a finding** (see the correction at ASSUMPTION-1123)
  Presumptions: **836 total** | 0 tested this run
    - 8 added tonight (PRESUMPTION-829 … 836): 3 Critical-risk, 3 High, 2 Medium
    - by type: epistemic 3 · structural 2 · normative 1 · methodological 1 · scaling 1
    - **[measured] 836 unique ids, max 836 — no gap, no collision**
  Validated premises: **max PREMISE-174** [measured], unchanged. `validated_premises.md` carries the
    line "**NO PREMISE WAS MINTED BY THIS RUN. PREMISE-175 REMAINS UNALLOCATED.**" — **exact** against
    the lit pipeline's stated result. First cohort in the record to mint zero premises: "Every item's
    core was already held in the register."
  Lit search dispositions (from tonight's 15a/15b/15c run on the 08-16 intake):
    **6 REVISE (341–346) · 3 MONITOR (532–534) · 0 INCORPORATE** [measured — `revision_flags.md` holds
    REVISE-341..346, `monitor_queue.md` holds MONITOR-532..534; both **exact**]
    - **OPEN-153 measured at 10 of 10.** Two 15b instances, disjoint item sets, no coordination, each
      independently found 5 of 5 items had an ACTIVE governing premise unconsulted at intake — against
      4 of 10 on the previous cohort. Systemic-risk flag filed at Critical.
    - the run declared itself in breach: "**PREMISE-118 is violated by this run** … Not closable by the
      run that files it."
    - standing backlog: "**163 items still carry `Status: [QUEUED]` with no search tags.**"
  Lit search queue: 10 new items queued tonight, all `[QUEUED]` with provenance headers
  Decisions: **DECISION-001..078, unchanged** [measured]. **Forty-third consecutive day with no new
    decision.**
  Open questions: **max OPEN-156** [measured], +3 tonight
  Intra-tradition consensus rate: N/A until Stage 2
  Cross-tradition survival rate: N/A until Stage 3
  Health ratio r: N/A until both rates available

## Fleet reliability

  Sessions today: **29** [boundary: every session above `local_51df64c3` (08-16 self-awareness run)]
  Attended sessions: **0** — sixteenth consecutive unattended day; seventh consecutive day with zero
    identifiable attended sessions. Every `[user]` turn in all 29 transcripts is either a
    `<scheduled-task>` wrapper or the system string `[Request interrupted by user]`. `Morning walk
    cowork handoff` was checked specifically because its name implies attendance; it does not.
  **Mute runs: 7 / 29 = 24.1%** (08-16: 11.4%; 08-15: 11.5%). **The rate roughly doubled overnight.**
    - `morning-system-health` — died on its second `request_cowork_directory`; second consecutive day;
      the only instrument watching the 05:45 launchd gates
    - `metabolism-regen-daily` — died at `start_process`, no assistant text at all
    - `agentic-cost-tracker` — died at `create_directory`; no cost figures produced for 2026-08
    - `morning-walk-cowork-handoff` — cut after finding "No walk notes found"; no briefing written
    - `morning-project-status` — cut at `start_process`; the voice summary was never composed
    - `reviewer-review-weekly` — cut immediately after issuing its write; **whether the brief landed is
      unknown and recorded as unknown**, since `~/Documents/Claude/Reports/` is not mounted here
    - `summa-commentary-reviewer` (`local_54f0a391`) — "**API Error: 529 Overloaded**", nothing else
    Every one of the first five died at a tool boundary. The named mechanism: "nothing times these out
    — measured over 110 runs, 39 never finished, the worst hanging 5.1 days until the Mac slept."
    **No instrument in the fleet reports this number; it exists only here** (OPEN-154, PRESUMPTION-829).
  Loud failures (completed, but the task failed): 2
    - `openstory-agents-telemetry-refresh` — "no Documents mount — the run could not start … I could not
      write `$AGD/REFRESH_STATUS.md` … **exactly the silent-stall mode this status file exists to
      prevent**". Third consecutive stall; its reader was mute, so the failure is doubly unobserved.
    - `c2a2-morning-chat-scrape` — "**The scrape failed — no Chat content was retrieved.**" Extension
      disconnected *and* the claude.ai profile signed out. Second consecutive day, both directions.
  Scheduler health: **78 OK / 1 WARN / 5 FAIL** (08-16: 77/1/6; 08-05 baseline: 78/4/0)
    - FAIL: `com.c2a2.metabolism-publish` (exit 1, now `runs = 1` — it fired and failed rather than never
      firing), `com.tloughran.summa-vault-sync` (exit 1), `com.tomloughran.openstory.watchdog` (exit 1),
      `metabolism-regen-daily` ("the task may report a run and write nothing"), and git debris
    - WARN: `execution-assistant` — enabled, no cron, never run
    - **git debris grew seventeenfold**: "3 stale lock(s) (HEAD.lock, ORIG_HEAD.lock, main.lock) …
      **248 stranded tmp_obj**" against 2 locks and 14 `tmp_obj` on 08-16
    - the daily run "has now finished cleanly three mornings running (08-15, 08-16, 08-17) yet
      **committed nothing since 08-14 01:00Z**"; from inside it, "a 0-byte `.git/index.lock` … the mount
      forbids unlinking it … clear it Mac-side or tomorrow's 05:45 commit will no-op on **331 dirty
      paths** (319 under `wiki/`)"
    - the register again logged **FAIL and OK for the same run in the same minute** (09:45Z) — second
      consecutive night
  Budget compliance: **0 of ~12 substantive runs.** Lit pipeline ~**1.03M** vs 30k (≈34×), with the
    first structural diagnosis on record: "**The pipeline's house format (~25KB per result file, 20
    files) makes that structural rather than incidental.**" Every one of the six completed QC/reviewer
    runs breached, each naming the same fixed cost: "the contract reads plus the log tail are the
    standing cost before any pair opens." Four runs stated coverage rather than implying it — "Day 100
    was selected and not read — **not skipped-and-passed, just not done**"; "I reviewed 3, not the 6 the
    skill caps at"; "**The transcript frame was not run this pass at all**"; "no transcript frame ran …
    **In the log that can look identical to a both-frames run, so I named it.**"
  Channels: chat sync down in **both** directions for the second consecutive day (extension disconnected
    *and* claude.ai profile signed out); Gmail walk-notes channel **empty**; the walk-handoff fallback
    never reached. **[measured]** `2026-08-17_chat_summary.md` **2,062 bytes** — a failure record, not a
    summary; `2026-08-17_cowork_summary.md` **11,384 bytes** at 18:40 — written, delivery failed.
    **Three independent channels for Tom's intent produced nothing on the same day.**
  Vault connectivity: no 2026-08-17 row in `connectivity_log.csv` [measured] — expected; the sewing
    census is weekly and last ran 2026-08-16 (`3552 / 666 / 56 / 4274`).

## Reconciliations attempted this run

  Nine attempted; **eight exact, one refuted.**
  - `pending/` 53 vs the daily run's stated 53 — **exact**
  - `2026-08-17_review.html` 664,184 bytes vs stated 664 KB — **exact**
  - approved 301 · denied 1 · needs_review 1, all unchanged → 10-day gap vs stated 10 — **exact**
  - `revision_flags.md` holds REVISE-341..346 vs stated six REVISE — **exact**
  - `monitor_queue.md` holds MONITOR-532..534 vs stated three MONITOR — **exact**
  - max PREMISE-174 + the explicit "PREMISE-175 REMAINS UNALLOCATED" line vs stated — **exact**
  - `2026-08-17_chat_summary.md` 2,062 bytes vs "no content retrieved" — **exact**
  - `assumptions.md` 1,122 unique / max 1123 / offset 1 at intake — **exact**, and stated as the known
    ASSUMPTION-459 gap rather than as a finding
  - **Refuted: `metabolism/metabolism_data.json` is dated 2026-08-13 21:00 on disk — four days stale —
    against the scheduler's stated "generated 2026-08-12 09:59Z, 5.0 days ago."** The two instruments
    disagree by a day and neither states which clock it reads. The producer was mute today, so the
    discrepancy is unobserved from both ends.
  **Not reconcilable from this sandbox and recorded as unknown rather than as absent:**
  `reviewer-review-weekly` issued a write to `~/Documents/Claude/Reports/` and was cut before
  confirmation; that path is not mounted here.

## Notes

  Two findings tonight are about causes rather than counts, and both are new shapes.

  The first: a check was inert for nineteen runs under a recorded explanation that was never the
  explanation. When the real cause was found — a hardcoded `/tmp` path, not YouTube — the frame worked
  first attempt. In the same day three more recorded *causes* were overturned: a length guard the record
  calls dead is alive and exits 0; a 2026-08-05 escalation "had inferred a transcript drop from the
  *commentary's* silence"; eight backlogs are closed and the record does not say so. The registers tag a
  number as `[measured]` and give a cause the same standing with no tag at all (PRESUMPTION-830,
  OPEN-155). The run that found the first of these could not repair the second: "I can't edit the memory
  file that carries the wrong claim, so it's logged for you."

  The second: the week's corrections have all run one way — a pass mark shown false — and today they ran
  the other. Five MacIntyre "hard misses" retracted, three fresh hits shown to be correct hygiene, one
  five-week-old escalation refuted, eight backlogs already closed. "**Automated attribution fails in both
  directions.**" The status lifecycle in `provenance_protocol.md` has five terminal states for a claim
  that fails verification and none for a flag that is withdrawn, so this system can report its
  false-clearance rate and cannot report its false-alarm rate (PRESUMPTION-833).

  Against those, the day's only tradition-level material came from a retrieval change, not a source
  change: "**YouTube channel RSS feeds are served where search-page scraping is not**", which surfaced
  two July meetings in which Hawkins enumerates three unresolved problems in his own theory — "it sat one
  feed away for a month." Four traditions were upgraded from *the query found nothing* to *the
  source-of-record holds nothing*. And last night's headline did not survive: the Rohr/Stump univocity
  finding is **half-unattested**, the fourth consecutive night on which a headline has been overturned by
  its own follow-up.

  **A correction to this run, made by this run, after filing.** A field-completeness check over
  tonight's twenty-six new register entries reported all fields present. It was wrong: PRESUMPTION-832
  was missing `Why it was unstated:`, and the check passed it because its in-record flag was set at the
  entry heading and never cleared at the next one, so the field was satisfied from the following entry.
  **The checker's scope was unbounded and its failure mode was a pass mark** — the same shape this
  snapshot records in three other instruments tonight. Caught by a second, record-bounded check; field
  added; all twenty-six re-verified. Second consecutive night on which this pipeline has caught its own
  error before publication rather than having it refuted the following night, and on both nights the
  catch came from a verification pass that no contract requires.

  The fleet reliability picture is the worst on record. A quarter of today's runs produced nothing, the
  two instruments that would have noticed were among them, and the scheduler counted 78 OK. **The
  measured reliability of this fleet is an upper bound on its actual reliability by an amount that grew
  today and that nothing in the system computes.**
