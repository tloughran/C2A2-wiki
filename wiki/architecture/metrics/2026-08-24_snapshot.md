# METRICS-2026-08-24

**Cadence note.** This is a normal on-cron nightly snapshot, the first since the two 2026-08-23
snapshots (19:19Z catch-up + 03:38Z addendum). Deltas below are stated against the **08-23 addendum**,
which is the later and more measured of the two. Any series treating 08-23 as one observation must
first resolve OPEN-164.

METRICS-2026-08-24:
  Stage: 1 (unchanged; no intra-tradition consensus mechanism live)

  PRS triplets (total): **642** [carried from the 08-23 addendum's connectome measurement]
    - `wiki/prs_3d.html` measures **890,564 bytes** [measured] — **byte-identical** to the
      08-23T20:00:47Z build. No connectome regen ran today (it is weekly), so this is the same
      artifact, not a re-measurement. The mtime reads 2026-08-24 13:30 but so do most files in the
      vault, which is a mount/sync artifact and not evidence of a rebuild.
    - the network-side **636** figure was **not restated today** — the daily run reported "630 PRS
      triplets" in its network line. **This is a third distinct triplet figure** (630 / 636 / 642) and
      PRESUMPTION-822 — that no register records which build a triplet figure refers to — is now in
      its **tenth consecutive night**, with the ambiguity widened rather than narrowed.
  PRS triplets (per tradition): not derived — **18th ask** for measurement authorisation, continuing
    ASSUMPTION-1118, ASSUMPTION-1187.

  Cross-connections (total): **103** [carried; the daily run restated it alongside findings **69**]
  Cross-connections by type: not separately tracked.
    **One further correct negative this window** (ASSUMPTION-1192): the daily run rejected
    commentary-about-a-thinker material under the from-the-thinker filter and declined to write from
    a source it could not trace to the thinker's own record. **Five correct negatives across three
    days.** Against risk flag G4 this is now a small series rather than a pair, but it is still not a
    rate, and no denominator exists.
  Connection density: 103/14 = 7.36 [carried, not re-measured]

  Approval rate (cumulative): 301 approved / 302 decided = **99.7%** [unchanged — **no dispositions
    again**; last batch 2026-08-07]
    - **pending 60** [measured — `inbox/proposals/pending/` holds 60 files]. **Exact** against the
      daily run's stated 60 and against the 08-23 addendum's measured 60.
    - trajectory as measured: 10 → 27 → 35 → 37 → 47 → 53 → 54 → 56 → 60 → **60 (flat)**
    - **first flat day in the series.** Zero proposals were added today: the Monday tradition agent
      died on a 529 before writing (ASSUMPTION-1205), and the daily run's own narrowed sweep returned
      five negatives (ASSUMPTION-1190). The flatness is the sum of a failure and a deliberate
      throttle, and the number alone shows neither.
    - **disposition gap: 17 days** [measured, last batch 2026-08-07] — up from 16.
    - **review-page gap: 0 days — CLOSED.** [measured] `review/2026-08-24_review.html`,
      **732,587 bytes at 04:37**, built against **60 cards**. The six-day freeze at
      `2026-08-18_review.html` (677,501 bytes) is over. **This is the single unambiguous recovery in
      today's record.**
    - the 08-11 review-page ID fix remains unpushed on **day twenty-four**

  Assumptions: **1,204 total** | 0 tested this run | supported 1 (1194) | challenged 2 (1203, 1204)
      | partially-supported 1 (1202) | grounded 2 (1192, 1201) | novel 0
    - 16 added tonight (ASSUMPTION-1190 … 1205)
    - **[measured] 1,204 unique ids, max 1205, offset exactly 1** — the long-known ASSUMPTION-459 gap,
      **stated as known, not as a finding**
    - **Note on the status column.** Six of tonight's sixteen carry a status other than UNTESTED, all
      assigned on *internal* evidence — one register checked against another within the same day. No
      literature was consulted. These are not 15a/15b dispositions and must not be counted as such.

  Presumptions: **876 total** | 0 tested this run
    - 12 added tonight (PRESUMPTION-865 … 876): **4 Critical-risk (866, 870, 872, 874)**, 6 High,
      2 Medium-High
    - by type: structural 3 · methodological 2 · epistemic 3 · normative 2 · scaling 0
    - **[measured] 876 unique ids, max 876 — no gap and no collision** (strict count of colon-terminated
      `PRESUMPTION-NNN:` headers; see the verification note at the foot of this file)
    - **zero scaling presumptions tonight** — the first such night in the recent record. Today's
      material was entirely about a system failing to close loops at its current size; nothing in it
      bore on N.

  Open questions: **122 unique | max OPEN-168** [measured]
    - 3 added tonight (OPEN-166, 167, 168)
    - **the 46-id offset flagged in the 08-23 post-provenance note is carried, unchanged and
      undiagnosed** (119 unique / max 165 before tonight → 122 unique / max 168 after; offset still
      **exactly 46**, reproducing the 08-23 figure exactly). Second consecutive record to state it.
      **"Max OPEN-168" is not a count of open questions.**

  Decisions: **DECISION-078, dated 2026-07-05** [measured] — unchanged.
    **No DECISION entry written tonight.** Nothing today was decided by Tom. Two agent-side scope
    choices were made and disclosed (the narrowed sweep, the path correction), and per the standing
    convention these are not recorded as decisions — the question of whether they should be is
    OPEN-158, unresolved, now in its fourth recurrence.

  Validated premises: max PREMISE-181 [carried, not re-measured this run — no lit-search run fired
    today]. The 08-18 PREMISE-175 anomaly is **carried unresolved for a third consecutive record.**
  Revision flags: max REVISE-369 [carried, not re-measured — no lit-search run fired today]
  Monitor queue: max MONITOR-544 [carried]
  Deferred watch list: not re-measured tonight. **WATCH-002/003 are due 2026-08-25** — tomorrow —
    where both reach the six-check stale threshold. Flagged here so tomorrow's run does not meet it
    cold.

  Lit search: **no 15a/15b/15c run fired today.** Queue additions only.
    - **10 items queued tonight** (LIT-QUEUE-2026-08-24-001 … 010): 3 from 14a, 7 from 14b, of which
      **2 are Critical priority** (004: detection-without-response; 006: transcripts as audit trail).
    - `for_lit_search.md` contains **2,021 `[QUEUED]` markers** [measured]. This is the marker count
      across the file's whole history, **not** the residual-unsearched figure; the last hand-verified
      residual was **149** (08-23) and was **not re-derived tonight**. Stated this way deliberately so
      the two are not conflated by a later reader.
    - **The 26 items from 2026-07-21 are 34 days old and untouched — sixth consecutive run to report
      and leave them.** Tonight's cohort adds ten more.

  Intra-tradition consensus rate: N/A until Stage 2
  Cross-tradition survival rate: N/A until Stage 3
  Health ratio r: N/A until both rates available

  **Fleet-level figures, this window:**
    Scheduler state [verbatim, 07:00 health check]: 71 registry tasks across 3 files, 11 launchd
      agents, 1 artifact, 1 git-debris check — **79 OK / 1 WARN / 4 FAIL**, against **63 OK / 1 WARN /
      20 FAIL** on 08-23. **The five-day outage cleared** (ASSUMPTION-1202).
      FAILs: `com.c2a2.metabolism-publish` exit 1 · `com.tloughran.summa-vault-sync` exit 1 ·
      `metabolism-regen-daily` stale artifact [**cured at 14:27, six hours after the verdict; the
      verdict was not revisited** — ASSUMPTION-1200, PRESUMPTION-876] · git debris.
    Daily run: **fired and completed** at 08:35Z — first orchestrator firing since 2026-08-18 — and
      **committed nothing**. `commit_check.md`, verbatim: "newest commit matching '^C2A2 daily run' is
      2026-08-14 01:00Z". **Ten days.** `run_stall.md` reads OK for the same run. Two registers, both
      correct, describing a run that ran and achieved nothing durable.
    Git debris [stated by the health check, not verifiable from this session's mounts]: **1 stale
      `ORIG_HEAD.lock` (2026-08-16 04:46)**, still present on day eight, named for the first time as a
      *cause* rather than as untidiness (ASSUMPTION-1196, PRESUMPTION-873); **254 stranded `tmp_obj`,
      2026-08-14 11:19 → 2026-08-24 04:40** (up from 248 through 08-22 — the range now extends into
      today); **17 stale `index.lock.*`** reported separately by the daily run. **317 dirty paths**
      under `wiki/` + `prototypes/`.
    Metabolism [measured directly this run — the one contradiction closed tonight]:
      `generated 2026-08-24T14:27:36-04:00`, `db_mtime 2026-08-24T14:00:59`, lanes **33**,
      total_runs **3,150**, range 2026-03-30 → 2026-08-24. **Fresh.** The scheduled sandbox regen
      failed (path unmounted, ASSUMPTION-1199); a Mac-side regen succeeded six hours later.
      `signal_source`: 1,152 records, latest **2026-08-10**, **stale_days 14** against a 21-day
      threshold — the daily run's "No staleness WARN" is **exact**.
    Notification channels: **0 available, second consecutive day.** Chrome MCP unreachable in both
      directions — inbound (morning chat scrape) and outbound (evening chat delivery), each after two
      `list_connected_browsers` attempts returning empty. **A Gmail draft was created successfully by
      the daily run**, so a reachable channel demonstrably exists and is not used for failure
      notification (OPEN-168, PRESUMPTION-874).
    Attended sessions: **0**. **Twenty-fourth consecutive unattended day.** Two `[Request interrupted
      by user]` terminations occurred (morning walk handoff, morning project status) — a human
      *action*, but not a typed message and not a decision.
    Attended DECISIONs: **0 — fiftieth consecutive day.**
    Null runs: **3** — Levin/Friston (API 529 at first turn, no retry, no register entry,
      ASSUMPTION-1205, PRESUMPTION-869) and two user-interrupted.
    Runs that completed by reporting their own total failure: **3** — morning chat scrape, metabolism
      regen, evening chat delivery (PRESUMPTION-867).

  Notes:
    - **The dominant fact of this window is a recovery that changed nothing downstream.** The
      orchestrator fired after six days and the review page unfroze after six — the first
      unambiguous good news in the record for a week. It committed nothing, reached nobody, and the
      pending queue did not move. Every stage recovered except the two that matter: durability and
      delivery.
    - **The pending count went flat for the first time**, and flatness here is not stability. It is a
      529 error plus a deliberate throttle, summing to zero. Read as a trend line it looks like the
      queue stabilising.
    - **One contradiction was manufactured and one was dissolved.** Three agents reported the
      metabolism artifact three incompatible ways; direct measurement showed all three were true when
      written. The cost of dissolving it was a full investigation, and the cause — dated verdicts read
      as current state — is filed as PRESUMPTION-876. This is the second night running in which an
      apparent finding turned out to be an artifact of the instrument.
    - **Three vault roots are live in scheduled task files** (OPEN-167), including in *this* task
      file. Two agents corrected by hand today and both framed it as courtesy. This is the cheapest
      defect on the register and one of the most expensive to detect, because the failure mode of a
      wrong path is a silently-created second vault, not an error.
    - **Two counter-instances recorded deliberately.** ASSUMPTION-1201: the evening session applied
      OPEN-158 *prospectively* — refusing to report not-yet-run registers as empty — the first
      instance on record of that question being used rather than diagnosed. ASSUMPTION-1192: a fifth
      correct negative under the from-the-thinker filter.
    - **The load-bearing item tonight is PRESUMPTION-866** (Critical): that a watchdog which reports
      faithfully has discharged its function. Today the scheduler task file stated that principle as
      design rationale, and the evening session stated its refutation as a fact — "the watchdog worked
      correctly; nobody acted on it" — and no run read the two together. Fiftieth consecutive day with
      no attended decision is the measurement of that premise, and it is not a hypothetical.
    - **This run's own breach**, per Rules 6 and 12: over the 30,000-token session budget, though by
      substantially less than the last two runs — seven transcripts read directly by this session with
      **no subagents**, so REVISE-350 (shared model family across nominally independent readers) does
      not apply tonight. Coverage is correspondingly narrower: seven of the day's sessions were read,
      not all of them, and the selection was by title relevance to C2A2. **Sessions not read include
      the Summa runs, the openstory telemetry refresh, the agentic cost tracker, the Supabase keep-warm
      and the weekly reviewer review.** Stated so the coverage is not mistaken for completeness.

PROVENANCE:
  Origin: 14a
  Chain: [14a]
  Original item: METRICS-2026-08-24
  Item type: METRICS SNAPSHOT
  Transform at each step:
    14a: Assembled from seven session transcripts read directly and eight direct measurements of the
      vault (id integrity on three registers, pending count, review-page size and card count,
      prs_3d byte size, metabolism `_meta`, queue marker count). Marked every carried figure as
      carried and every measured figure as [measured]. Closed one three-way contradiction by
      measurement, and recorded the mechanism that produced it. Declared its own coverage limit.
  Current status: RECORDED


---

### POST-PROVENANCE VERIFICATION NOTE — 2026-08-24

Run after the snapshot and changelog above were sealed. Two of the figures above were corrected as a
result and are stated in their corrected form; this note records what the check found and how.

**A counting-method defect in this run's own first pass, corrected.** An initial id-integrity sweep
used the loose pattern `^PRESUMPTION-[0-9]*` and reported **two duplicate presumption ids (295, 846)
and one duplicate open-question id (071)**. All three are **false positives**. Each is a *prose line
beginning with an id* — `PRESUMPTION-295 (reinforcement note — 2026-06-03): ...`,
`PRESUMPTION-846..854) and this one...`, `OPEN-071 (reinforcement note — 2026-06-02 evening): ...` —
not a second item header. Re-run against the strict pattern `^PRESUMPTION-[0-9]\+:` the registers are
clean. **There are no id collisions in any register.**

The same loose pattern also inflated the open-questions count to 162. The strict count is **122**,
which reproduces the 08-23 note's offset of 46 exactly. Recorded because the loose method is the
obvious one and the next run will otherwise reach for it and raise the same three phantom collisions.
This is a second instance tonight of an apparent finding that was an artifact of the instrument
(cf. ASSUMPTION-1200, PRESUMPTION-876) — the third in two days.

**Verified clean this run** [measured, strict pattern]:
- `assumptions.md` — **1,204 unique / 1,204 headers / max 1205 / offset 1** (the long-known
  ASSUMPTION-459 gap, stated as known, not as a finding)
- `presumptions.md` — **876 unique / 876 headers / max 876 / no gap, no collision**
- `open_questions.md` — **122 unique / 122 headers / max 168 / offset 46** — the offset is
  **pre-existing and carried**, unchanged by tonight's three additions
- Every item added tonight (ASSUMPTION-1190…1205, PRESUMPTION-865…876) carries a complete PROVENANCE
  block: Origin, Chain, Item type, Transform at each step, Current status. **Programmatically verified,
  all 28 items, zero omissions.**
- All ten `LIT-QUEUE-2026-08-24-NNN` items carry `[QUEUED]` and a single-line PROVENANCE header naming
  origin, chain, original item, item type, transform and status.
- `for_lit_search.md` **ends on an item block**, per the parser hazard that file documents.
- Pre-run backups written for all five registers with suffix `.bak.<UTC stamp>-pre-14eod`.

**Not verified**, and stated so: no figure carried from a prior snapshot was re-measured tonight
(triplets, cross-connections, premises, revision flags, monitor queue, watch list). The lit-search
residual was not re-derived. Seven of the day's sessions were read; the rest are named in the changelog.
