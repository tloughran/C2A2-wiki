# METRICS-2026-09-17

**Cadence note.** Previous snapshot 2026-09-16 (covering 09-15 and 09-16). This is the end-of-day snapshot
for 09-17, one day; deltas are against the 09-16 evening figures. Run began 23:38 EDT, wrote past
midnight; dated as the run's date.

**Coverage declaration.** No attended Cowork session on 09-17. **~28 sessions reachable in
`list_sessions` since the 09-16 self-awareness run; fifteen read** (three parallel intake readers plus
this pass): `Morning walk cowork handoff`, `C2a2 morning chat scrape`, `Morning project status`,
`C282 wiki agent daily run`, `C2a2 agent stump fredrickson`, `C2a2 deferred action monitor`,
`C2a2 evening cowork to chat`, `C2a2 lit search pipeline` (still running at 23:48), `Scheduler health
check`, `Morning system health`, `Metabolism regen daily`, `Openstory agents telemetry refresh`,
`Supabase c2a2 keep warm`, `Summa 2026 nightly verification`, `Summa 2026 daily batch`. **Not read:** ~11
same-shape Summa qc-sweep / commentary-reviewer repeats (digested by the evening sync, which was read).
**Transcripts expose assistant prose and tool-call names only**; "verified at source" means the mount.
Twenty-six of twenty-nine assumptions verified at source in whole or part.

Chat→Cowork **FAILED** (the scrape says third day; Agent 16 fourteenth; the evening sync fifteenth; the
directory says every file since 06-20 — OPEN-236). Cowork→Chat written, **delivery FAILED** (Chrome
extension not connected ×2; built-in pane on the sign-in page). No designer speech in fifteen of fifteen.
**Eighteenth consecutive day on which every extracted item is agent-stated** (PRESUMPTION-912).

METRICS-2026-09-17:
  Stage: 1 (unchanged) — ASSUMPTION-1244's challenge unresolved.

  PRS triplets (total): **867** [unchanged, seventh consecutive day; verified at source:
    `cat traditions/*/prs_triplets.md | grep -cE '^PRS-[0-9]+:'` → 867. Phase 1 ingested 0 —
    **seventh consecutive zero-ingest day** (the counter's 09-16 line is no longer in the master wiki;
    ASSUMPTION-1488).]
  PRS triplets (per tradition): **served tonight, first time in 35 nights** (per-file header counts,
    verified): Levin 123, Carroll 85, Kastrup 82, Rohr 79, McGilchrist 78, Friston 71, Wright 66,
    Wolfram 62, Hawkins 51, Hoffman 44, Fredrickson 42, Stump 38, Arkani-Hamed 31, Loughran 9,
    MacIntyre 6. Sum 867.

  Cross-connections (total): **135** [verified at source: distinct `CROSS-\d{3}` in
    `master/cross_program_index.md` → 135, max CROSS-135. Master wiki status line consistent today.]
  Cross-connections by type: not separately tracked — 36th ask.
  Connection density: **9.64/14** (135/14), unchanged.

  Findings: **90** [verified; contiguous 001–090.]
  L2 signals: **1,501** [unchanged; 87 pairs, span 2026-04-03 → 2026-09-09, staleness **8 days** (from 7),
    no WARN — threshold 21.]

  Approval rate (cumulative): **414 / 415 = 99.8%** [unchanged — no decisions arrived.]
    - **DECISION-083 stands, 21 days** (attended, 08-27). **Last decision-channel act: 8 days** (the
      09-09 email). **Review-pass gap: 7 days** (last archive `2026-09-10_decisions.md`, verified).
    - `pending/` **20 → 21** (+1 Stump, PROP-2026-09-17-001 — a 2018 argument reprinted 2025, admitted
      under "significant work"; ASSUMPTION-1501). Review page 21 cards (230,043 B, verified). Gmail
      decision draft `r8191906678905695603` created, not sent, **carrying one error the run disclosed and
      could not correct** ("7th failed attempt" — ASSUMPTION-1485, OPEN-233).
    - **Phase 6: commit landed** — `commit_check OK … committed 09:45Z` (05:45 EDT), "first clean commit
      since 09-14"; **before every artefact the run produced** (review page 06:39, L2 stream 06:40, draft
      10:40Z — ASSUMPTION-1486, OPEN-234). `index.lock` gone (by whom: still OPEN-230 limb 2); 6
      `tmp_obj_*` remain (scheduler WARN). `COMMIT_ME_2026-09-07.sh` still present (verified; 10 days).
    - ingest_ledger: **414 / 382 / 30 / OPEN 1** — transcribed, not re-measured (ASSUMPTION-1404
      continued); OPEN 1 is Wright, "not re-attempted this run" — and "7th failed attempt" in the draft.
    - Wright PROP-2026-08-14-033: three descriptions in one day (Agent 16 "live on Apple Podcasts, sixth
      attempt to publisher surfaces only"; daily run "six prior failures, not re-attempted"; draft
      "7th") — ASSUMPTION-1487. Agent 16: grep-before-retrieval "(escalated)"; the rule is still the
      "(Tom's)" line, at l.**5728** (the 09-16 register said 5725 — corrected).
    - **Known, unread, uncarded:** Levin lab bioRxiv 10.64898/2026.08.13.744473 — no card (by rule), no
      watch, no ledger row (ASSUMPTION-1490, PRESUMPTION-1025).

  Assumptions: **1,507 headers | max ASSUMPTION-1508 | 29 added (1480–1508)**.
    - tonight: **16 SUPPORTED in-house**, **3 CHALLENGED** (1487, 1492, 1493), 1 CHALLENGED-partial (1483
      — a correction to this register's 1442), **2 PARTIALLY-CHALLENGED** (1499, 1506), 2 PARTIALLY-
      SUPPORTED (1491, 1500), **5 UNTESTED-held** (1481, 1482, 1497, 1502, 1505 — three wait on the
      lit-search run's artefacts).
    - **Every entry carries a `Source:` line**; 26 of 29 verified at source in whole or part.
    - **Three corrections to this register's own prior entries:** 1483 (1442 partly false —
      `_cycle5`/`_cycle7` files pre-existed), 1484 (the lane is 139, not 129/132), 1503 (l.5725 → 5728).
  Presumptions: **1,037 headers | max PRESUMPTION-1033 | 11 added (1023–1033)**
    - by type: structural 4 (1024, 1025, 1030, 1032), epistemic 2 (1026, 1029), methodological 2 (1023,
      1031), normative 1 (1027), scaling 2 (1028, 1033).
    - **No Critical-risk item tonight** (first night since 09-13); three High (1023, 1025, 1029); two
      Medium-High (1024, 1032); five Medium; one Low-Medium.
    - **One routed (1024)**; five withdrawn at pre-check on named covering premises (PREMISE-004, -109,
      -107, -006/OPEN-223, -124); five held with no covering premise and a named test. **One surfaced and
      discharged in the same pass** (1031: five registers byte-identical to their 09-16 backups over the
      backup's length — the 15:55 restamp touched and did not write). Two self-referential (1027, 1031).

  Literature queue: **1 item routed of 40 produced** (PRESUMPTION-1024 — creation permitted, correction
    denied). **The 09-17 15abc run was still in flight at 23:58**: one 15a file (`PRESUMPTION-1019_for.md`,
    "PARTIALLY-SUPPORTED — transferability Strong; 'without an argument' not supported; NOVELTY-FLAG NOT
    RAISED"), two 09-13 re-trigger cycle-1 files (MONITOR-554/575 — not the items due today), **no 15b
    file, no run note, no 09-17 line in any register**; transcript ends on two `Agent` spawns
    (ASSUMPTION-1481, OPEN-235). Ids unchanged: DISPOSITION max 968, REVISE max 478 (468–478 OWNER
    unassigned, ninth cycle), MONITOR max 610, PREMISE max 205 — **eleventh consecutive effectively-
    zero-mint cycle** if the run lands empty. **Untagged re-trigger lane: 139, not 129** (ASSUMPTION-1484).
  15a/15b independence: **two `Agent` contexts tonight — structurally separate for the first time in seven
    cycles — undeclared by the run, self-attested by the subagents** (ASSUMPTION-1482; PRESUMPTION-1023
    withdrawn on PREMISE-004: same-family attestation is not independence). Verdict waits on the 15b
    file.
  Connectivity: 82 vs 65 — **not re-measured for a fifth day** (OPEN-206/214).
  Infrastructure: scheduler **87 OK / 2 WARN / 3 FAIL of 92** — **first movement after three days at
    87/1/4**; OK conserved; one FAIL → WARN (git debris, lock cleared); no run diffed rows
    (ASSUMPTION-1496). `permissionMode` absent **13 → 14 days**. `run_stall` FAIL "no record of that run"
    beside `commit_check OK` — "a new shape … worth a look," second day. Metabolism regen **FAILED an
    eleventh time**, both prior blockers in sequence (path, then `database or disk is full` at
    `src.backup`); **db 6.78 GB = 6.31 GiB** (first run to give both units); `/` 4.4 G, `/sessions` 6.0 G
    free; "structurally unable to succeed from the sandbox … a method decision for you" — the first run
    to state the constraint as a constraint (ASSUMPTION-1498). Snapshot **14 days** stale (13.7 by
    `_meta.generated`; three figures today, all consistent — from four on 09-16). DB growth **6.57 → 6.63 →
    6.70 → 6.78 GB** (≈80 MB today); the copy-outgrows-volume date PRESUMPTION-1013 asked for is past.
    OpenStory refresh **FAILED day three**: sandbox `AttributeError: 'list' object has no attribute
    'strip'` (`resolve_ref` l.134 — confirmed at the line; one-line `isinstance` fix, not applied); Mac
    step2b non-zero exit at 10:15Z; `REFRESH_STATUS.md` **two writers, day three** — the sandbox claimed
    "written with the FAIL line" and "the Mac-side run did not update" it; **both false at source**
    (ASSUMPTION-1499, PRESUMPTION-1029). Telemetry (a) *was* regenerated Mac-side at 06:22 (33 agents).
    **Two mtime restamps today: 59 files at 15:55:36–37 and 9 at 22:00:07** (ASSUMPTION-1480, OPEN-231);
    the 09-16 event was 28 at 22:00:05. `morning-system-health`: "Machine itself is fine — Mac disk 58%,
    sandbox idle" (the word "sandbox" appears for the first time; ASSUMPTION-1500); "313 new broken
    wikilinks" (since the 05-28 baseline). `summa_index.json` missing exactly Day-001/Day-035 (verified).
    Cost: **no dollar figure in any of fifteen sessions**; four lanes declared Rule 6 breaches, none
    proposed a number, this pass included (ASSUMPTION-1507).

  Intra-tradition consensus rate: N/A (Stage 2).
  Cross-tradition survival rate: N/A (Stage 3). Zero new CROSS entries. The Stump specialist recommends
    **CROSS-008 be restated** as form/levels rather than final causality ("three sources deep without
    Stump saying it") and declines to cross-file Stump↔Kastrup/Hoffman as convergence — an agent auditing
    the wiki's drift from its primary source; filed as a proposal card because that is the only channel
    (ASSUMPTION-1502, PRESUMPTION-1032).
  Health ratio r: N/A.

  Notes — trajectory observations:

  1. **The register's first drafts are its final drafts.** The daily run wrote a wrong ordinal into the
     decision draft, saw it, and could not fix it: `create_draft` is permitted, `update_draft` is not.
     The disclosure went to PROCESSED_LOG; the draft goes to Tom. The permission tier is inherited and
     nobody has argued it; that argument is the one item routed tonight (PRESUMPTION-1024) and the one
     policy question for Tom (OPEN-233).

  2. **Two restamps a day, and the registers survived both.** Fifty-nine files at 15:55, nine at 22:00;
     every mtime the estate uses to date its own work. This pass ran the diff it had been presuming
     (PRESUMPTION-1031): the five registers are byte-identical to their 09-16 backups over the backup's
     length. So the 15:55 process touches and does not write — for those five. The other fifty-four are
     still an inference, and the two processes are still unnamed (OPEN-231, extending OPEN-224).

  3. **The commit won its race against the artefacts.** Phase 6 unblocked; the 05:45 commit landed;
     the review page landed at 06:39. "First clean commit since 09-14" carried yesterday's files. On
     short days this is invisible; on a 32-call Phase 2 day it is a day's lag. The specialist skip has the
     same shape — a SKILL that says "before" and a clock that says otherwise (ASSUMPTION-1486, -1489;
     PRESUMPTION-1030; OPEN-234).

  4. **The scheduler headline moved and nobody saw it move.** 87/1/4 for three days, then 87/2/3; OK
     conserved again; the delta inferred by this pass, not stated by the reader whose SKILL forbids
     checking. The one-line SQL is on its seventh night (ASSUMPTION-1496, -1497). PREMISE-107's
     discriminating test, named on the FAIL row itself, has now outlived the FAIL row's neighbour.

  5. **Four counters for one streak, and the smallest was read from yesterday.** The scrape said
     "third"; the directory says every file since 06-20 is a failure note. A counter that resets to the
     previous note's value cannot see a three-month outage. Each of the estate's counters is derived by
     the run that prints it, from prose (ASSUMPTION-1492, -1488, -1506; PRESUMPTION-1033; OPEN-236).

  6. **The 30-day window has no home.** Three specialist runs in three days applied it and redefined it
     — and no file under the mount states it (the SKILL says 60; the WARN threshold is 21). OPEN-228 asks
     whether it binds; OPEN-232 asks where it is. Every card on the 21-card page was admitted under a
     clause nobody can cite.

  7. **The lit-search lane may have changed shape tonight, or died.** Two `Agent` spawns as the
     transcript's last entries, one 15a file, no 15b file, no run note at 23:58 — either the first
     structurally separate 15a/15b in seven cycles, or the second OPEN-222 instance in three days. The
     09-18 pass will know in one grep (ASSUMPTION-1481, -1482; OPEN-235). And the lane it is working is
     139 items, not 129 — the 09-16 arithmetic omitted the 08-30 and 09-13 cohorts (ASSUMPTION-1484).

  8. **Eleven agent-made rules in three days; one ask.** Five today ("Active" redefined; non-attempt ≠
     failure; no card without a read source; cycle-suffixed paths applied; "significant work" for a
     2018 argument). The metabolism run alone returned "a method decision for you" instead of a rule.
     Recorded in the decisions index as agent-made; REVISE-476's field still does not exist.

  9. **Rule 6, and the conflict this register cannot resolve alone.** Every lane breached and declared;
     none proposed a number; ASSUMPTION-1463 and -1507 record that this register has not either. Agent
     14a's definition says it "does not make design recommendations"; Rule 6 and two nights of
     ASSUMPTION entries ask for one. **Surfaced as a conflict (Rule 7), not averaged:** the number is
     Tom's to set or to delegate; this pass reports its own cost so the number can be set against a
     figure — three intake readers at 150,892 / 137,383 / 126,295 tokens, plus this synthesis.

  **Budget.** Per-task 4,000 and per-session 30,000 exceeded by a wide margin. Declared, not concealed;
  see note 9.
  **Coverage.** 15 of ~28; ~11 Summa repeats unread; the lit-search run read in flight.
