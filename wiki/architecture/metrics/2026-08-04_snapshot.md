METRICS-2026-08-04:
  Stage: (carried — not recomputed this run)
  PRS triplets (total): **511 carried authoritative; NOT RE-COUNTED TODAY BY ANY AGENT.** No run published a triplet count this day — the first day in this series with no PRS figure at all — because `c282-wiki-agent-daily-run`, which publishes the network line, was interrupted in Phase 0 (ASSUMPTION-702). The nine circulating values recorded on 08-03 (222 / 300 / 364 / 447 / 511 / 513 / 517 / 519 / 609) are therefore carried unchanged and unreconciled for a second day. MONITOR-489's settling test — a recount under an explicitly stated counting rule — **again NOT performed, third consecutive day.** `traditions/stump/prs_triplets.md` still carries BOTH "Total PRS triplets: 25" (line 243) and "Total PRS triplets: 28" (line 274): **unrepaired on DAY 5** (ASSUMPTION-728g). `metrics/prs_yield_log.csv` was MODIFIED today at 15:05 and its last row is still `2026-07-31,2,514,1` — written to without gaining a row (ASSUMPTION-728i).
  PRS triplets (per tradition): 2 new proposals filed 03:08 by the Tuesday agent (Hawkins ×1, Hoffman ×1 — PROP-2026-08-04-001/002). The Hawkins side was under-filled on a stated rule ("No new Hawkins material inside the 30-day window") and one candidate was declined for lack of corroboration; the Hoffman item was filed as a **conflict** with Kastrup rather than a bridge (ASSUMPTION-716). Second consecutive day an agent declined the easy connection.
  Cross-connections (total): **165** carried authoritative; not recomputed. Findings **55** carried. Neither the connectome extractor nor the daily run published today — see above.
  Cross-connections by type: (not decomposed — blocked on the same counting-rule reconciliation, third day.)
  Connection density: (not recomputed) — blocked on counting-rule reconciliation.
  Vault connectivity: no `connectivity_log.csv` row today; last row `2026-08-02,3083,660,64,3807` (weekly cadence, next due 08-09). Separately measured: `wiki_narration.html` regenerated at 13:31 to **3,864 nodes / 98,201 links / 40,342,121 bytes**, and the artifact is **legal on none of the three surfaces that specify its limits** — `CLAUDE.md` 2,000/3,000, `generate_visualization.py:1322/1328` MAX_NODES 20,000 / MAX_SIM_EDGES 30,000, actual 3,864/98,201. The link count exceeds the CODE ceiling by 3.3× and no limit warning fired (ASSUMPTION-722; PRESUMPTION-675, HIGH, queued).
  Approval rate (cumulative): pending **34** measured directly (approved 254, denied 1, needs_review 1). Agent 16 reported "16 → 32" the same day and was correct when it measured; the Tuesday agent's 03:08 filings took it to 34 (ASSUMPTION-715). Review service 0/day, **30th day**. Review-pass gap **12 days** — measured from `review/archive/` (16 files, latest `2026-07-23_decisions.md`), and reported as 12 by Agent 16; **for the first time in a week only one figure circulates for this quantity**. `tools/generate_review_page.py` line 304 UNCHANGED (mtime 2026-05-18 20:49, **TENTH consecutive day named**); a review pass run today discards all 34.
  Assumptions: max entry ID **728** | **+28 today** (ASSUMPTION-701..728, all UNTESTED) | block count 727 | the known ASSUMPTION-459 numbering gap (07-14 crash artifact) exactly accounts for the max-ID/block-count offset of 1.
  Presumptions: max entry ID **677** | **+14 today** (PRESUMPTION-664..677, all UNTESTED) | block count 677 under the strict `^PRESUMPTION-NNN:` pattern, equal to the max ID. The 07-08 P-295 duplicate (PRESUMPTION-507) still unrepaired and still visible only under the loose pattern.
  Decisions on record: 78 | +0 today. **THIRTIETH consecutive day with no decision.** `decisions.md` unchanged since 2026-07-20, untouched this run. Two candidates named against the day and NOT logged, per the standing convention against unilateral promotion: (a) the 15a/15b separate-instance arrangement, recorded only in a disposition preamble — the evening summary argues "If the way the pipeline runs can change without a DECISION, the decisions register is not a complete record of how the system got to its current shape"; (b) the extractor determinism fix, which changed the supported regeneration path and has no session, no decision and no owner on record (ASSUMPTION-720).
  Open questions: max entry ID **139** | +0 today. `open_questions.md` unchanged since 2026-07-28. OPEN-138 and OPEN-139 both **OPEN — awaiting Tom** for a twelfth day. Candidates surfaced and NOT logged: (a) whether a scheduled run that terminates without a verdict is a failure the system is entitled to not see — PRESUMPTION-664, and the first open question that bears on whether the daily record exists at all; (b) whether the production rate of this pipeline is now reducing rather than raising the probability of action — PRESUMPTION-677, which queries PREMISE-138 directly; (c) the eternity/timelessness question (FINDING-055), carried unchanged from 07-31 onward.
  Validated premises: max ID **140** | block count **97** | **+0 today**, mtime 2026-08-02 — third consecutive day with no write. 140 − 97 = 43, still exactly the absent PREMISE-001..043 cohort (OPEN-133). PREMISE-114 and PREMISE-124 **UNEDITED ON DAY 11**. No INCORPORATE was issued today and the run said so explicitly rather than letting silence imply a clean check — recorded as a positive instance.
  Monitor queue: max ID **501** | block count **134** | +1 today (MONITOR-501, PRESUMPTION-653).
  Revision flags: max ID **277** | **+10 today** (REVISE-268..277) | **block count 118, UNCHANGED FOR A SECOND CONSECUTIVE DAY — AND THAT IS A MEASURED DEFECT.** The `REVISE-NNN — <title>` heading style introduced on 08-03 was used again today, so the strict `^REVISE-[0-9]+:` pattern this register has counted by since inception misses all sixteen entries from the last two days. Loose `^REVISE-[0-9]+` returns 136. The counting rule stayed broken through the day it was used to count ten new flags, and nothing noticed (ASSUMPTION-728b).
  Dispositions: max ID **600** | block count **568** | +11 today — shortfall exactly **32**, UNCHANGED for a FOURTH consecutive day. Cause still unstated anywhere.
  Lit-pipeline throughput (2026-08-04): 15a/15b/15c searched and net-evaluated the 08-03 EOD intake **PRESUMPTION-646, 647, 648, 651, 653, 654, 655, 656, 657, 660, 661** — 11 items, 22 result files + 1 batch flag. Mix: **0 INCORPORATE · 10 REVISE · 1 MONITOR (High) · 1 batch-level SYSTEMIC-RISK-FLAG (Critical) · 0 NOVELTY**; DISPOSITION-590..600. Yield series with denominators: 07-29 1/8 · 07-30 0/4 · 07-31 0/3 · 08-01 2/6 · 08-02 3/7 · 08-03 0/7 · **08-04 0/11** — largest cohort in the series and the second consecutive zero. **VERIFICATION: THE INDEPENDENCE ARRANGEMENT RECOVERED AND EXCEEDED 08-02's.** 15a and 15b ran as separate agent instances with disjoint contexts, 15b explicitly barred from `lit_search_results/for/`, and 15b's transcript shows no such read. The residual is stated rather than banked: "same model family, shared pretraining and alignment, one coordinator whose framing both inherited. It is *not* independence in REVISE-262's sense." This partially answers REVISE-262 on its third day and reverses the 08-03 regression (ASSUMPTION-724; cf. ASSUMPTION-678). Provenance headers on all 22 result files were "checked programmatically rather than by eye." **15d did NOT run** (weekly, next 08-10). Queue measured: `for_lit_search.md` 15,161 → 15,216 lines before this run's append (15,292 after), **1,527 `Status: [QUEUED]` lines — IDENTICAL to 08-03 across eleven dispositions**, 1,793 total `[QUEUED]` occurrences pre-append (1,803 after), 117 `[QUEUED-EMPIRICAL]` unchanged. The run's own post-write check reports the balance as 115 − 115 = 0 against a direct grep of 1,347 − 1,347 = 0 — balance agrees, magnitudes differ tenfold, surfaced by the evening summary and unresolved (ASSUMPTION-725; PRESUMPTION-670, HIGH, in-house). Backlog drain **zero for a 28th consecutive day**, and it is now an open question whether the counter could show a drain if one occurred. Tonight's 14a/14b intake: **PRESUMPTION-664, 666, 668, 669, 673, 675, 676, 677 [QUEUED]** to lit (8 of 14); **PRESUMPTION-665, 667, 670, 671, 672, 674 tagged [GOVERNED-CANDIDATE → IN-HOUSE]** (6 of 14); **ASSUMPTION-701..728 routed [IN-HOUSE]** (28), with ASSUMPTION-720 flagged OBJECT-LEVEL/POSITIVE.
  Deferred items watching: **3** — WATCH-002 (Wright) and WATCH-003 (Rohr) both checked today at **3 of 6**, threshold 2026-09-01; Agent 16 ran clean on all three intake channels. **New: the WATCH-002 caption route is permanently unexercisable** — `web_fetch` refused the URL with "URL not in provenance set" and "That extension, added 07-28, will fail identically every run." The check counter advances on runs that cannot check (ASSUMPTION-714). `deferred/watch_list.md` measured **316,393 bytes**, up **10,294** from 08-03 — the largest single-day growth recorded for this file — and still above the Read-tool ceiling; archival recommendation renewed.
  Intra-tradition consensus rate: N/A (pre-Stage 2)
  Cross-tradition survival rate: N/A (pre-Stage 3)
  Health ratio r: N/A
  Notes:
    - **HEADLINE 1 — FOUR SCHEDULED RUNS VANISHED WITHOUT A VERDICT, AND THE WATCHDOG WAS ONE OF THEM.** `c282-wiki-agent-daily-run`, `metabolism-regen-daily`, `scheduler-health-check` and `morning-walk-cowork-handoff` all end at `[user] [Request interrupted by user]` with no report of any kind; two died immediately after `mcp__Desktop_Commander__start_process`. None emitted a failure notice — each is absent rather than failed. `scheduler-health-check` exists to detect this class. PRESUMPTION-661 (dispositioned today as REVISE-277) concerned sessions listed as running that were not advancing; this is the strictly worse case, and it was reported by nobody (ASSUMPTION-702; PRESUMPTION-664, **Critical, queued**; PRESUMPTION-665, in-house).
    - **HEADLINE 2 — THE MORNING STATUS REPORT NAMED THE ONE RUN THAT FAILED AS HAVING RUN ON TIME.** `morning-project-status`: "everything due overnight ran on time — the C2A2 wiki orchestrator…" and "No failures to report." The orchestrator had terminated in Phase 0 with no output. Three hours earlier `morning-system-health` read the commit record and reported the opposite: "The daily wiki run committed nothing again… Second FAIL in three days." Two health instruments, one morning, one subject, contradictory verdicts, no reconciliation in any artifact — and the reassuring one is the one Tom reads first. This is SYSTEMIC-RISK-FLAG 2026-08-04's shape, emitted **seven hours before the flag was filed**, by an instrument the flag does not cover (ASSUMPTION-701, ASSUMPTION-713; PRESUMPTION-666, HIGH, queued).
    - **HEADLINE 3 — THIRTY-THREE ARTIFACTS CARRY A PASS-MARK ISSUED BY A CRASH.** `summa-qc-sweep`: 33 transcripts render **32–69%** of raw ASR against a ±25% threshold (worst Day 111 at 0.32 — 1,406 body words against 4,283 spoken), and "All 33 currently read `fidelity_checked: true` / `last_qc_outcome: pass`, and none carries a `length_note`." Mechanism stated: `fidelity_check.py` raises `FileNotFoundError` on a cold `/tmp` cache — "which is every fresh sandbox" — killing the run before the length check, after which "the pair gets pass-marked on form alone… a cold cache and a clean pair look identical unless you read `stderr`." Instrument error excluded by refetching 19 days. **This is the systemic flag's defect confirmed with a count, in a subsystem the flag does not name, found the same day by a run that had not read the flag** (ASSUMPTION-703; PRESUMPTION-667, **Critical**, governed → in-house). A second defect in the same instrument, `fidelity_check.py:86`, produces false *failures* by sampling `## Notes` as transcript prose (ASSUMPTION-705). Neither patched, on the stated rule that the scripts are Tom's.
    - **HEADLINE 4 — A DISCLOSED CAVEAT'S FAILURE TO PROPAGATE, MEASURED END TO END IN ONE HOP.** `openstory-agents-telemetry-refresh` disclosed that `PRAGMA quick_check` was replaced by a header-consistency check and torn-copy detection skipped, then published "✅ OPENSTORY REFRESH OK … (33 agents)" while its body says "32/33 roster captured." `REFRESH_STATUS.md`, the artifact downstream consumers parse, carries only the PASS line. `morning-system-health` duly reported "OpenStory feeds passed clean." PRESUMPTION-660 was dispositioned as REVISE-276 for exactly this, hours after it recurred. This is the concrete case for the flag's second recommendation — a provenance block on every assurance-bearing artifact (ASSUMPTION-711, ASSUMPTION-712).
    - **HEADLINE 5 — THREE SELF-RETRACTIONS FROM THREE RUNS, AND NO INSTRUMENT CHANGED.** `summa-2026-nightly-verification` withdrew 9 false PRS-nonexistence hits before publishing ("Had I reported them, you'd have been sent to repoint citations that are already correct"). The 20:30 `summa-qc-sweep` discarded a 17-item audit and named the recurrence — "Same failure profile as the 07-31 keyword audit" — and separately reverted a pass-mark it had issued hours earlier, preferring the 18:40 run's reasoning to its own. In every case the output was corrected and the producing method was not (ASSUMPTION-706, ASSUMPTION-707; PRESUMPTION-668, HIGH, queued). Recorded neutrally: this is also the strongest display of falsifiability-in-practice in this series.
    - **HEADLINE 6 — A HOLD WITH ONE STATED SCOPE WAS READ BY TWO RUNS AS A BLANKET.** "That hold is *length-only* — the memory says don't flag the 0.31-0.41x ratios as novel, not that the pairs are unreviewable. Their semantic content had never been reviewed. Reviewing them found six sound commentaries and the new criterion-M band." The correction came from a run that read the hold's terms rather than its label, and it had to leave a note so future runs stop. The adjacent case is WATCH-002, whose terms are known to be unsatisfiable while its counter advances (ASSUMPTION-719, ASSUMPTION-714; PRESUMPTION-669, HIGH, queued). Holds are the one category no counter tracks.
    - **HEADLINE 7 — THE DAY'S BEST ENGINEERING HAS NO SESSION, NO DECISION AND NO OWNER ON RECORD.** The extractor determinism fix — `list(set(...))` → `sorted()`, closing a defect that "rewrote the whole 40MB wiki_narration.html blob in git on every regen even when no content had changed," 1,499 differing leaves (1,262 wikilinks + 237 edges) — shipped with a test that forces its own failure path under two fixed PYTHONHASHSEEDs, and names the principle: "Two default-seed runs are a weak test… force the failure, do not wait for it." It is the second such remedy in this series (cf. the Sociogram guard, ASSUMPTION-675). **No attended Cowork session exists for 2026-08-04** — all 60 most recent sessions carry scheduled-task titles — and a sub-agent established directly that `list_sessions` omits its own live parent session. REVISE-268 ("an empty channel is not an empty day") was filed at 00:47 today; by evening the register held direct proof of it (ASSUMPTION-720, ASSUMPTION-726). The change also carries two figures for one quantity written nineteen seconds apart — "1122 node dates moved" vs "2103 architecture files took their graph date that way" (ASSUMPTION-721).
    - **HEADLINE 8 — THE DELTA GUARD'S BASELINE DESCRIBES NO BUILD, AND THE RACE EXPLANATION IS NOW EXCLUDED.** `build_meta.json` records `bytes: 40236674`; `wiki_narration.html` is 40,342,121. On 08-03 this was attributed to a test harness restoring files after the accepted build. Today's timestamps refute that: the HTML was written 13:31:03, the metadata 18:09:46 — **four hours thirty-eight minutes later**, describing a different artifact. The guard's own correctness rule is "the baseline must only ever advance on a build we accepted" (ASSUMPTION-723; PRESUMPTION-674, in-house).
    - **HEADLINE 9 — THE QUEUE COUNTER DID NOT MOVE WHEN ELEVEN ITEMS LEFT THE QUEUE.** `Status: [QUEUED]` measured 1,527 on 08-03 and 1,527 on 08-04, across eleven dispositions. The evening summary independently surfaced a tenfold magnitude disagreement on the same file (115 vs 1,347) and declined to average it — the right behaviour, recorded as such. But if the headline queue figure is invariant under disposition, then "backlog drain zero for 28 days" is not a finding about the backlog (ASSUMPTION-725; PRESUMPTION-670, HIGH, in-house). **Reflexive note, fail-loud:** this run's own append briefly incremented that counter to 1,528, because a line describing the counting pattern matched it. Detected and repaired within the run; recorded rather than removed.
    - **HEADLINE 10 — TWO RUNS BREACHED "THE BUDGET" AND MEANT DIFFERENT BUDGETS.** "well past the 4k per-task guideline" and "well past the 30k session guideline," same day, same rule number, no run stating which binds it until it exceeds one. NINTH consecutive day of disclosure. Yesterday a run proposed a concrete cap ("three pairs per run"); today a run made the opposite move — "Read with `--max 99`, not the 6-cap" — and breached (ASSUMPTION-727; PRESUMPTION-671, in-house).
    - **HEADLINE 11 — A PARTIAL SIDE EFFECT SURVIVED AN INTERRUPTED RUN, IN A SYSTEM THAT REASONS ABOUT PARTIAL FAILURE ONLY INSIDE THE FILESYSTEM.** `c282-wiki-agent-daily-run` fired `unlabel_message` against a Gmail decision email and then died before Phase 0 completed. Measured: `review/archive/` holds 16 files, latest 2026-07-23 — no 08-04 entry. A future run reading Gmail labels will see that email as processed. The task's spec anticipates partial failure carefully and entirely in-file (PRESUMPTION-676, HIGH, queued). It compounds with `generate_review_page.py` line 304: one path discards decisions loudly at review time, the other drops them quietly at intake.
    - **HEADLINE 12 — SEVENTH DARK DAY ON BOTH SYNC CHANNELS, TWO DIFFERENT CAUSES FOR THE FIRST TIME.** Morning: the extension's browser is signed out of claude.ai. Evening: two extensions connected, selection requires a human, so **no navigation was attempted and nothing is known about login state** — "Recorded as NOT ATTEMPTED, not as 'would have failed anyway'." That refusal is the three-valued vocabulary the systemic flag asks for, used correctly and unprompted, on the same day the flag recommended inventing it. Seven MCP servers still need OAuth re-authorisation, which no scheduled run can perform (ASSUMPTION-726).
    - **HEADLINE 13 — A PREDICTION FILED ON 08-03 CAME TRUE ON 08-04 AND WAS RECORDED AS CONFIRMATION.** "The bottom-frontmatter bug recurred exactly as the 08-03 entry predicted… Two consecutive nightly passes have now each paid this cost from scratch… a regex re-derived each night will keep failing." Independently the same morning: "the sandbox path is now a 7-call dance that I rebuild from scratch each morning." Both name the durable fix; neither is authorised to make it (ASSUMPTION-708; PRESUMPTION-673, queued).
    - **THE REFLEXIVE ITEM.** PRESUMPTION-677 asks whether this pipeline's production rate is now reducing the probability of action rather than raising it. Measured against the day: 34 pending, 136 REVISE (+10), 137 MONITOR, 30 days without a decision, 12 days without a review pass, PREMISE-114 unedited on day 11, the bulk retag unauthorised on request nine, line 304 named on day ten. PREMISE-138 asserts that repetition raises action probability; it has now been cited by four consecutive Critical flags with nothing firing. This register is among the largest producers, and 14b's own definition instructs it to err toward more. Queued to literature, where alert-fatigue and backlog-depth work bears directly.
    - POSITIVE ENTRIES, RECORDED NEUTRALLY: the lit pipeline's independence arrangement recovered past 08-02's and stated its residual instead of banking the win (ASSUMPTION-724); the nightly verification bounded its own coverage voluntarily — "roughly half the corpus's PRS mentions are form-checked but not existence-checked" — while reporting zero citation-existence failures (ASSUMPTION-710); the 18:40 reviewer stated its vantage before its verdicts ("the mount *was* live this run, so every citation check below is a real body-level check, not a form-check") and pass-marked nothing, on the rule that "clearing the queue by pass-marking held pairs is the failure mode the discipline exists to prevent" (ASSUMPTION-718); the Tuesday agent fixed a wrong path in flight and reported it, declined an uncorroborated source, and filed a cross-tradition item as a conflict rather than manufacturing a bridge (ASSUMPTION-716); `summa-2026-daily-batch` asked to be retired for a second consecutive day (ASSUMPTION-717).
    - COVERAGE GAP, STATED: four scheduled runs produced no output at all and are not represented above except as absences (Headline 1). `agentic-cost-tracker` and `weekly-agent-ecosystem-report` are unrepresented for a third consecutive night. No attended session transcript was read, because none exists — for a second consecutive day the day's largest change was reconstructed from source comments and mtimes.
    - CARRIED FOR TOM (nothing from 08-03 was actioned): Sign in to claude.ai in Chrome, or select one browser interactively — one action, both syncs, seventh dark day. **Diagnose the four `[Request interrupted by user]` terminations, starting with `mcp__Desktop_Commander__start_process`** — new tonight, and the watchdog is among the affected. **Reconcile the Gmail decision label against `review/archive/` for 08-04** — a decision may be orphaned. Fix `generate_review_page.py` line 304 — one line, blast radius now 34, tenth day named. Fix `fidelity_check.py` (cold-cache crash before the length check; line 86 Notes-fallback) and decide the 33-day 32–69% band — re-render or `length_note`. Delete or repoint the `paradigm_flags.md` stub — eighth instance pending. Resolve the three-way node/edge limit conflict (docs 2,000/3,000 · code 20,000/30,000 · artifact 3,864/98,201). Make `build_meta.json` a product of the build rather than a separate write. Establish which budget binds a run — 4k per-task or 30k session. Establish the queue counting rule; the headline figure did not move across eleven dispositions. Run the three owed one-command joins (MONITOR-500 / P-643 / P-645) — named by two agents, none run, third day. Decide REVISE-262 (the arrangement improved today; the residual is model-family shared). Rule on REVISE-263..277 (fifteen open). Answer REVISE-259 (fourth run), REVISE-260, REVISE-261. Ratify or amend PREMISE-114 (day 11) and PREMISE-124. Correct the wiki root path in the Tuesday agent's SKILL.md. Authorise striking the caption route from WATCH-002, or paste the URL once. Publish the regenerated PRS connectome and commit today's wiki work (Mac-side git). Fix the 07-21 `push rejected` on the metabolism publisher (14 days). Retire or re-scope `summa-2026-daily-batch` (307/307, second request) and `agentic-cost-tracker` (five months silent). Roll `watch_list.md` to dated archives (316,393 bytes, +10,294 today). Authorise or decline the 26-item bulk retag — NINTH request. Re-authorise the seven plugin MCP servers. Decide whether the 15a/15b arrangement change and the extractor determinism fix warrant DECISION entries — the evening summary argues the first does, and this register argues the second does.
    - FAIL-LOUD (Rule 6 / Rule 12): this run exceeded the 4,000-token per-task budget — **ninth consecutive day** a scheduled task has said so, and the fourth task to say it today. Mitigation applied and disclosed: four parallel sub-agents read ~21 session transcripts at tail depth, keeping transcript bulk out of this context; no multi-MB register was read whole; tails, targeted greps and counts only; all register writes are shell heredoc appends. Backups: `assumptions.md`, `presumptions.md`, `for_lit_search.md`, `open_questions.md`, `decisions.md` → `*.bak.20260804-pre-14eod`. NOT DONE and stated rather than omitted: **no attended session transcript was read, because none exists** (second consecutive day); the four interrupted runs' work is unrepresented; the three owed one-command joins were NOT run here (outside 14a/14b remit, queued); the 26-item legacy retag (ninth run, unauthorised); `decisions.md` and `open_questions.md` deliberately unmodified and the candidates NOT logged, per the standing convention; the REVISE block-count pattern break was measured but NOT repaired, because `revision_flags.md` is outside 14a's write set — second day. **Self-inflicted and repaired within the run:** this run's own append to `for_lit_search.md` incremented the `Status: [QUEUED]` counter by one, because a line describing the counting pattern matched it; detected on post-write measurement, the line was rewritten to break the match, and the count returned to 1,527. Recorded here rather than removed, since it is a live instance of PRESUMPTION-670.
