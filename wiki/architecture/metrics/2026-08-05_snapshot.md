METRICS-2026-08-05:
  Stage: (carried — not recomputed this run)
  PRS triplets (total): **519 — RECOUNTED DIRECTLY TODAY, first direct recount in four days.** `c282-wiki-agent-daily-run`: "Counted directly against the live files today: **519 PRS** (513 across 15 traditions + 6 master) and **90 connections** (distinct IDs through CROSS-090). The 07-31 narrative had it right; 08-03 was the outlier." This SUPERSEDES the "511 carried authoritative" line in the 08-04 snapshot and performs, for the first time, MONITOR-489's settling test — a recount under an explicitly stated counting rule (ASSUMPTION-738). Not retired: the other circulating values from 08-03 (222 / 300 / 364 / 447 / 511 / 513 / 517 / 609), and the master wiki header's own third set of figures (PRESUMPTION-688). The run also named a fourth broken counting rule: "a `^CROSS-NNN:` line-start grep returns 54, because later entries use a different line format. Don't trust that pattern."
  PRS triplets (per tradition): 6 new proposals filed by the Wednesday agents (McGilchrist x4, Kastrup x2 — PROP-2026-08-05-001..006, no collisions). Two candidates seen and REJECTED for lack of evidence; one cross-tradition item filed as a contradiction with Carroll rather than a bridge. **Wright and Rohr now both have `wiki.md` and `prs_triplets.md` on disk (36 and 33 triplets)** — the canonical-works fallback allowance can be retired and Days 099/100 carry now-stale annotations (ASSUMPTION-751, 758, 759).
  Cross-connections (total): **90** (through CROSS-090), recounted today, superseding **165**. Findings: **55** (daily run) vs **56 recorded / 9 flagged** (morning walk) vs **35** (master wiki header) — three values, unreconciled, reported not averaged per Rule 7 (ASSUMPTION-739).
  Cross-connections by type: (not decomposed — fourth day, blocked on the same counting-rule reconciliation, which today gained a named cause.)
  Vault connectivity: no `connectivity_log.csv` row today (weekly, next due 08-09). Level-2 signal stream: **953 signals, 81 pairs, 2026-04-03 -> 2026-07-31, stale_days=5, no staleness WARN, coverage gate PASS (219/219)**, and the rebuild produced a file **byte-identical to the committed blob** — "the stream is genuinely current and the pipeline is deterministic. The six-week freeze that motivated this phase is closed." Recorded as a POSITIVE; the inference from identity to currency is queried at PRESUMPTION-694 (ASSUMPTION-760).
  Approval rate (cumulative): pending **40** measured directly (approved 254, denied 1, needs_review 1) — up from 34; queue trajectory 26 -> 32 -> 34 -> 40 over five days. Review service 0/day, **31st day**. Review-pass gap **13 days** (`review/archive/` 16 files, latest `2026-07-23_decisions.md`). `tools/generate_review_page.py` line 304 UNCHANGED (mtime 2026-05-18) — **ELEVENTH consecutive day named**; blast radius now **40**: "A review pass run now would discard all 34 decisions" (measured by Agent 16 before the day's six filings). Two dates circulate for the last decision email — 2026-07-20 and 2026-05-26, a 55-day spread (ASSUMPTION-740, 741; PRESUMPTION-692).
  Assumptions: max entry ID **765** | **+37 today** (ASSUMPTION-729..765, all UNTESTED) | strict block count **764**; max-minus-blocks offset **1**, restored to the known ASSUMPTION-459 gap. **THE OFFSET WAS 2 WHEN THIS RUN STARTED: `assumptions.md` HAD LOST ASSUMPTION-217.** Present in `assumptions.md.bak.20260804-pre-14eod` and in every backup tested back to 2026-06-05; absent from the live file tonight; still referenced from `presumptions.md`. Restored verbatim by this run. Reported by no run; detected only because the 08-04 snapshot asserted this offset was "exactly accounted for" and it no longer was (ASSUMPTION-764).
  Presumptions: max entry ID **695** | **+18 today** (PRESUMPTION-678..695, all UNTESTED) | strict block count **695**, equal to max ID. The 07-08 P-295 duplicate (PRESUMPTION-507) still unrepaired and still visible only under the loose pattern (loose 696).
  Decisions on record: 78 | +0 today. **THIRTY-FIRST consecutive day with no decision.** `decisions.md` content unchanged since 2026-07-20 (DECISION-078, dated 2026-07-05); its mtime reads 2026-08-05 21:00 with no content change — recorded as a measurement, NOT as an event, because vault mtimes are now known to be bulk-restamped (ASSUMPTION-749). THREE candidates named against the day and NOT logged, per the standing convention against unilateral promotion: (a) the coordinator write-centralisation arrangement — "Queue-file and returns-file writes were centralised in the coordinator rather than performed by 15a/15b/15c" — the second pipeline-execution change in three days recorded only in a run preamble (ASSUMPTION-731); (b) whether `metabolism-regen-daily` moves behind a launchd agent, since it is now established as structurally impossible where it is scheduled (ASSUMPTION-743); (c) the two candidates carried unactioned from 08-04.
  Open questions: max entry ID **139** | +0 today. `open_questions.md` unchanged since 2026-07-28. OPEN-138 and OPEN-139 both **OPEN — awaiting Tom** for a THIRTEENTH day. Candidates surfaced and NOT logged: (a) **whether 14b should continue to operate under line 88 of its own definition**, which the lit pipeline today found to contradict six ACTIVE premises (003, 070, 095, 106, 119, 121) — this is the first candidate that bears on whether this register should exist at its current production rate, and it is asked by the register itself (ASSUMPTION-730; PRESUMPTION-678); (b) whether the 4,000-token rule is satisfiable as written, after ten days of breach reports that presumed it was (ASSUMPTION-757; PRESUMPTION-689); (c) the eternity/timelessness question (FINDING-055), carried unchanged since 07-31.
  Validated premises: max ID **143** | strict block count **101** | **+3 today** — PREMISE-141 (from PRESUMPTION-664), PREMISE-142 (666), PREMISE-143 (668). 143 − 101 = **42**, down from 43 on a +3/+4 day: one block appeared beyond the three new premises, cause unmeasured. PREMISE-114 and PREMISE-124 **UNEDITED ON DAY 12**.
  Monitor queue: max ID **501** | block count **137** | **+0 today — the first zero-MONITOR batch in this series, and it was flagged as such rather than passed over**: "The 15d queue has shown zero consumption for 25+ days; a MONITOR entry there would itself be PREMISE-138 clause (1)" (ASSUMPTION-729).
  Revision flags: max ID **282** | **+5 today** (REVISE-278..282) | **strict block count 118, UNCHANGED FOR A THIRD CONSECUTIVE DAY.** Loose count 141. The heading-style break introduced 08-03 persists; the counting rule has now stayed broken through three days of counting.
  Dispositions: max ID **600** | strict block count **568, UNCHANGED** | +8 issued today, **and NONE of them moved either figure**: today's eight were written only as `(D-601)`…`(D-608)`, so the register of record still counts a max of 600. Surfaced by the evening summary's own verification pass — "the third instance in three days of a register changing its write format while the counting rule that reads it silently stopped tracking" — and re-measured here. Shortfall from max now **32 for a FIFTH consecutive day**, cause still unstated (ASSUMPTION-737).
  Lit-pipeline throughput (2026-08-05): 15a/15b/15c searched and net-evaluated the 08-04 EOD intake **PRESUMPTION-664, 666, 668, 669, 673, 675, 676, 677** — **8/8, zero searched-but-undispositioned**. Mix: **3 INCORPORATE · 5 REVISE · 0 MONITOR · 0 NOVELTY**, D-601..D-608; PREMISE-141/142/143; REVISE-278..282. **TWO batch-level flags:** a **Critical** on 664/666/668/669 (declaration substituted for measurement; a monitor sitting inside its own failure domain — not re-minted, recorded as an enforcement gap inside PREMISE-141 per PREMISE-096), and a **High** on 673/677 (single-authoriser bottleneck, positive feedback, no damping), plus a secondary High on 673/675/676 (record-effect divergence). **FIFTH consecutive day carrying a Critical flag; none of the preceding four has fired an action.** INDEPENDENCE: maintained a second consecutive day — "15a and 15b ran in separate contexts and 15b read nothing in `lit_search_results/for/`." INSTRUMENT DOUBT: carried forward and only partly answered — today's mix is "weak evidence against the 'tilts toward REVISE regardless of input' reading, but it is one run and the three INCORPORATEs all came from the same 15b batch"; REVISE-271's distinguishing study "remains un-run"; MONITOR-498 reported open, not closed (ASSUMPTION-732). UNVERIFIED CITATIONS disclosed in full; one fetch BLOCKED and excluded from provenance; one search "returned nothing verifiable and was omitted rather than guessed at" (ASSUMPTION-733). **ROOT CAUSE NAMED IN 14b's OWN DEFINITION:** line 88 — "false positives are cheap" — "contradicts six ACTIVE premises (003, 070, 095, 106, 119, 121) and had never been flagged" (ASSUMPTION-730). Queue measured: `for_lit_search.md` 15,290 lines before this run's append (15,366 after); **`Status: [QUEUED]` = 1,527 — IDENTICAL for a THIRD consecutive day**, now across nineteen dispositions; all-`[QUEUED]` occurrences 1,803 -> 1,810 after tonight's six queued items, i.e. **the loose counter moved and the headline counter did not, measured within this run**. Backlog drain zero for a **29th** consecutive day, and it remains an open question whether the counter could show a drain if one occurred. Tonight's 14a/14b intake: **PRESUMPTION-678, 685, 689, 690, 691, 695 [QUEUED]** to lit (6 of 18); **PRESUMPTION-679, 680, 681, 682, 683, 684, 686, 687, 688, 692, 693, 694 [IN-HOUSE]** (12 of 18); **ASSUMPTION-729..765 routed [IN-HOUSE]** (37), with ASSUMPTION-738 and ASSUMPTION-758 flagged OBJECT-LEVEL/POSITIVE.
  Deferred items watching: **3** — Agent 16 ran clean on all three intake channels: "Nothing due, nothing resolved, nothing stale." WATCH-002 (Wright) and WATCH-003 (Rohr) at **3 of 6**, checked 08-04 at weekly cadence, next due **2026-08-11**, stale threshold 2026-09-01. The WATCH-002 caption route remains permanently unexercisable while its counter advances (carried from 08-04). `deferred/watch_list.md` **323,120 bytes, up 6,727** from 08-03; archival recommendation renewed. The two undisposed 2026-07-19 proposals still have no record; the `vshC_TxwrVo` provenance fix still needs one paste.
  Scheduler / infrastructure: `scheduler-health-check` RAN AND PRODUCED A VERDICT today, after being one of four runs that vanished without one on 08-04 — **70 registry tasks across 3 files, 11 launchd agents, 79 OK / 1 WARN / 2 FAIL**. `com.c2a2.metabolism-publish` never fired (runs=0), last run 2026-07-21 `FAIL: push rejected` — **15 days**. Phase 6 git commit BLOCKED (sandbox cannot write `.git` objects); commit-failure streak reported as **three days** by one instrument and **four** by two others, unreconciled (ASSUMPTION-736, 761).
  Intra-tradition consensus rate: N/A (pre-Stage 2)
  Cross-tradition survival rate: N/A (pre-Stage 3)
  Health ratio r: N/A
  Notes:
    - **HEADLINE 1 — THE ROOT CAUSE WAS FOUND IN 14b's OWN CHARTER, BY SOMEONE ELSE, ON DAY 118.** The lit pipeline named line 88 of `14b_presumption_detector_agent.md` — "Err on the side of surfacing too many rather than too few. False positives are cheap; false negatives are invisible by definition." — as contradicting six ACTIVE premises (003, 070, 095, 106, 119, 121), and filed a High batch flag on PRESUMPTION-673/677 for a single-authoriser bottleneck with positive feedback and no damping. 14b's definition instructs it to audit the system's premises and contains no instruction to audit itself. Handled tonight by routing 6 of 18 to literature rather than ~11, stated in the register rather than applied silently, with the norm conflict named as an OPEN candidate and NOT resolved (ASSUMPTION-730; PRESUMPTION-678, **Critical**, queued).
    - **HEADLINE 2 — `assumptions.md` LOST A RECORD, AND ONLY AN OFFSET CHECK NOTICED.** ASSUMPTION-217 (KSGA sociogram node panels, 2026-05-22, GROUNDED) is in every backup back to 2026-06-05 and was absent from the live file tonight, while still referenced from `presumptions.md`. The 08-04 snapshot had asserted the strict-block/max-ID offset was "exactly accounted for by the ASSUMPTION-459 gap"; it was 2, not 1. Restored verbatim by this run and recorded rather than silently repaired. The only writers between the two states are 14a's 08-04 append and 15c's status pass; cause unstated (ASSUMPTION-764).
    - **HEADLINE 3 — THE FALSE ALL-CLEAR RECURRED, WORD FOR WORD, THE DAY AFTER IT WAS DOCUMENTED.** `morning-project-status`: "all of this morning's runs fired on time, including the C2A2 wiki daily run… No failures overnight" — against `scheduler-health-check`'s 2 FAIL and `morning-system-health`'s commit failure and 15-day-stale publisher, the same morning. CHANGE-2026-08-04-002 recorded this defect into `architecture/`, a directory that instrument does not read. Third consecutive day, and the clearest evidence yet for OPEN-138 (ASSUMPTION-735; PRESUMPTION-679, High, in-house).
    - **HEADLINE 4 — THE 45-SECOND WALL BECAME AN EPISTEMIC INSTRUMENT.** Four checks were replaced by weaker substitutes or abandoned today on one resource ceiling: `PRAGMA quick_check` skipped ("let torn pages surface as `DatabaseError` — none did"); `extract_agent_node_refs.py` past the cap and replayed under memoization ("output identical by construction"); the metabolism builder timing out at 40s on a 3.7 GB db; and `openstory_ingest_lag.py` exiting 2 = "cannot determine" with a direct DB assertion substituted. Every substitution was disclosed in prose to a reader who was not present; **none was encoded in the artifact the run wrote** (ASSUMPTION-746, 747; PRESUMPTION-680, High, in-house).
    - **HEADLINE 5 — THE PASS-MARK DEFECT IS 3.8x LARGER THAN YESTERDAY'S COUNT.** `raw_ASR_word_count` is duplicated on **125 of 307** transcripts (41%), disabling the ±25% check on each — "Those 125 days are unmeasured, not clean" — against 33 measured on 08-04, of which 33 is a subset. The run refused to clear them: "marking them would retire the tripwire." `fidelity_check.py`'s cold-cache crash was named independently by four separate runs today and remains unpatched on the standing rule that the scripts are Tom's, second day (ASSUMPTION-752, 753).
    - **HEADLINE 6 — A RUN NEARLY REVIEWED AGAINST ANOTHER SESSION'S STATE, AND THE ERROR THAT WOULD HAVE SAID SO WAS DISCARDED.** "`/tmp/rep.json` [was] owned by `nobody` and dated Aug 4 16:17 from a *different* session — my redirect was denied, `2>/dev/null` ate the error, and I read yesterday's queue back and nearly reviewed against it." Second consecutive run to hit it. Two presumptions in one line: that `/tmp` is private, and that an error on stderr is an error seen — the latter being the identical mechanism behind `fidelity_check.py`'s false pass-marks (ASSUMPTION-754; PRESUMPTION-681, 682, **Critical**, in-house).
    - **HEADLINE 7 — ONE COMMAND, NINE ANSWERS, AND THREE IDENTICAL CALLS THAT DISAGREED WITH EACH OTHER.** `report --max 99` returned 42 / 31 / 24 / 31 / 25 / 20 / 15 / 15 / 13 / 13 across today's Summa runs, and three identical calls inside a single run returned **31 / 24 / 31 with disjoint membership**. Every hold, pass and escalation issued today rests on a set that may not be the set. Separately, one run reported a sibling's verdict as "all six passing" against that run's own "0 pass, 6 held" (ASSUMPTION-755, 756; PRESUMPTION-683, 684, in-house).
    - **HEADLINE 8 — THE BUDGET RULE WAS NAMED UNSATISFIABLE, NOT MERELY EXCEEDED.** "the mandatory contract-and-memory reading alone runs past 4,000 before any review starts, so that ceiling isn't reachable for this task as specified." Ten consecutive days of breach disclosures had presumed the fault lay with the runs. Same day: 6 of 11 Summa runs breached 4k, one at ~495k across five parallel subagents; the lit pipeline breached both 4k and 30k and said so — "Surfaced, not hidden" (ASSUMPTION-757; PRESUMPTION-689, High, queued).
    - **HEADLINE 9 — A TASK ESTABLISHED AS IMPOSSIBLE WHERE IT RUNS IS STILL SCHEDULED THERE.** "**The regeneration did not run.** I am not marking this successful." Two blockers: a path-detection miss (one line), and `bwrap --unshare-pid` plus a 45s cap meaning "no multi-minute job can survive here at all." The run argued for its own relocation behind a launchd agent. It also caught its own false positive — `pgrep -f metabolism_monitor` matching the `bash -c` wrapper — and named why its watchdog cannot see it: "the artifact dates itself and is still under 24 h, so the 'did it produce?' question reads OK today and only turns red this afternoon" (ASSUMPTION-743, 744, 745; PRESUMPTION-686, 690).
    - **HEADLINE 10 — THE COUNTERS DID NOT MOVE, AND THIS RUN WATCHED IT HAPPEN TO ITSELF.** Four headline counters are now measured not to move when their contents do: DISPOSITION (568 / max 600, +8 today, none counted), REVISE (118, third day, +5 today), `[QUEUED]` (1,527, third day, +19 dispositions), and `^CROSS-NNN:` (54 against 90 actual). Tonight's own append moved the loose `[QUEUED]` occurrence count 1,803 -> 1,810 and left the headline `Status: [QUEUED]` figure at 1,527 — measured within this run, in the current house write format, and recorded rather than worked around (ASSUMPTION-737, 765; PRESUMPTION-687, High, in-house).
    - **HEADLINE 11 — THE FIRST DIRECT RECOUNT IN FOUR DAYS, AND IT RETIRED NOTHING.** 519 PRS / 90 connections, counted against the live files, with the outlier named and the broken grep pattern named. MONITOR-489's settling test, performed at last. But 511/165 stands in last night's snapshot, the master wiki header carries 35 findings / 6 proposals / a 76-vs-222 PRS table, and nothing was amended — the correction was published into a vault that still holds two other values for the same quantity (ASSUMPTION-738, 739; PRESUMPTION-688, in-house).
    - **HEADLINE 12 — THE DAY'S BEST OBJECT-LEVEL RESULT CAME THROUGH ITS OWN DISCLOSED BLIND SPOT.** PROP-2026-08-05-003 "supplies the missing warrant for the accelerator's core premise: if freedom is width of attention, a tradition locked in one mode is *unfree*, not merely limited." PROP-002 (Wolfram x McGilchrist) is "both a content source and a specimen of the interaction protocol the accelerator is meant to instrument." PROP-004 records McGilchrist adopting Kastrup's formulations verbatim and endorsing Smolin/assembly theory against the block universe — "a clean, citable contradiction with Carroll." And the same run disclosed that "PRS triplets are drawn from publishers' own descriptions and works-cited lists, not from the recordings themselves," one from a paywalled session's host write-up. This register holds ~1,460 items about the machinery and comparatively few about the content it produces (ASSUMPTION-758, 759; PRESUMPTION-695, High, queued).
    - **HEADLINE 13 — EIGHTH DARK DAY, AND FOR THE FIRST TIME BOTH DIRECTIONS FAILED ON ONE FULLY-DIAGNOSED CAUSE.** Chrome is signed out of claude.ai; morning and evening both reached `/login?from=logout`. Both runs refused to write a silently empty artifact — "I wrote a failure note in place of today's summary rather than a silently empty file" — which is the three-valued reporting the systemic flag asked for, used unprompted for a second day. One sign-in fixes both. Seven MCP servers still need OAuth, which no scheduled run can perform (ASSUMPTION-762).
    - **POSITIVE ENTRIES, RECORDED NEUTRALLY:** the nightly verification disclosed **two false results of its own before publishing** — a part-blind Q-number match (17 flags, 16 false: "Q.23 means charity in II-II and predestination in I") and `\bhylomorph\b` failing to match *hylomorphism*, which "silently dropped Day 129 to report a tidy 0/74" and "was caught only because it disagreed with the 08-04 entry" — and shipped a self-test that "fails loudly instead of returning an empty list" (ASSUMPTION-748); it also named two spec checks as "inconclusive by construction" (ASSUMPTION-749), which PARTIALLY RETRACTS ASSUMPTION-728(i) since vault mtimes are bulk-restamped; the daily run reclassified its own recurring failure as structural rather than flaky (ASSUMPTION-742); Agent 16 ran clean and reported nothing due rather than manufacturing findings; the Wednesday agents rejected two candidates rather than "writing PRS triplets I could not evidence" and marked a vendor claim Medium (ASSUMPTION-759); the lit pipeline held independence a second day and stated its residual rather than banking it (ASSUMPTION-732); the watchdog recovered and produced a verdict (ASSUMPTION-761); Wright and Rohr came on disk, retiring a standing fallback allowance (ASSUMPTION-751).
    - **COVERAGE GAP, STATED:** no attended session transcript was read, because none exists — **third consecutive day**, confirmed by two independent methods (a 60-session sweep, and the lit pipeline's own "Orchestration choices made (no human present)"). `agentic-cost-tracker` and `weekly-agent-ecosystem-report` are unrepresented for a fourth consecutive night. The day's record is entirely the self-report of scheduled runs, several of which are today measured to be non-deterministic, cross-contaminated via `/tmp`, or reporting each other's verdicts incorrectly.
    - **CARRIED FOR TOM (nothing from 08-04 was actioned):** Sign in to claude.ai in the extension's Chrome profile — one action, both syncs, **eighth dark day**. Fix `generate_review_page.py` line 304 — one line, blast radius now **40**, **eleventh day named**. Investigate the loss of ASSUMPTION-217 from `assumptions.md` (restored tonight; cause unknown; 15c's status pass is the untested suspect). Fix `fidelity_check.py` (cold-cache crash; line 86 Notes-fallback) and decide the now-125-day unmeasured band. Namespace `/tmp` caches by session — a foreign session's file was nearly reviewed against, twice. Establish whether the 4,000-token rule is satisfiable as written. Establish the queue counting rule — four headline counters do not move when their contents do. Reconcile the commit-failure streak (3 vs 4) and the last-decision-email date (2026-07-20 vs 2026-05-26). Move `metabolism-regen-daily` behind a launchd agent, or accept it will never run. Add the sandbox db path to `_DB_CANDIDATES` (one line). Install `com.c2a2.openstory-log-rotate`; diagnose `com.c2a2.metabolism-publish` (runs=0; `git pull --rebase`; 15 days). Amend the master wiki header (35 vs 56 findings; 6 vs 40 proposals; 76 vs 222 PRS). Retire the canonical-works fallback allowance and the stale Days 099/100 annotations. Correct the wiki root path in the Wednesday agents' SKILL.md — second day. Rule on REVISE-263..282 (twenty open). Ratify or amend PREMISE-114 and PREMISE-124 (day 12). Answer OPEN-138 and OPEN-139 (day 13). **Decide whether 14b should keep operating under line 88 of its own definition** — the pipeline that tests 14b's output says it contradicts six ACTIVE premises. Authorise or decline the 26-item bulk retag — TENTH request. Re-authorise the seven plugin MCP servers. Run Phase 6 git commit on the Mac.
    - **FAIL-LOUD (Rule 6 / Rule 12):** this run exceeded the 4,000-token per-task budget — **tenth consecutive day**. Mitigation applied and disclosed: two parallel sub-agents read ~15 session transcripts at tail depth, keeping transcript bulk out of this context; no multi-MB register was read whole; tails, targeted greps and counts only; all register writes are shell heredoc appends. Backups: `assumptions.md`, `presumptions.md`, `for_lit_search.md`, `open_questions.md`, `decisions.md` -> `*.bak.20260805-pre-14eod`. **NOT DONE and stated rather than omitted:** no attended session transcript was read, because none exists (third consecutive day); `agentic-cost-tracker` and `weekly-agent-ecosystem-report` unrepresented; the owed one-command joins (MONITOR-500 / P-643 / P-645) were NOT run here, fourth day; the 26-item legacy retag (tenth run, unauthorised); `decisions.md` and `open_questions.md` deliberately unmodified and the candidates NOT logged, per the standing convention; the `revision_flags.md` and `lit_search_returns.md` block-count breaks were measured but NOT repaired, both being outside 14a's write set — third and first day respectively. **ONE REPAIR MADE INSIDE 14a's WRITE SET AND DECLARED:** ASSUMPTION-217 was restored verbatim from `assumptions.md.bak.20260804-pre-14eod` after being measured absent; the post-restore offset returned to 1. **REFLEXIVE:** tonight's own append left the headline `[QUEUED]` counter at 1,527 while moving the loose count by 7 — this run is a live instance of the presumption it filed (PRESUMPTION-687), written in the house format rather than worked around, per Rule 11.
