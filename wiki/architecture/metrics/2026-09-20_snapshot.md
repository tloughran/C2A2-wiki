# METRICS-2026-09-20

**Cadence note.** Previous snapshot 2026-09-19 (one day). End-of-day snapshot for 09-20; deltas are
against the 09-19 evening figures. Run began ~23:35 EDT. Sunday.

**Coverage declaration.** No interactive Cowork session on 09-20 — **twenty-first consecutive day**
(ASSUMPTION-1555). **Six sessions read first-hand** by this pass: `C2a2 evening cowork to chat`,
`C282 wiki agent daily run`, `C2a2 sewing agent weekly`, `C2a2 periodic monitor weekly` (15d),
`Openstory agents telemetry refresh`, `C2a2 wiki janitor weekly` — plus **three live probes** of
`C2a2 lit search pipeline`, Agent 16's run read first-hand in `deferred/watch_list.md`, the
`sewing_agent_bootstrap_2026-09-20.md` audit read in full, and a **`find -newermt` mtime sweep** showing
**156 files** touched today, all agent-written. **Not read:** the Summa nightly verification, the
same-shape Summa qc-sweep / commentary-reviewer repeats, the weekly projects backup, the Supabase
keep-warm, and the **still-running** weekly agent ecosystem report.

**The night's finding is that the lit-search pipeline is alive and hung.** 15d said it could not tell a
pause from a stop (ASSUMPTION-1567). Three probes ten minutes apart returned an identical state — 55
turns, same trailing message — so it is neither: the session is running and stationary, and has been
since at least the 17th (ASSUMPTION-1568). Today 23 re-triggers and 149 appended lines of
`for_lit_search.md` were routed into it, and tonight's eleven items follow, with the stall recorded in
the intake header rather than discovered later. **Nothing in the estate reports a hung session**
(PRESUMPTION-1054, OPEN-247).

**The day's other theme is instruments measuring themselves.** Three agents found it independently —
the sewing agent (34% of broken wikilinks inside its own reports, a four-week trend partly self-made, a
published recommendation withdrawn), the janitor (failed unlinks counted as fixes; deltas against the
original baseline re-presenting chronic findings as new), Agent 16 (a figure restated from its own flag's
text). None generalised (PRESUMPTION-1053). This pass applied the finding to **this register** and
reports against interest: 7 wikilinks emitted, 4 broken, all of them syntax examples, ~1% of the vault's
403 — the mechanism is present, the magnitude is negligible, and the mitigation was accidental
(ASSUMPTION-1574).

**First-hand / second-hand ratio (PRESUMPTION-1041, third measurement).** Of twenty-nine assumptions,
**~25 first-hand** (read in the originating transcript or artifact, or measured at the mount) and **~4
second-hand** (reaching this pass only through the evening sync). Comparable to 09-19's ~24/~4. Per
PRESUMPTION-1049's caveat, this counts hands and not roots: the instrument-contamination class has three
independent roots today, which is the first time that distinction has favoured the count.

Chat→Cowork **FAILED** and Cowork→Chat **delivery FAILED**, both paths dead — Chrome extension not
connected (`list_connected_browsers` → `[]`), built-in browser pane signed out. **Eighteenth consecutive
day** by the evening sync's count, seventeenth by Agent 16's — see ASSUMPTION-1565. No designer speech
in six of six — **twenty-first consecutive day** (PRESUMPTION-912).

METRICS-2026-09-20:
  Stage: 1 (unchanged) — ASSUMPTION-1244's challenge unresolved.

  PRS triplets (total): **867** [unchanged, **tenth** consecutive day; verified at source:
    `cat traditions/*/prs_triplets.md | grep -cE '^PRS-[0-9]+:'` → 867. Phase 1 ingested 0 — the daily
    run calls it the **10th** zero-ingest day, the evening sync the **ninth**; see ASSUMPTION-1565.]
  PRS triplets (per tradition): unchanged; not re-served as the total is unchanged and nothing ingested.

  Cross-connections (total): **135** [verified at source with the anchored pattern:
    `grep -oE 'CROSS-[0-9]{3}' master/cross_program_index.md | sort -u | wc -l` → 135.]
  Cross-connections by type: not separately tracked — **39th ask**.
  Connection density: **9.64/14** (135/14), unchanged.
  **CROSS-008 restated final → formal causality** (ASSUMPTION-1560) — narrower scope, stronger support,
    with an independent mechanism from the 09-14 Levin/Cervera microRNA card. Not a count change; a
    content correction, and the only movement in the CROSS layer this week.
  **Attribution drift across the 135 is unmeasured** (PRESUMPTION-1058) — queued as the night's
    highest-value empirical item.
  Conditional, not entered: **CROSS-017 / CROSS-024** — Wolfram ↔ Arkani-Hamed. Still CONDITIONAL
    pending the 09-18 recording (ASSUMPTION-1546); the Wolfram retrieval has now failed three times
    running on the `youtube.com` approval.

  Findings: **90** [unchanged; 24 Active. Not re-counted tonight.]
  L2 signals: **1,501** [unchanged; 87 pairs; coverage gate **PASS 379/379**; staleness **11 days**
    (from 10), no WARN — threshold 21. Agent-stated by the daily run, read first-hand.]

  Approval rate (cumulative): **414 / 415 = 99.8%** [unchanged — no decisions arrived.]
    - **DECISION-083 stands, 24 days** (attended, 08-27). `decisions.md` unmodified by this pass and
      absent from today's mtime sweep (ASSUMPTION-1583).
    - `pending/` **27** (+3), `approved/` **414** — both verified at source. Three cards added since
      Agent 16's morning census of 24: the 09-19 Wolfram card and two 09-20 Rohr cards.
      **Agent 16's corrected leak-card figure was stale by evening, and the evening sync published both
      "pending 27" and "the 24-card page" in one document** (ASSUMPTION-1579) — the failure mode Agent 16
      named this morning, recurring the same day in the document reporting it.
    - Review page `review/2026-09-20_review.html` built, **294,488 bytes**, 27 proposals (verified at
      source); auto-open failed — no macOS `open` in the sandbox. 09-17 page retired to `_superseded/`.
    - Review log: 6,395,563 bytes; cards **482** (+3); address-clean.
    - ingest_ledger: **414 / 382 / 30 / OPEN 1** — transcribed, not re-measured (ASSUMPTION-1404
      continued, ~7 weeks).
    - **Queue age: eleven days, 27 cards, no decision email since 09-09.** Named independently by the
      daily run and the sewing agent as the binding constraint (ASSUMPTION-1562). Retrieval runs
      ~3 cards/day into a blocked step.
    - Wright PROP-2026-08-14-033: the daily run recommends **DENY as source-unretrievable, not
      content-rejected** (ASSUMPTION-1563) — three ntwrightpage posts, same bare-media-embed signature,
      so attempt ten produces the same null. Attempt count disputed **for the second consecutive night**,
      now eight vs nine (ASSUMPTION-1564).
    - Leakage count **sixteen**. Agent 16: 0 due / 0 run / 0 resolved / 0 added; WATCH-003 next 09-22
      (twelfth identical check). **Ruling deadline 2026-09-24 — four days.** Five leak-shaped cards on
      the page; an en-bloc APPROVE swallows all five.

  Lit-search pipeline: **ALIVE AND STATIONARY** (ASSUMPTION-1568). `lit_search_returns.md` last run
    section `## 2026-09-16 RUN` at line 48078 — **four-day gap**, verified at source. 15d queued 23 first
    re-triggers (MONITOR-585..599, -601..-608; 8 literature / 15 empirical) and advanced ~205 weekly
    carry-overs to 09-27 without re-queueing. 3 lane exits (MONITOR-404 → REVISE-477, MONITOR-415 →
    REVISE-478, MONITOR-403 cycle 1→2). `for_lit_search.md` +149 lines today before this pass;
    `monitor_queue.md` +267.
  **Counting-method divergence, recorded rather than reconciled** (ASSUMPTION-1575): the evening sync
    publishes 2,813 QUEUED / 4,160 SEARCHED / 372 MONITOR entries; anchored line counts by this pass give
    2,630 / 2,140 / 276. Neither is wrong; neither document states its method. Third consecutive night a
    headline count proves method-dependent (cf. ASSUMPTION-1548, the CROSS anchoring defect).

  Validated premises: **170 total, 159 ACTIVE**; **100 past `Re-check due`** (89 last week), **19 carry
    no due date** — not the 4 previously reported. The 09-13 run's report of advancing PREMISE-145/-147/
    -148 is contradicted by the file: **a report of a write is not a write** (ASSUMPTION-1570).
  Monthly INCORPORATED re-check: did not fire (day 20). **Second consecutive monthly catch-up flag.**

  Connectivity (weekly sewing, verified at source): row `2026-09-20,4287,744,86,5117` against
    `2026-09-13,4231,738,82,5051` — orphans **+56**, sparse **+6**, connected **+4**, pages **+66**.
    10 pages sewn, **61 agentic calls** across 14 thinkers, **16 bridge notes** (two seeding previously
    empty files: `hoffman_loughran`, `kastrup_loughran`), **zero collateral movement** confirmed by
    post-run census (ASSUMPTION-1577). **The `connected` column carries the instrument defect**
    (ASSUMPTION-1559) and has not been corrected for it.
  **Clean broken-link rate, instrument excluded: 11.0% (266/2,416), not the reported 15.8%**
    (ASSUMPTION-1557). Orphan headline: `architecture/` 3,348 of 4,287 (78%) + `inbox/` 796 = 96.6%;
    curated vault's real orphan population ~143 and improving invisibly. **Thirteenth filing, and the
    agent declined to file a fourteenth** (ASSUMPTION-1581).

  Infrastructure: OpenStory refresh **failed a third consecutive time** — `Errno 28`, `open-story.db`
    **7.0 GB** against 2.5 GB free on `/` and **0 bytes** on the `/sessions` filesystem `TMPDIR` points
    to; `agent_node_edges.json` **3 days stale**, silently frozen since 09-17; two orphaned
    `os_agent_local_*.db` files (196 MB, 1.65 GB, 09-17, `nobody:nogroup`) unremovable and adding
    pressure (ASSUMPTION-1572). **Third data point for OPEN-244**, and PRESUMPTION-1059 asks whether the
    substrate is even the variable.
  Janitor run #30, exit 0: real auto-fix count **13, not 16** — three `.DS_Store` unlinks failed
    `Operation not permitted` and were counted as fixes; `src_pub_refs_drift ×7` is structural
    (`sync_vault.sh` syncs only `transcripts/` and `synthesis/`); the "337 new broken wikilinks" headline
    is chronic, not a regression (ASSUMPTION-1573).
  **mtime is not a reliable write-time signal here**: 44 files share the exact mtime 2026-09-17 15:55
    (ASSUMPTION-1576), after last night's 134-file restamp window.

  Assumptions: **1,583 headers | max ASSUMPTION-1584 | 30 added (1555–1584)**. [colon-anchored:
    `grep -cE '^ASSUMPTION-[0-9]+:'` → 1,583 headers, 1,583 distinct, **zero duplicates**.]
    Tested: 0 returned from 15a/15b this pass — **and now known to be because the reader is hung, not
    because it is slow.** Supported 14 · Challenged 5 · Untested 11.
    **One entry answers a question another agent stated it could not answer** (1568 against 1567).
    **Three entries are one defect** (1564, 1565, 1575 — an absent counting convention).
    **One entry turns the day's headline finding on this register and reports against interest** (1574).
    **One entry records this pass committing, in its own verification step, the defect it had filed
    against other runs an hour earlier** (1584).
    **Six entries record positives**: 1558, 1563, 1566, 1569, 1577, 1578.
  Presumptions: **1,060 headers | max PRESUMPTION-1060 | 9 added (1052–1060)**. [colon-anchored:
    `grep -cE '^PRESUMPTION-[0-9]+:'` → 1,060 headers, 1,060 distinct, **zero duplicates**. The figure
    first written here was 1,064, from an unanchored count — **this pass's own instance of the defect
    it filed tonight as ASSUMPTION-1575; corrected in place and recorded as ASSUMPTION-1584.**]
    epistemic 3 · methodological 3 · structural 2 · normative 1. **No Critical-risk item — sixth
    consecutive night.** High **4** (1053, 1054, 1058, 1060) — highest in a week, and three of the four
    concern the estate's instruments rather than its subject matter. **One self-referential** (1060).
  Open questions: **max OPEN-247 | 3 added (245–247)**. **OPEN-245 needs Tom** — it asks whether the
    ASSUMPTION marker still means what the provenance protocol says it means after twenty-one designer-
    silent days. OPEN-246 is a command (write the counting convention down). OPEN-247 asks for an owner.
  Lit search queue: **11 items queued** (4 assumptions, 7 presumptions), 4 of them in-house empirical
    needing no literature, 1 carrying a **pre-registered** success criterion stated before the instrument
    is built (PRESUMPTION-1057's back-test). All queued into a lane recorded as stalled.

  Intra-tradition consensus rate: N/A until Stage 2.
  Cross-tradition survival rate: N/A until Stage 3.
  Health ratio r: N/A until both rates available.

  Notes: **Three agents independently discovered that their instruments are inside their own
    measurements, and none of them said so in general.** That is the trajectory observation for tonight.
    The estate's self-auditing is working well at the level of the individual agent — a false alarm was
    withdrawn, an assignment was retired on evidence, an evidence standard was upgraded mid-run, errors
    were disclosed rather than quietly fixed — and not at all at the level of the network, where no one
    owns the question "which other instrument has this defect?" The second observation is that the
    defects found today are overwhelmingly **measurement** defects, not **subject-matter** defects: the
    traditions layer has been frozen for ten days and the only movement in it, CROSS-008's restatement,
    was an improvement won by weakening a claim. The system is currently learning much more about itself
    than about its subject, which is what a blocked review queue and a hung literature pipeline predict.
