# METRICS-2026-08-25

**Cadence note.** Normal on-cron nightly snapshot. Deltas stated against the **08-24 snapshot**.
This run read **seven** of the day's sessions directly and used the 18:40 `cowork_to_chat` summary as
a secondary source for four it did not open; coverage is declared at the foot of this file and must
not be mistaken for completeness.

METRICS-2026-08-25:
  Stage: 1 (unchanged; no intra-tradition consensus mechanism live)

  PRS triplets (total): **642** [carried from the 08-23 addendum's connectome measurement]
    - `wiki/prs_3d.html` measures **890,564 bytes, mtime 2026-08-24 13:30** [measured] — **byte-identical
      to the 08-23T20:00:47Z build and to last night's reading.** No connectome regen ran (weekly).
      Second consecutive night of an unchanged artifact; the mtime is the mount/sync stamp, not a rebuild.
    - the 630 / 636 / 642 three-way ambiguity was **not touched today**. PRESUMPTION-822 — that no
      register records which build a triplet figure refers to — is in its **eleventh consecutive night.**
  PRS triplets (per tradition): not derived — **19th ask** for measurement authorisation
    (ASSUMPTION-1118, ASSUMPTION-1187).

  Cross-connections (total): **103** [carried, not re-measured]
    - the nightly verification resolved **47 distinct CROSS ids** and **9 distinct FLAG ids** across the
      complete series [measured by that run]. It also reports **CROSS-090..103 and nine FLAGs
      un-back-cited corpus-wide** — a new figure, and one whose status as a finding rather than as work
      outstanding is exactly PRESUMPTION-882.
  Cross-connections by type: not separately tracked.
  Connection density: 103/14 = 7.36 [carried, not re-measured]

  Approval rate (cumulative): 301 approved / 302 decided = **99.7%** [unchanged — **no dispositions**;
    last batch 2026-08-07]
    - **pending 74** [measured — `inbox/proposals/pending/` holds 74 `.md` files]. **Exact** against the
      daily run's stated 74 and the review page's 74 cards.
    - trajectory as measured: 10 → 27 → 35 → 37 → 47 → 53 → 54 → 56 → 60 → 60 → **74**
    - **the largest single-day rise in the series: +14, +23%.** Yesterday's flat reading was a 529 error
      plus a throttle summing to zero; today the throttle was not applied and fourteen proposals landed
      across Levin (3), Friston (3), Rohr (3), Wright (2), Stump, Hawkins, Carroll. **The gate moved
      backwards on the day every automated stage around it turned green.** Filed as PRESUMPTION-883 and
      OPEN-171.
    - **disposition gap: 18 days** [measured, last batch 2026-08-07] — up from 17.
    - **review-page gap: 0 days — CLOSED, second consecutive day.** [measured]
      `review/2026-08-25_review.html`, **872,376 bytes at 04:53**, **74 cards against 74 pending files,
      zero phantoms** (ASSUMPTION-1220, SUPPORTED on direct measurement).
    - the 08-11 review-page ID fix remains unpushed on **day twenty-five**

  Assumptions: **1,220 total** | 0 tested by literature this run | supported 2 (1217, 1220)
      | challenged 3 (1206, 1213, 1215 — and 1215 challenges 1196 and 1204 by reference)
      | grounded 1 (1214) | novel 0
    - **16 added tonight (ASSUMPTION-1206 … 1221)**
    - **[measured, strict pattern] 1,220 unique ids / 1,220 headers / max 1221 / offset exactly 1** —
      the long-known ASSUMPTION-459 gap, **stated as known, not as a finding.**
    - **Note on the status column, restated from 08-24 because it still applies.** Six of tonight's
      sixteen carry a status other than UNTESTED and **every one was assigned on internal evidence** —
      one register or transcript checked against another within the same day. **No literature was
      consulted by this run.** These are not 15a/15b dispositions and must not be counted as such.
    - **One genuine retirement:** ASSUMPTION-1214 marks `length_actual_words` GROUNDED on seven exact
      readings across two days. Retirements are rare in this register; most items accumulate.
    - **One genuine falsification:** ASSUMPTION-1215. The git-debris hypothesis was named, its cure
      predicted, the cure occurred, and the behaviour did not change. **No replacement hypothesis was
      offered.** The hypothesis space for the missing daily-run commit is now empty.

  Presumptions: **884 total** | 0 tested this run
    - **8 added tonight (PRESUMPTION-877 … 884): 2 High-risk (877, 880), 5 Medium-High
      (878, 879, 881, 883, 884), 1 Medium (882). Zero Critical** — the first night in the recent record
      with no Critical-risk presumption, against four last night.
    - by type: **epistemic 3** (877, 880, 884) · **methodological 2** (878, 881) · **structural 2**
      (879, 883) · **scaling 1** (882) · normative 0
    - **[measured, strict pattern] 884 unique / 884 headers / max 884 — no gap, no collision.**
    - **A scaling presumption returned** (882) after last night's zero. Today's material bore on N for
      the first time this week, because the Summa series reached completion.

  Open questions: **125 unique | max OPEN-171** [measured, strict pattern]
    - 3 added tonight (OPEN-169, 170, 171)
    - **the 46-id offset is carried, unchanged and undiagnosed** for a third consecutive record
      (122 unique / max 168 before → 125 unique / max 171 after; offset still **exactly 46**).
      **"Max OPEN-171" is not a count of open questions.** Third record to state it, still not filed.

  Decisions: **DECISION-078, dated 2026-07-05** [measured] — unchanged. **Day 51.**
    **No DECISION entry written tonight. Nothing today was decided by Tom.** `decisions.md` has not
    been written since 2026-08-06 and this run did not write to it, per the standing convention that
    agent-side scope choices are not decisions. Three such choices were made and disclosed today —
    the QC sweep stopping at one pair, the reviewer holding Day 76 rather than pass-marking it, and
    the moving of Openstory telemetry onto launchd. Whether these belong in the register is OPEN-158,
    unresolved, **fifth recurrence.**

  Validated premises: max **PREMISE-1536** [measured across `architecture/*.md`]. Note: this figure is
    an id maximum drawn by pattern across the directory, **not** a count and **not** the same measure
    as the 08-24 snapshot's "max PREMISE-181", which was carried rather than derived. The two are not
    comparable and the discrepancy is recorded rather than reconciled — a re-derivation with a stated
    method is needed and is not in this run's remit.
  Revision flags: max **REVISE-392** [measured] — up from 369. REVISE-392 is the escalation of the
    26-item 2026-07-21 cohort.
  Monitor queue: max **MONITOR-553** [measured] — up from 544.
  Deferred watch list: **2 active, both flagged STALE** (WATCH-002, WATCH-003) — **the first
    STALE-WATCH-FLAGs in that agent's history.** Both checks fired (count 5 → 6), both conditions
    unmet, both recommend **Escalate to Tom** rather than Cancel or Continue (ASSUMPTION-1218).
    Next checks 2026-09-01. `watch_list.md` is **~494 KB, past the Read-tool ceiling** — Agent 16 now
    works entirely through shell greps and the run-log split is the most overdue maintenance item.

  Lit search: **15a/15b/15c all fired overnight, 00:47–01:00, and the intake lane is drained.**
    - 26 items searched by 15a, 33 by 15b, **35 dispositioned by 15c**. **Zero bare-[QUEUED] remain.**
    - **59 result files written under `lit_search_results/{for,against}/` today** [measured] — covering
      ASSUMPTION-1160…1203 and PRESUMPTION-690…876.
    - **14 items queued tonight** by this run (6 from 14a, 8 from 14b), of which **4 are Critical**
      (ASSUMPTION-1206; PRESUMPTION-877, 880, 881) — one from 14a and three from 14b. Five
      in-house-testable items were **deliberately not queued**; they need a measurement, not a
      literature search, and are named in the changelog.
    - `for_lit_search.md` contains **2,038 `[QUEUED]` markers** [measured] against 2,023 before tonight
      — **+15, of which 14 are items and one is the phrase "zero bare-[QUEUED]" inside this cohort's
      header note.** This is the whole-file historical marker count, **not** the residual-unsearched
      figure, which as of the 00:47–01:00 run is **zero**.
    - **The 26-item 2026-07-21 cohort is now 35 days old.** It was escalated as REVISE-392 rather than
      re-filed for a seventh time. Tonight's intake **did not re-file it** and records only that this
      cohort must not be admitted ahead of it.
    - **One unresolved contradiction left visible inside that run**, per PREMISE-006: 15a reports
      Frese & Fay (2001) obtained in full text; 15b reports it unobtainable at every indexed OA
      location, the same day. One fetch settles it and either outcome is informative.

  Intra-tradition consensus rate: N/A until Stage 2
  Cross-tradition survival rate: N/A until Stage 3
  Health ratio r: N/A until both rates available

  **Fleet-level figures, this window:**
    Scheduler state [stated by the health check via the 18:40 summary; **not verified from this
      session's mounts** — `scheduler_health.md` is not at the expected path under `architecture/`]:
      **85 OK / 1 WARN / 2 FAIL**, against 79/1/4 on 08-24 and 63/1/20 on 08-23. **Four FAILs cleared
      overnight**: the missed-fire storm, `com.tloughran.summa-vault-sync` exit-1, the 12-day
      `metabolism_data.json` staleness, and the git debris. **The recovery from the 08-22/08-23 outage
      held for a second day.**
      Standing FAILs: `com.c2a2.metabolism-publish` exit 1 (**second Sunday running**) ·
      `com.tomloughran.openstory.watchdog` — **new today.**
    Daily run: **fired 08:34Z and completed every phase — and committed nothing.** `commit_check`
      reports the newest `C2A2 daily run` commit is still **2026-08-24 17:30Z**. **Eleven of the last
      twelve days had no daily-run commit.** The git-debris hypothesis is **dead** (ASSUMPTION-1215):
      the debris cleared and the behaviour did not change. **Cause unknown and no replacement
      hypothesis named.** This is the register's cleanest falsification to date.
    OpenStory: **NEW FAILURE.** `com.tomloughran.openstory.watchdog` FAILED at 05:15 —
      `/Volumes/H-Drive` not mounted. Session creation stops within ~24h of the volume going away and
      **a restart cannot fix it** (ASSUMPTION-1216). **The only item in today's record with a clock
      on it.**
    Openstory telemetry: **moved onto launchd and proved.** `REFRESH_STATUS` logs
      `2026-08-25T17:40Z PASS — telemetry 33 agents, node_edges current, DB age 6h`. The apparently
      hung run was block-buffered, not stalled (ASSUMPTION-1217, SUPPORTED). **Open race, stated in
      the same breath and unresolved: the Cowork scheduled task and the launchd agent now both write
      the same files and should not both be enabled.**
    Summa: **307 synthesis files verified — Days 001–307, the complete series, for the first time.**
      979 attributable PRS references, 9 FLAG ids, 47 CROSS ids, all resolving. **Zero dead citations.**
      Also: 146 mtime-drift items, 4 convention violations (3 authorship — one *new tonight* in a file
      rewritten today at 12:25 — and 1 metaphysical-guardrail, Day 129, carried), 226 length items of
      which 21 reviewable. **Three Summa working passes ran** (reviewer 18:25–18:32 and 22:24–22:35,
      QC sweep 22:35): **7 pairs read across the day, 8 repaired, 2 held, 1 escalation, 0 escalations
      from the late reviewer.** QC queue: **307 pairs, 99 flagged, 98 of 99 for staleness alone.**
    Notification channels: **0 available — fourth consecutive Chrome failure**, both directions,
      08-24 morning/evening and 08-25 morning/evening. `list_connected_browsers` returned empty on
      every attempt. Chat has had no Cowork context since 08-23; Cowork no walk context for the same
      span. **The loop is open at both ends for two full days** (OPEN-168, PRESUMPTION-874).
    MCP authorisation: **seven servers report needing interactive OAuth and are unavailable to any
      scheduled run** — Atlassian, Figma, Intercom, Linear, Notion, Slack, Datadog. Stated by two
      independent runs today.
    Attended sessions: **0. Twenty-fifth consecutive unattended day.**
    Attended DECISIONs: **0 — fifty-first consecutive day.**
    Runs that completed by reporting their own total failure: **2** — morning chat scrape, evening
      chat delivery (PRESUMPTION-867).
    Self-declared budget breaches: **4** — nightly verification (~10x the 4,000-token task cap),
      QC sweep (per-task and per-session), deferred action monitor, **and this run.**
      **Fifteenth consecutive breach on the Summa cap specifically.** (ASSUMPTION-1221,
      PRESUMPTION-881.)

  Notes:
    - **The dominant fact of this window is that the machine got healthier and the gate got worse.**
      Scheduler 79/1/4 → 85/1/2. Lit-search intake drained to zero for the first time in the record.
      Telemetry fresh and on launchd. Review page reconciling exactly, second day. Git debris — named
      yesterday as the #1 next step — cleared on its own. **And pending went 60 → 74**, the largest
      single-day rise ever recorded here, on day seventeen of the review gap. Every stage that can
      clear its own blockers did so, and the one stage with a human in it took delivery of a 23%
      larger pile.
    - **Yesterday's #1 hypothesis died cleanly.** The git-debris theory predicted the commit would
      land once the debris cleared; the debris cleared and the commit did not. That is a proper
      falsification — the first in this register in some time — and it leaves the missing daily-run
      commit with **no** live explanation on day eleven of twelve.
    - **The citation layer has a hole that tonight's headline number cannot see.** "Zero dead citations
      over 979 references" and "five citation defects found by hand today" are both true and describe
      disjoint classes. Every one of today's five defects resolves. The Day 237 case — a frontmatter
      phrase, "Stump corporate-substance node", with no id in it — is the limiting case: **it cannot
      fail an id-existence test because there is nothing in it to test.** OPEN-169, PRESUMPTION-877.
    - **A generator-level hypothesis appeared for the first time and nobody picked it up.**
      ASSUMPTION-1211 — "the writer assembles a plausible gloss from two real neighbouring fields",
      demonstrated by the "everything belongs" splice — relocates the label-trap series from a defect
      log to a claim about how the defect is produced. Five runs have now named four downstream checks
      and built none. The remedy space has one member (PRESUMPTION-878).
    - **Three diagnosed fixes were declined on remit grounds in one day**, each disclosed by the agent
      that declined it, each addressed to a gate that has not moved in seventeen days. Day 76's
      fidelity failure (ratio 0.610) has now been read past by two agents on day eight. Every agent
      followed its definition exactly. That is OPEN-170, and it is the mirror of OPEN-164.
    - **The instrument-artifact pattern reproduced for a third consecutive night.** 08-23/24: dated
      verdicts read as current state (PRESUMPTION-876). Tonight: the tier rule possibly computed off
      truncated transcripts (ASSUMPTION-1207); a blanket cold-cache attribution over a population
      known to contain a true positive (ASSUMPTION-1209, PRESUMPTION-884); and the length clause
      firing on citation apparatus rather than argument (ASSUMPTION-1213). **Four apparent findings in
      one night that are properties of the measuring device.** This is now the most reproducible result
      in the register and it deserves to be read as such rather than as four separate items.
    - **Six consecutive downward corrections, and the question of why is under-specified.**
      ASSUMPTION-1219 offers two readings; PRESUMPTION-880 argues a third is live and unexamined —
      that the repair pass, which proposes, applies and scores its own corrections with no independent
      check, may itself be directionally biased. This matters because ASSUMPTION-1219 explicitly routes
      the answer to **OPEN-165**, the register's most consequential standing question. OPEN-165 should
      not be adjudicated on a sample whose bias has not been tested.
    - **This run's own breach**, per Rules 6 and 12: over both the 4,000-token task budget and the
      30,000-token session budget. **No subagents were used**, so REVISE-350 (shared model family
      across nominally independent readers) does not apply. **Coverage:** seven sessions read directly
      — Summa 2026 nightly verification, Summa commentary reviewer (late), Summa qc sweep (late),
      C2A2 deferred action monitor, C2A2 morning chat scrape, C2A2 evening cowork-to-chat, plus the
      18:40 `cowork_to_chat` summary read as a document. **Sessions NOT read directly and covered only
      through that summary:** the daily run, the scheduler health check, the openstory telemetry
      refresh, the earlier Summa reviewer and QC passes, the Supabase keep-warm, the metabolism regen,
      the Hawkins/Hoffman tradition agent, and the three morning sessions. **Stated so the coverage is
      not mistaken for completeness, and so that any figure above sourced to the summary is treated as
      second-hand.**
    - **One measurement disagreement this run did not resolve.** `max PREMISE-1536` derived tonight by
      pattern across `architecture/*.md` against `max PREMISE-181` carried on 08-24. The methods differ
      and the figures are not comparable. Recorded rather than reconciled; a re-derivation with a stated
      method is needed.

PROVENANCE:
  Origin: 14a
  Chain: [14a]
  Original item: METRICS-2026-08-25
  Item type: METRICS SNAPSHOT
  Transform at each step:
    14a: Assembled from seven session transcripts read directly, one same-day summary document, and
      eleven direct measurements of the vault (strict-pattern id integrity on three registers; pending
      file count; review-page size and mtime; prs_3d byte size; for_lit_search marker count; today's
      lit-result file count; REVISE/MONITOR/PREMISE id maxima). Marked every carried figure as carried,
      every measured figure as [measured], and every second-hand figure by its source. Declared its own
      coverage limit, its own budget breach, and one measurement disagreement it could not resolve.
  Current status: RECORDED


---

### POST-PROVENANCE VERIFICATION NOTE — 2026-08-25

Run after the snapshot and changelog above were sealed. **Nothing above required correction.**

**Verified clean this run** [measured, strict pattern `^ID-[0-9]+:`, the method established by the
08-24 note after the loose pattern produced three phantom collisions]:
- `assumptions.md` — **1,220 unique / 1,220 headers / max 1221 / offset 1 / zero duplicates.** The
  offset is the long-known ASSUMPTION-459 gap, **stated as known, not as a finding.**
- `presumptions.md` — **884 unique / 884 headers / max 884 / offset 0 / zero duplicates.**
- `open_questions.md` — **125 unique / 125 headers / max 171 / offset 46 / zero duplicates.** The
  offset is **pre-existing and carried**, unchanged by tonight's three additions, and reproduces the
  08-23 and 08-24 figures exactly. **Third consecutive record to state it and not file it.**
- **All 24 items added tonight** (ASSUMPTION-1206…1221, PRESUMPTION-877…884) carry a complete
  PROVENANCE block — Origin, Chain, Original item, Item type, Transform at each step, Current status —
  plus every field their agent definition requires. **Programmatically verified, zero omissions.**
  All eight presumptions additionally carry `Evidence it was operative`, `Why it was unstated`,
  `Risk if wrong`, and the mandatory `[inferred]` tag on the statement.
- **All 14 `LIT-QUEUE-2026-08-25` items** carry `[QUEUED]`, a literature question, and a single-line
  PROVENANCE header naming origin, chain, original item, item type, transform and status.
  **Zero omissions.**
- `for_lit_search.md` **ends on an item block**, per the parser hazard that file documents — correcting
  the 08-24 run's deliberate deviation, which ended on a prose block.
- Pre-run backups written for four registers with suffix `.bak.20260826T034056Z-pre-14eod`.

**Not verified**, and stated so: no figure carried from a prior snapshot was re-measured tonight
(PRS triplets, cross-connections, connection density). `scheduler_health.md` was not found under
`architecture/` on this session's mounts, so **every scheduler figure in this snapshot is second-hand**
from the 18:40 `cowork_to_chat` summary. The `PREMISE` maximum disagreement between tonight's
pattern-derived 1536 and 08-24's carried 181 is **recorded and unresolved**. Seven of the day's
sessions were read directly; the rest are named in the changelog.
