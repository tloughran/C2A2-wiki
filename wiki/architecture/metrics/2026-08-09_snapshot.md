METRICS-2026-08-09:
  COVERAGE: single day, 2026-08-09 (Sunday, tradition day). **35 sessions**, boundary set at the prior self-awareness run (`local_a3742efa`). **Four of the 35 produced no summary** — the ecosystem report, the wiki daily run, a Summa reviewer, and the lit pipeline (ASSUMPTION-862). Their content is represented only as far as partial transcripts and on-disk artefacts support; no state is imputed. `read_transcript` returns assistant text and tool-call names but **not tool outputs**, so every figure below is *as an agent stated it* except where marked [measured] (ASSUMPTION-921).
  Stage: (carried — not recomputed this run)
  PRS triplets (total): **THREE LIVE FIGURES, REPORTED NOT AVERAGED (Rule 7): 528, 530 (both 08-08) and 615 (today).** The connectome run: "**519 -> 615 triplets** (cross 90, coils 32, findings 62, generative 23). All validation gates passed." **Those sub-counts sum to 207**, unreconciled to 615 or to the +96 delta, and no field distinguishes what each figure counts (ASSUMPTION-916). **Not published** — manual step, Tom's, on the Mac. The publish recipe opens with `rm -f .git/index.lock .git/refs/heads/main.lock`, embedded as routine.
  PRS triplets (per tradition): **NO INGESTION TODAY.** The wiki daily run died after Phase 0 with **41 approved proposals across 13 traditions** dispatched to eight subagents and never logged; `PROCESSED_LOG.md`'s newest rows remain 2026-08-08 (ASSUMPTION-864) [measured]. New proposals filed: **2 (Rohr)**, both to `pending/`; **0 Wright, deliberately** (ASSUMPTION-917). Four of ten sewing-agent pages carried explicit do-not-ingest-yet gates (ASSUMPTION-911). `inbox/proposals/`: **301 approved, 4 pending**; oldest unprocessed inbox files date to **2026-04-07** [measured].
  Cross-connections (total): **90**, carried; no CROSS or FINDING minted (the run that mints them died). **The register's interpretation is in open contradiction**: MC0001 was recorded 08-08 as "the first external venue at which three tracked traditions are co-present" and today as "**a competitor, not a corroboration. The wiki holds none of the three MC0001 talks**" — same evidence, opposite valence, one day apart (ASSUMPTION-910; PRESUMPTION-747, High, queued).
  Cross-connections by type: (not decomposed — eighth day.) Candidate audit criterion carried from 08-08 and now sharpened by the MC0001 reversal: of the ninety, how many are joined by contact in the world rather than by C2A2's own inference?
  Vault connectivity: **TWO SAME-DAY CENSUSES DISAGREE ON EVERY FIELD.** Bootstrap (04:09): **3,994 / 3,281 orphan / 657 sparse / 56 connected; 2,116 wikilinks; 188 unresolved / 42 targets.** Sewing weekly (later): **3,995 / 3,272 / 666 / 57; 2,132 wikilinks; 204 unresolved / 42 targets.** Reconciliation offered: "**these figures agree… to within resolver tolerance; neither is drift**" — a tolerance never derived, and one that absorbs three zero-byte stubs filled between the two runs (ASSUMPTION-914; PRESUMPTION-741, queued). Orphan series: 2,483 -> 2,567 -> 2,759 -> 2,943 -> 3,083 -> **3,272**. Growth by directory: `lit_search_results` +86, `inbox` +70, `daily_sync` +14. **106 of 188 broken links are bare thinker names** across 24 variants; 14 alias files would convert them and need a human yes. **STRUCTURAL FINDING, retracting last week's guess:** all 307 Summa Contemporary pages carry **exactly one backlink, from their own transcript**; 306/307 transcripts likewise; **614 pages = 15% of the vault**, rewritten **282 times by `sync_vault.sh` since 08-02**. "**The sync is actively maintaining content it never wires in**" (ASSUMPTION-913; PRESUMPTION-749, queued). Backlink column: **`bl 0->0` on every page, every week, six runs** (ASSUMPTION-906). OpenStory telemetry **PASS with a disclosed provenance substitution** — `_meta` has no `generated` key, mtime used per the stated fallback; 33 agents injected, 27 agent_nodes, DB frontier 1.5h, roster **32/33**, **28 discovered agents pending curation** (ASSUMPTION-895).
  Approval rate (cumulative): **`approved/` 254 -> 301 (+47); `pending/` 47 -> 2.** Review-pass gap closed by the 08-08 overnight pass, the first in 16 days. **The line-304 diagnosis was retracted and replaced:** the offset theory "should be retired as stated"; the real defect is that `submitDecisions()` looks up a synthetic positional key, so "**the export can only ever emit `PENDING`**… **not mislabeling, it's total loss of the payload**," and `notes[pid]` drops in the same line. Evidence: 47/47 DOM-order match, 47/47 frontmatter match, 47 cards / 188 buttons paired. **Consequence: "the 47 APPROVEs didn't come from your clicks"** — provenance unresolved, source email unread, and the 47 are already ingested (ASSUMPTION-873, 874). Line 304 **UNCHANGED — fourteenth-plus consecutive day named**, now with a precise cause and a 2-item test bed standing ready.
  Assumptions: max entry ID **921** | **+60 this run** (ASSUMPTION-862..921, all UNTESTED) | strict block count **920**; max-minus-blocks offset **1**, the known ASSUMPTION-459 gap, unchanged. No losses detected [measured].
  Presumptions: max entry ID **750** | **+15 this run** (PRESUMPTION-736..750, all UNTESTED) | strict block count **750**, equal to max ID [measured]. The 07-08 P-295 duplicate persists in the loose count.
  Decisions on record: max ID **78** | **+0. THIRTY-FIFTH CONSECUTIVE DAY WITH NO DECISION.** Ten candidates named and not logged (enumerated at ASSUMPTION-920): the canonical-file decision (now blocking a **fifth** tradition); line 304; retire-or-repurpose `summa-2026-daily-batch` **and** the seventh-firing "ONE-TIME" bootstrap audit; `/tmp` namespacing (sixth day); the Summa `length_tier` call (**seven weeks**); the batch repoint for the seven held pairs; authorisation for an agent to run in-house measurements (REVISE-259 / MONITOR-496); the 14 alias files; the control arm; the Carroll vault-wide sweep.
  Open questions: max entry ID **139** | +0. Unchanged since 2026-07-28. OPEN-138 and OPEN-139 **OPEN — awaiting Tom** for a **SEVENTEENTH day**.
  **REGISTER MEASUREMENT — INSTRUMENT DEFECT, SURFACED (Rule 12):** 14a's block-count instrument gives a different answer under a one-character pattern change on every register but one. Strict (`^ID-NNN:`) / line-start-any (`^ID-NNN`) / max id, measured this run: **DECISION 47 / 76 / 78 · OPEN 93 / 133 / 139 · REVISE 118 / 153 / 294 · MONITOR 137 / 144 / 504 · PREMISE 105 / 106 / 149 · DISPOSITION 583 / 667 / 629 · PRESUMPTION 750 / 751 / 750 · ASSUMPTION 920 / 920 / 921.** DISPOSITION's line-start count **exceeds its maximum id by 38**. The 08-08 "register decay" headline (ASSUMPTION-859) used the strict pattern alone; it is **not retracted** — the strict counts are real — but it is now known to be pattern-sensitive and no reader was told (ASSUMPTION-918). Eighth instance in five days of the PRESUMPTION-687 class, this one in the self-awareness pipeline's own instrument.
  Lit-pipeline throughput (2026-08-09): **ZERO. The run died before writing.** "Launching 15a and 15b as **separate agents in separate contexts** — this run achieves architectural, not merely procedural, independence" — the exact defect flagged on 08-08 — then `[Request interrupted by user]` at "Both searches returned. Writing result files and running 15c." Verified: **zero** `SEARCHED-15a: 2026-08-09`, `SEARCHED-15b: 2026-08-09`, `DISPOSITIONED-15c: 2026-08-09`; newest `lit_search_results/` files Aug 8 21:00 [measured]. **Backlog drain zero for a THIRTY-THIRD consecutive day** — today because the run died, not because the queue resisted (ASSUMPTION-865, 866). Yield series: 07-31 0/3 · 08-01 2/6 · 08-02 3/7 · 08-03 0/7 · 08-04 0/11 · 08-05 3/8 · 08-06 5/6 · 08-08 0/5 · **08-09 —/0**. Queue measured: strict `Status: [QUEUED]` **1,527 — IDENTICAL FOR A SEVENTH CONSECUTIVE DAY**; loose `[QUEUED` **1,980 -> 1,996** (+16, exactly tonight's intake) [measured]. **REFLEXIVE, per Rule 12: tonight's intake uses the inline `[QUEUED]` form the 15d entries use, not the `Status:` block form, so part of the strict counter's seven-day freeze is intake format rather than drain** — the same defect class as ASSUMPTION-918, contributed to by this run. Tonight's 14a/14b intake: **PRESUMPTION-736..739, 741..744, 746..750 and ASSUMPTION-892, 909, 910 [QUEUED]** (16 items, 5 literature / 11 empirical); **PRESUMPTION-740, 745 [IN-HOUSE]** (2); **ASSUMPTION-862..891, 893..908, 911..921 [IN-HOUSE]** (57).
  15d monitor cycle (2026-08-09): **17 first re-triggers (MONITOR-483..499, cycle 0 -> 1); 135 weekly carry-overs advanced to 2026-08-16 with cycle counts held; 0 new stale flags; coverage reconciles to all 504; backlog 228 standing 15d blocks / 272 total; 1 literature item added vs 5 consumed; 16 of 17 routed `[QUEUED-EMPIRICAL]`.** REVISE raised on MONITOR-001, -007, -010. **HEADLINE: "Your stale detector has a measured false-negative rate of 5/5"** — "cycles 1-5 were **cadence ticks**," named rather than quietly fixed, with 15c's dated dispositions deliberately left unrewritten (ASSUMPTION-868). "**21 of the last 22 monitored items are stalled on an activity this system does not perform**" — independent confirmation, from a different register, of yesterday's mis-routing headline (ASSUMPTION-870). Stated omission: **19 premise re-checks 7 days overdue "while their due dates already read as advanced"** (ASSUMPTION-871). Budget breached, fifth consecutive 15d run; routing "my call alone, no second reader" (ASSUMPTION-872).
  Deferred items watching: **2 watching (WATCH-002/003, check count 3), 0 due, 0 stale, 0 new.** Next due 2026-08-11.
  Summa corpus: **307/307 pairs on disk, contiguous** — existence-only verification, no content check; "**this scheduled task can be retired**," third such recommendation (ASSUMPTION-912). Nightly verification: **44 drift items across 33 files, 40 of them the two-id-systems defect; 1 unresolvable path cited by Days 191/192/194/218; 2 convention violations; 5 new pairing candidates; 0 filename violations; 19 unexplained length deviations; Day-074 at 2.10x for a fourth night; 614 files confirmed unchanged.** **Four detector defects found in the verifier itself before writing** — a doubled `wiki/` prefix that invented **427 phantom missing paths**, guardrail false positives **26 -> 1**, a missed bare-`kastrup/wiki.md` form, and 235 files with no Part marker; "three parser defects would have produced badly wrong reports had I not checked counts before writing." **The prior night independently derived the same two extractor fixes; neither night persisted them** (ASSUMPTION-884, 885). Disclosed and unfixed: **65 bottom-of-file-frontmatter files "silently going unchecked rather than failing loudly."** QC/reviewer tier: **eight runs**, of which one died. **Held set 7 pairs (106, 110, 111, 173, 194, 260, 294), "zero rotation," six-plus consecutive all-held sweeps; NO HELD PAIR PASS-MARKED BY ANY RUN ALL DAY** (ASSUMPTION-887). **Root cause converged on independently by four runs: `length_actual_words` is self-reported and never recomputed; `length_ratio_to_target` inherits it; `fidelity_check.py`'s pass "is hollow"** (ASSUMPTION-877). **Day 97 passes at 40% of its ASR** (ASSUMPTION-882). **A QC log row asserted `pass|pass` that `mark` never wrote**; **Day 76's error was installed by a record claiming to correct it** (ASSUMPTION-878). **A false forward-pointer survived three consecutive run summaries** and was killed by one grep, retiring the basis of yesterday's 236-240 prediction (ASSUMPTION-879). Two detector-blind defect shapes named, one new (ASSUMPTION-888). Re-queue cadence reported as **four hours** by one run and **seven days** by another, unreconciled (ASSUMPTION-886).
  Scheduler / infrastructure: **80 OK, 2 WARN, 0 FAIL by the headline tally — with two FAIL lines for `c282-wiki-agent-daily-run` in the same report body** ("committed nothing"; "never finished — silent for 1.0h, blocked on `mcp__workspace__bash`"; "third stall/no-commit in five days"). Most likely an ordering artefact between the 05:45 script and a 09:45 check; no field marks it (ASSUMPTION-903). **Structural timing hole: the 05:45 check cannot see `com.c2a2.metabolism-publish`'s 06:30 first fire, so its proof is deferred one day every week.** Metabolism: **regen FAILED** (snapshot 23.97h, "breaches within the hour"; mount refuses deletes; `_snapshot_db()` has no free-space precheck) and **the weekly census FAILED** on an 18-minute freshness miss — "**Two weeks of census and delta-tracking are absent, not empty**"; `state.json` `last_run` **2026-07-28** (ASSUMPTION-897). Telemetry **PASSED** by executing the very 4 GB mount copy regen refused, three hours earlier, and left the artefact truncated to zero bytes (ASSUMPTION-895). The monitor attributed its sibling's file to "**someone or something**" (ASSUMPTION-896). Metabolism publisher unpushed since 2026-07-21 — **nineteen days**. Janitor run #22: **165 new findings against a 62-item baseline, 251 open, 146 broken wikilinks, 15 auto-fixes (13 ok / 2 blocked by the mount); two Dispatch conditions tripped with no Dispatch tool**; the downstream morning report carried **two items** (ASSUMPTION-900, 901). Connector health: **2 active / 19 dormant / 0 suspect / 4 orphaned — identical for a seventh consecutive run, with both enumeration tools returning empty and the headline self-downgraded to low-confidence**; `list_scheduled_tasks` returns **1 of 70**; cost tracker silent **five months** (ASSUMPTION-898, 899). Backup **OK**: 1.6 GB, **85,960 files**, `nohup` improvised, "the script itself was not modified" (ASSUMPTION-905). Supabase kept warm. Eleventh dark day, both sync directions, both runs verifying rather than assuming (ASSUMPTION-875).
  Intra-tradition consensus rate: N/A (pre-Stage 2)
  Cross-tradition survival rate: N/A (pre-Stage 3)
  Health ratio r: N/A
  Notes:
    - **HEADLINE 1 — FOUR SILENT DEATHS IN ONE DAY, AND THE MARKER HAS STILL NEVER BEEN DIAGNOSED.** On 08-08 the interrupt marker had one instance and was the first break in 118 days. Today it has four, on a day with **no attended session** — nobody was present to interrupt anything. The string names an agent, so it reads as already-explained, and no run, monitor or self-audit anywhere in this system treats it as a diagnosable condition. If it is endogenous, the system's failure rate is a property of the runtime and is currently attributed to the absent user — the one attribution that guarantees nobody investigates (ASSUMPTION-862; **PRESUMPTION-736, Critical, queued**).
    - **HEADLINE 2 — THE INSTRUMENT BUILT TO FIND DEAD AGENTS IS THE ONE THAT DIED, AND NOTHING HOLDS THE INVARIANT.** The weekly ecosystem report — whose task file names the stranded-agent audit "the most important section" — produced nothing. Second consecutive day this class lands; first time on the instrument built to detect it. **No agent in this network holds "every scheduled task produced an artefact today."** Four died; the remaining instruments reported health (ASSUMPTION-863; **PRESUMPTION-750, Critical, queued**).
    - **HEADLINE 3 — INDEPENDENCE WAS ACHIEVED AND LEFT NO TRACE.** The lit pipeline fixed, in one line, the exact defect it self-reported on 08-08 — and died before writing. Zero artefacts. The next reader of `for_lit_search.md` will find the procedural-independence defect standing uncontradicted. **A quality property that never reaches disk is, to the register, identical to its absence** — and every process virtue this system claims about itself is asserted in summaries, not carried by artefacts (ASSUMPTION-865; **PRESUMPTION-738, High, queued**).
    - **HEADLINE 4 — A DETECTOR MEASURED ITSELF AT 5/5 FALSE NEGATIVES, AND THE MECHANISM GENERALISES.** "Cycles 1-5 were **cadence ticks**." Two other registers showed the identical shape the same day: 19 premise re-checks overdue "while their due dates already read as advanced," and a QC row asserting a pass no write created. **Every scheduled counter in this system is read by its consumers as an evidence counter, and no register has a column that distinguishes them** (ASSUMPTION-868, 871, 878; **PRESUMPTION-739, High, queued; PRESUMPTION-742, High, queued**).
    - **HEADLINE 5 — A RUN SUMMARY IS NOT A SOURCE, AND EVERY REGISTER HERE IS BUILT FROM RUN SUMMARIES.** A false claim propagated across three consecutive summaries and was killed by one grep of the per-day rows. The rule was stated once, locally, as a correction. Its scope is the whole architecture — `assumptions.md`, `presumptions.md`, `decisions.md`, `monitor_queue.md`, every snapshot in this directory. **This snapshot is one.** No register carries a field distinguishing a measured claim from a reported one, and this run's own coverage declaration concedes it saw no tool outputs (ASSUMPTION-879, 921; **PRESUMPTION-743, High, queued, REFLEXIVE**).
    - **HEADLINE 6 — 14a's OWN INSTRUMENT IS PATTERN-SENSITIVE, ON SIX REGISTERS OUT OF EIGHT.** DISPOSITION's line-start count exceeds its maximum id by 38; DECISION reads 47, 76 or 78 depending on one character. The 08-08 register-decay headline used the strict pattern alone and is not retracted, but it was reported without that caveat. **Eighth instance in five days of the PRESUMPTION-687 class, now inside the self-awareness pipeline.** Reflexively, tonight's intake format is itself part of the strict `[QUEUED]` counter's seven-day freeze (ASSUMPTION-918).
    - **HEADLINE 7 — A FINDING IS NOT A REPAIR, AND TWO NIGHTS PAID FULL PRICE FOR THE SAME FOUR DEFECTS.** The verifier caught a doubled prefix inventing **427 phantom paths** and a Part-blind guardrail inflating false positives 26-fold — then noted the prior night had independently made the same fixes, and read the agreement as corroboration. Nothing was persisted by either. Nothing in this system distinguishes a defect that has been *found* from one that has been *fixed*; both terminate in a summary (ASSUMPTION-884, 885; **PRESUMPTION-737, High, queued**).
    - **HEADLINE 8 — THE SYSTEM CANNOT SEE ITSELF ACTING.** One run attributed to "someone or something" a file its sibling disclosed creating two minutes earlier, and attributed a third run's working-tree output to "someone else's runs." Two runs faced one constraint and made opposite defensible decisions three hours apart, neither able to see the other's validated result. **A twenty-agent network has no mechanism by which one member recognises another member's footprint**, so its own activity appears to its own instruments as environmental noise (ASSUMPTION-895, 896; **PRESUMPTION-740, PRESUMPTION-745, queued**).
    - **HEADLINE 9 — THE PROPOSAL MECHANISM AND THE WRITE PERMISSIONS ARE DISJOINT.** The queue-diff precheck: proposed by three-plus agents across six-plus runs, "six runs overdue," and every proposing agent is explicitly forbidden to write it. Same for the `length_tier` call (seven weeks), the `held` first-class state, `/tmp` namespacing (sixth day), line 304 (fourteenth-plus day), the in-house measurement authorisation ("**no agent is authorised to run it**"). **The open-item backlog is being read as a workload when part of it is a permissions boundary, and no amount of agent time reduces that part** (ASSUMPTION-919; **PRESUMPTION-744, High, queued**).
    - **HEADLINE 10 — MC0001 FLIPPED VALENCE IN A DAY, AND NINETY CROSS ENTRIES REST ON THE QUESTION IT EXPOSES.** Corroboration on 08-08; "**a competitor, not a corroboration**" today, from the same evidence, written into the bridge unsoftened. Underneath both readings: **when the field converges without C2A2, is that evidence the method works or evidence it is not needed?** The register has no field for what a cross-connection is evidence *of*, and its count is the system's headline output metric (ASSUMPTION-910; **PRESUMPTION-747, High, queued — the strongest literature item in tonight's cohort**).
    - **HEADLINE 11 — THE CLEANEST FALSIFICATION TARGET WAS NAMED, AND ITS CONTROL ARM DOES NOT EXIST.** "C2A2 predicts cross-tradition exposure should *reduce* measured overconfidence… the wiki should say so in advance rather than afterwards" — correct, and correctly routed: "the blocking step is a control arm… a design decision for you." But **every agent in this network has run with cross-tradition input since the first bridge note.** The decision awaits a construction, not an allocation, and the asset gets more expensive every day the network runs (ASSUMPTION-909; **PRESUMPTION-748, High, queued**).
    - **HEADLINE 12 — 15% OF THE VAULT IS BEING MAINTAINED AND NEVER WIRED IN, AND THE AUDIT RETRACTED ITSELF TO SAY SO.** "That guess was wrong, and the truth is worse." 614 Summa pages, one backlink each, from their own transcript, rewritten 282 times since 08-02. "**Every day it runs, the islands get better written and stay islands.**" The orphan metric's sign presumes this is decay; the measurement suggests it is faithful pipeline output (ASSUMPTION-913; **PRESUMPTION-749, queued**).
    - **POSITIVE ENTRIES, RECORDED NEUTRALLY:** 15d **named its own detector's 5/5 failure rather than fixing it quietly** and disclosed a single-reader routing call (ASSUMPTION-868, 872); Agent 16 **retracted a thirteen-day-old diagnosis by name** and replaced it with a line-level cause (ASSUMPTION-873); the connector census **downgraded its own headline's confidence class** rather than let seven identical weeks read as verified health (ASSUMPTION-898); the nightly verifier **validated its detectors before writing** (ASSUMPTION-884); five Summa runs **refused to pass-mark a held pair to clear a queue** (ASSUMPTION-887), one **declined a seven-week-old call rather than pre-empt it** (ASSUMPTION-890), one **computed an overlap by hand rather than read a warnings-only exit as clean** and **confirmed a logged falsifiable prediction exactly** (ASSUMPTION-891), one **recorded a negative finding with a close instruction** (ASSUMPTION-894), one **corrected its own memory before a repoint that would have made a visible mislabel invisible** (ASSUMPTION-880), and one stated the standing rule that **"a high hit rate on a vault-wide scan is evidence about the scanner, not the vault"** (ASSUMPTION-883); the regen run **refused to strand a 4 GB artefact** and root-caused its blocker to a function (ASSUMPTION-897); both sync runs **verified rather than assumed their failure and refused to fabricate** (ASSUMPTION-875); the Wright hunt **returned zero rather than invent a summary** and named an unreachable source class (ASSUMPTION-917); the bootstrap audit **declined Phase 3 against the no-blind-push rule** and **identified its own SKILL as the stale party** (ASSUMPTION-912, 915); the sewing agent **wrote the uncomfortable reading into the bridge rather than softening it**, **stated a prediction's failure consequence in advance**, and **caught its own byte-vs-character miscount** (ASSUMPTION-908, 909, 910); and at least seven runs **surfaced budget breaches**.
    - **BUDGET, SURFACED (Rules 6 / 12):** this run substantially overran both the 4,000-token per-task and 30,000-token session guidance. Cause: 35 sessions read via seven parallel sub-agent digests, plus register measurement. Declared, not hidden. PREMISE-146 holds the per-task ceiling unsatisfiable as specified; today adds that the fix for the recurring 18-20k preamble breach **cannot be written by any agent that suffers from it** (ASSUMPTION-919; PRESUMPTION-744).
