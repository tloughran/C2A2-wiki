# Architectural Changes — 2026-09-20

*Agent 14a end-of-day run. Sunday. No interactive Cowork session — twenty-first consecutive day.*

## Narrative

Sunday's six scheduled agents produced no architectural decisions and one genuine discovery, and the
discovery was about the agents themselves: three of them, independently and without reference to one
another, found that their measuring instruments are inside the populations they measure. The sewing
agent found 34% of the vault's broken wikilinks living in its own reports, retracted a recommendation it
had published a week earlier as a false alarm, and reported a corrected clean rate of 11.0% against the
15.8% its own series had been publishing for four weeks. The janitor found it was counting failed
deletions as fixes and computing weekly deltas against an original baseline, so chronic findings
re-present as new. Agent 16 found a card count it had carried forward through three runs by restating
its own flag's text instead of re-deriving it from the artifacts — and then that corrected figure was
itself stale within hours, republished by the evening sync alongside the current one, in the same
document. None of the three named the general case, so 14b filed it (PRESUMPTION-1053) and 14a turned it
on this register and reported the result against interest (ASSUMPTION-1574).

The night's second finding is that the literature pipeline is alive and hung. 15d reported that
`lit_search_returns.md` had gone unwritten for three days and said plainly it could not tell a pause
from a stop. Three probes of the running session ten minutes apart returned an identical turn count and
an identical trailing message, which answers the question in a third way neither option covered: the
session is running and stationary, and has been since at least the 17th. Twenty-three re-triggers and
149 lines went into that lane today before anyone knew, and tonight's eleven items went in after, with
the stall recorded in the intake header rather than left to be discovered.

Nothing moved in the subject-matter layer. PRS holds at 867 for a tenth day, CROSS at 135, findings at
90, and the pending queue reached 27 cards spanning eleven days with no decision since 09-09 — named
independently by the daily run and the sewing agent as the binding constraint. The one content change
in the CROSS layer was a correction: CROSS-008 restated from final to formal causality, narrower and
better supported, with a mechanism arriving independently from a Levin paper three days earlier. It was
caught because a weekly run happened to read the fourth source, which is why 14b asks whether anyone is
auditing the other 134 (PRESUMPTION-1058).

## Decisions

**None minted.** DECISION-083 (2026-08-27) stands, 24 days attended. `decisions.md` was not modified by
any run today and is absent from the day's mtime sweep. Review-pass gap 10 days; last decision-channel
act the `[C2A2-review-decision]` email of 09-09, 11 days. Agent-made, id-less rules continue to
accumulate faster than decisions are minted (ASSUMPTION-1583).

## Changes detected

- **Registers.** `assumptions.md` +30 (1555–1584); `presumptions.md` +9 (1052–1060); `open_questions.md`
  +3 (245–247); `for_lit_search.md` +11 items. All backed up as `*.bak.20260920-pre-14eod` before append.
- **15d weekly cycle** (earlier today): 23 first re-triggers queued, ~205 weekly carry-overs advanced to
  09-27, 3 lane exits. `for_lit_search.md` +149 lines, `monitor_queue.md` +267, with its own `.bak`s.
- **Weekly sewing run:** 10 pages sewn, 61 agentic calls, 16 bridge notes, 2 previously-empty bridge
  files seeded, zero collateral movement confirmed. `connectivity_log.csv` row
  `2026-09-20,4287,744,86,5117`.
- **Daily run:** review page 294,488 B / 27 proposals; review log 482 cards (+3); L2 gate PASS 379/379,
  staleness 11 days; commit deferred to the Mac-side script.
- **Agent 16:** `deferred/watch_list.md` updated; 0 due / 0 run / 0 resolved / 0 added; leakage count 16;
  ruling deadline 09-24.
- **Failures:** OpenStory refresh (third consecutive, `Errno 28`); both sync directions (eighteenth day);
  `.DS_Store` unlinks ×3; auto-open of the review page.
- 156 files touched on the mount today, all agent-written.

## Rationale drift

- **Wright PROP-2026-08-14-033.** The stated reason for continuing has changed shape: previously a
  retrieval failure to be retried, now (daily run) a property of how ntwrightpage renders, warranting
  DENY as *source-unretrievable* rather than *content-rejected*. Recorded as a clarification, not drift
  — but the attempt count moved again, eight vs nine, second consecutive night (ASSUMPTION-1564).
- **The orphan metric.** Thirteen filings of the same recommendation; the fourteenth was withheld and
  replaced with three alternatives including deleting the column (ASSUMPTION-1581). The rationale for
  publishing the number at all has never been stated.

## Technical debt

- **Counting convention undefined** — three same-day off-by-one disagreements and three method-dependent
  headline counts trace to it (OPEN-246). Cheapest fix on the board.
- **No liveness-versus-progress check** — a hung session is invisible to every check the estate has
  (OPEN-247).
- **`open-story.db` unowned at 7.0 GB** — third failure on the same wall in two days (OPEN-244).
- **Reported-but-unmade writes** — the 09-13 run's premise re-check advances are absent from the file;
  nothing reconciles a report of work against the work (ASSUMPTION-1570).
- **Instrument self-inclusion uncorrected in published series** — the `connected` column and the
  broken-link curve both carry it (ASSUMPTION-1559).
- Run-log archival split: fifteenth consecutive recommendation, `watch_list.md` now 720,769 B / 6,003
  lines (verified at source).

## Self-referential notes

- ASSUMPTION-1574 applies the day's headline finding to this register: 7 wikilinks emitted, 4 broken, all
  syntax examples, ~1% of the vault's 403. The mechanism generalises; the magnitude does not; the
  mitigation — naming items by ID rather than quoting them as links — was accidental.
- PRESUMPTION-1060 is the night's principal item and indicts this pass: the provenance protocol defines
  `ASSUMPTION (stated)` as "Original designers were aware of this premise," and for twenty-one days the
  marker has been applied to agent self-reports. Raised to Tom as OPEN-245.
- ASSUMPTION-1584: the verification step of this pass used an **unanchored** id count, reported four
  duplicate presumption ids, and was wrong — all four are prose lines beginning with an id. Colon-anchored
  re-derivation: `assumptions.md` 1,583 / 1,583 / zero duplicates, `presumptions.md` 1,060 / 1,060 / zero.
  The register is clean; the check was not. This pass filed the same defect against other runs earlier
  tonight (ASSUMPTION-1575) and against the CROSS index on 09-18 (ASSUMPTION-1548). **Knowing the failure
  mode by name did not prevent it**, and it surfaced only because the check ran twice. The snapshot figure
  was corrected in place from 1,064 to 1,060 rather than quietly overwritten.
- ASSUMPTION-1582: token budget breached and declared, alongside the sewing agent's thirteenth
  consecutive declared breach and 15d's, whose diagnosis is the useful part — the cost is in parsing
  three files because ~148 entries carry no per-entry schedule.

## Open questions raised

OPEN-245 (needs Tom) · OPEN-246 (command) · OPEN-247 (needs an owner). Register now at OPEN-247.
