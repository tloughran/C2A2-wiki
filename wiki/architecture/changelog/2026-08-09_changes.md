# 2026-08-09 — EOD self-awareness run (Agents 14a / 14b)
### Sunday (tradition day). 35 sessions. Seventh consecutive unattended day.

Four runs died at `[Request interrupted by user]` — the ecosystem report, the wiki daily run, a Summa
reviewer, and the lit pipeline. Yesterday that marker had one instance and was recorded as the first
break in 118 days; today it has four, and still not one run anywhere in this system has asked what
produces it. The instrument that died first was the weekly agent ecosystem report, whose own task file
names the stranded-agent audit as "the most important section." The lit pipeline achieved architectural
independence for the first time — separate agents, separate contexts, the exact defect flagged on 08-08 —
and died before writing, so the register carries no trace that the fix ever happened. Against that: 15d
measured its own stale detector at a 5/5 false-negative rate and named it rather than quietly repairing
it; Agent 16 retired a thirteen-day-old diagnosis and replaced it with a line-level root cause; the
connector census downgraded its own headline's confidence class rather than let seven weeks of identical
numbers read as seven weeks of verified health; and five Summa runs independently declined to pass-mark a
held pair to clear a queue. No new DECISION — thirty-fifth straight day. No new OPEN. And this run found
that 14a's own block-count instrument gives a different answer under a one-character pattern change on
every register but one.

## Changes Detected

- **CHANGE-2026-08-09-001 — Four silent deaths in one day, and every alarm reported health.** `local_05323ba8` (weekly agent ecosystem report), `local_71de8d6e` (wiki daily run), `local_8262ef61` (Summa reviewer), `local_7dcf6514` (lit search pipeline), each ending at `[Request interrupted by user]` with no summary, on a day with **no attended session**. The morning instruments reported the pipeline healthy. No run investigated the marker; none ever has (ASSUMPTION-862, 863; **PRESUMPTION-736, Critical, queued; PRESUMPTION-750, Critical, queued**).

- **CHANGE-2026-08-09-002 — The wiki daily run died with eight subagents in flight and 41 approved proposals unprocessed.** Its only prose: "Dispatching Phase 1 ingestion — **41 approved proposals across 13 traditions**." Phases 1-6 never ran, including Phase 5.6, which exists specifically to prevent a repeat of the six-week frozen-artifact failure. On-disk: newest review page `2026-08-08_review.html`, newest `PROCESSED_LOG` rows 2026-08-08. **Whether the eight subagents wrote anything before the interrupt is recorded nowhere** (ASSUMPTION-864).

- **CHANGE-2026-08-09-003 — Independence was achieved and left no trace.** "Launching 15a and 15b as **separate agents in separate contexts** — this run achieves architectural, not merely procedural, independence." Then: zero `SEARCHED-15a: 2026-08-09` entries, newest results files Aug 8 21:00, backlog drain zero for a **thirty-third** consecutive day. The next reader of the register will find yesterday's procedural-independence defect standing uncontradicted (ASSUMPTION-865, 866; PRESUMPTION-738, queued).

- **CHANGE-2026-08-09-004 — A detector was measured against itself and failed 5/5.** "**Your stale detector has a measured false-negative rate of 5/5.** Those five items were recorded at 'cycle 5, trajectory stable, no new sources' — but **zero searches had actually run. Cycles 1-5 were cadence ticks.**" Named with a number rather than quietly fixed; 15c's dated dispositions deliberately not rewritten. Same day, same shape, two other registers: 19 premise re-checks "**7 days overdue while their due dates already read as advanced**," and a QC row asserting `pass | pass` that `mark` never wrote (ASSUMPTION-868, 871; **PRESUMPTION-739, High, queued**).

- **CHANGE-2026-08-09-005 — The review-page diagnosis was retracted and replaced with a line-level cause.** "**The previously-logged 'card/button ID offset' is not present in this page version… That claim should be retired as stated.**" The real defect: click handlers key on real proposal IDs, `submitDecisions()` iterates a synthetic positional array — "**the export can only ever emit `PENDING`**… **not mislabeling, it's total loss of the payload.**" Consequence, stated in the same run and independently by the evening run: "**the 47 APPROVEs didn't come from your clicks.**" Their provenance is unresolved; the source email was not read; the 47 are already ingested (ASSUMPTION-873, 874).

- **CHANGE-2026-08-09-006 — Both sync directions dead, same cause, both runs verifying rather than assuming.** "`claude.ai/recents` redirected to `/logout`; the Chrome profile the extension is attached to is signed out. **Same blocker as this morning's inbound scrape, so the sync is broken in both directions.**" Eleventh dark day. Recorded as POSITIVE for the refusal to fabricate; recorded as a defect that the morning run's failure note now occupies the slot a consumer reads for content, with no consecutive-failure counter anywhere (ASSUMPTION-875, 876).

- **CHANGE-2026-08-09-007 — Four independent runs converged on the same root cause: the length check grades a number nobody measures.** "`length_actual_words` is self-reported and `qc_sweep` never recomputes it"; "`length_ratio_to_target` … **inherits the error and goes stale independently**"; "**understated on all six files I touched** … the writer and the reviewer are counting different regions"; "`fidelity_check.py` says 'All checks passed' … **that pass is hollow**." Measured deltas up to ~370 words. None of the four cited another (ASSUMPTION-877, 889).

- **CHANGE-2026-08-09-008 — A log row asserted state it never created, and a repair record installed the error it claims to have fixed.** "**The 10:32 log row is wrong** … its *status columns* assert durable state they didn't create. **I nearly inherited that as fact.**" And Day 76's wrong count "**was written by a previous reviewer that logged it as 'corrected to honest 1698.'**" (ASSUMPTION-878; **PRESUMPTION-742, High, queued**).

- **CHANGE-2026-08-09-009 — A false claim was traced across three consecutive run summaries, and the rule that kills it was stated once, locally.** On "236-240 remains the un-worked Tertia Pars gap": "**It's false.** … propagated across three consecutive run summaries without anyone grepping the log." The rule: "**a run-summary assertion about *other* days is not evidence; only the per-day rows are.**" This retires the basis of yesterday's falsifiable prediction (ASSUMPTION-842/843) — the predicted range was already worked. **Every register in this system is built from run summaries, including this one** (ASSUMPTION-879, 893; **PRESUMPTION-743, High, queued, REFLEXIVE**).

- **CHANGE-2026-08-09-010 — Fourth and fifth anti-sweep warnings in four days, and a counter-instance the same day.** "Days 93-100 use `P1/P2/P3` as *Problem* enumeration and would be corrupted by a naive sweep." "**Had the repoint followed my memory, it would have moved every broaden-and-build citation onto a wrong id — converting a visible mislabel into an invisible one, which is worse than leaving it.**" Against that, Carroll's labels came back **aligned 1:1** with its PRS ids. The id space is a namespace in one tradition and not in others, discoverable only per file — and the two-id-systems defect reached a **fifth** tradition today, with the canonical-file decision still unlogged and **40 of 44** nightly drift items belonging to that one class (ASSUMPTION-880, 881).

- **CHANGE-2026-08-09-011 — The nightly verifier found four defects in itself before writing anything, and lost the fixes at session end.** "Numbers are large enough that **I should validate the detectors before logging anything**": a doubled `wiki/` prefix that **invented 427 phantom missing paths**; guardrail false positives **26 -> 1**; a missed bare-`kastrup/wiki.md` form; 235 files with no Part marker. Then: "**Prior night's entry independently made the same two extractor fixes**" — read as corroboration, not as a measurement that nothing persisted. Disclosed and unfixed: "the **65 bottom-of-file-frontmatter files silently going unchecked** rather than failing loudly" (ASSUMPTION-884, 885; **PRESUMPTION-737, High, queued**).

- **CHANGE-2026-08-09-012 — Two runs, one constraint, opposite decisions, three hours apart.** Telemetry executed a chunked 4 GB `dd` onto the mount and left the artefact truncated to zero bytes; regen refused the identical action — "permanent, undeletable garbage in your Documents. **I did not do it**" — and declared the trick no longer fit, **three hours after it had fit**. Both cite 3.3 GiB free. Neither could see the other's validated snapshot. And the monitor attributed to "**someone or something**" a file its sibling had disclosed creating two minutes earlier (ASSUMPTION-895, 896; **PRESUMPTION-740, PRESUMPTION-745, queued**).

- **CHANGE-2026-08-09-013 — The metabolism census failed by eighteen minutes and refused to soften it.** "`FRESHNESS FAIL: snapshot is 24.3h old (limit 24h)`"; "**Do not read this as a quiet week**"; "**Two weeks of census and delta-tracking are absent, not empty.**" Disclosed defects: `os.remove` "swallows that error silently"; "`_snapshot_db()` has **no free-space precheck**" (ASSUMPTION-897).

- **CHANGE-2026-08-09-014 — A census ran with both of its enumeration tools returning empty, and downgraded its own headline.** "**Both `list_plugins` and `list_connectors` returned empty.**" Result identical to last week **for a seventh consecutive run**. "**The zero is honest, but it now rests entirely on one auth-failure grep.**" "**I've flagged the headline numbers as low-confidence rather than let seven weeks of unchanging output read as seven weeks of verified health.**" Recorded as the strongest POSITIVE in the block. Also found: `list_scheduled_tasks` returns **1 task out of 70** — the same N=1 blindness the scheduler skill was rewritten to fix, live in a second tool; and a cost tracker silent for five months while the scheduler logs it clean (ASSUMPTION-898, 899).

- **CHANGE-2026-08-09-015 — Two morning instruments contradicted each other on the size of the system and on its health.** "**Thirty-one scheduled agents are active** … the C2A2 wiki daily run … **fired on schedule overnight. Nothing broken otherwise**" against, the same morning, "**70 registry task(s) across 3 file(s), 11 launchd agent(s)**" and two FAIL lines: "**committed nothing**," "**never finished — silent for 1.0h, blocked on `mcp__workspace__bash`**," "third stall/no-commit in five days." The scheduler's own headline reads **0 FAIL** above those lines, most likely an ordering artefact with no field to mark it (ASSUMPTION-902, 903).

- **CHANGE-2026-08-09-016 — A dispatch condition was met twice and no dispatch exists.** "Two anomaly conditions tripped, **so flagging here since no Dispatch tool is available in this session**." The janitor's **165 new findings against a 62-item baseline, 251 open, 146 broken wikilinks** reached the downstream morning report as **two items**. Root cause named for one class: "`sync_vault.sh` only rsyncs `transcripts/` + `synthesis/`, so **`refs/` never propagates**" (ASSUMPTION-900, 901; **PRESUMPTION-746, High, queued**).

- **CHANGE-2026-08-09-017 — Six runs, every page, `bl 0->0`, and the agent said what that means.** "**the sewing agent is currently writing into a queue nobody drains, and the log should stop implying otherwise.**" The cheap five-call test that would settle it was proposed for a second consecutive day and not run. Backlog arithmetic stated and non-convergent: **218 of 305 proposals never called; inflow ~19/wk against coverage 10/wk** (ASSUMPTION-906, 907).

- **CHANGE-2026-08-09-018 — C2A2's cleanest falsification target was named with its failure consequence stated in advance, and its blocker correctly routed.** "**C2A2 predicts cross-tradition exposure should *reduce* measured overconfidence and unsupported assertion**… if the prediction fails, C2A2 loses its cleanest external falsification target, **and the wiki should say so in advance rather than afterwards**." And: "**The blocking step is a control arm** … **a design decision for you, not a research question for an agent.**" The routing is right; the asset is not there to allocate — every agent in the network has run with cross-tradition input since the first bridge note (ASSUMPTION-909; **PRESUMPTION-748, High, queued**).

- **CHANGE-2026-08-09-019 — MC0001 was re-read as a competitor, one day after being recorded as corroboration.** "**MC0001 is a competitor, not a corroboration. The wiki holds none of the three MC0001 talks.**" Written into `loughran_wolfram_bridge.md` "rather than softened." Yesterday the same venue was ASSUMPTION-823's positive. Same datum, opposite valence, one day apart — and ninety CROSS entries have been built without the register ever asking what a cross-connection is evidence *of* (ASSUMPTION-910; **PRESUMPTION-747, High, queued — the one strong literature item in this cohort**).

- **CHANGE-2026-08-09-020 — The bootstrap audit retracted its own prior week, and the correction is worse than the error.** "Last week I said the 307 pages sit at 1-2 backlinks … **Wrong.** … **That guess was wrong, and the truth is worse.**" Every Summa page's only backlink is its own transcript. **614 pages, 15% of the vault**, rewritten 282 times by the sync since 08-02. "**The sync is actively maintaining content it never wires in. Every day it runs, the islands get better written and stay islands**" (ASSUMPTION-913; **PRESUMPTION-749, queued**). Seventh firing of a "ONE-TIME" task, second retirement recommendation, Phase 3 deliberately not run against the no-blind-push rule (ASSUMPTION-912).

- **CHANGE-2026-08-09-021 — Two censuses of the same vault disagree and the tolerance that absorbs it was asserted, not derived.** 3,994/3,281/657/56 against 3,995/3,272/666/57; 2,116 against 2,132 links; 188 against 204 broken. "**These figures agree… to within resolver tolerance; neither is drift.**" Part of the gap is real change: three zero-byte stubs listed by the first census were filled by the second (ASSUMPTION-914; **PRESUMPTION-741, queued**).

- **CHANGE-2026-08-09-022 — A third live triplet figure, and sub-counts that do not sum.** "**519 -> 615 triplets** (cross 90, coils 32, findings 62, generative 23). All validation gates passed." **90+32+62+23 = 207.** The system now carries **528, 530 and 615** with no field distinguishing what each counts. Not published. The standing publish recipe opens with `rm -f .git/index.lock .git/refs/heads/main.lock`, routine and flagged temporary by nobody (ASSUMPTION-916).

- **CHANGE-2026-08-09-023 — Zero Wright proposals, deliberately, with the reason stated.** "**Writing a proposal would have meant inventing a summary, so it is deferred rather than skipped.**" One source class unreachable: "`ntwrightonline.org/blog/` was **refused as outside the fetch provenance set**." Two Rohr proposals filed; one framed as "a **construct-boundary probe** … **not a convergence**"; one flagged as a FLAG-NN candidate **with no number assigned** (ASSUMPTION-917). Third consecutive day agents gated ingestion on unread or unlicensed primary sources — four of ten pages carried explicit gates (ASSUMPTION-911).

- **CHANGE-2026-08-09-024 — 14a's own instrument is pattern-sensitive, and this run says so.** Strict / line-start-any / max id, measured this run: DECISION **47 / 76 / 78**; OPEN **93 / 133 / 139**; REVISE **118 / 153 / 294**; MONITOR **137 / 144 / 504**; PREMISE **105 / 106 / 149**; DISPOSITION **583 / 667 / 629** — the line-start count **exceeds the maximum id by 38**. The 08-08 "register decay" headline (ASSUMPTION-859) used the strict pattern alone. It is not retracted; the strict counts are real. But it is now known to be pattern-sensitive, and no reader was told. **Eighth instance in five days of the PRESUMPTION-687 class, this one in the self-awareness pipeline's own instrument** (ASSUMPTION-918). Reflexively: tonight's intake uses the inline `[QUEUED]` form, so the strict `Status: [QUEUED]` counter reads **1,527 for a seventh day while the loose counter moved +16** — part of that freeze is intake format, not drain.

- **CHANGE-2026-08-09-025 — At least seven budget breaches, one cause, and the fix is unreachable by the agents that need it.** "roughly 20k tokens of contract before the first query"; "over the 30k session guidance, **with zero pairs reviewed**"; "the queue-diff precheck … **six runs overdue**"; "The protocol in `SKILL.md` and the budget in `CLAUDE.md` are **not compatible as written, and no run of this agent has ever been within budget**." Every proposing agent is forbidden to edit the Tom-owned task file that holds the fix. **This run is in breach and declares it** (ASSUMPTION-919; **PRESUMPTION-744, High, queued**).

## Decisions

**No new DECISION. Thirty-fifth consecutive day.** Ten candidates were named today and none logged; they are enumerated at ASSUMPTION-920. The two with new evidence tonight are (b) line 304 — now with a precise root cause and a 2-item pending queue standing ready as a test bed — and (g) authorisation for an agent to run in-house measurements, which 15d states would "either answer unblock the register."

## Open Questions

**No new OPEN.** OPEN-138 and OPEN-139 remain OPEN — awaiting Tom for a **seventeenth day**.

## Positives, recorded neutrally

15d named its own detector's 5/5 failure rather than fixing it quietly, and disclosed that its routing was "my call alone, no second reader." Agent 16 retracted a thirteen-day-old diagnosis by name. The connector census downgraded its own headline's confidence class. Five Summa runs independently refused to pass-mark a held pair to clear a queue, and one declined a fix that "has been yours for seven weeks and I'd rather you make it than find it made." A verifier validated its detectors before writing and caught three parser defects that "would have produced badly wrong reports." A reviewer computed an overlap by hand rather than read a warnings-only exit as clean, and logged a falsifiable prediction that then came true exactly. A sweep recorded a negative finding with a close instruction attached — "that sweep is closed — don't re-measure." Both sync runs verified their failure rather than assuming it, and neither fabricated. The regen run refused to strand a 4 GB artefact and root-caused its blocker to a function. The Wright hunt returned zero rather than invent a summary. The sewing agent wrote the uncomfortable reading into the bridge rather than softening it, and stated a prediction's failure consequence in advance. And at least seven runs, this one included, declared their budget breaches instead of hiding them.
