# Architectural changes — 2026-09-22

Twenty-seven sessions ran and none contained designer speech. For the first time all three channels
through which Tom's decisions arrive were dark on the same day. The work that did happen was mostly sound
and sometimes very good: the lit pipeline built a block parser that ends the grep-anchoring defect at the
script level, and two infrastructure runs established by trial why their jobs cannot run in the sandbox.
The day's structural fact is that findings still do not travel. The scheduler check asked a question the
metabolism run had answered. The health check read yesterday's status line as today's. Later QC runs
repeated an "egress" diagnosis that the first run had already traced to a local directory. A held pair was
marked passed, because the task file that should have prevented it is one only Tom may edit.

## What ran

- **27 sessions read first-hand** by four parallel readers. One reviewer run failed with an API 529 error
  and produced no output (ASSUMPTION-1650). One nightly verification could not be dated.
- **Daily run**: 1 Carroll card; 12th zero-ingest day; review page 363,072 B / 33 proposals (verified).
  Recovered from `/sessions` 100% with `TMPDIR=/tmp` (ASSUMPTION-1623).
- **Hawkins/Hoffman specialist**: 3 cards. Both Hawkins cards fall outside the 30-day recency window. The
  Hoffman card is an announcement filed as "monitoring" that carries candidate triplets and sits in
  `pending/` (ASSUMPTION-1628, -1629).
- **Lit pipeline**: 12 items worked; PREMISE-209–213 (all verified present); true backlog 147.
- **Agent 16**: WATCH-003 check 12, not met. **Unwatched 09-24 deadline** (ASSUMPTION-1632).
- **Summa**: 4 of 5 reviewer runs completed (the fifth hit the 529 error); 6 QC sweeps; batch no-op 7;
  nightly 250 violations.
- **Failures**: OpenStory (3rd day), metabolism (18.7 days frozen), both Chat bridges (20th day), 5
  scheduler FAILs, `/sessions` 100% across at least four runs.

## Rationale drift

- **Criterion (a) was reversed again the same day**: "Every tail exoneration on record, including this
  morning's six, rests on a probe blind to the dominant form" (ASSUMPTION-1643).
- **YouTube "egress"**: the 04:25 QC run traced it to a foreign-uid `/var/tmp/qcseg`. Later runs called it
  environmental on the strength of one control (ASSUMPTION-1647, PRESUMPTION-1076).
- **Schedule**: Agent 16 says the daily run fires at 04:35. The daily run's own report says "8am"
  (ASSUMPTION-1631).
- **Health vs OpenStory**: "the script itself is erroring" against a disk-exhaustion cause established by
  trial (ASSUMPTION-1636).

## Technical debt

- **Five task-file defects left unrepaired**, all because "the file is the user's to change": memory path,
  Step 2 command, hold check, `permissionMode`, schedule text (ASSUMPTION-1651, PRESUMPTION-1074,
  OPEN-251).
- **Large-db jobs have outgrown the sandbox**: OpenStory db 7.14 GB, metabolism snapshot 6.7 GB
  (OPEN-252).
- **Counting convention (OPEN-246), third night**: pending reported four ways, PRS three ways.
- **Three zero-byte bridge files**, second night, no instrument names them.
- **CLAUDE.md crash limits (2k nodes / 3k edges)** against a graph of 5,189 / ~159k, per the evening sync
  (unverified; PRESUMPTION-1075).
- **PRESUMPTION-1069 (Critical, no measurement role) is unaddressed.** 16 of tonight's 22 queued items are
  in-house.

## Self-referential notes

- **A positive general fix**: `queue_scan.py` replaces line grep. It is the first repair of the anchoring
  defect at the level of a shared script rather than a single run (ASSUMPTION-1625).
- **The pipeline's independence fix contradicts its own premise register.** PREMISE-004 holds that
  same-model agents make correlated errors (PRESUMPTION-1072).
- **This pass**: the day boundary is inferred from list order, and the pass ran far over the token
  guideline. Both are declared in ASSUMPTION-1654 and the snapshot.
- Registers were backed up as `*.bak.20260922-pre-14eod` before any append.
