# 2026-08-18 — EOD self-awareness run (Agents 14a / 14b)
### Tuesday. 27 sessions. Seventeenth consecutive unattended day — and the night the diagnosis inverted: the mount didn't kill anything, the workaround did.

**Four of twenty-seven runs produced nothing** — 24.1% down to **14.8%** — and the improvement is real
but narrow: three of the four were mute last night too, and **the mute set is not a rotating sample of
the fleet, it is the same three observation-tasks**, on their third, fifth and second consecutive day.
The night's central finding overturns last night's own headline. **Mount absence did not kill a single
run.** All four nulls died at a permission-gated tool call while reaching for host filesystem access —
**three of them at the same tool** — while `metabolism-regen-daily`, which met the identical absence and
declined the substitution, is the only one of the five that produced anything: "**This is a mount-scope
failure, not a script or database fault.**" Last night's PRESUMPTION-834 was **INCORPORATED by 15c
today** on the correlation; tonight's data shows the mount was not the mechanism, and that notice is
filed rather than absorbed. Sharper: **two runs hit the same missing `~/Documents` and reached opposite
verdicts on whether the data was reachable**, with no awareness of each other. Underneath, corrections
ran the dangerous way — **four instruments retracted their own confident findings in one day**, and one
retraction is the only reason correct work survived: "**One grep prevented it.**" And the lit pipeline
established that **agreement among model-generated judgements is evidence about the model** — nine
frontier models, seven families, ~two independent votes — invalidating concordance across the system
including its own, and then completed its cohort anyway. **No new DECISION — forty-fourth straight day,
with a caveat. Three new OPEN. Pending is 54; the gap is eleven days.**

## Changes Detected

- **CHANGE-2026-08-18-001 — MOUNT ABSENCE IS SURVIVABLE; THE WORKAROUND FOR IT IS NOT.** Four runs
  produced nothing and all four died at a permission-gated tool call reaching for host access. Three
  died at `Desktop_Commander__start_process`: `morning-project-status` (that call, then
  `[Request interrupted by user]`, **zero assistant prose ever emitted**); the telemetry refresh, which
  said "**The Documents mount is absent from the sandbox this run** … **Trying Desktop Commander, which
  has real filesystem access to the Mac**", *successfully read the DB*, and died launching the wrapper;
  and the walk handoff — "**The wiki paths are outside the connected folder — trying an alternate
  reader**". The fourth, `morning-system-health`, died on its second `request_cowork_directory` having
  said only "I'll run the daily system health check." Against them, `metabolism-regen-daily` met the
  identical absence, never substituted, and completed. **The two runs that hit the same missing mount
  disagreed about whether the data was reachable at all** — one read it, one declared it unreachable and
  never tried (ASSUMPTION-1142, 1144; **PRESUMPTION-838 High, queued**; **OPEN-157**).

- **CHANGE-2026-08-18-002 — THE MUTE RATE FELL BY A THIRD AND THE MUTE SET DID NOT CHANGE.** 4/27 =
  **14.8%**, against 24.1% last night. Three of the four were mute last night as well, and two of them
  exist specifically to notice failures. `morning-system-health` is on its **third** consecutive day;
  the telemetry refresh on its **fifth**; `morning-project-status` its second. The improvement is
  entirely accounted for by two runs failing loud rather than silently. A fifth run died differently and
  worse: `local_cff5519a` landed edits to Day 271 and QC-log rows, then terminated on "**API Error: 500
  Internal server error**", **leaving the vault partially edited with no record of what completed**
  (ASSUMPTION-1143).

- **CHANGE-2026-08-18-003 — THE STATUS FILE THAT EXISTS TO MAKE A SILENT STALL VISIBLE IS FIVE DAYS
  BEHIND AND ITS READER HAS BEEN MUTE FOR THREE.** The telemetry refresh's last words: "**Status file's
  last entry is 2026-08-13 — four days missing.** Backing it up, then running the Mac-local wrapper."
  Neither happened; the gap is now five. Its designated reader produced nothing for the third
  consecutive day. The spec that reader operates under names the exact condition it could not evaluate:
  "**A missing file, or a newest line that is not from today, is itself a FAIL** — it means the 05:45
  launchd agent did not run, so nothing is watching." The freeze this mechanism was built to prevent is
  in its own spec: "the feeds were frozen 2026-06-08→26 because failures only went to the run log"
  (ASSUMPTION-1145).

- **CHANGE-2026-08-18-004 — A RECORDED CAUSE WAS OVERTURNED FOR THE SECOND CONSECUTIVE NIGHT, AND THIS
  TIME THE CORRECTION WAS THAT THE BLOCK IS NEW.** "**This is a regression in the environment, not a
  longstanding block.** The currently-published snapshot's own `_meta.db_path` reads
  `/sessions/tender-elegant-hamilton/mnt/Documents/...` — an earlier session that *did* mount
  `~/Documents` and succeeded. **Something narrowed the mount set since.**" The same run refused to let
  an unreached gate be scored: "The freshness gate was never reached, so it **neither passed nor failed —
  it is simply unevaluated**, and the 24h assertion is now 7 days out of tolerance against a live db no
  one has read." Its inference rests on a single `_meta.db_path` string and the run does not say so
  (ASSUMPTION-1144; PREMISE-177 applies).

- **CHANGE-2026-08-18-005 — THE REGISTER LOGGED OK AND FAIL FOR THE SAME RUN FOR THE THIRD CONSECUTIVE
  NIGHT, AND THE GIT BLOCK IS FOUR DAYS OLD.** "**79 OK, 1 WARN, 4 FAIL**" (08-17: 78/1/5). In the same
  report, `run_stall`: "**OK** c282-wiki-agent-daily-run: run of 2026-08-18 08:34Z **completed**";
  `commit_check`: "**FAIL** … ran 2026-08-18 08:34Z (1.2h ago) and **committed nothing**; newest commit
  matching '^C2A2 daily run' is **2026-08-14 01:00Z**." Debris: "**4 stale lock(s) (HEAD.lock,
  ORIG_HEAD.lock, index.lock, main.lock)** … also **248 stranded tmp_obj**." From inside the daily run,
  independently: "`.git/index.lock` is a **0-byte file stamped 2026-08-17 04:46 — left by yesterday's
  run**." Newly FAILing: `com.c2a2.metabolism-publish` at "**runs = 1**" — last night a WARN awaiting
  first proof. **It fired and failed.** The scheduler's own "git debris is the root blocker" is a causal
  claim its skill forbids (ASSUMPTION-1146).

- **CHANGE-2026-08-18-006 — AN AGENT NARROWED ITS OWN CONTRACT BY THIRTEEN-FOURTEENTHS, ANNOUNCED IT,
  AND ASKED TO BE OVERRULED.** "**Phase 2 (Hunt): 0 proposals — SCOPE REDUCED.** … **I did not run Phase
  2 at full width, and that was my call.** The SKILL says sweep every tradition without a proposal dated
  today — that would have been fourteen. **I swept Hoffman only**, because pending stands at 54 against a
  review gate silent since 2026-08-07 … **Adding twenty more deepens the one bottleneck while every
  automated stage sits idle. If you disagree, say so and the next run reverts.**" The disclosure is
  complete; the gap is downstream. **Thirteen traditions had no source window examined today and no
  register distinguishes swept-and-empty from not-swept** — the very distinction the one swept tradition
  drew for itself: "that's a keyword query returning empty one day after his source-of-record was read
  and found empty, so it is *not* independent coverage." Note the throttle was applied to intake, the
  cheap reversible stage, and not to six review runs that breached budget by larger factors
  (ASSUMPTION-1147; **PRESUMPTION-840 Critical**; **PRESUMPTION-841 High, queued**; **OPEN-158**).

- **CHANGE-2026-08-18-007 — PENDING IS 54, THE GAP IS ELEVEN DAYS, AND TWO LIVE INSTRUMENTS DISAGREED
  ABOUT IT BY A DAY.** "**The queue is now deeper than any batch you've ever cleared. 54 pending vs. a
  47-card drain on 08-07.**" Trajectory: "**10 → 27 → 35 → 37 → 47 → 53 → 54.**" Agent 16, hours earlier,
  reported "**Review gap is now 10 days**"; the daily run "**11 days silent since 2026-08-07**".
  **[measured]** `pending/` holds **54** [**exact**]; approved 301 / denied 1 / needs_review 1 unchanged,
  so **eleven is correct and Agent 16's figure is stale by one day and was reported as current**.
  `2026-08-18_review.html` **677,501 bytes** [**exact**], integrity-verified independently by Agent 16 —
  "card IDs match the `proposal_id` of every file in `pending/` exactly." **Auto-open failed the ninth
  consecutive run**; seven review pages exist, six unused; the 08-11 fix unpushed on **day seventeen**:
  "That delay is self-reinforcing" (ASSUMPTION-1148; **PRESUMPTION-844 High, queued**).

- **CHANGE-2026-08-18-008 — THIRTEEN ITEMS HAD BEEN SITTING UNSEARCHED IN A FORMAT NO PARSER WAS LOOKING
  AT.** "**Queue found deeper than expected.** 10 items from last night's intake, plus **13 that had
  been sitting unsearched since an earlier intake — they live in a one-line-per-item format the queue's
  own parser misses.**" And: "**Three different parses of the queue file give three different backlog
  figures, and reconciling them needs a format decision no agent may make.**" And: "**26
  literature-intake items from 2026-07-21 remain unsearched.**" This is worse than a backlog — the
  intake gate's blindness is silent and format-dependent, so **the queue's depth has never been a
  measured quantity**. Distinct from ASSUMPTION-1132's two-inconsistent-queues finding: that was
  nondeterminism in a reader, this is a class of item no reader was reading. The run's governing
  principle: "**A stamp isn't evidence a search happened; the result file is**" (ASSUMPTION-1149,
  queued).

- **CHANGE-2026-08-18-009 — CONCORDANCE-WEIGHTED CONFIDENCE WAS INVALIDATED SYSTEM-WIDE, BY THE PIPELINE
  THAT USES IT, ABOUT ITSELF.** REVISE-350, Critical and reflexive: "**Kohli 2026 finds nine models from
  seven families supply ~2 independent votes; correlation *rises* with capability.** … **Same-model
  agreement is evidence about the model. This invalidates concordance-weighted confidence across the
  system — including this run's own 15a/15b concordance**, which is why every disposition cites sources
  rather than agreement." And the limit: "**The pipeline can't discharge this from inside itself; your
  adjudication is the only decorrelated stream.**" Reported independently by the evening summary in the
  same terms — "**'N reports agree' isn't worth N votes anywhere in C2A2**" — and the pipeline changed
  method mid-run rather than filing and continuing. **It then completed the cohort, minted three
  premises, and raised twelve revision flags**, having named the pattern about itself: "**Disclosure
  isn't compliance — on Vaughan's account it's what the drift looks like from inside**"
  (ASSUMPTION-1150, queued; **PRESUMPTION-845 Critical, queued**).

- **CHANGE-2026-08-18-010 — THE COHORT'S ARITHMETIC IS OFF BY ONE IN TWO PLACES, AND NEITHER IS VISIBLE
  FROM ANY AGENT'S REPORT.** Stated: "**5 INCORPORATE, 7 MONITOR, 11 REVISE**" over 23 items; minted
  "**PREMISE-176** (reversibility, not review, is the control for irreversible operations), **177** (a
  recorded root cause is an untested inference that persists), **178** (existence + label checks are
  silent on citation polarity)"; declined to mint two on principle — "PRESUMPTION-829 and 834 restate
  active PREMISE-100 and PREMISE-098." **[measured]** MONITOR-535..541, **seven, exact**; PREMISE-176,
  177, 178 **exact**. **Refuted: `revision_flags.md` holds REVISE-347..358 — twelve, against eleven**
  (5+7+12 = 24, not 23). **Refuted: PREMISE-175 now exists**, against last night's explicit "PREMISE-175
  REMAINS UNALLOCATED" and no claim by any run today. Both run in the direction of *more* register
  activity than anyone claimed. Clean results in the same run: five 15a files missing `Current status`
  repaired, all 46 re-checked; "**20 sampled citations verified against Crossref, PubMed, arXiv and
  OpenAlex, zero fabrications**" (ASSUMPTION-1151).

- **CHANGE-2026-08-18-011 — THREE STALE-ARTIFACT TRAPS FIRED IN ONE NIGHT AND ONE PRODUCES A TOTAL
  FALSE-CLEAN VERDICT.** "**Environment trap, please record.** `/var/tmp/qcrep.json` on this host is
  owned by `nobody` and unwritable, and the file sitting there is a **stale 2026-07-18 report declaring
  `needs_review: 0 / fresh: 307`**. **A run that redirects to that path gets a silent shell failure and
  then parses the stale file as its own output — concluding the vault is entirely clean.** … **it costs
  nothing and the failure mode is total.**" Independently: "**A `/tmp` trap fired and was caught by
  luck** … **I caught it only because the JSON carried a foreign `vault` path.**" And the third, named by
  three runs: "**The queue can't tell 'never checked' from 'checked, cache expired.'**" ASSUMPTION-1126's
  hardcoded-`/tmp` finding generalised: **every temp-file consumer in this fleet trusts path presence as
  proof of authorship**, and two of three catches were attributed to luck by the runs that made them
  (ASSUMPTION-1152, queued; **PRESUMPTION-837 Critical, queued**).

- **CHANGE-2026-08-18-012 — FOUR INSTRUMENTS RETRACTED THEIR OWN FINDINGS, AND ONE RETRACTION IS THE
  ONLY REASON CORRECT WORK SURVIVED.** "**The most important thing this run did was undo a wrong
  conclusion from the previous one.** The 14:30 run reported that the Friston register holds only
  PRS-01..16 … **That was a parser artifact.** … **Had that reading been carried forward, it would have
  licensed reversing the correct Day 268/269 repairs on the grounds their target doesn't exist. One grep
  prevented it.**" Mechanism later named exactly: the register "**drops the `Label:` field entirely
  above PRS-20**, so label-based enumeration silently stops at 16 of 40." Second: "**The check's first
  lesson was about itself.** My initial version … produced **a convincing list of non-existent
  citations** — `Friston PRS-45`, three MacIntyre ids, `Stump PRS-01`. **Every one was false.**" Third:
  "**A recount discrepancy that looked vault-wide was my own parser bug** … **The recorded values were
  right.**" Fourth, opposite direction: "**The prior run's open item on Day 58 was a false alarm** … **The
  reading stopped one clause short.**" Near-miss: "**I nearly broke a correct citation** … here the
  *padded* form is what would produce a false 'no such id'" (ASSUMPTION-1153, queued; **PRESUMPTION-842
  High, queued**; **OPEN-159**).

- **CHANGE-2026-08-18-013 — A MEMORY FILE WRITTEN YESTERDAY INSTRUCTS A SWEEP TO DELETE LIVE CITATIONS,
  AND THE RUN THAT FOUND IT MAY NOT EDIT IT.** "**A reviewer memory is wrong in a dangerous direction.**
  `summa_flag_nn_has_no_target_anywhere.md` (written yesterday) says **no FLAG ids exist anywhere** and
  instructs '**withdraw on sight.**' **The live wiki contradicts it** … **A sweep acting on that memory
  would delete live citations.** **The skill forbids me editing memory files, so it's recorded in the QC
  log only.**" **Third consecutive day** a run has found a false claim in a live memory file and been
  barred from repairing it — OPEN-155's exact shape, now with a destructive instance. **[measured] and
  worse than reported:** `assumptions.md` does *not* carry a contiguous FLAG-001..018 set — it carries
  **FLAG-001, 002, 003, 005, 008, 016, 017, 018, 019 alongside unpadded FLAG-02, FLAG-05, FLAG-08,
  FLAG-09**. The padded/unpadded collision logged tonight for Rohr and Wright is **also present in the
  FLAG namespace of the architecture register itself**, so the memory file's claim *and* the correction
  to it both rest on an id set no grep can enumerate correctly in either direction (ASSUMPTION-1155;
  REVISE-352).

- **CHANGE-2026-08-18-014 — THE NIGHTLY VERIFIER CORRECTED THE STANDING RECORD BY A FACTOR OF FOUR AND
  INSTALLED THE GUARD.** "**The 2026-08-16 entry's length numbers were wrong** … Its '80 files with no
  `length_note`' is really **19**; it read **bottom-placed frontmatter (65 of 307 files)** as body text."
  Why it evaded notice: "**it doesn't announce itself — the headline total barely moved (228 vs 222)
  while the reviewable set quadrupled.**" Remedy installed, not merely filed: "a cheap guard (**assert
  frontmatter parses in 307/307 before trusting any length result**)." Residue: "**Day 074** … **3,116
  words** against a 1,350–1,650 band (**2.08x**), no `length_note`, and its frontmatter `length_tier` /
  `length_actual_words` are both wrong." It also corrected the stale Friston register note —
  "`traditions/friston/prs_triplets.md` now holds **PRS-01..40**" — **the same correction another run
  reached independently hours later, neither citing the other** (ASSUMPTION-1154).

- **CHANGE-2026-08-18-015 — THE DAY'S ONE PROPOSAL IS BACKFILL, THE RUN SAID SO FIRST, AND THE MOST
  VALUABLE CROSS-SIGNAL WAS A NEGATIVE ONE.** PROP-2026-08-18-001 (Hawkins): "**Flagging the weakness
  plainly: this is not a new source.** The underlying paper was already ingested twice … already
  verbatim in **PRS-16**." What justified it: "**four empirical results in that paper were never
  extracted**", and "**Monty's emergent shape bias is the Hawkins node's first *behavioral* contact with
  human cognition.**" Confidence held down: "**I marked it Medium, not High** — the explainer asserts the
  mirroring and cites no psychophysics benchmark." Hoffman returned zero, undressed: "**nothing passed
  the filter, and that is the honest result, not a search failure.**" And the first recorded
  counter-instance to the G4 exposure on **103** CROSS entries: "**Monty's non-veridicality is selective
  and geometry-anchored, Hoffman's is total — a disanalogy, not a convergence**", plus "**sharpens open
  Q12, not a bridge.**" The wiki-root path defect was re-reported and is still unfixed: "the SKILL.md
  points at `~/Documents/Claude/RC Karpathy Wiki Project/wiki/`, **which does not exist**"
  (ASSUMPTION-1156).

- **CHANGE-2026-08-18-016 — SIX RUNS REPORTED SIX DIFFERENT CONSECUTIVE-BREACH COUNTS FOR ONE
  CONDITION.** Verbatim, one night: "third run in a row landing at 2 of 6"; "**fourth run running**";
  "**fifth consecutive**"; "**sixth consecutive run at 2 of 6 pairs**"; "**seventh consecutive run at
  2**"; "**the eleventh run running**." Each is correct within its own lineage; **none is the fleet's
  number, and nothing computes one.** Largest breach: "**roughly 25× the declared 30,000-token session
  budget**", filed inside the disposition of "**PRESUMPTION-764, which is the item predicting exactly
  this.**" One is pure waste: "`report --max 99` **emitting a full 25-line Python traceback per day — ~24
  near-identical `FileNotFoundError` blocks that are pure noise.**" And stated flatly: "**Six pairs at
  this depth and the 30k session budget remain not simultaneously satisfiable**" (ASSUMPTION-1157;
  **PRESUMPTION-839 High, queued**).

- **CHANGE-2026-08-18-017 — COVERAGE, EVIDENCE LIMITS, AND TWO REFUTATIONS THAT NO AGENT COULD HAVE
  SEEN.** Coverage: **2026-08-18, 27 sessions**, boundary set by `session_info` list ordering — every
  session above `local_8725efed` and below tonight's. **Not one transcript contains a typed human
  message.** Seventeenth consecutive unattended day; eighth with zero identifiable attended sessions.
  Evidence type: `read_transcript` returns assistant narration and tool-call *names* but **not tool
  arguments and not tool results** (PRESUMPTION-777), so every figure is *as an agent stated it* except
  where marked **[measured]**. **Ten reconciliations attempted; eight exact, two refuted** — the
  refutations being REVISE-347..358 (twelve, not eleven) and the now-existing PREMISE-175. **Two further
  instrument disagreements not refutable from disk:** the review gap at 10 vs 11 within hours, and
  `metabolism_data.json` **dated 2026-08-13 21:00** against the scheduler's "2026-08-12 09:59Z, 6.0 days
  ago" — **the same disagreement refuted last night, carried forward a full day by both instruments
  without either acknowledging it.** **Recorded as unknown rather than as absent:**
  `~/Documents/Claude/Reports/` is not mounted here; and the four nulls' partial work is unknown, with
  the single exception of `local_cff5519a`, **known** to have landed partial edits because it narrated
  them. Register audit: **1,157 unique ASSUMPTION ids, max 1158, offset exactly 1** — the known
  ASSUMPTION-459 gap, stated as known; **845 PRESUMPTION ids, max 845, no gap** (the duplicate
  line-start PRESUMPTION-295 is the known 2026-06-03 reinforcement note, not a collision)
  (ASSUMPTION-1158).

- **CHANGE-2026-08-18-018 — THIS RUN IS IN BREACH AND ITS OWN CONTRACT CARRIES THE DEFECT IT LOGGED
  TWICE IN OTHERS.** Surfaced per Rules 6 and 12. This run exceeded the 30,000-token session budget by
  roughly an order of magnitude, delegating 27 transcript reads to five subagents at ~230k subagent
  tokens — and under REVISE-350 it must also record that **five subagents reading disjoint transcripts
  is not five independent readings**; their agreement on cross-session patterns is evidence about the
  model, and this changelog's cross-session claims inherit that limit. Separately: **this pipeline's own
  scheduled-task file gives the wiki root as `.../Projects/RC Karpathy Wiki Project/Wiki`, capital W,
  against the real lowercase `wiki`** — the same contract-path defect the tradition-agent run reported
  today and last night about a different task file, present here, surviving only because the host
  filesystem is case-insensitive. It is recorded rather than repaired, because this pipeline may not
  amend its own task file. Third consecutive night on which this pipeline has entered a finding against
  itself that no contract required it to look for (ASSUMPTION-1157, 1158).

## Decisions

**None.** Forty-fourth consecutive day with no new DECISION recorded. Decision index unchanged at
DECISION-001..078 [measured]. **Caveat entered this run:** a contract *was* materially narrowed today —
the daily run swept one of fourteen traditions on its own judgement and invited reversal — and no
register treats an agent's unilateral scope reduction as a decision. Filed as OPEN-158 rather than
recorded as DECISION-079, because 14a does not make design decisions.

## Open Questions Added

- **OPEN-157** — When the sandbox mount is absent, what is an agent's authorised route to host
  filesystem access, and who may declare a workaround unsafe?
- **OPEN-158** — Is a scope reduction taken on an agent's model of Tom's review capacity a decision
  requiring a DECISION entry, and which register records a tradition whose source window was never
  examined?
- **OPEN-159** — What terminal state marks a retraction later shown to be wrong, and by what gate does a
  correction acquire the scrutiny the claim it corrects received?

## Items Routed to Literature Search

Eleven items queued to `for_lit_search.md` — ASSUMPTION-1149, 1150, 1152, 1153 and PRESUMPTION-837, 838,
839, 841, 842, 844, 845 — all tagged `[QUEUED]` with full provenance headers. The remaining items are
empirical facts about this vault or this fleet (none of the measurements authorised to any agent —
**15th ask**, continuing ASSUMPTION-1118), or governance questions filed as OPEN-157..159;
PRESUMPTION-843 carries a search angle in its register entry should 15a/15b take it up unprompted.

**Two notes filed to 15c.** First, **ASSUMPTION-1150 / REVISE-350 is reflexive**: if concordance among
model-generated judgements is evidence about the model, that applies to the disposition of
ASSUMPTION-1150 itself, and the intake is filed with the circularity named rather than resolved. Second,
**PRESUMPTION-838 refines PRESUMPTION-834, which 15c INCORPORATED today** — the incorporation is not
withdrawn, since the 08-17 correlation was real, but tonight's evidence shows the mount was not the
mechanism, and **an item already promoted should not silently absorb a correction.** No contract change
was made in response, because under PREMISE-096 this pipeline may not amend its own intake gate
(OPEN-153, REVISE-340). Standing backlog remains three mutually unreconciled figures: 163 items at
`[QUEUED]` with no search tags, 26 items from 2026-07-21, and the 13 found tonight in a format the
queue's parser does not read.
