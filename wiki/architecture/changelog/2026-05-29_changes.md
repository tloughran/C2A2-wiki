# C2A2 Architectural Changelog — 2026-05-29

## Daily Narrative

A multi-session attended demo-path day built around Sociogram navigation and the participant-registration principle Tom surfaced on his morning walk. Increment 1.5 — the deterministic friendly-label typeahead (jump-to-thinker, no LLM, the Pathway-27 substrate) — was built, verified in both standalone and Summa-embedded copies, and pushed to the feature branch; Version 1.6 (a bare-guess `focus: x ~ y` parser) was coded and logic-validated 16/16 but **held deliberately**, because a focus-fade bug surfaced — isolate computes the correct node set (185 nodes) but the fade does not render. The day's pivotal epistemic moment was Tom's foreground confirmation ("on focus: l ~ s: edges stay lit"), which established the bug as real and retired the agent's earlier hidden-tab probe (an rAF-throttling artifact); Tom also locked the interaction model ("leave the current model": search is a transient lens, checkboxes are hard filters, no sync). Tom's walk question — "if I add a new thinker, does everything just pick them up?" — was traced to ground and pinned as **Pathway 28: Single-Source Participant Registration**, with a Rule-12 fail-loud gap flagged (`get_group()` silently greys a directory absent from `COLORS`). Today's epistemic signature: 10 new ASSUMPTIONs + 10 new PRESUMPTIONs (balanced 1:1, the attended-demo-path-day signature), **OPEN-068 promoted** at the 5th instance of the binary-framing pattern, and the **5th consecutive attended cycle with zero PRS extraction** — strengthening the FLAG-I picture that the lit-pipeline now sees as a 5-route REVISE cluster.

## Changes captured

### New ASSUMPTIONs (10 total today: 253-262)

- **ASSUMPTION-253** — The Sociogram focus-fade bug is real (Tom's foreground `focus: l ~ s` -> edges stay lit), not a hidden-tab testing artifact.
- **ASSUMPTION-254** — Prime suspect is the d3 `.transition()` opacity calls; likely fix is plain `.attr('opacity')`.
- **ASSUMPTION-255** — v1.6 is held deliberately (coded, logic-validated 16/16, not pushed/regenerated) because its isolate/link share the buggy opacity mechanism.
- **ASSUMPTION-256** — Interaction model locked (Tom: "leave the current model"): search/`focus:` is a transient highlight-in-place lens; checkboxes are hard filters; no sync.
- **ASSUMPTION-257** — The recent crash was pure memory pressure, not the edge cap; `MAX_EDGES=30000` stays.
- **ASSUMPTION-258** — Increment 1.5's deterministic friendly-label typeahead (no LLM) is the correct Pathway-27 substrate, replacing the earlier library-science requirement.
- **ASSUMPTION-259** — (Pathway 28) The tradition/structure vocabulary fans out from one `COLORS` dict; checkboxes and focus typeahead are siblings of that source and cannot drift.
- **ASSUMPTION-260** — Adding a participant is a single-source operation: one `COLORS` line + vault files + regen.
- **ASSUMPTION-261** — The session-handoff rail (gitignored handoff doc + CLAUDE.md "read handoff first on resume" rule) fixes next-session resume because CLAUDE.md auto-loads and steers it.
- **ASSUMPTION-262** — 16/16 logic validation establishes 1.6 parser-level correctness; visual/fade behavior is a separate foreground-tab verification deferred behind the hold.

### New PRESUMPTIONs (10 total today: 277-286)

- **PRESUMPTION-277** — The fade-bug verdict generalizes from one foreground query/user/browser to the whole fade mechanism (single-observation generalization). Risk: Medium.
- **PRESUMPTION-278** — The hidden-tab rAF confound is presumed isolated; remote-Chrome probes still trusted for other visual diagnoses without re-examination. Risk: Medium.
- **PRESUMPTION-279** — Holding all of v1.6 presumes "ship nothing broken" dominates shipping the validated parser with the fade flagged off; partial release not considered. Risk: Low-Medium.
- **PRESUMPTION-280** — Pathway 28's "cannot drift" presumes COLORS is the only coupling surface; the get_group -> 'root' silent fallback is an existing leak. Risk: Medium-High.
- **PRESUMPTION-281** — "One COLORS line + regen" presumes registration stays cheap at N=33/100 (palette distinctness, regen time, ~20MB HTML size unexamined). Risk: Medium.
- **PRESUMPTION-282** — The handoff rail presumes the next session honors the rule and keeps the doc current; no failure mode defined (success-criteria gap). Risk: Medium.
- **PRESUMPTION-283** — Framing the rail as "the system practicing its own thesis" presumes self-application is evidence for the pathway (self-referential confirmation risk). Risk: Medium.
- **PRESUMPTION-284** [5th binary-framing instance] — The interaction-model choice was offered as two clean options and resolved by preference without a usability test, subordinating the reframe/unify third category. Triggered OPEN-068. Risk: Medium-High (pattern-level).
- **PRESUMPTION-285** — "16/16 logic validation" presumes the 16 cases cover the parser's input space; coverage undefended, and the fade bug shows logic-pass != visually working. Risk: Medium.
- **PRESUMPTION-286** — Today's demo-path day is read as "correct prioritization, not recursion" within the same apparatus that flags the 5-cycle zero-PRS streak — closed-loop self-diagnosis bias at the prioritization layer. Risk: Medium-High.

### New OPEN questions (1 new today: OPEN-068)

- **OPEN-068** — *What is the project's bias toward binary diagnostic/design framings, what third category is repeatedly subordinated, and what is the structural cost of the recurrence?* Promoted at the **5th instance** of the binary-framing pattern (PRESUMPTION-253/259/262/267/284); the prior two runs deferred promotion "pending one more cycle," and today's interaction-model choice is that cycle. OPEN-067 carried (4th consecutive cycle unresolved). Near-promotion watch: the un-numbered-DECISION-accumulation pattern, now at 4 candidates.

### Decisions (0 new numbered; 1 new candidate + accumulation worsens)

- **NEW today, un-numbered: Sociogram interaction-model decision** (ASSUMPTION-256). Tom-locked verbatim ("leave the current model"); more firmly settled than the other candidates yet still un-numbered. AWAITING-TOM-NUMBERING; 1st cycle.
- **NEW today, un-numbered: Sociogram v1.6 deliberate hold** (ASSUMPTION-255) and **MAX_EDGES=30000 retained** (ASSUMPTION-257).
- **Candidate DECISION-048** (4th cycle), **DECISION-049** (3rd cycle), **AI-search-delegation** (2nd cycle): all carried, AWAITING-TOM-NUMBERING. The accumulation is now **4 un-numbered candidates** — ASSUMPTION-251's tracking-blind-spot framing strengthens; PRESUMPTION-271 surfaces whether DECISION-numbering is itself a FLAG-I gate.
- **Pathway 28 — Single-Source Participant Registration** pinned as a principle (not a numbered DECISION); Rule-12 `get_group()` fail-loud gap flagged for fix.

### REVISE backlog status

18 items AWAITING-REVIEW carried at start of day; today's 2026-05-29 lit-pipeline (15c) dispositioned the 2026-05-28 batch (243-252 + 267-276), filing **7 new REVISEs (REVISE-065..071)** on PRESUMPTION-267/269/270/272/273/274/275. Total AWAITING-REVIEW backlog: **25 items** (18 carried + 7 new), **the highest count on record**. HIGH-urgency: 047/048/050/056/063 carried + REVISE-071 (self-referential elevation, completes the 063+064 cluster) = **6 HIGH**. The FLAG-I REVISE cluster now spans **5 routes** (REVISE-053/056/058/066/070). **4th consecutive day OPEN-gate with no REVISE-backlog action.** *(REVISE/MONITOR figures are maintained by the 15-pipeline; this pass reports them as carried context.)*

### Pipeline status (after this run)

- **Lit-search daily-cycle queue:** 0 queued at start of this pass (the 2026-05-28 20-item batch was drained earlier today by the lit-pipeline: 13 MONITOR + 7 REVISE + 0 INCORPORATE; new IDs MONITOR-264..276, REVISE-065..071, DISPOSITION-087..106). **20 new items routed to `for_lit_search.md` as QUEUED (cycle 0) tonight** — 10 ASSUMPTIONs (253-262) + 10 PRESUMPTIONs (277-286). Next 15d weekly review due 2026-06-03.
- **PREMISE high-water mark:** **PREMISE-043** (unchanged since 2026-05-21 — **8 days without a new validated premise**).

### Reconciliation performed this run (0 items)

This 14a/14b pass is **extraction-only**; today's lit-pipeline dispositions are reflected in `monitor_queue.md` / `revision_flags.md` (written by the 15-pipeline agents directly).

### Carry-forward / RECONCILIATION TODO (fail-loud)

- **The 2026-05-20 lit batch reconciliation remains UNDONE** (ASSUMPTION-201,202,204-208 + PRESUMPTION-221-230 still read UNTESTED on disk; **seventh consecutive run carrying this TODO**).
- **The 2026-05-25 dated changelog/snapshot artifact gap** carried (PRESUMPTION-257/264; REVISE-059/064 touch it; not actioned). The REVISE-059 atomicity streak advanced to N=7/N=6 at the start of today (both 2026-05-28 artifacts wrote cleanly); tonight's two artifacts are the next atomicity test.
- **The first-newline truncation auto-send path** (ASSUMPTION-240/242; PRESUMPTION-263 HIGH via REVISE-063) remains unfixed at the code level. **No code-level fix today.** Tonight's evening cowork-to-chat delivery **did not run** — Chrome is logged out of claude.ai — so there was no fresh truncation test this cycle, but the logged-out failure is itself a delivery-path fragility (the evening sync silently depends on a live claude.ai session).
- **REVISE backlog at 25 AWAITING-REVIEW** (6 HIGH; FLAG-I cluster 5 routes) — recommended for the next attended session.
- **Four un-numbered DECISION candidates** persist (048/049/AI-search-delegation/Sociogram-interaction-model); numbering remains the fastest blind-spot to close.
- **NEW today**: Pathway 28 Rule-12 gap — make `get_group()` fail loud on a tradition present on disk but absent from `COLORS`.
- **NEW today**: The focus-fade bug must be diagnosed in a **foreground** tab (`.transition()` -> `.attr('opacity')`); it gates the 1.6 push. Resume cue in the handoff doc: "fix the focus-fade bug."

---

## Provenance

PROVENANCE:
  Origin: 14a
  Chain: [14a, 14b]
  Original item: CHANGELOG-2026-05-29
  Item type: CHANGELOG
  Transform at each step:
    14a/14b: Compiled from today's attended Cowork sessions (Sociogram navigation work local_c03ffdb8 — increment 1.5 push, Pathway 28 capture, handoff-rail build; Version 1.6 proceeding local_b2468f6e — 1.6 parser coded + held, fade-bug confirmed in Tom's foreground tab, interaction-model locked; C2a2 morning chat scrape local_a0e8cb60; Morning walk cowork handoff local_9555ec26; C2a2 history scrub prep local_3a6baadc; C2a2 evening cowork to chat local_430eacb6 — assembled the central daily-sync digest at architecture/daily_sync/cowork_to_chat/2026-05-29_cowork_summary.md). Extracted 10 ASSUMPTIONs (253-262) + 10 PRESUMPTIONs (277-286); routed 20 new testable items to for_lit_search.md as QUEUED at cycle 0; promoted OPEN-068 (binary-framing-pattern bias, 5th instance); recorded the Sociogram interaction-model decision as a NEW un-numbered candidate (4th accumulating). The 2026-05-20 reconciliation remains deliberately UNDONE; the 2026-05-25 artifact gap is not back-filled. REVISE/MONITOR/PREMISE counts read from the 15-pipeline's files (it ran earlier today).
  Current status: COMPLETE

---

*Run by: c2a2-self-awareness-daily scheduled task (autonomous EOD; Tom not present at run time)*
*Run timestamp: 2026-05-29 EOD (covers 2026-05-29)*
*Next scheduled run: 2026-05-30 EOD*
