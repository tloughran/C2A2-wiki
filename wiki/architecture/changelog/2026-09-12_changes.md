# Architectural Changes — 2026-09-12

## Narrative

The day the estate produced evidence against one of its own glosses, and discovered that it has no way to
hold a correction. The hunt surfaced a preprint reporting that invasive tumours *over*express connexins,
which is incompatible with the cancer-as-loss-of-coupling reading this network repeats in several places;
the daily run declined to ingest it, marked it as the wiki's interpretation rather than the authors', and
asked that Levin PRS-02's downstream citations be reviewed. The Summa verification run measured that set
the same night at 24 syntheses, 23 of them carrying the vocabulary — and recorded that a QC pass had
re-run one of those files **28 minutes after the challenge was filed** and left the gloss unhedged. Three
standing architectural claims collapsed on contact with their artefacts, all three caught by opening a
file: a month-old TOOLING flag whose defect was repaired on or before 2026-08-12, a defect list built on
reading a maximum id as a count, and a phantom 261-file backlog. The lit pipeline returned a fifth
consecutive zero-mint cycle and adopted rather than filed a Critical flag whose claim is that 178 days of
named in-house measurement stand unexecuted across four premises, every remedy a grep. Two scheduled jobs
failed on the same day for the same reason, and the reason turned out not to be permissions: the OpenStory
database has grown 3.05 → 6.43 GB in six weeks and the sandbox path is now permanently broken. The
cross-connection count reached five figures, two of them minted today by runs that could not see each
other. Sixteen runs read against nine last night, which closed the Summa coverage gap and yielded the
register's highest single-day count — thirty-six items, one of which indicts the register's own output
format. No attended session; outbound sync dark a fifth day, inbound a tenth; thirteenth consecutive day
on which every item extracted here is agent-stated.

## Changes Detected

- **CHANGE-2026-09-12-01 — the hunt contradicted the wiki, and handled it correctly.**
  Grodstein–Mathews–Levin (bioRxiv, 09-02): invasive tumours *over*express connexins, non-invasive solid
  tumours underexpress them. The network's gloss treats coupling as monotonic with cooperation; the
  abstract does not. "An invasive tumour may be a well-coupled collective pursuing the wrong target, which
  is a different failure mode." Carried Speculative, marked as the wiki's reading and not the authors',
  **not ingested**. ASSUMPTION-1336 — the cohort's only genuinely external question, routed to 15a/15b.
- **CHANGE-2026-09-12-02 — the challenge's blast radius was measured, in a different project, and is held
  by nothing.** 24 syntheses cite Levin PRS-02; **23 carry the coupling-loss vocabulary**; Day 080 cites
  PRS-02 without it, so the set cannot be swept. **Day 199 was re-QC'd at 22:28, 28 minutes after the
  challenge was filed at 22:00, and still carries the gloss unhedged.** A review request written into
  prose is not a mechanism — the deferred-condition leakage pattern reproducing in a second subsystem on
  the day it was created. ASSUMPTION-1337, OPEN-201.
- **CHANGE-2026-09-12-03 — a month-old standing flag was withdrawn, by the agent that misdiagnosed it
  yesterday.** The Gmail compose **is** `generate_review_page.py`'s own submit button
  (`submitDecisions()`, 303–312); one `pid` per proposal at line 116; position-based numbering survives
  only as a fallback for **1 file of 414**. Repaired on or before 2026-08-12. "The error cost: runs kept
  reading the flag's text instead of the file." **OPEN-196 answered in substance, unratified.**
  ASSUMPTION-1341.
- **CHANGE-2026-09-12-04 — two more claims collapsed the same way.** The daily run flagged
  `stump/prs_triplets.md` (footer 38, last id PRS-41), then killed its own alarm by counting headers —
  **"last id is not count"** — which also invalidates the 09-10 sweep's defect list, **whose remaining
  entries were not re-checked**. And a naive filename scan claimed **261 unprocessed** files against an
  empty measured queue: the known slug trap, discarded rather than acted on. ASSUMPTION-1334,
  ASSUMPTION-1335; generalised as PRESUMPTION-971.
- **CHANGE-2026-09-12-05 — fifth consecutive zero-mint cycle, and the Critical flag was adopted rather
  than filed.** 4 items, both directions: 0 INCORPORATE · 1 MONITOR limb · 4 REVISE. 15b's claim —
  that in all four an ACTIVE premise already named the cheap in-house measurement and none was run
  (PREMISE-108 54d, 120 53d, 135 45d, REVISE-342 26d; **178 days owed, zero executed**) — supersedes
  ASSUMPTION-1321's propagation diagnosis for this cohort. "Consumption has occurred; discharge has not."
  All five remedies are greps. ASSUMPTION-1342.
- **CHANGE-2026-09-12-06 — this register's own pre-check was found defective in both directions.** The
  09-11 intake named PREMISE-174/173/108 as covering for ASSUMPTION-1321; both search directions
  independently found **PREMISE-123 and 116**, neither named, and that 174 does not bear. It routed
  PRESUMPTION-968 on the stated ground that no external base rate existed — the register has held one
  since 2026-08-17. **Two subagent searches spent on an answered question.** ASSUMPTION-1343, REVISE-460.
  Tonight's routing carries a per-item declaration of whether its pre-check was a grep or a reading.
- **CHANGE-2026-09-12-07 — the cross-connection count reached five figures, two minted today.** Daily run
  banner **108** ("counted from headers"); evening sync **136** (distinct `CROSS-` ids); against 107 / 131
  / 135 already standing. **Both new figures come from runs inside the same day.** Connection density
  non-computable for a second day. The settling measurement is one `grep -c`. ASSUMPTION-1333, OPEN-195,
  PRESUMPTION-969.
- **CHANGE-2026-09-12-08 — the lit queue's backlog was measured, not carried: 151 against the 163 held
  since 09-04.** Oldest item 2026-07-05, **69 days**. The discrepancy was declared unresolved with both
  methods named — the day's better handling of a divergence, and the contrast that makes
  PRESUMPTION-969 visible. ASSUMPTION-1344. 15d lane unserved an eighth cycle.
- **CHANGE-2026-09-12-09 — failure moved from permission to physics.** The OpenStory db has grown
  **3.05 GB (2026-08-01) → 6.43 GB**; at ~45 MB/s the backup needs ~140 s against a 45 s per-call ceiling,
  and 6.4 GB does not fit ~7.2 G free. Metabolism regen **failed a sixth time**; OpenStory telemetry
  refresh failed the same day on `ENOSPC` (5.99 GiB vs 4.0 GiB free) with both workarounds tried, rejected
  and reasoned. "The sandbox path is now permanently broken by DB growth." ASSUMPTION-1348,
  PRESUMPTION-978, OPEN-197. **Collateral correction owed by this register:** 6.43 GB and 5.99 GiB are the
  same number in different units, so yesterday's three-way db-size divergence was two unit conventions and
  one stale reading, not three disagreeing measurements.
- **CHANGE-2026-09-12-10 — two runs named the same one-line test and neither could run it.** The
  metabolism run refused to convert its own failure into a verdict about the upstream — "'the producer
  didn't run' is never evidence about the upstream, and the two fail apart" — and the scheduler check
  built the same disambiguation into its FAIL row, naming
  `sqlite3 open-story.db 'select max(timestamp) from events'`. The day's best epistemic conduct and its
  sharpest instance of the measurement debt. ASSUMPTION-1349.
- **CHANGE-2026-09-12-11 — a guard applied nine days ago has silently reverted.** `permissionMode` is
  absent from `c282-wiki-agent-daily-run`; an unattended run will hang on the first prompt; it was set
  2026-09-03 after **8 stalls in 30 days on 8 different tools**; re-applying requires the desktop app to
  be quit "or the app will write the field back out from memory." Today's run completed without it.
  ASSUMPTION-1347. Scheduler: 88 OK / 1 WARN / 3 FAIL.
- **CHANGE-2026-09-12-12 — the review queue refilled to five within hours of being declared structurally
  safe, and the five are of three warrant classes.** Agent 16 noted this morning that a one-card queue made
  FINDING-089's batch-approve leak impossible "until the queue refills." By evening: three Levin cards
  from read sources, one Wolfram card written as a **retrieval instruction** with its Solution field
  unclaimed, one Carroll card **verification-gated** with its transcript unread — one page, one button,
  **PREMISE-204's four conditions satisfied by none of them**. The evening sync also corrected the day's
  own census from 4 to 5. PRESUMPTION-973.
- **CHANGE-2026-09-12-13 — an empty window was read as a fact about a person, not a tradition.** The
  Wolfram specialist found the 30-day window genuinely empty and argued explicitly that a seven-week
  silence after a bereavement "reflects that hiatus, not a quiet research program; the vault has been
  bitten by that exact confusion before." One proposal written — the 2026 SRI Opening Keynote, "the only
  source in the wiki where Wolfram would be setting a research agenda rather than answering someone else's
  question" — as a retrieval instruction, **the recording unheard**. No CROSS entry proposed: **"an
  unheard recording cannot warrant one."** ASSUMPTION-1338, ASSUMPTION-1339, OPEN-203.
- **CHANGE-2026-09-12-14 — the retrieval floor set the yield for a second consecutive day.** Two in-window
  Levin leads (Erickson et al.; Fields & Levin on cognitive offloading, which bears on the cognitive-virus
  card) were **not** written up because their abstracts would not retrieve: "building a card from a title
  is the Wright mistake." Wright PROP-2026-08-14-033 failed a **fourth** time; one new hand-checkable lead
  (KSBJ's *Between Beliefs* index). ASSUMPTION-1340, OPEN-198.
- **CHANGE-2026-09-12-15 — one parser trigger, three independent hits, one near-miss at 69 items.** A
  blank line after a `---` separator: the nightly verification counted 65 files' frontmatter as body and
  "nearly produced a 69-item false report"; the qc sweep found its word count off by the entire YAML
  footer; the reviewer specified the fix. **The failure direction is upward** — it manufactures
  overlength, "which in a run whose business is withdrawing false bracket claims would have looked like
  diligence." Each run repaired its own copy; none proposed a fixture. ASSUMPTION-1351, PRESUMPTION-976,
  OPEN-202.
- **CHANGE-2026-09-12-16 — a repair surfaced a citation defect the gap had been hiding.** Writing Day 29's
  missing difference-from-Aquinas accounting exposed that Q.56 a.1's non-mediated self-knowledge rests on
  a bare stipulation, and that Kastrup PRS-02, the record cited for it, "supplies privacy, not
  transparency." Generalised by its own author: **"an unexamined warrant never gets checked against its
  record."** Also: Day 29 sits nine days off the D019–D022 chain and convicts identically, so the defect is
  "a house habit of the commentary voice, not a propagation failure — the 21-file candidate list should be
  worked as a list, not a chain." ASSUMPTION-1352, ASSUMPTION-1353.
- **CHANGE-2026-09-12-17 — a warrant survived a 373-word arithmetic error because it was not pinned to a
  number.** Day 35's `length_note` justifies by article count rather than ratio: **"a note pinned to a
  number would have gone false."** Left unrepaired per the standing 08-15 decision. Day 30's ±25%
  certification was struck (honest recount 4357, ratio 1.452). The reviewer declined three criterion-(b)
  rewrites in one unattended pass — "a batch edit on a judgement call." Understated
  `length_actual_words` worsened to **83% of re-QC'd files, from 51%**. ASSUMPTION-1354.
- **CHANGE-2026-09-12-18 — a health report disclosed that two of its own green sections looked at nothing.**
  "`uptime`, `free`, and `ps` report on the Linux sandbox, not macOS … Sections 3 and 4 say 'nothing
  unusual' because there is nothing visible — not because your Mac's processes were checked."
  PRESUMPTION-962's shape confirmed from the inside one day later, by the run's own honesty. The
  disclosure is in prose; the green is in the status field. ASSUMPTION-1355, PRESUMPTION-979 (routed).
- **CHANGE-2026-09-12-19 — a governing rule was declared unmeetable by construction.** "Budget: per-task
  4,000 breached (contracts and six memory files are ~15k before any work begins)." Every task in the
  estate loads the same contract layer; a rule breached universally at t=0 selects nothing. Still cited as
  governing in task files. **This register's run breaches it identically.** ASSUMPTION-1356,
  PRESUMPTION-980.
- **CHANGE-2026-09-12-20 — the register filed an item against its own output format.** PRESUMPTION-974,
  **Critical**, routed: has a named-but-unowned in-house test in this estate ever been anything but a
  declined one? Eleven of tonight's twelve presumptions name such a test. **OPEN-200 is upstream of
  OPEN-192 and OPEN-199 both**, since each of those is itself waiting on one.
- **CHANGE-2026-09-12-21 — infrastructure and channel state.** Commit path **worked**: `be3245b`, 39 paths
  committed on the Mac, deliberately unpushed, 2 held. `metabolism_data.json` 9 days stale; its 36 h
  freshness gate red and unread a sixth day. Janitor flagged `reindexer_freshness` (Summa index missing
  Day-001 and Day-035). BOSCO: 30,529 emails indexed, **28,516 snippet-only**, enrichment heartbeat
  disabled. Summa daily batch reports the series **complete at Day 307** and recommends its own
  retirement. Chat→Cowork failed a tenth day, Cowork→Chat a fifth; Channel 2 unexercised 20 days;
  Channel 3 deaf a ninth. `execution-assistant` enabled, no cron, never run. Seven MCP servers await
  OAuth; three fail on dynamic client registration.

## Items Produced

- `assumptions.md`: **24** (ASSUMPTION-1333–1356). 22 SUPPORTED in-house, 1 routed, 1 framework
  commitment. 8 cross-project. Highest single-day yield on record, driven by coverage.
- `presumptions.md`: **12** (PRESUMPTION-969–980). 1 Critical, 7 High, 1 Medium-High, 1 Medium.
  4 routed, 8 held in-house.
- `open_questions.md`: **5** (OPEN-200–204). OPEN-196 moves to *answered, unratified*.
- `for_lit_search.md`: **5 routed** of 36 produced, with the reasoning and the against-interest
  declaration written in; 31 held in-house and declared.
- `decisions.md`: no new decisions. **DECISION-083 stands at sixteen days. Twenty-one rulings owed.**
- `metrics/2026-09-12_snapshot.md`: written.

*Written by the 14a/14b evening run, 2026-09-12. Registers snapshotted as `*.bak.20260912-pre-14eod`
before any append. Sixteen of twenty-six reachable runs read; the eight unread are same-shape Summa
repeats of two that were read. No attended session.*
