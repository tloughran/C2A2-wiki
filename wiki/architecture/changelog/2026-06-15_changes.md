# 2026-06-15 Changelog

## Narrative
An attended design session ("Metabolism visualization and conscious realist monism") drove three workstreams. WS1 surfaced and named all four cut-offs in the Agent Metabolism view: the two view-layer ones (sparse yield trailing-zeros and a stale Jun-10 right edge) were fixed and rendered into a previewable build from existing data, while the two database-layer ones (the Apr-6 "interactive cliff" holding ~95% of captured output, and a 28/33-lane output-token flatline) were diagnosed but left for a Mac-side probe, made "honest" in the view rather than read as real zeros. WS2 settled the source for the PRS-triplet yield dimension (git history of `traditions/*/prs_triplets.md`) without yet building it. WS3 produced three interactive CRM-team mockups grounded in the 15 `traditions/` folders. 14a extracted four stated assumptions (the files-added headline metric, the git-history triplet source, gap-honest rendering, and the 15-folder roster); 14b surfaced six unstated presumptions clustering on measurement commensurability, version-control-as-clock, visibility-vs-comprehension, the capture-vs-reality framing of the cliff, folder-as-membership, and a competitive paradigm metaphor. Two new decisions and one open question were registered.

## Changes
- NEW: ASSUMPTION-318 (stated) — files-added/day is the right headline yield series for the Metabolism view. Routed MEDIUM. QUEUED (cycle 0: 2026-06-15).
- NEW: ASSUMPTION-319 (stated) — git history of `traditions/*/prs_triplets.md` yields valid "triplet-completed" dates (PRS-NN per commit-day). Routed MEDIUM. QUEUED.
- NEW: ASSUMPTION-320 (stated) — gap-honest visualization (showing absence explicitly) is preferable to interpolation/silent zero. Routed LOW-MEDIUM. QUEUED.
- NEW: ASSUMPTION-321 (stated) — the CRM "team" = the 15 `traditions/` folders (14 thinkers + MacIntyre as rationality-epistemologist, Loughran as integrator). Routed LOW (framework commitment). QUEUED.
- NEW: PRESUMPTION-349 (unstated) — one added file = one unit of yield (commensurability beneath the files-added metric). Type epistemic/methodological; risk Medium. QUEUED.
- NEW: PRESUMPTION-350 (unstated) — git commit timestamps are faithful clocks for knowledge-production events. Type methodological/epistemic; risk Medium. QUEUED.
- NEW: PRESUMPTION-351 (unstated) — a visible gap-marker is an understood gap (visibility = comprehension). Type epistemic/methodological; risk Low-Medium. QUEUED.
- NEW: PRESUMPTION-352 (unstated) — the post-Apr-6 cliff/flatline is a capture artifact, not a real activity change (probe not yet run; reading unverified). Type epistemic/methodological; risk Medium-High. QUEUED; raised in parallel as OPEN-083.
- NEW: PRESUMPTION-353 (unstated) — vault folder-count is the authoritative source of team membership (1 folder = 1 member = 1 seat). Type structural; risk Low-Medium. QUEUED.
- NEW: PRESUMPTION-354 (unstated) — adversarial inter-paradigm contest ("rival team," "open seat") is the right frame for CRM's relation to alternatives. Type normative/structural; risk Low-Medium (touches telos). QUEUED.
- NEW: DECISION-057 — Metabolism measurement charter: files-added/day as headline yield + gap-honest rendering of cut-offs; `--from-json` render path added; previewable build `metabolism_view_REVIEW.html`. View layer ADOPTED; DB-layer root-cause + regen PENDING on the Mac; nothing pushed.
- NEW: DECISION-058 — PRS-triplet yield source = git history of `traditions/*/prs_triplets.md` (PRS-NN per commit-day). Source ADOPTED; metric UNBUILT (next increment).
- NEW: OPEN-083 — is the post-Apr-6 cliff/flatline a capture artifact or a real output change? Decidable by `probe_openstory.py` (Mac). Load-bearing on metabolism-metric validity.
- CHANGE (view layer, not pushed): `metabolism_view.html` generator patched (dashed "interactive capture ends" horizon, hollow cadence-only rings, gap-honest day-bars, staleness badge, `--from-json`); rendered to `metabolism_view_REVIEW.html`; `CUTOFF_RECOVERY.md` + `probe_openstory.py` written for the Mac. JS validated (`node --check`, all integrity checks passed) per project rules.
- CHANGE (exploratory): WS3 `crm-team-mockups/crm_team_mockups.html` — Roster / 40-Step Dialogue Track / Paradigm Constellation, dependency-free, 15 members from the vault. No numbered decision (mockup).
- FLAG (Rule-7 divergence): the 2026-06-15 evening cowork→chat summary reported "No new decisions or open questions." 14a registers DECISION-057/058 and OPEN-083 anyway — they set durable, load-bearing instrumentation choices and a metric-validity question that meet the bar applied to DECISION-056. Registries are authoritative; the summary's count is a reporting-granularity difference.
- FLAG (carry, fail-loud): the two database-layer cut-offs (A interactive cliff, B output flatline) remain UNVERIFIED as artifacts — `probe_openstory.py` must run on the Mac before the "honest" view's capture-ended claim is confirmed (PRESUMPTION-352 / OPEN-083). Live `open-story.db` unreachable from this session's mount.
- CARRY: OPEN-082 (parser/linker bottom-frontmatter wall) still open and load-bearing on both Summa pipelines; sync-channel re-auth / model-config items (PRESUMPTION-347/348) still need Tom; extraction freeze; proposal queue (now +3 new tradition proposals in `inbox/proposals/pending/`); REVISE-111 (HIGH) pre-registration outstanding; M7/M8 verdicts pending; AWAITING-REVIEW backlog 65.
- NOTE: three new tradition proposals landed in `inbox/proposals/pending/` (per the evening sync) — relevant to PRESUMPTION-353 (folder-as-membership drift).

## Pipeline accounting (this run)
- Registry deltas: assumptions 317 → 321 (+4); presumptions 348 → 354 (+6); self-awareness total 665 → 675 (+10). Decisions 56 → 58 (+2). Open questions 82 → 83 (+1).
- Lit-search queue: +10 QUEUED (cycle 0: 2026-06-15); no 15a/15b/15c run today.
- Backups written (pre-14eod, 20260615): assumptions.md, presumptions.md, for_lit_search.md.
- Source basis: full read of the 2026-06-15 attended session transcript (WS1/WS2/WS3) + the 2026-06-15 evening cowork→chat summary; registry/ledger state for numbering and dedup.

PROVENANCE:
  Origin: 14a
  Chain: [14a], [14b]
  Item type: CHANGELOG (daily)
  Transform at each step:
    14a/14b: Produced from the 2026-06-15 attended metabolism session transcript and the evening cowork summary. 14a extracted four stated assumptions, registered two decisions and one open question, and produced the metrics snapshot; 14b surfaced six unstated presumptions, checked against 14a's output to avoid duplication. All testable items routed to for_lit_search.md.
  Current status: COMPLETE
