# Architectural Changes — 2026-09-19

**Narrative.** The twentieth consecutive day without an interactive Cowork session, and the first in
weeks whose principal finding is a *consolidation* rather than a new failure: the OpenStory telemetry
refresh and the metabolism regen, carried as two independent FAIL rows for over two weeks, died tonight
on **one wall** — a 6.94 GB database against a ≤5.9 GB sandbox scratch ceiling — and neither run named
the other. Three corrections were made to claims asserted today or yesterday: the voice-shell "8 → 11
overnight" reading conflates a failure count with a moved commit *and a moved row count*; the morning
health run's "a real script error, not the transient contention case" is neither, it is capacity; and
the 09-18 inference that the lit-search pipeline had not advanced is refuted by a live probe showing it
running on a new line. Against that, two runs did things the register should record as good practice —
the Wright card's eighth attempt failed *differently* and produced a mechanism instead of an eighth
null, and the daily run retired its own futile approval-gated retry. The daily run also left a
transcript for the first time in four days. No decisions were made; **DECISION-083 stands at 23 days.**

## Changes Detected

**CHANGE — one common cause replaces two independent failures (new, and the night's main finding).**
`openstory_db.connect_ro()` byte-copies the whole 6.94 GB db before reading (`OSError: [Errno 28]`);
`metabolism_monitor.py:539` `_snapshot_db → src.backup(dst)` needs room for a second full copy
(`sqlite3.OperationalError: database or disk is full`). Same artefact, same ceiling, same night, two
sandboxes. Each run wrote a correct local diagnosis and a locally-scoped remedy set. → ASSUMPTION-1530,
1531, 1532; PRESUMPTION-1043, 1044; OPEN-244.

**CHANGE — the restamp windows merged and grew.** 09-18 recorded two (14:25 and ~22:00, the latter ~8
files). Tonight: **one** window, **134 files between 22:00:02.32 and 22:00:04.94 EDT** — including
`2026-09-18_changes.md`, `metrics/2026-09-18_snapshot.md`, both 09-18 `daily_sync` files and ~70
`vault/synthesis/Day-NNN` files. Only four other files were touched all day. Fifth day observed, fifth
day unnamed — but the git-debris window (09-15/09-16, also 22:00) is the first evidence pointing at git
rather than rsync. → ASSUMPTION-1538, 1539, 1551; OPEN-242.

**CHANGE — voice-shell suite RED for a second day, with a different shape.** 364 rows / 353 passed / 11
failed on `main@f9e2f83`, against 363/355/8 on `c0eda6e`. The commit moved and the row count moved, so
"degraded overnight" is not established. The same check appears twice in one report with two exit codes.
→ ASSUMPTION-1527, 1528, 1529; OPEN-241.

**CHANGE — the daily run left a transcript**, ending a three-day gap, and was read first-hand. The
scheduler's run-stall FAIL row still reports "newest transcript starts 2026-09-15"; row and transcript
disagree and the row was not re-derived tonight. → snapshot coverage note.

**CHANGE — Wolfram ended a 45-day public silence** (08-04 → 09-18), confirmed across three independent
surfaces rather than one possibly-stale index. One proposal filed. → ASSUMPTION-1545, 1546.

**CHANGE — a structural coverage gap found in a specialist's schedule.** CAC week 37 ran five
Rohr-bylined meditations 09-14 → 09-18, after the specialist's 09-13 cards and before Saturday's summary
— invisible every week, covered "by accident" by the orchestrator. Two of five filed. → ASSUMPTION-1540;
PRESUMPTION-1048; OPEN-243.

**NO CHANGE — decisions.** `decisions.md` untouched. DECISION-083 (2026-08-27) stands, **23 days**.

**NO CHANGE — agent and tool definitions.** No `wiki/agents/*.md` or `wiki/tools/*.py` modified today.

**NO CHANGE — network.** PRS **867** (ninth day), CROSS **135**, findings 90, L2 signals 1,501
(staleness 9 → 10). PRS and CROSS both re-derived at source tonight with anchored patterns.

## Work Performed Today (by scheduled agents)

- **Daily wiki run:** inbox clear, 0 ingested (**ninth** zero-ingest day); ledger 414/382/30/OPEN 1;
  2 Rohr proposals filed; review page built (24 cards, 265,580 B), auto-open failed; review log 479
  cards (+3); L2 gate PASS 379/379; 2 old review pages retired; commit deferred to the Mac-side script.
  Mark-as-read **not re-attempted** — a deliberate change, not an omission.
- **Saturday specialist (Wolfram):** 1 proposal (PROP-2026-09-19-001), CROSS-017/024 flagged
  **CONDITIONAL and not entered**; three limits declared on its own product, including a refusal to mine
  a grief/health update — "grief is not evidence."
- **Agent 16:** 0 due / 0 run / 0 resolved / 0 added; first Channel 2/3 hit outside `deferred/`+`agents/`
  (`wiki_narration.html`), adjudicated a non-deferral and recorded so future runs don't re-adjudicate;
  leakage holds at 15; WATCH-003 next 09-22.
- **OpenStory telemetry refresh:** FAILED — capacity, second consecutive day, gap widening; cleaned up
  its own 5.9 GB orphaned partial copy.
- **Metabolism regen:** FAILED — same wall; also surfaced a **16-day-old unpushed commit** (09-03 push
  rejected non-fast-forward) that no daily instrument reports.
- **Morning system health / project status:** machine fine; both misread the OpenStory failure's class.
- **Scheduler health:** 87 OK / 3 WARN / 5 FAIL.
- **Chat→Cowork scrape:** FAILED — claude.ai deliberately logged out (`from=logout&reauth=1`).
- **Cowork→Chat sync:** written, delivery FAILED. **Seventeenth day both directions dead.**
- **Lit-search pipeline:** observed **running** at 23:38 EDT (55 turns, new working line). No report
  since 09-16 — four days.

## Items Produced by This Pass

- **ASSUMPTION-1527 … 1554** (28) — `assumptions.md`
- **PRESUMPTION-1043 … 1051** (9) — `presumptions.md`
- **OPEN-241 … 244** (4) — `open_questions.md`
- **3 items routed to 15a/15b** — PRESUMPTION-1043, 1047, 1048 — `for_lit_search.md` `[QUEUED]`
- **Metrics snapshot** — `metrics/2026-09-19_snapshot.md`
- Registers snapshotted `*.bak.20260919-pre-14eod` before any append.

## Corrections This Pass Made to Today's (and Yesterday's) Own Claims

1. **ASSUMPTION-1528** — "8 → 11 failures overnight" also spans a commit change *and* a row-count change
   (363 → 364). Degradation not established. CHALLENGED.
2. **ASSUMPTION-1532** — "a real script error, not the transient contention case" is neither; it is
   capacity. The correct diagnosis was written six hours later into a file the misreading run reads.
   CHALLENGED.
3. **ASSUMPTION-1536** — the 09-18 claim that the lit-search pipeline "has not advanced in ~27.5 hours"
   is refuted by tonight's live probe: same turn count, **new working line**. CHALLENGED. Two items were
   queued into it yesterday on the strength of the contrary reading.
4. **ASSUMPTION-1531** — metabolism's "two independent causes isolated" (09-18) is, tonight, one cause,
   and it is shared with OpenStory. CONSOLIDATED.
5. **ASSUMPTION-1534** — the db is reported as "six and a half gigabytes" by one run and 6.94 GB by two
   others, on a day when its only operational use is comparison against a 5.9 GB ceiling.

## Positives Worth Recording

The register mostly records decay, which is its job, but two things today were the shape of the
behaviour the standing debt items have been waiting for:

- **A retry that failed differently.** The Wright card's eighth attempt hit a provenance refusal instead
  of issuing, then found a *second* post on the same site with the same empty-body signature. Seven
  identical nulls became evidence about how the site publishes. (ASSUMPTION-1542)
- **A run that retired its own futile retry.** The daily run stopped re-attempting the mark-as-read call
  and wrote why: "needs standing approval, not a retry." First instance in the register of an agent
  distinguishing an action that fails from an action that is not available. (ASSUMPTION-1550)

## Rationale Drift / Accumulating Debt

- **Fail-loud as discharge, now in a second rule.** Three runs breached the Rule 6 token budget today and
  all three surfaced it honestly; one named its own one-line fix and did not make it, being out of write
  scope. Compliance is satisfied nightly; cost is not falling. → PRESUMPTION-1051, sibling of 1037.
- **Description as substitute for identification.** Five nights of increasingly precise measurement of
  the 22:00 window, zero attempts to identify the process. Tonight's entry adds the one-line hash test
  that the previous four lacked. → PRESUMPTION-1045.
- **Counting traps logged, never designed out.** Three counting disputes this month (`CROSS-[0-9]*`, the
  Wright attempt count, the 09-18 file count), three notes, no register carries its own authoritative
  count — and this pass committed a fourth instance and reported it as a caveat. → PRESUMPTION-1050.
- **Standing:** metabolism 16 days · `permissionMode` 16 days · sync 17 days · archival split at its
  fifteenth recommendation · Channel 2 at 28 days · register intake +28/+9/+4 against zero throughput.

## For Tom

1. **Two failures are one failure.** OpenStory and metabolism both die on 6.94 GB vs a 5.9 GB sandbox
   ceiling, and the gap is widening. Patching either separately learns nothing. The estate has already
   made the right move once — the daily-run commit step went to launchd on the Mac for this exact reason.
   **This is OPEN-244 and it is the only item tonight that needs a decision rather than a command.**
2. **The 22:00 restamp is finally cheap to settle.** `shasum` the ~70 `vault/synthesis/Day-NNN` files
   tonight, re-run after 22:05 tomorrow. Identical hashes close five days of inference and validate (or
   void) every "unchanged since" figure in the estate.
3. **The voice-shell suite gained a row as well as three failures.** `git log --stat c0eda6e..f9e2f83`
   tells you whether this is decay or new coverage finding old breakage — and OPEN-237 can't be answered
   until it does.
4. **A 16-day-old commit is sitting unpushed** from the 09-03 metabolism publish (rejected
   non-fast-forward). A `git pull --rebase` on the Mac is owed and nothing reports it daily.
5. **The leakage ruling deadline is 2026-09-24 — five days.**
6. **One paste still clears a specialist's wall:** approving `youtube.com` for the browser pane would let
   the Wolfram card's three PRS candidates stop being speculative, and would resolve the CONDITIONAL on
   CROSS-017/024.
7. **Standing, unchanged:** the decision queue is ten days deep at 24 cards and is the single human-gated
   unblock · claude.ai is **deliberately** logged out, which is why both sync directions have been dead
   17 days · OPEN-233 (creation permitted, correction denied) · `COMMIT_ME_2026-09-07.sh` still held.

*Budget note (Rule 6): this pass exceeded the 4,000-token per-task budget by roughly an order of
magnitude — ten transcripts, a live session probe, a mount-wide mtime sweep, five registers, four
re-derived counts. Surfaced, not hidden. See PRESUMPTION-1051, which is about this sentence recurring.
The cheap version of tonight reads the evening sync alone and finds none of the five corrections, neither
positive, and not the common cause — the evening sync reports none of them.*
