# Architectural Changes — 2026-08-30

*Agents 14a (assumption extraction) and 14b (presumption detection). Gap-filling run: the pipeline
last fired 2026-08-27; 08-28 and 08-29 produced no changelog and no snapshot.*

## Narrative

The day the system broke two of its own registers and told on itself twice. The 15-pipeline ran its
largest cycle ever — 19 items dispositioned, 3 premises minted, 14 monitors opened — and mid-run
truncated `for_lit_search.md` from 1,847,543 bytes to zero by opening it for writing before it had
computed what to write; it rebuilt the file from the previous night's backup plus a replay of the
08-29 tags from in-session memory, and filed REVISE-412 at HIGH rather than repairing quietly. Hours
earlier the sewing agent, on the eleventh firing of a "one-time" bootstrap, ran `git status` on a
read-only census and left a zero-byte `.git/index.lock` the sandbox cannot unlink, which will fail
tomorrow's 05:45 commit until Tom deletes it. Both incidents are disclosed, both are traced to a
write the agent's job did not require, and both remedies proposed today are permission-shaped —
which is where this run's most interesting finding sits, because the sewing agent's out-of-scope call
is what produced the one verified piece of good news on the record (the uncommitted tree cleared,
309 → 82 paths). Sixteen assumptions and eleven presumptions were extracted; the sharpest of them are
not about the defects but about what the day took for granted while handling them: that in-session
memory counts as an independent copy, that self-report is a detector, that adopting every challenge
into a premise makes it better, and that three tradition agents converging on the same tension is a
duplication problem.

## Items Produced

| Register | Added | Range |
|---|---|---|
| `assumptions.md` | 16 | ASSUMPTION-1233 – 1248 |
| `presumptions.md` | 11 | PRESUMPTION-893 – 903 |
| `open_questions.md` | 4 | OPEN-175 – 178 |
| `decisions.md` | 0 (dated run note appended) | — |
| `for_lit_search.md` | 21 items routed (27 tag lines) | — |
| `metrics/` | 1 snapshot | `2026-08-30_snapshot.md` |

Backups written before all appends: `*.bak.20260830-pre-14eod` for assumptions, presumptions,
open_questions, decisions and for_lit_search.

## Changes Detected

**Registers moved by other agents (not 14a/14b):**

- `revision_flags.md` — REVISE-410, 411, 412. **REVISE-412 is the first self-observed 15-pipeline
  defect on the register**, and the first flag whose source item is "none — self-observed."
- `validated_premises.md` — PREMISE-191, 192, 193, all ACTIVE, **all three minted by adopting 15b's
  challenge into the statement rather than outweighing it.**
- `lit_search_returns.md` — DISPOSITION-851–869, MONITOR-571–584.
- `monitor_queue.md` — 12 first re-triggers (MONITOR-532–543, cycle 0→1), 178 carries to 2026-09-06,
  5 lane exits, 24 undated intake entries dated. Standing lane 249 → 238 → 250.
- `for_lit_search.md` — 19 items re-tagged, and the truncation/rebuild described above.
- `synthesis/` — 11 bridge notes stamped; **six of the eleven involve Rohr.**
- `inbox/proposals/pending/` — PROP-2026-08-30-001/-002/-003, three Rohr proposals. Pending 0 → 9.
- `review/2026-08-30_review.html` — generated, 9 proposals.

**No changes** to `wiki/agents/*.md` or `wiki/tools/*.py` this run.

## Rationale Drift

**One instance, within a single document.** `2026-08-30_cowork_summary.md` reports that the three
Rohr proposals "all *independently* raise the same false-self/individuation tension" — independence
carrying evidential weight — and two paragraphs later recommends treating them "as one paradigm flag
rather than three before the wiki carries three near-duplicates," where the same independence is a
duplication problem. Filed as ASSUMPTION-1242 (the stated recommendation), PRESUMPTION-900 (the
premise it rests on) and OPEN-177 (the question for Tom).

## Accumulating Technical Debt

- **The 26 alias notes: third week at 0 of 26.** Links behind them grew 146 → 165; ~20 accrue per
  deferred week; 59% of all broken links close on execution. **PRESUMPTION-898 / OPEN-178** ask why
  a task this size is routed to a human by an agent that wrote 243 verified insertions elsewhere in
  the same run without asking.
- **`.git/index.lock`** — blocking tomorrow's commit; one line from the Mac.
- **`DEFERRED_ACTIONS_2026-08-27.md` still unreachable** — 17 deferred actions untriaged, purely
  mechanical.
- **PRESUMPTION-822** (no register records which build a PRS-triplet figure refers to) — sixteenth
  consecutive night. **PREMISE-191, validated today, is exactly this claim, and its own generating
  case remains unresolved** — the same anomaly REVISE-411 raises against PREMISE-122.
- **Reciprocal `## Cited by` index on tradition hubs** — 0 of 15, unchanged since 08-16.
- **`connectivity_log.csv` exclusions** — machine trees (raised 06-28) and `node_modules` (08-23)
  still unfiltered; the difference is exactly 97 paths, the node_modules population.

## Self-Referential Observations

Three this run, and they are the substance of it rather than decoration.

1. **An agent that self-reports damage it cannot prove it repaired correctly is in the same epistemic
   position as a tradition reporting its own progress.** The summary raises this rhetorically;
   PRESUMPTION-894 files it as a live premise the reconstruction depended on, and PRESUMPTION-895
   sharpens it — the architecture has no read-side integrity control at all, so Rule 12 is not a
   backstop to detection, it *is* the detection.

2. **The premise register may be reporting on its own amendment practice.** 147 of 148 premises
   ACTIVE, and today all three new ones were minted by absorbing the challenge against them.
   PRESUMPTION-901 asks the Lakatosian question this project cannot avoid asking of itself, since
   MacIntyre's criterion for a progressive tradition — that it can say what its rivals could not —
   is not satisfied by absorbing what they said. Filed as OPEN-176.

3. **This run is its own evidence.** 14a/14b could not reach an interactive transcript for 2026-08-30
   and extracted from a derived daily digest instead. A digest is a record of what someone already
   noticed; 14b exists to surface what nobody noticed. **A presumption detector reading a digest is
   reading the output of the process it audits.** Filed as PRESUMPTION-903 — and the reason the
   three-day pipeline gap should be treated as evidence destroyed, not work deferred.

## Requires Tom

1. `rm -f "/Users/tomloughran/Documents/Claude/Projects/RC Karpathy Wiki Project/.git/index.lock"` — before 05:45.
2. **REVISE-412's human check** — do the reconstructed 08-29 entries for ASSUMPTION-492/497/498/500
   and PRESUMPTION-517/519/527 read as expected? Anything else edited on 08-29 is gone.
3. **OPEN-175–178** — four rulings, none requiring research.
4. **OPEN-168, day eight** — authorise a notification channel, or formally accept the disconnection.
5. **`review/2026-08-30_review.html`** — 9 proposals, 3-day review gap.

---

*Budget disclosure (Rule 6): this run exceeded the 4,000-token per-task budget. Cause: a three-day
gap-fill across a 1.8 MB / 2.0 MB register pair plus two unplanned incident reports. Disclosed, not
absorbed — and PRESUMPTION-892 applies to this record too, which likewise names no number.*
