# Changelog — 2026-04-09

**Narrative:** Major architectural session. Began with a bug fix: the review page generator was producing duplicate HTML element IDs because hunt agents independently assign proposal_id values from a small cycling pool (005-008). Fixed by overriding with sequential IDs in the generator script. Then pivoted to a deep structural read of the Hawkins Thousand Brains paper (arXiv 2412.18354), which revealed that C2A2's 13-agent architecture is a structural implementation of the Thousand Brains Theory — not by design, but by convergent evolution. Six new PRS triplets were extracted (beyond the 2 already proposed), including a paradigm-shift-grade insight about C2A2 itself. A redesign proposal was produced with 6 changes in 4 phases over 2 weeks. The Architectural History Agent (Agent 14) was created to ensure future design rationale is preserved.

---

CHANGE-2026-04-09-001:
  Component: tool script (generate_review_page.py)
  What changed: Line 112 — replaced `p.get("proposal_id", f"PROP-{run_date}-{i+1:03d}")` with `f"PROP-{run_date}-{i+1:03d}"` (always sequential). Line 299 — matching fix for the submitDecisions JS pid array.
  Why: "The 11 hunt agents each independently assign proposal_id values in their frontmatter using a small counter (005-008), so the 20 proposals shared only 4 unique IDs. Since HTML getElementById returns the first match, clicking any sidebar item past #4 would show and highlight the wrong card/item." [from session]
  Alternatives considered: (a) Fix the IDs in each agent's prompt so they generate unique IDs upstream. Rejected because it requires coordinating 11 agents and the generator is the single point of control. (b) Use the proposal filename as the ID. Not discussed.
  Downstream effects: All future review pages will have unique sequential IDs. Agent-assigned proposal_ids in frontmatter are now ignored by the review page generator (they remain in the .md files for agent-internal tracking).
  Reversibility: Trivial
  Status: Implemented

CHANGE-2026-04-09-002:
  Component: workflow (proposal pipeline)
  What changed: Created a supplementary proposal file convention. File: `2026-04-09_hawkins_thousand-brains-deep-read-supplement.md` with proposal_id `PROP-2026-04-09-SUPP-001`. Contains 6 additional PRS triplets from a deep cross-tradition reading of the Thousand Brains paper.
  Why: "The existing proposals correctly flag Friston, Levin, and Wolfram connections; this supplement identifies new bridges to Hoffman, Kastrup, Fredrickson, Stump, and — most significantly — to the C2A2 system itself." [from supplement file]
  Alternatives considered: (a) Edit the existing proposal files to add new triplets. Rejected to preserve the original agent-generated analysis as a clean baseline. (b) Create separate proposal files per new triplet. Rejected as excessive file proliferation.
  Downstream effects: The review page generator picks up any .md file in pending/, so supplementary proposals are automatically included. The SUPP-NNN convention distinguishes them from agent-generated proposals.
  Reversibility: Trivial
  Status: Implemented

CHANGE-2026-04-09-003:
  Component: meta-architecture
  What changed: Produced redesign proposal (C2A2_redesign_proposal_2026-04-09.md) identifying C2A2 as a Thousand Brains system and proposing 6 changes: (1) dispatch protocol enhancement, (2) lateral agent communication, (3) active inquiry cycle, (4) voting/consensus protocol, (5) PRS displacement vectors, (6) Architectural History Agent.
  Why: "The structural mapping is architecturally precise, not metaphorical: tradition agents are learning modules, wikis are reference-frame-based models, dispatches are the Cortical Messaging Protocol, and the Pattern Detector is the voting mechanism." [from session analysis]
  Alternatives considered: None explicitly discussed — the mapping was considered self-evident once identified.
  Downstream effects: If approved, triggers a 4-phase, 2-week implementation sequence affecting all 11 agent definitions, the Master Agent, the dispatch format, and the PRS format.
  Reversibility: Moderate (each phase is independently reversible)
  Status: Proposed — pending Tom's approval

CHANGE-2026-04-09-004:
  Component: agent definition
  What changed: Created Agent 14 — Architectural History Agent. File: `wiki/agents/14_architectural_history_agent.md`. Created directory: `wiki/architecture/changelog/`. Seeded `wiki/architecture/decisions.md`.
  Why: "We need an architectural history agent: it should inspect our daily conversations, generate and archive .md files describing changes made and detailed rationale." [direct request from Tom]
  Alternatives considered: None — requested directly.
  Downstream effects: Agent count rises from 13 to 14. Master wiki overview should be updated to reflect. Agent 14 needs scheduling (end-of-day or end-of-session).
  Reversibility: Trivial
  Status: Implemented (pending first run + scheduling)
