# 2026-06-16 Changelog

## Narrative
An attended build session ("PRS triplet yield WS-2") realized the WS2 PRS-triplet yield metric that DECISION-058 had settled-but-left-unbuilt on 06-15. The metric counts triplet *production* as the first git appearance of each (tradition, PRS-NN) in `traditions/*/prs_triplets.md`: 264 cumulative produced across 6 commit-days (04-07 → 06-16), 262 unique on disk, with two retired ids and one reused id flagged fail-loud; a single commit-message cross-check (06-07 "+38") matched the series and the metric was declared verified. The same session fixed a 3D-connectome layout collision — position keyed only on (thinker, year) had collapsed 269 triplets onto 47 stacks — with a deterministic fan that restores 269 separable positions. 14a extracted six stated assumptions (production definition, the single-point cross-check, the gross-cumulative headline, supersession of the static 269, metric-before-view sequencing, and the deterministic fan); 14b surfaced six unstated presumptions clustering on the measurement validity of the new metric (creation-as-yield, one-point-verification, one-true-count-vs-three-constructs, resolvability-as-fidelity, git-as-census, and provenance-as-validity). One decision and one open question were registered; the 06-15 lit-search cohort was searched and dispositioned today by the 15-pipeline, adding PREMISE-063.

## Changes
- NEW: ASSUMPTION-322 (stated) — PRS-triplet production = first git appearance of each (tradition, PRS-NN). Routed MEDIUM. QUEUED (cycle 0: 2026-06-16).
- NEW: ASSUMPTION-323 (stated) — a commit-message self-report ("+38") is an adequate cross-check that verifies the yield series. Routed MEDIUM. QUEUED.
- NEW: ASSUMPTION-324 (stated) — yield headline = gross cumulative produced (264) reported alongside on-disk-unique (262); retired/reused ids kept in cumulative. Routed LOW-MEDIUM. QUEUED.
- NEW: ASSUMPTION-325 (stated) — the git-derived series supersedes the static "269 network" count as authoritative. Routed MEDIUM. QUEUED.
- NEW: ASSUMPTION-326 (stated) — build the metric before iterating the view layer that depends on it ("cart-before-horse"). Routed LOW-MEDIUM. QUEUED.
- NEW: ASSUMPTION-327 (stated) — a deterministic (reproducible) layout fan beats random jitter for separating co-located 3D nodes. Routed LOW. QUEUED.
- NEW: PRESUMPTION-355 (unstated) — the meaningful event is a triplet's creation, not its revision/use/retirement; yield = birth-rate. Type epistemic/methodological; risk Medium. QUEUED.
- NEW: PRESUMPTION-356 (unstated) — one corroborating point (06-07) verifies the whole 6-day series. Type epistemic/methodological; risk Medium. QUEUED.
- NEW: PRESUMPTION-357 (unstated) — the three counts (269/264/262) estimate one quantity to reconcile, not three distinct constructs. Type epistemic/structural; risk Medium-High. QUEUED; raised in parallel as OPEN-084.
- NEW: PRESUMPTION-358 (unstated) — making all 269 nodes resolvable increases fidelity (visual resolvability = informational fidelity); the fanned grid positions are semantically empty. Type epistemic/methodological; risk Medium. QUEUED.
- NEW: PRESUMPTION-359 (unstated) — git history is a complete census of production ("264 produced", not "264 git can see"). Type methodological/empirical; risk Medium. QUEUED.
- NEW: PRESUMPTION-360 (unstated) — building the metric dissolved the cart-before-horse concern (data-backed ⇒ trustworthy-enough-to-build-on). Type epistemic/normative; risk Medium. QUEUED.
- NEW: DECISION-059 — PRS-triplet yield build charter: production = first git appearance; gross-cumulative (264) headline alongside on-disk-unique (262); git series supersedes the static 269; fail-loud on duplicate/retired ids; `prs_yield.py` + outputs in `architecture/metrics/`. BUILT + locally verified (validate_prs_3d.py / node --check / 06-07 commit cross-check). PUSH PENDING on the Mac.
- NEW: OPEN-084 — are 269 (connectome) / 264 (git cumulative) / 262 (on-disk) one quantity to reconcile or three distinct constructs to report separately? Sharpens OPEN-081; pairs with PRESUMPTION-357.
- CHANGE (view layer, not pushed): PRS 3D connectome (`template_prs_3d.html` → `prs_3d_review.html`) layout-collision fix — deterministic fan (group by (thinker, year), grid within each thinker's wedge, stable index order) restores 269 distinct separable positions from 47 stacks; indicator "Showing 269 / 269." JS validated (`validate_prs_3d.py` PASS, `node --check` PASS) per project rules. Recorded under DECISION-059.
- LIT-PIPELINE (today, on the 06-15 cohort): ASSUMPTION-318..321 + PRESUMPTION-349..354 all SEARCHED-15a/15b + DISPOSITIONED-15c (2026-06-16). Dispositions: INCORPORATE 1 (ASSUMPTION-320 → PREMISE-063 gap-honest visualization); REVISE 2 (PRESUMPTION-349 → REVISE-115; PRESUMPTION-351 → REVISE-116); MONITOR 7 (MONITOR-345..351, incl. MONITOR-349 HIGH = run the post-Apr-6 probe). Validated premises 62 → 63.
- FLAG (Rule-7 divergence): the 2026-06-16 cowork→chat summary reported "No new DECISION-NNN registered yet today" (citing overnight-pipeline timing). 14a registers DECISION-059 and OPEN-084 anyway — durable, load-bearing operational choices for a deployed self-measurement metric, meeting the bar applied to DECISION-057/058. Registries authoritative.
- FLAG (fail-loud): the WS2 series is "verified" on one commit-day only (06-07); the other 5 are unaudited (ASSUMPTION-323 / PRESUMPTION-356). Treat as cross-checked-on-one-day, not fully audited.
- FLAG (carry, fail-loud): `probe_openstory.py` still not run — the two data-layer metabolism cut-offs remain UNVERIFIED as artifacts (PRESUMPTION-352 / OPEN-083 / MONITOR-349 HIGH). Run before any metabolism regen.
- CARRY: OPEN-082 (parser/linker a/b/c) still open and load-bearing on both Summa pipelines (~65 reviewed-but-unmarkable files); pinned-model config fix (`claude-fable-5` → `claude-opus-4-8`) still needs Tom; extraction freeze; proposal queue 12 (review overdue); REVISE-111 (HIGH) pre-registration outstanding; M7/M8 verdicts pending; AWAITING-REVIEW backlog 65; ANTHROPIC_API_KEY blocks Ch. IX/X ingest.
- NOTE: sync channel restored (claude.ai signed in) — first successful morning scrape since 06-11; morning chat→cowork and evening cowork→chat syncs both delivered. Proposal queue 13 → 12 (one cleared).

## Pipeline accounting (this run)
- Registry deltas: assumptions 321 → 327 (+6); presumptions 354 → 360 (+6); self-awareness total 675 → 687 (+12). Decisions 58 → 59 (+1). Open questions 83 → 84 (+1). Validated premises 62 → 63 (+1, via 15c).
- Lit-search queue: +12 QUEUED (cycle 0: 2026-06-16). Separately, the 06-15 cohort (10 items) was searched + dispositioned today by 15a/15b/15c.
- Backups written (pre-14eod, 20260616): assumptions.md, presumptions.md, for_lit_search.md, decisions.md, open_questions.md.
- Source basis: full read of the 2026-06-16 attended WS2 session transcript (PRS-yield build + 3D fan-fix) + the 2026-06-16 morning chat→cowork and evening cowork→chat sync summaries + the on-disk `prs_yield_*` outputs; registry/ledger state for numbering and dedup.

PROVENANCE:
  Origin: 14a
  Chain: [14a], [14b]
  Item type: CHANGELOG (daily)
  Transform at each step:
    14a/14b: Produced from the 2026-06-16 attended WS2 build session transcript and the day's two sync summaries. 14a extracted six stated assumptions, registered DECISION-059 and OPEN-084, and produced the metrics snapshot; 14b surfaced six unstated presumptions, checked against 14a's output to avoid duplication. All twelve new items routed to for_lit_search.md (cycle 0: 2026-06-16). The 06-15 cohort's 15-pipeline dispositions are recorded for continuity (not produced by this run).
  Current status: COMPLETE
