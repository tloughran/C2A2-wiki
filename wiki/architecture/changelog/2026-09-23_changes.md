# Architectural changes — 2026-09-23

Twenty-seven sessions ran, all scheduled, and none contained designer speech. That makes twenty-four days;
the Chat bridges have failed for twenty-one. The day's best work was self-critical. The lit pipeline
tested its own independence premise, found it wanting, and downgraded its for/against split to "retrieval
diversity, not confirmation" (REVISE-483). The QC hold convention held across eleven runs; the 09-22
breach did not recur. The structural problem from yesterday persists and widened: findings do not travel
between jobs. The OpenStory failure had four accounts. Morning health said a regen "didn't run" when it
had run and failed. The scheduler again recommended a check two jobs had already done. A pattern newly
visible today: almost every unresolved item now ends with an escalation to Tom, which does not reach him
(PRESUMPTION-1079). Tomorrow two deadlines lapse with no owner.

## What ran

- **27 sessions** (list positions 1–27, above 7316a3f7) read by three parallel readers (ASSUMPTION-1655).
- **Daily run**: review page `2026-09-23_review.html`, 387,775 B (verified); 35 proposals; 13th
  zero-ingest day. **Wednesday agents**: Kastrup PROP-001 (5 triplets, secondary sources; 1660); Rohr
  PROP-002; McGilchrist 0 (Substack needs JS).
- **Lit pipeline**: the 09-22 cohort of 22 worked in full; PREMISE-214–217 and REVISE-483 (verified);
  SYSTEMIC-RISK flag on same-model independence. Backlog 147 → **155** after tonight's 8 literature items
  (`queue_scan.py`, verified).
- **Agent 16**: 2 new untracked conditionals, not tracked; **09-24 retrieval unowned** (1676).
- **Summa**: 6 reviewer runs (one died at a Desktop Commander call) and 6 QC sweeps (one blocked on disk),
  all held and none marked. Batch no-op 8. Nightly 250 violations, unchanged.
- **Failures**: OpenStory refresh (4th day; 7.2 GB db vs 5.9 GB free); metabolism (20 days stale; host
  fallback auto-declined); both Chat bridges (21st day); scheduler 87/3/5, identical to 09-22.

## Rationale drift

- **OpenStory**: "did run on time at 6:17" / "failed three days running" / "a script bug" / Errno 28 by
  trial (ASSUMPTION-1662).
- **Disk**: "any agent that needs the shell will fail", while other sessions ran bash normally and this
  pass's sandbox is at 38% (1666, PRESUMPTION-1080).
- **YouTube**: a missing library ("working again") in one run, `IpBlocked` in another; the qcseg framing
  from 09-22 is gone (1672).
- **Rohr**: Day 299 silently dropped; escalation softened into "flag every time, or advisory?" (1673).
- **Evening sync** says the lit run note "isn't in the file". It is (verified; 1680).

## Technical debt

- **Kastrup date error in an approved record**, three lines (verified). Noted, not fixed (OPEN-254).
- **Host fallback by unattended runs**, three attempts, only stopped by auto-decline (OPEN-255,
  PRESUMPTION-1082).
- **Task-file defects** now also include the wrong Kastrup wiki root and the missing
  `~/Documents/Projects/` (OPEN-251).
- **Tag vocabulary**: 14a/14b intake now uses the scanner's `[IN-HOUSE]` tag (1658).
- **Three zero-byte bridge files**, third night. **Master header** stuck at 09-20. **L2 stale_days 14**
  with no WARN (1677).

## Self-referential notes

- **REVISE-483 applies to this pass.** Same-model readers summarised same-model transcripts. Eleven claims
  were spot-checked at source, and two reader claims were corrected by that check (disk estate-wide;
  run note missing) (PRESUMPTION-1086).
- **Repetition-as-confirmation** appeared in five instruments on one day, although PREMISE-209 already
  names the remedy (PRESUMPTION-1083).
- **Budget**: ~205k reader-subagent tokens plus this pass. That is roughly 7x the 30k session guideline:
  lower than 09-22 (~290k) but still a breach. Declared (OPEN-250).
