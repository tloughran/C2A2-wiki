# Architectural Changelog — 2026-06-06 (Saturday)

**Narrative.** A second consecutive attended day, and an **execution** day: the headline is that yesterday's falsification got *acted on the next session*. The 2026-06-05 finding that the curated graph (CC-001…, 156) and the cards directory (C0001…, 855) were disjoint id spaces (ASSUMPTION-277) was answered today by **building the join** — the 156 curated communities were merged into the Cards directory under their own `CC-xxx` ids (cards now 1006 after deduping 5 bulk overlaps), so the graph is now a **literal id-subset of the cards** (DECISION-051). Two falsehoods Tom flagged in the "?" popover were corrected: records are now disclosed as **publicly-seeded without consent** (no community has approved its record), and the graphed set is now genuinely a subset of the carded set. A new source-of-truth doc (`explorer_tabs_complementarity.md`) was written to anchor the popover. Demo polish landed (graph controls hidden on Cards, empty-heatmap-row collapse, subtype dropdown), and a console error at `app.js:1314` was actively **disproved** as a stale buffer artifact (a positive downstream-handler test), a verification *success* rather than the usual produced≠verified gap. Today's extraction is **5 ASSUMPTIONs (278-282) + 5 PRESUMPTIONs (312-316)** — an even, assumption-leaning attended-day batch.

### Agent 14a (stated assumptions)
- **+5 ASSUMPTIONs:**
  - **ASSUMPTION-278** — the curated→cards subset-merge under `CC-xxx` ids is the right way to make directory⊇graph true and unblock the deferred cross-nav hand-off on the shared key. Type: architectural. (Routed, LOW.)
  - **ASSUMPTION-279** — Cards and Graph are "two surfaces, one instrument," mutually upbuilding (directory feeds graph; graph gives directory its purpose). Type: architectural/methodological. Refines 275/276. (Routed, LOW.)
  - **ASSUMPTION-280** — because records are public-seeded without consent and unapproved, the tool must disclose this in-product (popover + source-of-truth doc). Type: normative/methodological. (Routed, MED.)
  - **ASSUMPTION-281** — a source-of-truth doc that the generated popover mirrors ("edit here and regenerate") is the right anti-drift discipline. Type: methodological/architectural. **Held — framework commitment, parallels ASSUMPTION-274.**
  - **ASSUMPTION-282** — **[GROUNDED this session]** the `app.js:1314` null-throw can be dispositioned as a stale buffer artifact because it didn't recur on a clean reload and a handler wired *after* the throwing line still fired (subtype change handler synced the URL). Type: methodological. **Verified in-session — not routed.**
- **Decision index (`decisions.md`): +1 — DECISION-051** (graph becomes a literal id-subset of cards via the CC-xxx merge + in-product consent/provenance disclosure; realized in code on the feature branch, merge attended-only). Max now **DECISION-051** (was 050). An execution decision under the DECISION-050 umbrella, numbered because it makes a dated, substantive commitment.
- **Open questions (`open_questions.md`): +1 — OPEN-076** (is cross-join EDGE density sufficient to carry the P3 promotion pipeline now that the join is mechanically real?). **OPEN-075 downgraded OPEN → PARTIALLY-ANSWERED** — today's merge gives a partial empirical answer (the join is now real); density and shared-id-vs-identity remain open.
- **Carried-context resolution (reported, not a registry status change owned here):** the 2026-06-05 disjoint-id finding (ASSUMPTION-277, GROUNDED) was *acted on* this session by building the join — recorded as the development behind DECISION-051 and the OPEN-075 downgrade, not by rewriting 277.
- Metrics snapshot: `metrics/2026-06-06_snapshot.md` written.

### Agent 14b (unstated presumptions)
- **+5 PRESUMPTIONs:**
  - **PRESUMPTION-312** (High) — assigning shared `CC-xxx` ids presumes shared-key = genuine entity identity; the merge may have *manufactured* the identity yesterday's finding said was missing, rather than discovering it. *(Structural; pairs ASSUMPTION-277/278; the strongest fail-loud item today.)*
  - **PRESUMPTION-313** (Medium-High) — disclosing non-consent in the popover presumes in-product disclosure *discharges* the consent obligation; the don't-list / opt-in alternatives were never raised. *(Normative; pairs ASSUMPTION-280.)*
  - **PRESUMPTION-314** (Medium) — "communities articulated to a quality bar" presumes an applied gate, but the 156 are curatorial fiat and unapproved; the present graph instantiates the opposite of the earned-membership telos the same doc advances. *(Epistemic; pairs ASSUMPTION-276/279.)*
  - **PRESUMPTION-315** (Low-Medium) — the stale-buffer disposition presumes single-reload non-reproduction + one handler-fires check is a *complete* exoneration; reachability of the throwing init-state from real user sequences was not established. *(Methodological; pairs ASSUMPTION-282.)*
  - **PRESUMPTION-316** (Medium) — the "mutually upbuilding" complementarity narrative presumes pure reinforcement and ignores the failure mode where graph-absence *stigmatizes* the carded-only majority. *(Normative; the inverse-facing twin of PRESUMPTION-308; pairs ASSUMPTION-279.)*
- Dedup check against 14a and prior batches: 278-282 (stated) vs 312-316 (inferred) are non-overlapping. Pairings: 312↔277/278, 313↔280, 314↔276/279, 315↔282, 316↔279/308. **Not re-surfaced:** the 06-05 batch (306-311) — instead extended where today's work bears on it (312 sharpens 311; OPEN-075 advanced).

### Lit search queue (seeded)
- **8 items routed** to `for_lit_search.md` as `[QUEUED]` at cycle 0: ASSUMPTION-278 (LOW), 279 (LOW), 280 (MED); PRESUMPTION-312 (HIGH), 313 (HIGH), 314 (MED), 315 (LOW), 316 (MED). Each carries a full PROVENANCE block and a 15a/15b search strategy line.
- **Held (not routed):** ASSUMPTION-281 (framework/anti-drift commitment); ASSUMPTION-282 (GROUNDED — verified in-session).

### Fail-loud (Rule 12)
1. **Shared-id may have manufactured identity, not discovered it (NEW):** yesterday's disjoint-id finding (ASSUMPTION-277) was "resolved" today by stamping `CC-xxx` ids onto both sides; whether shared-id constitutes genuine entity co-reference is unverified, and the entire P3 promotion pipeline rests on that key (PRESUMPTION-312, High; OPEN-076).
2. **Consent question resolved as a copy fix (NEW):** listing communities without consent was addressed by *disclosing* non-consent in the popover, not by reconsidering whether to list unapproved records; the don't-list / opt-in alternatives were never raised (PRESUMPTION-313).
3. **"Articulated to a quality bar" is curatorial, not earned (NEW):** the present 156 entered by curatorial fiat while the doc frames graph membership as earned by self-articulation; the Q2 gate that would make that true does not yet exist (PRESUMPTION-314).
4. **CE work awaits attended push/merge:** three commits (`56da6ab` → `64c64bc` → `8830d35`) sit on `feature/sociogram-search-integration`; the sandbox cannot push; merge feature→main is attended-only.
5. **claude.ai sign-in still LAPSED — 5th consecutive day:** the evening Cowork→Chat delivery failed again (redirect to `/login`, 22:42 UTC); re-auth remains the single attended fix (ASSUMPTION-263/270; PRESUMPTION-300 → MONITOR-297). Today's localhost work again proved the extension is fine — only the login is missing.
6. **Human review gate at 10 days:** the new Wolfram ruliology-of-competition proposal (2026-06-06) pushed the pending-review queue to **20**; the REVISE backlog and review queue remain gated on a single human.
7. Standing carries: 2026-05-20 lit-batch reconciliation UNDONE; 2026-06-01 / 2026-05-25 dated-artifact gaps not back-filled.

### Positive note (Rule 12 cuts both ways)
Today logged a **verification success**: the `app.js:1314` console error was not waved off but actively disproved by a positive downstream-handler test, and the disposition recorded as GROUNDED (ASSUMPTION-282). And the 2026-06-05 disjoint-id finding was acted on the very next session — the join was built and OPEN-075 advanced OPEN → PARTIALLY-ANSWERED. The usual produced≠verified gap reappears on a new axis (identity asserted, co-reference unverified — PRESUMPTION-312), but the day also produced a clean verification win.

### New ID counters (this 14a/14b pass)
ASSUMPTION-282 · PRESUMPTION-316 · DECISION-051 · OPEN-076.

### Verification
- Registry maxes confirmed post-write: ASSUMPTION max = 282; PRESUMPTION max = 316; DECISION max = 051; OPEN max = 076.
- All 8 routed items carry PROVENANCE headers (Origin / Chain / Original item / Item type / Status); 8/8 tagged `[QUEUED]` at cycle 0; correctly NOT marked searched.
- No new item written without a PROVENANCE block (278-282, 312-316, DECISION-051, OPEN-076, the snapshot, and this changelog each carry one).
- Ground-truth checks (Rules 1/8/12): pending-proposal queue = **20** by file count (2026-06-06_wolfram_ruliology-of-competition on disk); the new `explorer_tabs_complementarity.md` and the `sociogram_feature_review.md` 2026-06-06 UPDATE read directly; ASSUMPTION-278's "cards now 1006 / 5 bulk overlaps" and the three commit SHAs taken from the dated artifacts + the 2026-06-06 cowork_to_chat summary. Git state (3 commits on the feature branch; merge pending) is recorded as **reported context** — the repo is not introspectable from this mount; flagged per Rule 12.
- Backups written pre-run: `*.bak.20260606-pre-14eod` for assumptions / presumptions / for_lit_search / decisions / open_questions.

### Notes / autonomous choices
- **Attended execution day, even batch (5 ASSUMPTIONs / 5 PRESUMPTIONs)** — drawn from the CE build session and its dated artifacts. Distinct from a design-ratification day: today *executed* DECISION-050's first concrete step rather than ratifying a new architecture.
- **Numbered DECISION-051** even though it is an execution under DECISION-050 — it makes a dated, substantive design commitment (subset-merge + consent disclosure) that a future reviewer should be able to trace. (The 2026-06-06 cowork summary noted "no new numbered DECISION filed today" because the EOD pass had not yet run; this pass files it.)
- **Held ASSUMPTION-281 and ASSUMPTION-282 from routing** by type (framework commitment; verified in-session) — surfaced the reason rather than queuing reflexively, parallel to yesterday's handling of 274/277.
- **Advanced rather than re-extracted** the 06-05 family: OPEN-075 downgraded, PRESUMPTION-312 sharpens 311, rather than re-surfacing 306-311.
- **Token-budget note (Rule 6):** reading the session transcript + registry tails + format anchors + ground-truth checks exceeds the 4k/task advisory; the scheduled-task success criteria take precedence for this autonomous pipeline; breach surfaced per Rule 12.

---

## Provenance

PROVENANCE:
  Origin: 14a
  Chain: [14a, 14b]
  Original item: CHANGELOG-2026-06-06
  Item type: CHANGELOG
  Transform at each step:
    14a/14b: Compiled from the 2026-06-06 attended Community Explorer build session ("Resume explorer P1" continuation, read via session_info) and its dated artifacts (new explorer_tabs_complementarity.md; sociogram_feature_review.md 2026-06-06 UPDATE; 2026-06-06 cowork_to_chat summary), plus the evening Cowork→Chat sync report. Extracted 5 ASSUMPTIONs (278-282) + 5 PRESUMPTIONs (312-316); added DECISION-051 and OPEN-076; downgraded OPEN-075 to PARTIALLY-ANSWERED; routed 8 testable items to for_lit_search.md as QUEUED cycle 0; held ASSUMPTION-281 (framework) and 282 (grounded). Confirmed pending queue = 20 by file count; recorded the 3-commit feature-branch state as reported context (repo not introspectable from this mount). Did not re-extract the 306-311 family; advanced it where today's work bore on it.
  Current status: COMPLETE

---

*Run by: c2a2-self-awareness-daily scheduled task (autonomous EOD; ATTENDED execution day)*
*Run timestamp: 2026-06-06 EOD (covers 2026-06-06)*
*Next scheduled run: 2026-06-07 EOD*
