# 2026-08-02 — EOD self-awareness run (Agents 14a / 14b)

Autonomous day #28, and the day the pipeline repaired its independence protocol and disproved the
repair on the same run. 15a and 15b were instantiated as separate agents with disjoint contexts —
the strongest arrangement available — and both then independently selected the same under-stating
statistic from the same source, which the run recorded as evidence rather than as a caveat. It was
also the best yield in the series (3 INCORPORATE of 7) and the first run in three weeks to check a
citation, and one of the five it checked could not be found at all. A second consecutive Critical
batch flag named one mechanism for the whole intake — the record of a control being treated as the
control — and three subsystems that had never read it confirmed it independently within hours: a QC
sweep traded a forcing function for a written record, a reviewer hit the same trap for the sixth
time and was saved by an unrelated log row, and a backup discovered that two prior runs had done
their work and written no log line for seven weeks. The pipeline also found that its own live
premise prescribes an unsound instrument. Meanwhile a compounding risk this register has flagged
nightly for eight days stopped compounding because a page simply was not generated, and nobody
noticed. No new DECISION (28th straight day), no new OPEN.

## Changes Detected

- **CHANGE-2026-08-02-001 — Autonomous day #28; fifth dark day on both sync channels, one cause.** ~70 files under `wiki/` carry an 08-02 mtime and all trace to scheduled agents. Chrome is signed out of claude.ai in the profile the extension is attached to: the morning scrape failed at 08:53 and the evening delivery was verified failed at 18:44, both writing dated files rather than leaving gaps. The autonomy figure is now reported under PREMISE-140, minted today from PRESUMPTION-624, which qualifies exactly this class of single-channel metric (ASSUMPTION-649).

- **CHANGE-2026-08-02-002 — The independence protocol was strengthened and immediately measured against.** "I ran 15a and 15b as *separate agent instances with disjoint contexts* rather than sequentially in one context — stronger independence than the last two runs. It immediately produced a measurement against itself: both directions, independently, picked the same under-stating statistic from the same Cochrane review … Context separation does not buy model-layer independence, and that's now evidence rather than a caveat." The register already held the answer (DISPOSITION-409, on correlated same-family model errors) and the protocol was designed as if it did not (ASSUMPTION-651; PRESUMPTION-631, HIGH, queued).

- **CHANGE-2026-08-02-003 — 7 items dispositioned: 3 INCORPORATE · 2 REVISE · 2 MONITOR (HIGH) · 1 Critical batch flag.** DISPOSITION-576..582; PREMISE-138 (scope extension of PREMISE-131), PREMISE-139 (copy-forward), PREMISE-140 (single-channel activity metric); REVISE-260, REVISE-261 (two clauses); MONITOR-498, -499. Denominator series: 07-29 1/8 · 07-30 0/4 · 07-31 0/3 · 08-01 2/6 · **08-02 3/7** — best in the series. Totals: PREMISE→140/97 · MONITOR→499/132 · REVISE→261/118 · DISPOSITION→582/550 (ASSUMPTION-650).

- **CHANGE-2026-08-02-004 — Citation verification resumed, and one of five load-bearing sources could not be found.** After a run at 0 of ~44, five were checked. It changed two outcomes: PREMISE-138 was minted on the corrected Cochrane figure, and 15b's decisive 27.1%-vs-100.0% forcing-function contrast "could not be located across three searches," so PRESUMPTION-623's recommendation was re-based onto PREMISE-131. The queue entry records the failure correctly; the SYSTEMIC-RISK-FLAG document still lists the figure in its literature basis, unmarked (ASSUMPTION-652; PRESUMPTION-632, HIGH, queued).

- **CHANGE-2026-08-02-005 — SYSTEMIC-RISK-FLAG (Critical, second consecutive day, all seven items at once).** "THE RECORD OF A CONTROL IS BEING TREATED AS THE CONTROL, AND THE SYSTEM'S OWN SINGLE CHANNEL IS BEING TREATED AS AN INDEPENDENT WITNESS TO ITSELF." Cluster A — documentation substituting for execution (616, 621, 623); Cluster B — self-observation substituting for independent verification (617, 618, 624, 628). Root: "the system has no effector and no observer outside itself, and it is closing that gap with reports rather than with structure." Nine source families cited. Delivered into a channel dark for five days, on the day the pipeline minted PREMISE-138 saying a repetition of an observation is not a remedy (ASSUMPTION-654; PRESUMPTION-634, in-house under PREMISE-138).

- **CHANGE-2026-08-02-006 — The flag confirmed three times over, by subsystems that had not read it.** (i) The QC sweep granted pass-marks to Days 87/88/89 — reversing a standing hold — on the reasoning that the defects are "durably recorded" elsewhere, trading a forcing function for a record (PRESUMPTION-644). (ii) The commentary reviewer hit the `paradigm_flags.md` trap for the SIXTH recorded time: "Mine was caught by nothing except a log row written four hours earlier … it is one unread log row away from producing bad input rather than a near-miss" (PRESUMPTION-643, HIGH, queued). (iii) The weekly backup found the Jul 12 and Jul 26 runs completed zip, manifest and prune and appended no log line — invisible for seven weeks because the artifacts existed. That is the exact inverse of PREMISE-139, minted this morning (PRESUMPTION-645, HIGH, queued).

- **CHANGE-2026-08-02-007 — The pipeline found its own live premise's prescribed instrument unsound.** PREMISE-124 clause (a) prescribes capture-recapture; this run's AGAINST search found it underestimates by ~28% under positive source dependence and cannot bound zero-capture strata at all. Diagnosis stands, instrument clause does not (REVISE-261 clause 2). Second consecutive day the pipeline argued against its own intake with evidence it generated, and it exposes a schema gap: no register field carries a warrant for a remedy clause separately from its diagnosis (ASSUMPTION-653; PRESUMPTION-633).

- **CHANGE-2026-08-02-008 — Two decisions handed to Tom with no default, and one of them is about the premises minted 24 hours ago.** Whether PREMISE-136/137 stand given they were minted under a disclosed verification-independence deviation (REVISE-260, High); and the PREMISE-124 amendment plus promotion rule (REVISE-261). REVISE-259 is unanswered on its second run (ASSUMPTION-655).

- **CHANGE-2026-08-02-009 — 15d ran; the monthly cycle fired; the backlog did not move.** 30 first re-triggers (MONITOR-453..482, cycle 0→1), 105 carry-overs advanced with cycle counts DELIBERATELY held ("advancing cycle counts without a completed pass would inflate them"), 344 low-priority items advanced to 2026-09-06 and not re-queued, 19 premise re-checks queued; `for_lit_search.md` 14,596 → 14,956 lines. Drain: zero for a 26th consecutive day; no 15a/15b consumption of monitored items since 2026-07-08 (ASSUMPTION-656; PRESUMPTION-635, HIGH, queued).

- **CHANGE-2026-08-02-010 — 15d disclosed a hole in the register it maintains.** "15d has NOT verified block-by-block that every one of the 344 has a standing block — the 06-28/07-05 cohorts total 264, so up to ~80 low-priority items may have no live search request at all … the gap is real and is flagged here rather than assumed away." The audit is assigned to "queue-cleanup's job," a role that appears in no agent definition in `wiki/agents/` (ASSUMPTION-657; PRESUMPTION-636, HIGH, queued).

- **CHANGE-2026-08-02-011 — The weekly sewing agent executed: ten traditions, 46 calls, 20 bridge notes.** 10 orphans processed across ten distinct traditions ("widest spread yet"), 3 bridges created, 1 zero-byte stub filled, 16 appended; connectivity row `2026-08-02,3083,660,64,3807` logged at 04:47; append-only verified against a pre-run byte snapshot. The batch cap became the binding constraint for the first time — eight substantive orphans deferred against two last week — and evidence quality was disclosed as unusually weak, five of ten proposals carrying a self-declared retrieval failure with the gate restated rather than routed past (ASSUMPTION-658).

- **CHANGE-2026-08-02-012 — Sixth firing of a ONE-TIME task; census written, re-execution declined, and a prior report corrected.** Total pages 3,806 (+140), every one of the 140 new pages an orphan, wikilinks parsed unchanged to the digit at 2,071. And a retraction: the 07-26 report's +10 sparse / +9 connected reading was already inclusive of the 07-28 bridge batch, so "it was one pulse, not a trend … 'the weekly agent is now doing the connectivity work' is, on one more week of evidence, an overstatement: it did it once." Second self-correction of a figure this register had accepted, in two days (ASSUMPTION-659).

- **CHANGE-2026-08-02-013 — The census's headline was true at 04:10 and false by 04:47.** "Zero new inbound links vault-wide this week" was superseded 37 minutes later by the weekly agent's 20 bridge notes and its own CSV row. The 18:41 summary published the census figures as the day's connectivity and stated no row had been written. The bootstrap deferred correctly on the WRITE path — "the weekly agent owns that file" — and competed accidentally on the READ path (PRESUMPTION-637, PRESUMPTION-638).

- **CHANGE-2026-08-02-014 — The first bounded connectivity target the project has had.** "All 307 `vault/synthesis/Day-NNN - … - Contemporary.md` pages sit at 1–2 backlinks — 47% of the entire sparse bucket. Each is 18–34 KB, names 9–10 of the 14 thinkers, carries outbound links. That's the vault's richest cross-tradition material sitting one link from invisible, and it's a bounded 307-page job rather than an undirected sweep of 3,093 orphans." Needs one yes or no before the next weekly run (ASSUMPTION-660).

- **CHANGE-2026-08-02-015 — Metric inflation, sixth consecutive flag, and the two runs disagree about it.** 3,807 / 3,083 full versus 1,644 / 920 excluding `lit_search_results/` and `daily_sync/` — 57% of pages, ~70% of orphans, 98 of the week's 140 new pages. "The headline '+140 orphans' overstates real disconnection by more than 3×." The bootstrap reported the same exclusion as 1,643 / 930; neither run read the other (ASSUMPTION-661).

- **CHANGE-2026-08-02-016 — The day's substantive result: a four-way convergence, and the project's own thesis made falsifiable.** Wolfram, Bettencourt-via-Carroll, Levin and Rohr arrived within twelve days at "the collective constitutes something no member holds" — physics, urban science, developmental biology and contemplative practice. No CROSS entry covers it. Bettencourt independently names the founding metaphor ("a social accelerator, a bit like a physics accelerator") with a derived exponent, yielding a self-test on data already held: do bridges and CROSS entries accumulate superlinearly in the number of traditions, or linearly? "If linear, the system is an aggregator, not an accelerator." Uncosted warning: superlinearity carries crime and disease at the same exponent as patents (ASSUMPTION-662, OBJECT-LEVEL).

- **CHANGE-2026-08-02-017 — Two decidable cross-tradition comparisons opened.** `hoffman_wolfram` had no bridge in either direction and was created: both programs moved this month from asserting the observer matters to specifying what one is, with the same shape — enormous input → compression → single narrow thread — and "this is decidable, not decorative," because both objects have written-down definitions; the candidate difference is extrinsic (rulial position) versus intrinsic individuation. And the Hawkins/Friston thalamus disagreement — reference-frame transformer versus precision weighting — with exactly one available reconciliation and a distinguishing prediction (ASSUMPTION-663, OBJECT-LEVEL).

- **CHANGE-2026-08-02-018 — One failure, two incompatible root causes on consecutive days.** `metabolism-regen-daily` failed 08-01 on `database or disk is full`; today, "Disk was *not* the constraint this time" — 4.4 G free, the full 3,357,835,264-byte snapshot written cleanly — and the blocker is the sandbox's 45-second per-call cap against a ~90 s job. ≈76 hours stale, "failing silently for three days." Neither run cites the other; nothing supersedes the older diagnosis, and yesterday's backward inference about 07-31 is now unsupportable. Its own conclusion is a governance question: "this task is scheduled into an environment that structurally cannot run it" (ASSUMPTION-664; PRESUMPTION-640).

- **CHANGE-2026-08-02-019 — The same limit, routed around by a sibling within hours.** `c2a2-prs-connectome-weekly` met the identical 45-second cap and split its wrapper into three calls, regenerating and validating the connectome 517 → 519 triplets (`validator === PASS ===`), not published — Mac-side git required. Three rediscoveries of one environment constraint in two days, no register field holding it, one green and one red with no shared quantity (ASSUMPTION-665; PRESUMPTION-641).

- **CHANGE-2026-08-02-020 — The watchdog improved, and one of its red findings dissolved on inspection.** Scheduler health now separates "fired" from "produced" and reports each — the decomposition PRESUMPTION-626 asked for — and closed the `summa-2026-daily-batch` cron issue. But `connector-health-weekly`, recorded as ~8 weeks silent, in fact wrote its brief today to `Documents/Claude/Reports/`; the check looks in `connector-health/`. Found only by reading two reports that no agent reads together, and the same reasoning is applied unexamined to `reviewer-review-weekly`. Also new: `c2a2-voice-faq-weekly` is documented in CLAUDE.md and absent from the scheduler; `sync_vault.sh` is dead ten days on a 07-23 rebase conflict (ASSUMPTION-666, ASSUMPTION-667; PRESUMPTION-642).

- **CHANGE-2026-08-02-021 — Summa QC: an escalation, a recurrence, and a reversed hold.** Six pairs reviewed, queue `fresh 289` → `295`. Day 025 escalated (segments evicted, refetch hit the known YouTube `IpBlocked`), file left untouched per the escalate-don't-rewrite rule. The stale-report near-miss recurred for a second consecutive day: "Permission denied *while still leaving a file behind* — a report from a different session, generated a day earlier … do not round-trip this report through `/tmp`." And Days 87/88/89 were pass-marked, reversing a deliberate hold, freeing the queue to `[95, 106, 107, 108, 109, 110]`, with the reversal offered back (ASSUMPTION-669).

- **CHANGE-2026-08-02-022 — Six direct measurements, and one of them is that a compounding risk stopped compounding.** (a) `master/C2A2_master_wiki.md` unwritten a SECOND day (mtime 07-31 04:41, header 07-27); two scheduled tasks still executing at 23:38. (b) **NO REVIEW PAGE WAS GENERATED TODAY** — `review/2026-08-01_review.html` is the newest — ending an eight-day run this register flagged nightly, with no agent recording the omission. The generator is unfixed and the blast radius is 28 decisions; the exposure fell because a page was not produced, not because anything was repaired. (c) `decisions.md` unchanged since 07-20, 28th autonomous day; `open_questions.md` unchanged since 07-28. (d) `watch_list.md` 299,718 bytes, reported the same day as "293 KB". (e) `traditions/stump/prs_triplets.md` still holds both 25 and 28 — day 3. (f) Zero files under `wiki/vault/` newer than 2026-07-30, confirming the sync freeze independently (ASSUMPTION-671).

- **CHANGE-2026-08-02-023 — The summary that reaches Tom diverged from measurement on six counts, all in the same direction.** "Two scheduled pipelines ran" (at least eleven produced output); "no new [CSV] row written … deliberate" (written fourteen hours earlier); superseded connectivity figures; assumptions 649 against 648/647; monitor queue "569 items" against 132 blocks; lit queue "33 QUEUED" against 1,773 tags. It is the only artifact in `architecture/` with no provenance header, verification section or fail-loud footer, and the only one designed to reach a human (ASSUMPTION-672; PRESUMPTION-639, HIGH, queued).

- **CHANGE-2026-08-02-024 — Seventh consecutive budget-breach day, and the first stated mechanism.** Three tasks disclosed overruns. New tonight: "delegating both searches kept ~214k tokens of transcript out of context, which is the only reason it fit" — the first quantity any run has attached to a budget disclosure, partially answering PRESUMPTION-587 on day 5, of which this run is again an instance (ASSUMPTION-673).

- **CHANGE-2026-08-02-025 — Carried, unactioned, restated unchanged.** The 26-item bulk retag (seventh request, ninth run); `watch_list.md` archival; the two undisposed 07-19 proposals (Rohr, Wright), now 14 days open; PREMISE-114 and PREMISE-124 unedited on day 9, with MONITOR-494 still converting on ratification; PREMISE-001…043 absent while ~40 IDs are referenced (OPEN-133); MONITOR-495's corpus-scoped count at a seventh deferral; connectivity metric inflation at a sixth flag; `sync_vault.sh`; the review-generator fix; REVISE-256 clause (1); seven plugin MCP servers awaiting re-authorisation, flagged again by two independent runs. Nothing left the list today on either evidence or action.

## Assumptions Extracted (14a) — 25

| ID | Statement (abbreviated) | Type | Testability |
|---|---|---|---|
| ASSUMPTION-649 | Autonomous day #28; fifth dark day, one cause (Chrome signed out); ~70 files, all scheduled-agent | architectural / empirical | empirical (in-house) |
| ASSUMPTION-650 | 7 items → 3 INCORPORATE / 2 REVISE / 2 MONITOR-HIGH + Critical batch flag; best yield in series; totals measured | methodological / empirical | empirical (in-house) |
| ASSUMPTION-651 | Disjoint-context instances still selected the same under-stating statistic; "context separation does not buy model-layer independence" | epistemic / methodological | literature + in-house |
| ASSUMPTION-652 | Citation verification resumed at 5; the decisive 27.1%/100.0% contrast unlocatable across three searches; recommendation re-based, flag document unmarked | methodological / epistemic | empirical (in-house) |
| ASSUMPTION-653 | PREMISE-124's own prescribed instrument found conditionally unsound (~28% underestimate; zero-capture blind) → REVISE-261(2) | methodological / epistemic | literature |
| ASSUMPTION-654 | SYSTEMIC-RISK-FLAG (Critical, 2nd day): the record of a control treated as the control; two clusters, one root | architectural / epistemic | literature + in-house |
| ASSUMPTION-655 | Two no-default decisions handed to Tom (REVISE-260, REVISE-261); REVISE-259 unanswered on run two | architectural / epistemic | framework commitment |
| ASSUMPTION-656 | 15d monthly cycle fired: 30 re-triggers, 105 carry-overs, 344 advanced, 19 premise re-checks; +360 queue lines | methodological / architectural | empirical (in-house) |
| ASSUMPTION-657 | 15d discloses up to ~80 low-priority items may carry no live search request; audit assigned to an unstaffed role | architectural / methodological | empirical (in-house) |
| ASSUMPTION-658 | Sewing weekly executed: 10 orphans / 10 traditions, 46 calls, 20 bridges, CSV row 04:47; batch cap now binding | architectural / empirical | empirical (in-house) |
| ASSUMPTION-659 | Bootstrap (6th firing) declined re-execution; +140 pages all orphans, wikilinks unchanged; 07-26 optimism retracted | methodological / empirical | empirical (in-house) |
| ASSUMPTION-660 | 307 `Day-NNN … Contemporary` pages at 1–2 backlinks — 47% of the sparse bucket; a bounded, reviewable job | architectural / empirical | empirical (in-house) |
| ASSUMPTION-661 | Metric inflation, 6th flag: 57% of pages / ~70% of orphans are machine dumps; the two runs' exclusions disagree | methodological / architectural | empirical (in-house) |
| ASSUMPTION-662 | Four-way convergence on the constitutive power of plurality; Bettencourt names the founding metaphor; superlinearity self-test; uncosted pathology warning | empirical / epistemic | literature (OBJECT-LEVEL) + in-house |
| ASSUMPTION-663 | `hoffman_wolfram` bridge created — a decidable comparison of two written-down observer definitions; Hawkins/Friston thalamus prediction | empirical / epistemic | literature (OBJECT-LEVEL) |
| ASSUMPTION-664 | Metabolism regen failed again with disk explicitly ruled out; 45 s call cap named; "structurally cannot run it" | architectural / empirical | empirical (in-house) |
| ASSUMPTION-665 | PRS connectome 517 → 519, validator PASS, not published; same 45 s cap routed around by splitting the wrapper | architectural / empirical | empirical (in-house) |
| ASSUMPTION-666 | Scheduler layer clean and decomposed; four tasks fired-but-silent; `sync_vault.sh` dead 10 days; a documented task absent from the list | architectural / methodological | empirical (in-house) |
| ASSUMPTION-667 | `connector-health-weekly` DID write — to a directory the watchdog does not check; join made here from two reports | architectural / methodological | empirical (in-house) |
| ASSUMPTION-668 | `paradigm_flags.md` trap, 6th recorded instance, "caught by nothing except a log row written four hours earlier" | methodological / architectural | empirical (in-house) |
| ASSUMPTION-669 | QC: Day 025 escalated; stale-report near-miss recurs; Days 87/88/89 pass-marks reverse a deliberate hold | methodological / empirical | empirical (in-house) |
| ASSUMPTION-670 | Backup succeeded; two prior runs did the work and wrote no log line, invisible seven weeks | architectural / empirical | empirical (in-house) |
| ASSUMPTION-671 | Six measurements: master unwritten day 2, NO review page today (8-day streak ends), 28th decision-free day, watch_list, Stump, vault frozen | architectural / empirical | empirical (in-house) |
| ASSUMPTION-672 | The evening summary diverges from measurement on six counts, all toward a simpler day | methodological / epistemic | empirical (in-house) |
| ASSUMPTION-673 | 7th consecutive breach day, three tasks; first stated mechanism (~214k tokens kept out of context) | methodological / architectural | empirical (in-house) |

## Presumptions Surfaced (14b) — 15

All marked `[inferred]`. Seven queued to 15a/15b; eight tagged GOVERNED-CANDIDATE → in-house.

| ID | Presumption (abbreviated) | Type | Risk | Routing |
|---|---|---|---|---|
| PRESUMPTION-631 | Independence is a property of context, so disjoint contexts produce independent witnesses | epistemic / methodological | **High** | [QUEUED] |
| PRESUMPTION-632 | A citation that cannot be located is removed from the argument once the conclusion is re-based | epistemic / structural | **High** | [QUEUED] |
| PRESUMPTION-633 | A remedy named inside a validated premise inherits that premise's validation | methodological / structural | Medium-High | in-house (REVISE-261) |
| PRESUMPTION-634 | Raising the same systemic flag again raises its probability of being acted on | normative / structural | **High** | in-house (PREMISE-138) |
| PRESUMPTION-635 | Re-triggering a search request is progress on the item; queue activity is attention | methodological / scaling | **High** | [QUEUED] |
| PRESUMPTION-636 | The queue's contents are a lower bound on what is being watched | structural / epistemic | **High** | [QUEUED] |
| PRESUMPTION-637 | A census reports a state of the vault rather than a state of a moment | methodological / epistemic | Medium-High | in-house (PREMISE-137) |
| PRESUMPTION-638 | Write-path ownership settles authority; two measurers cannot both be authoritative | structural | Medium-High | in-house (REVISE-258) |
| PRESUMPTION-639 | The daily summary is a reading of the day, not a source about it | epistemic / structural | **High** | [QUEUED] |
| PRESUMPTION-640 | A recorded diagnosis is stable until something explicitly revises it | epistemic / methodological | Medium-High | in-house (PREMISE-096) |
| PRESUMPTION-641 | An environment limit is a property of the task that hit it | structural / scaling | Medium-High | in-house (PREMISE-104) |
| PRESUMPTION-642 | "Fired but produced nothing" is a claim about production, not about the check's path | structural / epistemic | Medium-High | in-house (PREMISE-110) |
| PRESUMPTION-643 | A trap caught six times is being caught *by* something; near-misses read as a working control | methodological / normative | **High** | [QUEUED] |
| PRESUMPTION-644 | A defect kept visible by withholding a mark stays visible once recorded elsewhere | methodological / structural | Medium-High | in-house (PREMISE-138) |
| PRESUMPTION-645 | A step that completed has been logged; audit trails are complete in the successful direction | structural / epistemic | **High** | [QUEUED] |

## Decisions and Open Questions

`decisions.md` — **not modified.** No new DECISION arose. Twenty-eighth consecutive autonomous day;
latest remain the 07-05 trio DECISION-076/077/078. The register has now been idle long enough that
its idleness is cited as evidence in two directions on the same page of this file: as closed-loop
failure by 15b, and as the decisive reason not to route HOLDs there by REVISE-258.

`open_questions.md` — **not modified.** Three candidates surfaced and, per the standing convention
against logging candidate items unilaterally, not logged:

1. *What, if anything, may the 15-pipeline write outside its four registers?* (REVISE-259) — now
   unanswered on its second run, and the pipeline's own PREMISE-138, minted today, says a repetition
   of the observation is not a remedy. The question has become an instance of itself.
2. *Is independence between reasoning agents achievable by any arrangement of contexts?*
   (PRESUMPTION-631) — new tonight, and the first open question to bear on the pipeline's own
   repair rather than on its findings. If the answer is no, REVISE-260 cannot be discharged by
   protocol at all.
3. *Do the emergent-time programs have room for eternity at all, or only for timelessness?*
   (FINDING-055) — carried unchanged from 07-31 and 08-01, still entangled with the unruled Stump
   attribution at MONITOR-495, whose decisive count is now at a seventh deferral.

## Provenance

All 40 items produced this run carry a PROVENANCE header per
`architecture/provenance_protocol.md`: 25 with `Origin: 14a` / `Item type: ASSUMPTION (stated)`
and 15 with `Origin: 14b` / `Item type: PRESUMPTION (unstated — surfaced by inference)`. Two 14a
items carry the chain `[14b → 15a/15b/15c → 14a]` (ASSUMPTION-650, ASSUMPTION-653), recording the
return of the seven presumptions this layer surfaced on 08-01. Three 14a items are marked
`[stated + measured]` and one `[stated, joined]` (ASSUMPTION-667), where the quoted halves are
stated by two agents and only the join is this layer's; ASSUMPTION-671 is marked `[measured]`
throughout.

## Fail-loud

This run exceeded the 4,000-token per-task budget — the eighth entry in this file's history to say
so, the seventh consecutive day, and the fourth task to say it today. Mitigations applied and
disclosed: no multi-MB register read whole; tails, targeted greps, sed ranges, diffs against the
day's own pre-run backups, and counts only; fourteen session transcripts read at message-tail depth
rather than in full; all register writes are shell heredoc appends. Backups written for
`assumptions.md`, `presumptions.md`, `for_lit_search.md`, `open_questions.md`, `decisions.md` →
`*.bak.20260802-pre-14eod`.

Not done, stated rather than omitted: the 26-item legacy bulk retag (unauthorised, ninth run);
WATCH-002/003 not re-checked (due 08-04, outside remit); `decisions.md` and `open_questions.md`
deliberately unmodified; the three open-question candidates above not logged; the ~80-item
MONITOR-001..344 audit named by 15d not performed here (outside 14a/14b remit, queued as
PRESUMPTION-636); PRESUMPTION-587's token decomposition unrun on day 5, which this run is itself a
further instance of, though one run partially answered it today with a figure.

**Coverage gap, stated plainly:** TWO scheduled tasks were still executing when this run completed —
`c282-wiki-agent-daily-run` (28 assistant turns) and `weekly-agent-ecosystem-report` (6 turns).
Their outputs are not represented anywhere in tonight's assumptions, presumptions or metrics. Fourth
consecutive night on which a scheduled task was observed running at this hour, and the first on
which two were. `master/C2A2_master_wiki.md` has now gone unwritten for two consecutive days, so
the narrative counter line is stale in the same direction twice and cannot be compared with
tonight's figures at all.

**One thing this layer owes its own record.** On 2026-08-01 this file recorded the `paradigm_flags`
trap as the THIRD recorded instance (ASSUMPTION-643), on the reviewer's own count at the time.
Tonight the reviewer counts six. Both numbers are on disk, neither is wrong on its own evidence, and
nothing reconciles them — which makes the recurrence count of a defect that produces false counts
itself an instance of the defect it is counting. It is corrected here rather than left for a later
pass, and the underlying question — whether six catches by six different accidents describe a
control or describe luck — is queued as PRESUMPTION-643.
