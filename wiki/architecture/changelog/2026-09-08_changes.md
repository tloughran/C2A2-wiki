# Architectural Changes — 2026-09-08

*Agents 14a (assumption extraction) and 14b (presumption detection), end-of-day run, ~23:45–23:55 EDT.
Previous changelog 2026-09-07 (two runs that date: a 10:53 gap-fill and a ~23:40 evening run).*

## Narrative

An unattended Tuesday in which the scripted layer did unusually good work and the reporting layer told
Tom the opposite. The lit-search pipeline ran a second consecutive clean cycle — four items, both
directions, zero orchestrator-written files — minted PREMISE-199 and 200, flagged REVISE-440 and 441,
and filed a High systemic-risk flag arguing that the estate credits mechanisms with effects it has never
measured, then applied that judgement to the flag itself. Agent 16 closed WATCH-002, the deferred
register's first closure ever, and did it by retracting a claim its own predecessor had carried
unchallenged for four runs. Against that: a dated prediction came due and was reasoned past rather than
run; two scheduled tasks failed silently under fail-loud specs; and the 08:00 status report told Tom
"Nothing is broken" on a morning when three of its four clauses were false. Eleven assumptions and nine
presumptions were extracted, all agent-stated, for the ninth consecutive day. Five were routed; the cap
is declared and is itself an instance of the presumption it defers.

## Items Produced

| Register | Added | Range |
|---|---|---|
| `assumptions.md` | 11 | ASSUMPTION-1283 – 1293 |
| `presumptions.md` | 9 | PRESUMPTION-929 – 937 |
| `open_questions.md` | 3 | OPEN-186, 187, 188 |
| `decisions.md` | 0 (dated run note appended) | — |
| `for_lit_search.md` | 5 routed; 15 declared not-routed | — |
| `metrics/` | 1 snapshot | `2026-09-08_snapshot.md` |

Backups: `*.bak.20260908-pre-14eod` for assumptions, presumptions, open_questions, decisions,
for_lit_search.

## Changes Detected

**Registers moved by other agents since the 09-07 evening run:** `lit_search_returns.md` 09-08 section
(DISPOSITION-913–916, SYSTEMIC-RISK-FLAG 2026-09-08); `validated_premises.md` +2 (PREMISE-199, 200);
`revision_flags.md` REVISE-440, 441 (both High); `lit_search_results/{for,against}/` +9 files, none
orchestrator-written; `for_lit_search.md` 09-08 15c run note and inline tags on the 09-07 evening
intake; `deferred/watch_list.md` and `deferred/resolved/2026-09-08_WATCH-002.md` (first RESOLVED entry);
`inbox/proposals/pending/` +6 (Hoffman ×2, Hawkins ×2, Wright, Rohr — 27 → 33); `inbox/`
+1 Levin item; `agents/openstory/{agent_telemetry.json,agent_node_edges.json,REFRESH_STATUS.md}`;
`heartbeat/data/digest.json` and snapshot `digest-20260908-144850.json`; twelve `synthesis/*_bridge.md`
files; `daily_sync/chat_to_cowork/2026-09-08_chat_summary.md` (FAILED) and
`daily_sync/cowork_to_chat/2026-09-08_cowork_summary.md` (written, delivery FAILED);
`architecture/metrics/connectivity_log.csv` (last row still 2026-09-06).

**No changes** to `wiki/agents/*.md` (latest 06-23) or `wiki/tools/*.py` (latest 08-12).

**Task-file path errors, two, both recurrences:** OPEN-167's `Wiki`/`wiki` (tenth run) and the
specialist agents' `SKILL.md` missing the `Projects/` segment — "I used the real one. Worth fixing in the
uploaded skill file or the next run may write into a phantom directory." Worked around in-run, left
unrepaired, third distinct instance in ten days. See PRESUMPTION-937.

**Still in flight at close:** `c282-wiki-agent-daily-run` fired at 08:00 and was mid-Phase 2 of 7 at
23:45, ~16 hours in — the exact stall shape the 09:45Z scheduler health check predicted for it that
morning (missing `permissionMode`). Its output is not in tonight's counts.

## Rationale Drift

**PASS was redefined inside a run.** The Openstory telemetry refresh wrote "PASS (verified-only)" having
declined the regeneration its task file defines as the work, and satisfied that file's fail-loud rule
("If any command exits non-zero, treat the whole run as FAILED") by not issuing the commands. The
justification is stated and reasonable; the status vocabulary has two values and now carries a third in
prose, addressed to a consumer that did not run today (PRESUMPTION-929, ASSUMPTION-1285).

**A prediction's own test was superseded by the argument that produced it.** ASSUMPTION-1280 (09-07)
named 2026-09-08 as its first scheduled test. On 09-08 the same claim was restated with "the copy is a
deterministic failure" and the test was not taken. No register recorded that the dated test came due
(PRESUMPTION-930). This is ASSUMPTION-1289's failure mode with the sign reversed.

**"Tom listening was never required."** WATCH-002's purpose was re-read at closing time, not at opening
time, and the watch closed on the disjunct its evidential question does not need — the resolution states
in the same file that "Nobody has heard the episode" (PRESUMPTION-936).

## Accumulating Technical Debt

- **Decision register twelve days static.** Six rulings owed and named: REVISE-436's FAIL LOUD binary
  (fifth unruled cycle), the shared WATCH-002/003 audit question, OPEN-185, OPEN-186, OPEN-187, OPEN-188.
- **15d re-trigger lane starved for a fifth consecutive cycle**; 2,111 bare-`[QUEUED]` tags, oldest
  2026-07-05 (sixty-five days). DISPOSITION-915 routed an item *away* from MONITOR because of it. This
  run capped its own routing at 5 of 20 for the same reason and says so.
- **Review pass**: master wiki and findings unwritten since 09-02 while pending grew 27 → 33.
  **ASSUMPTION-1231's window closes 09-10 — two days.**
- **Sync channels dark four consecutive days**, both directions, same two causes (Chrome extension not
  connected; browser pane lacks standing claude.ai approval). Watch-request channel deaf five days.
- **OpenStory ingest stopped since 09-03** (H-Drive unmounted); two metabolism staleness FAILs trace to
  it; the telemetry task has been unable to do its nominal work for four days and is green.
- **Premise count** 155 measured; 157 still carried by the digest, fifth day unexplained.
- **Two silent runs today** under fail-loud specs, neither recorded as silent by any consumer.
- **Rule 6**: this run overran the 4,000-token per-task budget by roughly an order of magnitude.
  Declared, not absorbed.

## Self-Referential Observations

1. **The flag is an instance of itself, and says so — then does nothing about it.** The 09-08
   systemic-risk flag holds that unmeasured mechanisms accrue unearned credit, notes "this flag is
   itself currently an unmeasured declared control … it should not be credited with force until
   something consumes it," names four measurements that would test it, and attaches no owner, threshold
   or date to any of them (ASSUMPTION-1289, PRESUMPTION-935). Second consecutive day on which the audit
   layer's flag describes its own authorship.
2. **This register corrected itself in one cycle.** REVISE-440 overturned the load-bearing axis of
   ASSUMPTION-1282, extracted here the previous evening — the fastest register-internal correction on
   record — and did it by naming a measurement rather than by argument (ASSUMPTION-1290). On the same
   day, elsewhere in the estate, an argument was substituted for a measurement (ASSUMPTION-1285). Both
   moves, opposite directions, twelve hours apart.
3. **Corroboration arrived from a project that cannot see this one.** The Summa nightly verification
   independently found its mtime staleness gate dead on 78 files two hours after PREMISE-200 was minted
   on the same question here (ASSUMPTION-1292). No channel exists that would have surfaced it; it was
   found only because one run read both transcripts — which is exactly what PRESUMPTION-932 says a
   file-only reconstruction cannot do.
4. **The day's own summary reported the day it could see.** "Every script-owned part of the system
   improved" is true of the artefacts and blind to the two runs that wrote nothing (PRESUMPTION-932,
   -933).
5. **Restraint went uncosted five times in one day** and was framed favourably every time — "reported
   rather than papered over", "declared rather than left silent", "not glossed", "the honest option".
   The comparative (declared inaction beats silent action) has become a positive (PRESUMPTION-934).

## Requires Tom

1. **REVISE-436's binary** — FAIL LOUD vs permitted orchestrator fallback. Fifth cycle unruled; REVISE-441
   is the measurement of what the waiting has cost.
2. **The WATCH-002/003 audit question** — why PROP-2026-07-19-001 left the pipeline undisposed and
   undeleted-from-record. One ruling retires both watches.
3. **OPEN-185** — is the lit-search layer in-corpus or out-of-corpus for connectivity? It is 53.5% of the
   vault and every page is an orphan by construction. Recomputation is a `find` away either way.
4. **OPEN-186** — the Openstory schedule: prune, read-in-place, ≥8 GB scratch, or repoint at the Mac
   wrapper. Four options named, none costed, task green for four days without doing its work.
5. **OPEN-187** — the standing rule for tradition attribution on collaborator-authored sources. Two of
   today's proposals ride on precedent; the agent asked explicitly for the rule.
6. **OPEN-188** — 30-day or 60-day recency window, and what "significant work not yet captured" licenses.
7. **Unchanged from 09-07:** the `COMMIT_ME_2026-09-07.sh` paste, and reconnecting the Claude in Chrome
   extension — which would restore both sync directions and the watch-request channel at once.

*Written by the 14a/14b evening run, 2026-09-08.*
