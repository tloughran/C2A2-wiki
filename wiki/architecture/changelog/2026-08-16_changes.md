# 2026-08-16 — EOD self-awareness run (Agents 14a / 14b)
### Sunday. 35 sessions. Fifteenth consecutive unattended day — and the day a guard was found not to have been running.

For fourteen runs the transcript frame could not execute because `/tmp` was unwritable. Today it cleared, and
within hours the thing it had been hiding was censused: **the anti-fabrication guard has been inert on 66 of
307 transcripts — 21.5% of the corpus — and its failure mode is a pass mark.** `could not extract sentences`
is emitted as a *warning*, and warnings do not fail a check, so the days where the guard does nothing at all
are exactly the days that report clean. A second run reached the same conclusion from the opposite end, on a
file that "**passes precisely by being unrenderable**." Around that finding, four more instruments were shown
today to be wrong by construction rather than wrong in a result — a metaphysical sweep keyed on question
numbers in a book that restarts numbering each Part, a length gate whose gauge eats half its own tolerance, a
connector audit that greps for tokens present in 7 of 36 files, and a review layer that "**checked whether the
ids *exist* rather than what they *say*.**" **Four runs of thirty-five produced nothing, including the only one
that watches the scheduler.** The scheduler itself went from 5 FAIL to 6. Against all of that: the sewing agent
moved a metric that had not moved in seven runs, broke its own append-only invariant doing it, caught it and
reverted byte-exactly; and two agents independently found that **Rohr grounds the Universal Christ in Scotist
univocity — the historic alternative to the Thomist analogy the Stump wing holds — so several recorded
Rohr↔Stump convergences may be convergences in English only.** That is the first content finding in weeks that
is about the traditions rather than about the instruments. **No new DECISION — forty-second straight day.
Four new OPEN.**

## Changes Detected

- **CHANGE-2026-08-16-001 — THE ANTI-FABRICATION GUARD HAS NOT BEEN RUNNING ON A FIFTH OF THE CORPUS, AND THE
  WAY YOU CAN TELL IS THAT THOSE DAYS PASSED.** "**The anti-fabrication guard is silently inert on 66 of 307
  transcripts (21.5%).** … the sentence splitter finds no `.!?` boundaries, returns one 3,169-word chunk, and
  the 8–60 word filter discards it. **Zero sentences survive to sample against the ASR.**" And the polarity:
  "`could not extract sentences` is a *warning*, and warnings don't fail the check — so **the days where the
  guard does nothing at all are exactly the days that report clean.** Several already carry
  `last_qc_outcome: pass`, and **that pass never tested for introduced content.**" Independently, on Day 086:
  "it currently carries `last_qc_outcome: pass`, because the anti-fabrication check emits 'could not extract
  sentences' as a *warning*, so **the file passes precisely by being unrenderable.**" Two fixes named, both
  declared Tom's: make zero-extraction a failure, or add token-window shingling so the guard actually runs
  (ASSUMPTION-1106; **PRESUMPTION-818 Critical, queued**; **OPEN-150**).

- **CHANGE-2026-08-16-002 — THE FOURTEEN-RUN BLOCKER CLEARED, WHICH IS WHY 001 WAS FOUND.** "**The fourteen-run
  blocker cleared.** `/tmp` is writable again and `youtube-transcript-api` was simply missing from the sandbox
  — reinstalling it plus a live refetch worked on all six days with no YouTube block." A later run found the
  fallback gone too: "**`/tmp` and `/var/tmp` were both unwritable, so the recorded fallback has now failed
  too.**" Where it did run, the corpus verified honest: "Four exact matches against live audio, two within
  seven words"; "all matching recorded ASR counts within 0.8%, all faithful"; "**now 8 of 8**". **A defect
  invisible for fourteen runs was found on the first day the instrument worked, and the instrument is
  intermittent, not fixed** (ASSUMPTION-1107).

- **CHANGE-2026-08-16-003 — A PASS MARK WAS SHOWN FALSE ON FOUR DAYS, AND THE MECHANISM WAS NAMED IN ONE
  SENTENCE.** "That pair passed on 2026-08-03. It had four citation defects at the time, and **the earlier run
  missed all four because it checked whether the ids *exist* rather than what they *say*.**" "That's a new
  shape worth naming: **an id-existence check clears it, a label check clears it, and only reading the entry
  body catches it.**" "Day 042 … **was `pass`-marked on 08-04**. Three of five PRS ids are wrong." "Day 201
  carried an editorial gloss in the transcript body at 0.00 ASR coverage — accurate, useful, and not in the
  episode … **Three prior QC passes read past it.**" 08-15 found seven pass-marked days defective; today
  supplies the cause rather than more instances (ASSUMPTION-1108; **PRESUMPTION-820 High, queued**).

- **CHANGE-2026-08-16-004 — THE LIT PIPELINE REFUTED THIS PIPELINE'S OWN REMEDIES A SECOND NIGHT, AND SAID THE
  FIX CANNOT BE WRITTEN BY THE AGENTS IT CONCERNS.** REVISE-340, High: "remedies are drafted before anything
  checks what the register already decided. **Four correctives this cohort were proposed against premises that
  explicitly exclude them.** The fix is two lines in the 14a/14b contracts, and **neither is actionable by those
  agents themselves under PREMISE-096.**" Detected twice over: "**Two 15b searchers on disjoint item sets
  independently filed the same systemic flag for the second consecutive night.**" The 08-15 intake ran clean —
  7 INCORPORATE (PREMISE-169–174), 4 MONITOR (528–531), 3 REVISE (337–339), DISPOSITION-715–728 — and
  **[measured]** `validated_premises.md` now tops out at PREMISE-174 against 08-15's 168, +6, exactly the
  minted range (ASSUMPTION-1109; **OPEN-153**).

- **CHANGE-2026-08-16-005 — FOUR RUNS PRODUCED NOTHING, INCLUDING THE ONE THAT WATCHES THE SCHEDULER AND THE
  ONE BUILT TO MAKE SILENCE VISIBLE.** `weekly-agent-ecosystem-report`, `morning-project-status`,
  `morning-system-health` and `openstory-agents-telemetry-refresh` each end at `[Request interrupted by user]`
  on a day whose **35 transcripts contain not one typed human message**. Mute rate **4/35 = 11.4%** against
  11.5% last night. Two are load-bearing: the ecosystem report's own spec calls the stranded-agent audit its
  most important section, and `morning-system-health` is the only instrument checking the 05:45 launchd gates.
  The telemetry contract states why its status file exists — "**STATUS FILE (always, both PASS and FAIL — this
  is how a silent stall becomes visible; the feeds were frozen 2026-06-08→26 because failures only went to the
  run log)**" — **and it stalled without writing it, for the second consecutive day** (ASSUMPTION-1110;
  **PRESUMPTION-819 Critical, queued**).

- **CHANGE-2026-08-16-006 — THE SCHEDULER GOT WORSE, AND ITS REGISTER LOGGED PASS AND FAIL FOR THE SAME RUN IN
  THE SAME MINUTE.** "**77 OK, 1 WARN, 6 FAIL**" against 78/1/5 last night and 78/4/**0** on 08-05.
  `metabolism-regen-daily` is the new failure — "**the task may report a run and write nothing**". The three
  never-fired agents persist at `runs = 0`. The load-bearing item is debris: "**2 stale lock(s) (HEAD.lock,
  main.lock), 2026-08-15 04:40 … this blocks the next commit**"; "**they are the plausible reason the daily run
  has completed but committed nothing on both 08-15 and 08-16.**" Confirmed from inside: "`.git/index.lock` was
  stale from yesterday's run and would have blocked `commit_daily_run.sh` at 05:45 … **`.git/` now holds twelve
  accumulated `index.lock.stale*` files going back to May.**" And the register contradicts itself:
  "`09:45Z **FAIL** … ran 2026-08-16 08:34Z and **committed nothing**`" beside "`09:45Z **OK** … run of
  2026-08-16 08:34Z completed`" (ASSUMPTION-1111).

- **CHANGE-2026-08-16-007 — A NEW FABRICATION CLASS: A REAL SOURCE WITH AN INVENTED DATE.** "**A new class of
  fabrication was caught.** Search asserted a Premier Unbelievable McGilchrist episode released 2026-07-15. The
  episode is real; the date is not — his own site files it under `uploads/2026/03/`. **Prior fabrications
  invented a *source*; this one invented a *date on a real source*, which no from-the-thinker check and no
  URL-resolves check would have stopped.**" The same run's remaining yield was unusually hard: six arXiv
  preprints, "every author list verified against arXiv metadata or INSPIRE-HEP rather than a search summary"
  (ASSUMPTION-1112).

- **CHANGE-2026-08-16-008 — LAST WEEK'S HEADLINE WAS RETRACTED BY ITS OWN AUTHOR, AND THE CORRECTION INVERTS
  IT.** "**The headline is a correction, not a measurement.** Last week's report called the 614-page Summa
  corpus '307 closed dyads… pointing at each other and at nothing else.' **It measured only inbound links and
  inferred outbound.**" Measured both ways: "those 307 pages emit **1,143 wikilinks, 53% of every wikilink in
  the vault**"; "`friston/prs_triplets.md` has 150 backlinks; **149 are from the Summa corpus. It isn't an
  island — it's the reason the hubs have connectivity at all.**" Also: "**81.3% of the vault (3,470 of 4,267
  pages) emits zero outbound wikilinks**"; "`architecture/lit_search_results` — 2,283 pages, **one** wikilink";
  "**sparse (657) and connected (56) haven't moved a single page in three weeks while the total climbed 273**".
  Third consecutive night on which a headline did not survive its own follow-up
  (ASSUMPTION-1113, 1114; **PRESUMPTION-826 Medium, queued**; **OPEN-152**).

- **CHANGE-2026-08-16-009 — THE BACKLINK COLUMN MOVED FOR THE FIRST TIME IN SEVEN RUNS, AND THE AGENT BROKE ITS
  OWN INVARIANT DOING IT.** "**The backlink column moved for the first time in seven runs** … nine of ten pages
  went 0 → 1+ backlinks, sparse 657 → 666." "**I broke append-only doing it, caught it, reverted byte-exactly.**
  The substitution ran file-wide rather than on my own section and rewrote 12 lines from earlier runs. **First
  repair was itself incomplete** … Fixed by reconstructing each file as `git show HEAD:<file>` plus my appended
  section." Final: "**Append-only verified: 194 insertions, 0 deletions.**" And the extension was declined:
  "roughly 60 more … and ~250 inside the calls themselves … **That modifies existing content, so it needs your
  sign-off**" (ASSUMPTION-1115; **PRESUMPTION-828 Critical, queued**).

- **CHANGE-2026-08-16-010 — TWO AGENTS INDEPENDENTLY FOUND THAT SOME OF THE NETWORK'S CONVERGENCES MAY BE
  CONVERGENCES IN ENGLISH.** "**The sharpest finding is an internal incompatibility.** Rohr grounds the
  Universal Christ in Scotist **univocity of being** — a word that appears nowhere in the Rohr tradition before
  today — and univocity is the historic alternative to the Thomist analogy the Stump wing holds. **Several
  recorded Rohr↔Stump convergences may be convergences in English only.** Staged as a tension … **not
  smoothed.**" The hunt agent, separately: "a term that a grep confirms appears nowhere in `traditions/rohr/`."
  The same run's other finding runs the other way: "Rohr and Wright both reach for Philippians 2:12–13 … **with
  no evidence of contact**." **This is the day's only finding about the traditions rather than about the
  instruments** (ASSUMPTION-1116; **PRESUMPTION-827 Critical, queued**; **OPEN-151**).

- **CHANGE-2026-08-16-011 — THE STALE DETECTOR FAILED 12/12 ON A SECOND DISJOINT POPULATION, AND THE SCHEDULE
  DID NOT CHANGE.** "**The stale detector failed 12/12 on a second, disjoint population (17/17 combined) …
  Recorded cycles were cadence ticks, not searches; every item reported 'stable, no new sources' turned out to
  have new material once anyone looked.**" "**five cycles of waiting resolved nothing and one search resolved
  10 of 17 items. The variable is the search, not the time.**" Filed in the same report, without remark: "**151
  weekly carry-overs advanced to next_check 2026-08-23**." The run also declined to read its own good news as
  a trend: "12 of the 13 blocks drained in a single day (08-11) … **That's one large draw, not improved
  throughput**" (ASSUMPTION-1117; **PRESUMPTION-824 High, queued**).

- **CHANGE-2026-08-16-012 — FIVE INSTRUMENTS WERE FOUND WRONG BY CONSTRUCTION, EACH BY THE AGENT OPERATING IT.**
  "**The metaphysical guardrail sweep is unsound as specified and I did not log its 20 hits as violations** …
  **the Summa restarts numbering in each Part — Q.44 in the Secunda Pars is not Q.44 on creation** … **No Prima
  Pars day failed.**" "**The 74% length failure rate is a policy/measurement disagreement, not 226 defects.**"
  "**The agent's own Step 3 is near-blind** … **only 7 of 36 enabled task files contain** [an `mcp__` token] …
  **the SKILL needs a rewrite before a narrow reading is trustworthy.**" "the ±25% length gate's gauge is
  unfit … **measurement error eats 30–50% of the tolerance.**" Plus the fidelity regex that "**cries wolf on
  the good files**" while being the only thing separating a good transcript from an unrendered paste
  (ASSUMPTION-1122).

- **CHANGE-2026-08-16-013 — THE STANDING ASKS ARE NOW IN DOUBLE DIGITS AND ONE OF THEM HAS WRITTEN FALSE
  CONTENT INTO THE CORPUS.** "**The ask, 13th time**: name an agent that may run in-house measurements, or
  state that none may. **Six more items this run are blocked** … including MONITOR-501's and -504's, which are
  one query and one grep." "**The recount, ninth ask** … **The harm stopped being hypothetical: Days 079 and
  178 each carried a `length_note` claiming deliberate compactness that the recount shows was a measurement
  artifact. Both retired.**" Also standing: metric inflation (8th), sewing-task retirement (3rd), the
  body-gloss checker ("**third run in a week arguing for it**"), the daily batch's own retirement (4th
  consecutive day). **Nothing in this system consumes a repeat count** (ASSUMPTION-1118;
  **PRESUMPTION-825 High, queued**).

- **CHANGE-2026-08-16-014 — EVERY SUBSTANTIVE RUN BREACHED BUDGET AND ONE DECLARED THE BUDGET UNSATISFIABLE.**
  "**the run consumed roughly 1.29M tokens against Rule 6's 4,000-token budget. The pipeline as specified
  cannot execute inside that budget and never has. Fourth day.**" (≈322×.) "**roughly 460k subagent tokens** …
  far past the 30k session guidance." "**Six pairs and 30k still aren't simultaneously satisfiable.**" "**this
  run overran its token budget after four pairs. Days 047 and 062 were selected but never examined — they are
  unreviewed, not clean. I've marked that plainly … rather than letting silence imply coverage.**" Exactly one
  run complied, "**the first run today to do so**" (ASSUMPTION-1119).

- **CHANGE-2026-08-16-015 — TWO CHANNELS DOWN ALL DAY; ONE MEANT NO BACKUP RAN AT ALL.** "**BACKUP FAILED — did
  not run.** `/Volumes/H-Drive` is not mounted." "**I did not invoke the script** (it would have failed or,
  worse, written a ~2 GB zip into a non-existent-mount path on the boot disk)". "**Chat delivery failed —
  Claude in Chrome is not connected** … the 08:52 morning scrape failed identically, so **the Cowork↔Chat sync
  was down in both directions for the full day.**" **[measured]** today's chat file is 1,215 bytes — the
  placeholder, not a summary (ASSUMPTION-1120).

- **CHANGE-2026-08-16-016 — THE REVIEW GATE IS THE ONLY FLAG DETERIORATING WITH TIME, AND THREE FIGURES WERE
  FILED FOR IT TODAY.** "**The review gate has been silent nine days.** Pending is at 47 … **The 08-11
  review-page ID fix that makes a 47-card batch reply safe has been waiting thirteen days on the same human,
  and is still an unpushed code change.**" "still the only flag getting worse every day it runs." The day's
  earliest instrument reported "**37 pending**, +2 since 08-14 … an all-time high", verified it
  programmatically, and pointed Tom at **yesterday's** review page. **[measured]** `pending/` holds **47**;
  37 + 10 new proposals today (6 daily run, 4 Wright/Rohr) = 47 [**exact**]. The divergence is a time-of-run
  artifact, and nothing in either report says which build it refers to
  (ASSUMPTION-1121; **PRESUMPTION-822 High, queued**).

- **CHANGE-2026-08-16-017 — A RUN REPORTED WRITING A FILE THAT IS NOT THERE, AND BOTH SELF-AWARENESS REGISTERS
  WERE FOUND DEFECTIVE.** "The brief went to `janitor/brief-2026-08-16.md`" — **[measured]** no such file
  exists anywhere in either mounted folder. The same class from the other end: "**A ten-week gap.** No brief
  between 2026-06-07 and today, though the task is enabled and weekly and reports running … Either the
  intervening runs wrote to the canonical path (invisible from the sandbox mount) or they produced nothing."
  **And this record drafted two register findings tonight that were false, caught them in verification, and
  is filing the correction instead.** It reported a previously-unnoticed hole at ASSUMPTION-459: the gap is
  real but has been named in ten metrics snapshots since 2026-07-17, one of them describing an offset as
  "exactly accounted for by the ASSUMPTION-459 gap" — **the check this run claimed had never been performed
  is one this pipeline has run for a month.** It also reported a collision at PRESUMPTION-295, which is not a
  collision but a dated reinforcement note stating "(Not given a new ID — folded into 295 per the established
  discipline of avoiding over-production)". **Both errors are exactly what PRESUMPTION-817 predicted last
  night — that this file is written as history without checking whether its findings are already known — and
  both were caught only by a deterministic verification pass run after drafting and before filing. The
  measured state is unremarkable: 1,122 unique ASSUMPTION ids, max 1123, offset exactly 1, the known gap.**
  (ASSUMPTION-1123; **PRESUMPTION-823 High, queued**; **OPEN-153**).

## Decisions

**None.** Forty-second consecutive day with no new DECISION recorded. Decision index unchanged at
DECISION-001..078 [measured].

## Open Questions Added

- **OPEN-150** — When a guard cannot execute, is that pass, warn, or fail, and where is that state recorded?
- **OPEN-151** — Are the network's cross-tradition convergences agreements about the world or about English?
- **OPEN-152** — What is the target orphan rate, and do machine-generated trees belong in the graph?
- **OPEN-153** — Who repairs the self-awareness registers, given PREMISE-096 forbids 14a/14b from doing it?

## Items Routed to Literature Search

Ten items queued to `for_lit_search.md` — ASSUMPTION-1106, 1108, 1112, 1116, 1117 and PRESUMPTION-818, 819,
820, 824, 827 — all tagged `[QUEUED]` with full provenance headers. The remaining items are empirical or
framework commitments and are marked as such in their register entries.
