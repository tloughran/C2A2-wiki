# Architectural Changelog — 2026-06-07 (Sunday)

**Narrative.** An **attended infrastructure/publishing day** — and, notably, one the system's own evening sync misfiled as "automation-only." The headline is that a long-silent gap finally closed: PRS triplets had been accumulating (231 published, 269 on disk) while the live connectome never changed, and today's attended session **regenerated and published the 231 → 269 connectome to `origin/main`** (commit 2f6356b). Getting there exposed a deeper fault — the weekly auto-regen task Claude had built rested on a **falsified assumption** that scheduled tasks run on Tom's Mac with git credentials and a normal `$HOME`; in reality they run in the same flattened-mount sandbox that *cannot push, cannot resolve `$HOME`, and cannot touch `.git` locks*. The trial run failed and left stale lock cruft, and two paste-run command blocks half-failed on divergent local state before a state-independent detached-worktree push landed. The fix was a **git-free redesign** of the weekly task (DECISION-052) and a durable conceptual yield Tom and Claude co-articulated: the 12 CLAUDE.md rules are *policy* (waivable) while the no-push limit is a *capability / constitutional* boundary (ASSUMPTION-286). Today's extraction is **4 ASSUMPTIONs (283-286) + 5 PRESUMPTIONs (317-321)** — a balanced, slightly presumption-leaning attended batch.

### Agent 14a (stated assumptions)
- **+4 ASSUMPTIONs:**
  - **ASSUMPTION-283** — automating regeneration on a weekly schedule is the right fix for "triplets accumulate but the viz never changes." Type: architectural/methodological. (Routed, LOW.)
  - **ASSUMPTION-284** — the right safety split is "approved data auto-publishes, generator/template code changes are gated for human visual review." Type: methodological/normative. (Routed, MED.)
  - **ASSUMPTION-285** — **[GROUNDED this session]** scheduled tasks run in the same no-credentials sandbox (no push, no `$HOME`, no `.git` mutation), so push/`.git` steps must be handed to Tom. Verified in-session by the failed push + the passing git-free rewrite. Type: architectural/infrastructural. **Held — GROUNDED, not routed** (a second verification-success, parallel to 277/282).
  - **ASSUMPTION-286** — policy-layer rules (the 12 CLAUDE.md rules) are waivable; capability/constitutional boundaries (sandbox credentials) are not; a policy rule may coincide with a hard wall. Type: epistemic/methodological. Co-articulated by Tom ("constitutional at a deeper level than my 12 claude.md rules"). (Routed, MED.) **The day's headline conceptual yield.**
- **Decision index (`decisions.md`): +1 — DECISION-052** (PRS-connectome weekly task made git-free: regeneration automated, publishing handed off; "stop at the capability wall" pattern; the 231 → 269 backlog published to origin/main 2f6356b). Max now **DECISION-052** (was 051).
- **Open questions (`open_questions.md`): +1 — OPEN-077** (should the other scheduled tasks be audited for the same sandbox-capability mismatch? — the generalization of today's bug). OPEN-076/075 carry; OPEN-073 (sync outage) now 6th day.
- Metrics snapshot: `metrics/2026-06-07_snapshot.md` written.

### Agent 14b (unstated presumptions)
- **+5 PRESUMPTIONs:**
  - **PRESUMPTION-317** (High) — the prior task design presumed **execution-context uniformity**: that scheduled tasks inherit the attended Cowork environment's capabilities. This is a **class bug**, not a one-off (→ OPEN-077). *(Structural; the day's strongest fail-loud; pairs ASSUMPTION-285.)*
  - **PRESUMPTION-318** (Medium) — the auto-push task was **built before** the sandbox's push capability was probed, against Tom's own Rules 1/8. *(Methodological; self-referential; pairs ASSUMPTION-283/285.)*
  - **PRESUMPTION-319** (Medium) — the data/code guard presumes **PRS-data regeneration is safe to publish unreviewed** (data treated as review-exempt). *(Normative/epistemic; pairs ASSUMPTION-284.)*
  - **PRESUMPTION-320** (Medium-High) — handing Tom **blind multi-command blocks** presumes the agent's model of his repo state is accurate enough to script state-mutating sequences; two blocks half-failed on divergent state, with real collateral. *(Methodological; pairs ASSUMPTION-285.)*
  - **PRESUMPTION-321** (Low-Medium) — the system presumes **"automation day" and "attended session" are mutually exclusive**; today's attended PRS session was mislabeled "automation-only" by the sync/summary. *(Structural/normative; self-referential — corrected in this pass.)*
- Dedup check against 14a and prior batches: 283-286 (stated) vs 317-321 (inferred) are non-overlapping. Pairings: 317↔285, 318↔283/285, 319↔284, 320↔285, 321↔283. **Not re-surfaced:** the 06-06 family (312-316) — it was dispositioned today by the 15-pipeline and is not re-extracted here.

### Lit search queue (seeded)
- **8 items routed** to `for_lit_search.md` as `[QUEUED]` at cycle 0: ASSUMPTION-283 (LOW), 284 (MED), 286 (MED); PRESUMPTION-317 (HIGH), 318 (MED), 319 (MED), 320 (MED-HIGH), 321 (LOW). Each carries a full PROVENANCE block and a 15a/15b search-strategy line.
- **Held (not routed):** ASSUMPTION-285 (GROUNDED — infrastructural fact verified in-session, not a literature claim).

### 15-pipeline context (autonomous, same day — not owned by this pass)
The autonomous lit-search pipeline ran today and **dispositioned the 06-06 batch**: ASSUMPTION-278 → MONITOR-308; 279 → MONITOR-309; 280 → INCORPORATE (**PREMISE-052**, the consent-disclosure floor); PRESUMPTION-312 → REVISE-091; 313 → REVISE-092; 314 → MONITOR-310; 315 → MONITOR-311; 316 → MONITOR-312 (DISPOSITION-165..172; 1 INCORPORATE / 5 MONITOR / 2 REVISE). The 15d weekly/monthly re-trigger cohort also re-queued. The connectivity/sewing agent injected 21 backlinks across 10 traditions (0 bridge notes). Recorded here as context; these are 15-pipeline outputs, not 14a/14b extractions.

### Fail-loud (Rule 12)
1. **The system mislabeled its own day (NEW):** the evening Cowork→Chat sync and `2026-06-07_cowork_summary.md` both wrote "no attended Cowork build session is on record" while an attended PRS-connectome session ran the same day (PRESUMPTION-321). The self-awareness layer was contradicted by its own session record; corrected in this changelog and snapshot.
2. **Latent capability-mismatch class bug (NEW, HIGH):** one scheduled task silently assumed attended-Mac capabilities it lacks in the task sandbox; fixed for the PRS task (DECISION-052) but possibly latent across the suite (PRESUMPTION-317 → OPEN-077).
3. **Real collateral from blind command blocks (NEW):** two paste-run blocks half-failed — a locked stale `/tmp/fakehome` worktree still holding `main`, and an accidental commit on `feature/sociogram-search-integration` that bundled `generate_community_explorer.py`; plus stale `.git` locks that briefly jammed Tom's git. All recovered in-session (269 pushed via a state-independent detached worktree; the stray commit lifted by Tom's `git reset --soft HEAD~1`), but the cost landed on Tom's machine (PRESUMPTION-320).
4. **CE work still awaits attended push/merge:** `feature/sociogram-search-integration` (56da6ab → 64c64bc → 8830d35) carries from 06-06; merge feature→main is attended-only.
5. **claude.ai sign-in still LAPSED — 6th consecutive day:** both today's syncs failed (/recents → /login). Re-auth is the single attended fix (ASSUMPTION-263/270; PRESUMPTION-300 → MONITOR-297). Top morning action item.
6. **Human review gate:** pending queue ~20; REVISE backlog grew (REVISE-091/092 added by today's 15-pipeline).
7. **Housekeeping handed back (sandbox can't unlink):** delete the 33 zero-byte 05-31 bridge stubs; decide whether to exclude `lit_search_results/` from the orphan metric.
8. Standing carries: 2026-05-20 lit-batch reconciliation UNDONE; 2026-06-01 / 2026-05-25 dated-artifact gaps not back-filled.

### Positive note (Rule 12 cuts both ways)
A long-silent gap closed: the connectome had been stale (231 live while data grew to 269) and is now live at 269, with a weekly task to keep it fresh. The day also produced a clean **verification-success** — a stated capability claim (ASSUMPTION-285) was *proven in-session* (the push really failed; the git-free rewrite tested end-to-end) rather than asserted — and converted a failure into a durable conceptual gain (the policy/capability distinction, ASSUMPTION-286) plus a reusable design pattern ("automate what the sandbox can do; stop at the capability wall").

### New ID counters (this 14a/14b pass)
ASSUMPTION-286 · PRESUMPTION-321 · DECISION-052 · OPEN-077.

### Verification
- Registry maxes confirmed post-write: ASSUMPTION max = 286; PRESUMPTION max = 321; DECISION max = 052; OPEN max = 077.
- All 8 routed items carry PROVENANCE headers (Origin / Chain / Item type / Status); 8/8 tagged `[QUEUED]` at cycle 0; correctly NOT marked searched.
- No new item written without a PROVENANCE block (283-286, 317-321, DECISION-052, OPEN-077, the snapshot, and this changelog each carry one).
- Source: the attended session was read directly via session_info ("PRS triplets narrative connectome"), with the regen confirmed against the on-disk backup `c2a2-prs-3d/prs_3d.PRE-regen-20260607_202243.bak.html` (timestamp 20:22) and today's `2026-06-07_cowork_summary.md`. The 269-on-origin/main publish (2f6356b) is taken from the session's pasted git output — **reported context**; the repo is not introspectable from this mount (flagged per Rule 12).
- Backups written pre-run: `*.bak.20260607-pre-14eod` for assumptions / presumptions / for_lit_search / decisions / open_questions.

### Notes / autonomous choices
- **Day-type re-classified.** The evening sync called today "automation-only"; the session record shows an attended PRS session, so this pass files it as an **attended infrastructure/publishing day** and logs the mislabel as PRESUMPTION-321 (fail-loud #1) rather than inheriting the sync's framing.
- **Held ASSUMPTION-285 from routing** (GROUNDED, infrastructural fact verified in-session) — surfaced the reason rather than queuing reflexively, parallel to the handling of 277/282.
- **Did not re-extract the 06-06 family (312-316)** — it was dispositioned by the same-day 15-pipeline; recorded as context, not re-surfaced.
- **Numbered DECISION-052** — the git-free redesign + "stop at the capability wall" pattern is a substantive, dated commitment realized in code and in the live viz.
- **Token-budget note (Rule 6):** reading the full attended transcript + registry tails + format anchors + ground-truth checks exceeds the 4k/task advisory; the scheduled-task success criteria take precedence for this autonomous pipeline; breach surfaced per Rule 12.

---

## Provenance

PROVENANCE:
  Origin: 14a
  Chain: [14a, 14b]
  Original item: CHANGELOG-2026-06-07
  Item type: CHANGELOG
  Transform at each step:
    14a/14b: Compiled from the 2026-06-07 attended PRS-connectome session ("PRS triplets narrative connectome", read via session_info) and corroborating artifacts (the regen backup prs_3d.PRE-regen-20260607_202243.bak.html; today's cowork_to_chat summary; the evening sync transcript). Extracted 4 ASSUMPTIONs (283-286) + 5 PRESUMPTIONs (317-321); added DECISION-052 and OPEN-077; routed 8 testable items to for_lit_search.md as QUEUED cycle 0; held ASSUMPTION-285 (GROUNDED). Recorded the same-day autonomous 15-pipeline dispositioning of the 06-06 batch (DISPOSITION-165..172; PREMISE-052; MONITOR-308..312; REVISE-091/092) as context. Re-classified the day from the sync's "automation-only" to attended (PRESUMPTION-321). Git/publish state recorded as reported context — repo not introspectable from this mount.
  Current status: COMPLETE

---

*Run by: c2a2-self-awareness-daily scheduled task (autonomous EOD; ATTENDED infrastructure/publishing day)*
*Run timestamp: 2026-06-07 EOD (covers 2026-06-07)*
*Next scheduled run: 2026-06-08 EOD*
