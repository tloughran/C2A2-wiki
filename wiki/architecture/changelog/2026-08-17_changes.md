# 2026-08-17 — EOD self-awareness run (Agents 14a / 14b)
### Monday. 29 sessions. Sixteenth consecutive unattended day — and the day a quarter of the fleet produced nothing while the scheduler counted 78 OK.

**Seven of twenty-nine runs produced nothing**, against four of thirty-five last night: the mute rate
roughly doubled, 11.4% → **24.1%**, and no instrument in this system reports that number. Five of the
seven died at a tool boundary with nobody to answer the prompt; the two runs whose job is to notice —
`morning-system-health` and the OpenStory telemetry refresh — were themselves among the losses, the
second stating that it could not write the status file that exists "**exactly [to prevent] the
silent-stall mode**" it was in. Underneath the outage, two findings that are about *causes* rather than
counts, and both are new shapes. **A check was inert for nineteen runs under a recorded explanation that
was never the explanation** — "that streak was never a YouTube problem" — and when the real cause was
found the frame worked first attempt; three further recorded causes were overturned the same day, and
the run that found the first could not repair the second: "**I can't edit the memory file that carries
the wrong claim, so it's logged for you.**" And for the first time the week's corrections ran backwards:
**five accusations were retracted, not five clearances** — "**automated attribution fails in both
directions**" — against a status lifecycle that has five terminal states for a false pass and none for a
withdrawn flag. Separately, **four ids were cited for claims their own bodies argue against**, each
caught by a different run, each remediable only by withdrawal. The day's only tradition-level yield came
from a retrieval change: a YouTube RSS feed surfaced two July meetings in which **Hawkins enumerates
three unresolved problems in his own theory**, and it "sat one feed away for a month." Last night's
headline did not survive its own follow-up for the fourth consecutive night. **No new DECISION —
forty-third straight day. Three new OPEN. Pending is 53, deeper than the last drain.**

## Changes Detected

- **CHANGE-2026-08-17-001 — A QUARTER OF THE FLEET PRODUCED NOTHING, AND THE ONLY PLACE THAT NUMBER
  EXISTS IS THIS FILE.** Seven of twenty-nine runs ended without an artifact — `morning-system-health`,
  `metabolism-regen-daily`, `agentic-cost-tracker`, `morning-walk-cowork-handoff`,
  `morning-project-status`, `reviewer-review-weekly`, and a Summa reviewer that returned only "**API
  Error: 529 Overloaded.**" Five died immediately after a tool call. The mechanism, quoted from the
  run_stall contract: "the run hung on a tool-permission prompt with nobody there to answer it, and
  **nothing times these out** — measured over 110 runs, 39 never finished, the worst hanging 5.1 days
  until the Mac slept." The dividing line was stated plainly: "**Mount availability is the dividing
  line.** The two sessions needing no host filesystem completed." Meanwhile the scheduler logged **78 OK
  / 1 WARN / 5 FAIL** (ASSUMPTION-1124; **PRESUMPTION-829 Critical, queued**; **PRESUMPTION-834 High,
  queued**; **OPEN-154**).

- **CHANGE-2026-08-17-002 — THE FILE THAT EXISTS TO MAKE A SILENT STALL VISIBLE COULD NOT BE WRITTEN,
  AND ITS READER WAS DEAD.** "⚠️ **OPENSTORY REFRESH FAILED: no Documents mount — the run could not
  start.** … **Note the important gap:** I could not write `$AGD/REFRESH_STATUS.md`, so
  `morning-system-health` will read the previous run's line and won't surface this failure — **exactly
  the silent-stall mode this status file exists to prevent.**" And the prediction: "**Until then every
  run will fail this way, invisibly.**" On the same morning `morning-system-health` died on its second
  `request_cowork_directory` call, so its own sections 8–10 — each specifying that "a missing file, or a
  newest line that is not from today, is itself a FAIL" — never executed. Third consecutive telemetry
  stall; second consecutive day its reader was mute (ASSUMPTION-1125).

- **CHANGE-2026-08-17-003 — A CHECK WAS INERT FOR NINETEEN RUNS AND THE RECORDED CAUSE WAS NEVER THE
  CAUSE.** "**the transcript fidelity frame came back after 19 runs of being silently skipped.** That
  streak **was never a YouTube problem** — `fidelity_check.py` hardcodes `/tmp/dayNNN_segments.json`,
  `/tmp` is sticky and owned by `nobody` in this sandbox, and **no prior run had installed the library
  and fetched to `/var/tmp`.**" It then worked immediately — "fourth run proving it works first-attempt"
  — while two runs the same day were still reporting the old symptom under the old explanation:
  "unusable for the **eighteenth** consecutive run"; "**nineteenth** consecutive run." The 08-16
  changelog recorded this blocker as `/tmp` plus a missing library; prior records blamed YouTube
  (ASSUMPTION-1126; **PRESUMPTION-830 High, queued**).

- **CHANGE-2026-08-17-004 — A GUARD THE RECORD CALLS DEAD IS ALIVE AND EXITS 0, AND THE RUN THAT FOUND
  IT MAY NOT CORRECT THE RECORD.** "`fidelity_check.py`'s length-sanity check **does** fire on Day 097
  (60% drift) — **the record's claim that it 'does not fire' was taken from a run where the hardcoded
  `/tmp` segments were missing, so the script never reached the check.** The real defect is that it
  fires as a *warning* and still exits 0. **The fix is promoting the warning, not building a checker. I
  can't edit the memory file that carries the wrong claim, so it's logged for you.**" This is OPEN-150's
  mechanism demonstrated to have written a false entry into a live memory file, and OPEN-153's
  permission gap reappearing outside the 14a/14b registers (ASSUMPTION-1127; **OPEN-155**).

- **CHANGE-2026-08-17-005 — PASS MARKS WERE RETRACTED IN BOTH DIRECTIONS FOR THE FIRST TIME.** "**The
  2026-08-16 entry's five MacIntyre 'hard misses' are retracted.** … **No synthesis anywhere cites a
  nonexistent MacIntyre ID.**" "All three new hits are false positives too … **Automated attribution
  fails in both directions.**" A second, five weeks old: "**The run's largest yield is a correction to a
  standing record.** Day 294's 2026-08-05 escalation accused the transcript of dropping Q.74 a.9's Basil
  flame-division passage. **The transcript carries it verbatim at `[09:48]`–`[09:57]` and the live ASR
  confirms it word for word** — the 08-05 run had inferred a transcript drop from the *commentary's*
  silence." A third: "**Eight recorded backlogs are already closed by repair and the record doesn't say
  so** … the same density causing **wasted runs hunting defects that no longer exist**", of which "I
  re-verified four … **the other four are untested**" (ASSUMPTION-1130; **PRESUMPTION-833 High,
  queued**).

- **CHANGE-2026-08-17-006 — FOUR IDS WERE CITED FOR CLAIMS THEIR OWN BODIES ARGUE AGAINST, AND EVERY ONE
  LOOKED CORRECT AT THE LABEL.** "Hawkins PRS-06, the hierarchy triplet, **exists precisely to argue that
  the strict-hierarchy model is *wrong*** … A sweep anchoring 'hierarchical architecture' bullets to
  PRS-06 **would have cited the wiki against itself, and every such anchor would have looked correct at
  the Label.** So the read-the-body rule has to extend past 'does the id exist' and past 'does the gloss
  match' to ***does the id's Solution support or undercut this sentence*. Two of four attempted anchors
  failed at exactly that step.**" Three more, independently: "**Kastrup PRS-09 is McGilchrist's
  counter-proposal *against* Kastrup's dashboard metaphor**", named the "**third location**" of that
  shape; "**Rohr PRS-03** … **the Label matched and the body did not — the recorded trap**"; "**Day 204
  cited Friston PRS-10 for 'hierarchical generative model'** … **So I withdrew the id rather than
  repointing it.**" And the structural note: "**It lived only in the frontmatter.** … The prose was
  honest; the metadata over-claimed" (ASSUMPTION-1131; **PRESUMPTION-831 Critical, queued**;
  **OPEN-156**).

- **CHANGE-2026-08-17-007 — AN INSTRUMENT WOULD HAVE DELETED REAL ENTRIES ON A FALSE ABSENCE, AND THE
  RUN STOPPED IT.** "I rebuilt that index and it reported Fredrickson PRS-05 absent from Day 090 —
  **which is flatly false** … **Both regexes bind only the *head* id after a tradition name, so every
  trailing id in an enumerated citation reads as absent.** That is precisely the claim a retirement pass
  would act on, and **deleting a memory entry on a false absence doesn't come back. Make the index
  list-aware first.**" The mirror case, from the nightly verification: "**Two of my own sweeps were
  broken and I'd rather flag it than let the clean numbers stand** … **That's the failure mode that
  hides violations rather than inventing them.**" Both switched method rather than filing: "**1,873
  attributions verified, 4,216 mentions left unverifiable rather than guessed**" (ASSUMPTION-1129,
  queued).

- **CHANGE-2026-08-17-008 — A DECLARED FIELD SUPPRESSES THE ONE CHECK THAT WOULD HAVE CAUGHT WHAT IT
  DECLARES.** "Day 76's transcript declared `word_count: 2520`. The body holds **1692** … **That number
  is exactly what made the Step-4 length-sanity check pass** … an overstated transcript count
  **suppresses the one automated check that detects under-rendering. Any day doing this looks clean by
  construction.**" Corroborated the other way — "Day 090's declared 1875 **understates** the true body
  by ~31%" — and tempered by a counter-finding filed in the same day: "`length_actual_words` verified
  honest on both days — **the second consecutive run to find it so, which should temper the record's
  framing of that defect.**" **Tenth ask** for the deterministic 307-day recount (ASSUMPTION-1128).

- **CHANGE-2026-08-17-009 — THE LIT PIPELINE REFUTED HALF OF LAST NIGHT'S HEADLINE, MINTED NOTHING, AND
  MEASURED THIS PIPELINE'S INTAKE FAILURE AT 10 OF 10.** "**ASSUMPTION-1116 is blocking and
  half-unattested.** … **finds no occurrence of 'analogia', 'univoc' or 'Scotus' anywhere in
  `traditions/stump/`. The Thomist conjunct was never checked.** Four Rohr↔Stump convergences were queued
  for audit **on a commitment the vault has no record of Stump holding.**" And: "**PRESUMPTION-827 is a
  register contradiction, not a gap.** PREMISE-043 holds at High confidence that lexical matching here is
  high-precision/low-recall; the literature says **the opposite error profile.** … **Mirror risk is the
  cohort's largest exposure.**" Cohort: **6 REVISE (341–346), 3 MONITOR (532–534), zero INCORPORATE**;
  "**No PREMISE minted** … **PREMISE-175 remains unallocated**" — **[measured] exact on all four**.
  "**OPEN-153 measured at 10 of 10.** Two 15b instances, disjoint item sets, no coordination … against 4
  of 10 on the previous cohort." And unprompted: "**PREMISE-118 is violated by this run** … **Not
  closable by the run that files it.**" **Fourth consecutive night a headline did not survive its own
  follow-up** (ASSUMPTION-1133, 1134).

- **CHANGE-2026-08-17-010 — THE QUEUE STOPPED BEING A WORKLIST, AND ITS SELECTOR DISAGREED WITH ITSELF
  FOUR MINUTES APART.** "**The queue has stopped being a worklist.** 25 of its 26 entries are parked,
  escalated, or in verified do-not-re-audit blocks." "**Queue is empty of live work** … **Zero genuinely
  live days.**" "**second consecutive run where the queue is pure residue.**" "**Thirteen of eighteen
  days will re-present next week with a clean verdict already on file.**" And: "**`report --max 99`
  returned two mutually inconsistent queues four minutes apart** — 31 days, then 24, sharing only nine …
  **A run that picks its six from one call can review a queue no other run agrees exists.** Suggested
  practice: call `report` twice, proceed only on agreement." The remedy every run proposed — suppress
  items with a clean verdict — is the change last night's PRESUMPTION-824 finding argues against
  (ASSUMPTION-1132, queued; **PRESUMPTION-832 Medium, queued** as a search angle only).

- **CHANGE-2026-08-17-011 — THE ONLY TRADITION-LEVEL FINDING OF THE DAY CAME FROM CHANGING THE FEED, NOT
  THE SOURCE.** "**YouTube channel RSS feeds are served where search-page scraping is not.**" The yield:
  "two Thousand Brains Project research meetings from July in which **Hawkins names three unresolved
  problems in his own theory (shared learning, voting using relative pose, distributed models)** … **A
  thinker publicly enumerating his program's open problems is unusually high-value for a PRS network,
  and it sat one feed away for a month.**" Confidence held down on purpose: "the videos were **not
  transcribed** … **don't round up.**" Four traditions were upgraded from *the query found nothing* to
  *the source-of-record holds nothing* — Hoffman, Kastrup, Carroll ("**#364 does not exist yet**"),
  Fredrickson. Against it, an honest null converted into four filings: "**neither thinker published
  anything new in the 30-day window that wasn't already captured**", filed under the "significant work
  not yet captured" clause "and **each carries a reviewer caveat saying so**" (ASSUMPTION-1135;
  **PRESUMPTION-835 Medium**).

- **CHANGE-2026-08-17-012 — THE SAME FABRICATION WAS PRODUCED A FOURTH TIME IN FIVE DAYS AND IS NOW
  REPRODUCIBLE ON DEMAND.** "**One fabrication caught, fourth sighting of the same shape in five days.**
  Search twice described *The Observing I* ep. 157 as an episode featuring Kastrup; the host's own page
  shows **a solo essay *about* him** … **The 08-14 run rejected this exact episode on the same rule, so
  it's reproducible.**" Three more author-list traps the same day: "'Open-Ended Discussion…' is led by
  **Viviane Clay — not by him**"; arXiv 2605.26856 "**authors are Nagarjuna & Karnam … No Levin**"; and
  "*Five Fristonian Formulae*" — "**A 65th-birthday festschrift piece *about* Friston, not by him.**"
  "**so the check earned its keep today**" (ASSUMPTION-1136, queued).

- **CHANGE-2026-08-17-013 — PENDING IS 53 AND THE QUEUE IS NOW DEEPER THAN THE LAST DRAIN.** "**Pending
  is 53** and has now *exceeded* the 47-card batch your 08-07 reply cleared — **the queue is deeper than
  the last drain, not just refilling.** Every automated stage is caught up; the only bottleneck is a
  reply. **The 08-11 review-page ID fix … is still unpushed after sixteen days.**" Earlier the same day:
  "ten proposals filed on 08-16 — **the largest single-day intake this log records** — pushing `pending/`
  from 37 to **47** … **Nine-day review gap, five unused proposal review pages.**" And between them,
  "(**47 → 51**)". **[measured]** `pending/` holds **53** [**exact**]; approved 301 / denied 1 /
  needs_review 1 all unchanged → **10-day gap**. The three figures are arithmetically consistent, and
  **two of the three reporters had themselves written the items that moved the counter**
  (ASSUMPTION-1137; **PRESUMPTION-836 Medium**).

- **CHANGE-2026-08-17-014 — EVERY SUBSTANTIVE RUN BREACHED BUDGET, AND FOR THE FIRST TIME ONE NAMED A
  FIXABLE CAUSE.** "**Budget breach, surfaced per Rule 6:** this run consumed roughly **1.03M subagent
  tokens** … **The pipeline's house format (~25KB per result file, 20 files) makes that structural
  rather than incidental** — worth a conversation about whether the format should shrink." (≈34×.) The
  reviewer fleet in six voices: "the contract reads plus the log tail are **the standing cost before any
  pair opens**." Four stated coverage rather than implying it: "**Day 100 was selected and not read —
  not skipped-and-passed, just not done**"; "**The transcript frame was not run this pass at all**"; "no
  transcript frame ran … **In the log that can look identical to a both-frames run, so I named it**"
  (ASSUMPTION-1138).

- **CHANGE-2026-08-17-015 — THE GIT DEBRIS GREW SEVENTEENFOLD AND NOTHING HAS BEEN COMMITTED FOR THREE
  MORNINGS.** "**3 stale lock(s) (HEAD.lock, ORIG_HEAD.lock, main.lock)** … also **248 stranded
  tmp_obj**" against 2 and 14 last night. "**the daily run has now finished cleanly three mornings
  running (08-15, 08-16, 08-17) yet committed nothing since 08-14 01:00Z, which is what a blocked `.git`
  looks like.** That same block **plausibly explains** the three exit-1 launchd agents." From inside:
  "**A 0-byte `.git/index.lock`** is present and **the mount forbids unlinking it** … **clear it
  Mac-side or tomorrow's 05:45 commit will no-op** on **331 dirty paths**." The register logged FAIL and
  OK for the same run in the same minute for the second consecutive night. **[measured]**
  `metabolism_data.json` is dated **2026-08-13 21:00** against the scheduler's stated "2026-08-12
  09:59Z, 5.0 days ago" — **the one figure tonight that would not reconcile**, and its producer was mute
  (ASSUMPTION-1139).

- **CHANGE-2026-08-17-016 — THREE CHANNELS FOR TOM'S INTENT PRODUCED NOTHING, A RUN SILENTLY CORRECTED
  ITS OWN CONTRACT, AND TWO RUNS ASKED TO BE RETIRED.** "**The scrape failed — no Chat content was
  retrieved.** … the extension isn't connected … **`claude.ai/recents` redirected to
  `claude.ai/login?from=logout`. The browser profile is signed out.** I stopped there rather than
  attempting to sign in." Its artifact says "**No conversation content was read. Nothing below is
  inferred from Chat**" and labels the tab list "**Treat this as unverified.**" The walk channel: "**No
  walk notes found**" — then the run was cut before it could fall back. The contract correction: "**the
  task file gives the wiki root as `Documents/Claude/RC Karpathy Wiki Project/wiki/`, but the real path
  is `Documents/Claude/Projects/…`. I used the real one. Worth correcting in the SKILL.md so a future
  run doesn't write into a phantom directory.**" The retirements: "**This is the fourth consecutive
  no-op run … can be retired whenever you'd like**"; "the watch list is now **422 KB / 4,307 lines, past
  the Read-tool ceiling**" (ASSUMPTION-1140; **PRESUMPTION-834 High, queued**).

- **CHANGE-2026-08-17-017 — COVERAGE, EVIDENCE LIMITS, AND ONE ITEM RECORDED AS UNKNOWN RATHER THAN AS
  ABSENT.** Coverage: **2026-08-17, 29 sessions**, boundary set by `session_info` list ordering — every
  session above `local_51df64c3` and below tonight's. **Not one transcript contains a typed human
  message**; `Morning walk cowork handoff` was checked specifically because its name implies attendance,
  and does not. Evidence type: `read_transcript` returns assistant narration and tool-call *names* but
  **not tool arguments and not tool results** (PRESUMPTION-777), so every figure is *as an agent stated
  it* except where marked **[measured]**. **Nine reconciliations attempted; eight exact, one refuted.**
  The refutation is the metabolism artifact's date. **One item is recorded as unknown rather than as
  absent:** `reviewer-review-weekly` issued a write to `~/Documents/Claude/Reports/` and was cut before
  confirmation; that path is not mounted here, so this run does not know whether its brief landed and
  says so rather than filing an absence. Register audit: **1,140 unique ASSUMPTION ids, max 1141, offset
  exactly 1** — the known ASSUMPTION-459 gap, stated here as known per the correction at
  ASSUMPTION-1123; **836 PRESUMPTION ids, max 836, no gap and no collision** (ASSUMPTION-1141).

- **CHANGE-2026-08-17-018 — THIS RUN'S OWN VERIFIER EMITTED A PASS IT WAS NOT ENTITLED TO, AND THE
  RECORD IS THE CORRECTION.** After filing, this run ran a field-completeness check over its own
  eighteen ASSUMPTION and eight PRESUMPTION entries. It reported all fields present on all twenty-six.
  **That result was false.** PRESUMPTION-832 was missing its `Why it was unstated:` field, and the check
  passed it because the awk predicate set its in-record flag at the entry heading and **never cleared it
  at the next heading** — so a field absent from 832 was satisfied by the same field in 833. **The
  checker's scope was unbounded and its failure mode was a pass mark.** This is, precisely, the shape
  this record spent the whole night documenting in other agents: ASSUMPTION-1129's head-id-only regex,
  ASSUMPTION-1128's self-declared word count, ASSUMPTION-1127's guard that exits 0 — an instrument wrong
  by construction whose wrongness reads as a clean result. **It was caught only because a second,
  record-bounded check was run against the first**, and nothing in the 14a or 14b contract requires
  either. The field was added, all twenty-six entries re-verified under the bounded check, and both the
  defect and the false pass are recorded here rather than silently repaired. Register state after
  repair: **1,140 unique ASSUMPTION ids / max 1141 / offset 1** (the known ASSUMPTION-459 gap);
  **836 PRESUMPTION ids / max 836 / no gap** (ASSUMPTION-1141; PRESUMPTION-830, 833).

## Decisions

**None.** Forty-third consecutive day with no new DECISION recorded. Decision index unchanged at
DECISION-001..078 [measured].

## Open Questions Added

- **OPEN-154** — What is a run that started and produced nothing, and which register records it?
- **OPEN-155** — Who may correct a reviewer memory file carrying a diagnosis now known to be false, and
  what marks a register entry as a cause rather than a measurement?
- **OPEN-156** — Does a PRS/CROSS id carry the polarity of the claim it anchors, and if not, how is the
  existing anchor set to be audited before the corpus-wide id index is built?

## Items Routed to Literature Search

Ten items queued to `for_lit_search.md` — ASSUMPTION-1126, 1129, 1131, 1132, 1136 and PRESUMPTION-829,
830, 831, 833, 834 — all tagged `[QUEUED]` with full provenance headers. The remaining items are
empirical facts about this vault or this fleet, or governance questions filed as OPEN-154..156, and are
marked as such in their register entries. A note to 15c was filed with the intake recording that
last night's ASSUMPTION-1116 was queued on an unchecked conjunct, and that **no contract change was made
in response, because under PREMISE-096 this pipeline may not amend its own intake gate** (OPEN-153,
REVISE-340).
