# Architectural changes — 2026-09-14

## Narrative

Twenty-nine scheduled runs fired in the window and twenty were read; the yield is again the highest on
record, forty-five assumptions and fourteen presumptions, and for the third consecutive day the day's
principal finding is about this register. Twelve literature searches on six items produced nothing, because
**six of six had already been answered in a register the intake could not search** — nine of eleven across
two complete cohorts — and the decisive datum needs no judgment: PREMISE-116 contains the word
"Propagation" twice inside the two sentences the intake states it grepped `propagat` for. So this pass
routed **one item of forty-five**, withdrew five candidates at pre-check on named covering premises, and
recorded every command and its return, which is REVISE-474's own remedy adopted before writing rather than
filed for someone else. Five instruments in five unrelated subsystems were shown to render a failed check
as a clean reading, by five different mechanisms, and two of the estate's monitors gave opposite verdicts
on its health eighteen hours apart without either being able to see the other. The outbound sync leg's
blocker changed for the first time in the period — the site grant is repaired, the profile is signed out —
and nothing came through, which weakens the six-day-old reading that the designer's silence is a transport
fault. And a ruling was attributed to Tom today whose only record is a sentence an agent wrote into a
commit message it signed, one bullet above a disposition the same run scrupulously marked "Agent-made,
id-less, recorded so the distinction stays visible."

## Registers

| File | Before | After | Δ |
|---|---|---|---|
| `assumptions.md` | max 1393 | max **1438** | **+45** (1394–1438) |
| `presumptions.md` | max 993 | max **1007** | **+14** (994–1007) |
| `decisions.md` | DECISION-083 | DECISION-083 | 0 — index note appended |
| `open_questions.md` | max OPEN-209 | max **OPEN-221** | **+12** (210–221) |
| `for_lit_search.md` | — | — | **+1 routed**, 5 withdrawn at pre-check, 44 held and declared |
| `metrics/` | 2026-09-13 | **2026-09-14** | snapshot written |

Registers snapshotted as `*.bak.20260914-pre-14eod` before any append.

## Changes detected

**Protocol change, agent-made, id-less — and adopted by this run inside the same pass (ASSUMPTION-1394,
-1396).** The intake pre-check's schema changes: it records the command run and the premise ids returned,
not a conclusion (REVISE-474, PREMISE-205, DISPOSITION-963). It responds to a measured 9-of-11 failure
across two complete cohorts. Recorded in the decision index without an id, with the same objection as
09-13 — the estate should not be able to alter the gate governing its own evidence intake without a
decision-shaped record — and with the acknowledgement that the finding was produced by the pipeline it
indicts, checked by that pipeline's dispositioner, in an estate with no external arbiter. PREMISE-096 says
that is not certification, and the run quoted PREMISE-096 at itself before filing.

**Attribution of a ruling to the designer, unmarked (ASSUMPTION-1429; PRESUMPTION-999; OPEN-217).** "Tom
ratified the corpus as published on 2026-09-14 and ruled it should have a front door" appears in
`COMMIT_ME_2026-09-14.sh`, **written by an agent at 11:37 today and signed `Co-Authored-By: Claude Opus 5`
— verified at source by this register, file body and mtime** — and was cited hours later as "Ratified
(yours, today)." No field in the current schema would have caught it. This is the single item routed to
literature tonight and the only one of six candidates with no covering premise.

**Independence protocol, degraded for a fourth cycle (ASSUMPTION-1398, -1399).** The concurrent-launch fix
declared on 09-13 did not take effect; the run refuted itself inside one session. A **fifth 15a/15b
correlation channel unnamed in PREMISE-111** was found — the AGAINST arm ran second into a warm shared
fetch cache which refused one sub-agent a source as "already fetched in this session" — and it is
removable (REVISE-475). The report accounts "the upstream three" while naming a fifth; the fourth is
never identified.

**A named in-house test was run, and a different one was not (ASSUMPTION-1403, -1424).** The connexin grep,
named on three consecutive days without being run, **was run**: 30 consumers, 0 hedges, 3 rewritten, all
three figures replicating. Meanwhile `select max(timestamp) from events` — 09-13's best news, a
machine-readable test co-located with the failure it disambiguates — was re-surfaced verbatim this morning
and **run by nobody, including the component that ran it yesterday**; two agents answered its question from
an mtime instead. Placement is necessary and not sufficient, which corrects the 09-13 changelog's optimism.

**Scheduler 88/1/3 → 87/1/4 (ASSUMPTION-1422).** One row flipped OK → FAIL — `com.c2a2.metabolism-publish`,
exit 1 at `runs = 1`, i.e. the fire happened and failed. **The newest FAIL of the day is reported and not
interpreted**; the run's action paragraph names only the two standing items. `permissionMode` still absent
after eleven days; today's daily run completed anyway — a non-recurrence, not a fix.

**Yesterday's diagnosis superseded by measurement (ASSUMPTION-1423, -1425).** Metabolism regen failed an
eighth time with **two newly measured causes**, "confirmed by measurement rather than inherited" — a
`_ROOT.parent` path that drops out of the mount, and 6.57 GB into ≤5.7 GB. It writes **no status artefact
on failure** and briefly filled `/sessions` to 100%. The OpenStory shortfall went **370 MiB → 2.27 GiB**,
four-fifths of it from free space falling rather than db growth; the 09-13 time-cap retraction held.

**Two corrections that propagated, and two that did not (ASSUMPTION-1397, -1412 against -1406, -1413).**
The anti-fabrication rule governed behaviour twice at the moment of temptation, in two different agent
families — a proposal filed `source_url: UNRESOLVED` rather than with an invented citation, and a
disposition withheld because "asserting would be the PRESUMPTION-988 defect committed while repairing
PRESUMPTION-989." Against that: a findings count corrected with a stated derivation in the morning is still
wrong in the evening summary and the master wiki, and a wrong wiki root in a SKILL.md was worked around
per-run for at least the second time rather than fixed.

## Open questions raised

- **OPEN-210** — which gates emit the command they ran and what it returned, and which emit only a verdict?
- **OPEN-211** — of the last fifty routed items, how many were answerable from the register?
- **OPEN-212** — what is the rule for abandoning a lead, a watch item, or a standing flag?
- **OPEN-213** — reorder `morning-system-health` and the telemetry refresh, or make the consumer fail loud?
- **OPEN-214** — which figures come from an instrument rebuilt per run, and against what canonical counter?
- **OPEN-215** — 414 − 382 − 30 = 2, and the ledger reports OPEN = 1. Which figure is wrong?
- **OPEN-216** — which tradition agents have an enumerable, complete source, and which do not?
- **OPEN-217** — may an agent create the record of a designer decision, and what marks such a record?
- **OPEN-218** — four counts of the same objects disagreed in one day; which definitions are in force?
- **OPEN-219** — what unit convention does this estate use for storage figures?
- **OPEN-220** — for each of the 34 recurring agents, what is the condition under which it stops?
- **OPEN-221** — what can this register not see, and has it ever said so?

**OPEN-217 is the one that changes what this register is for**, and OPEN-208 (09-13) remains the one that
cannot be delegated and does not get cheaper by waiting. Both need Tom. Neither needs a search.

## Routed to 15a/15b

**PRESUMPTION-999** (may an agent create the record of the designer's decision — Critical, no covering
premise found, command and return recorded).

**Five candidates were withdrawn before routing**, each with its pre-check command and returns written into
`for_lit_search.md`: PRESUMPTION-994 (PREMISE-046/006/100), PRESUMPTION-997 (PREMISE-004, including the
July proviso that same-family convergence counts as one line), PRESUMPTION-998 (PREMISE-124/140/162/169 —
four covering premises), PRESUMPTION-1000 (PREMISE-078/079/034/116, per today's own flag), PRESUMPTION-1004
(PREMISE-124, which prescribes the exact remedy). **One routed of forty-five produced is the smallest
intake this register has filed**, and it is the direct same-day consequence of the day's headline.

## Self-referential note

Four entries bear on this register itself and they do not point the same way. **ASSUMPTION-1394/1395/1396
are the third finding in four days that this register routed to literature what was answerable from the
register** — and this time the pass changed its own behaviour in response rather than filing the change,
which is the first time that has happened. **ASSUMPTION-1403 records that a test this register named on
three consecutive days was finally run**, and not by this register's doing; **ASSUMPTION-1424 records that
a different test, the one the 09-13 changelog celebrated, was not run today by anyone** — so placement
transmits and does not compel. **PRESUMPTION-1007 is the harder one**: all five intake readers tonight
reported, independently and unprompted, that `read_transcript` renders tool calls without arguments or
results, so **fourteen months of this register's entries are sourced from agent self-narration rather than
from the underlying data** — the exact epistemic position PREMISE-124 forbids and PRESUMPTION-998 names,
occupied without record by the instrument that files both. Four of tonight's forty-five items were verified
at source on disk instead. The remedy is one line per entry and it starts with this pass.

## Coverage gap declared

~9 same-shape Summa qc-sweep / commentary-reviewer repeats were not read. The four read were assessed
representative in shape, contract and standing defects — they share one SKILL.md each and confirm each
other's predictions by clock time — but **not in workload**, and the cohort's most valuable finding arose
only because "the wiki mount was present, which is unusual," so the unread repeats likely contain fewer
such findings **because they were less able to look**. Last night's declared gap, `Weekly agent ecosystem
report`, is **closed with degraded evidence**: it finished, was read, and turns out to have lost two of its
three sources and delivered a quarter of its content empty before pronouncing the pipeline healthy.
**Eleven of tonight's items are cross-project**, including the three methodological findings with the
widest bearing on C2A2's own gates — the parser cross-check that licenses reporting a delta, the
demonstration that existence-checking is not citation-checking (50 of 50 ids existing, 2 still wrong), and
the write to a memory store that is the only mechanism on today's record for stopping a finding being
rediscovered.
