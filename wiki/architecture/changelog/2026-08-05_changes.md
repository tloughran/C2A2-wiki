# 2026-08-05 — EOD self-awareness run (Agents 14a / 14b)

The day the audit found its own charter in the dock, and the register lost a record while asserting it had
not. The lit pipeline, dispositioning 14b's output, traced a High systemic flag back to line 88 of 14b's
own definition and found it contradicting six ACTIVE premises after ~118 days unexamined; separately,
`assumptions.md` was measured tonight to be missing ASSUMPTION-217, present in every backup since
2026-06-05 and still referenced from `presumptions.md`, detectable only because last night's snapshot had
declared the very offset that revealed it "exactly accounted for." Against that, the day's discipline was
the strongest in the series: the nightly verification disclosed two false results of its own before
publishing and shipped a self-test that fails loudly; the metabolism run refused to mark itself
successful; the daily run performed the first direct PRS recount in four days; and the Wednesday agents
produced a warrant for the accelerator's core premise while disclosing the unaudited route it arrived by.
No new DECISION (thirty-first straight day), no new OPEN, and no attended session exists for the third
consecutive day.

## Changes Detected

- **CHANGE-2026-08-05-001 — The root cause was found inside 14b's own charter, by another agent, on day 118.** The lit pipeline named `14b_presumption_detector_agent.md` line 88 — "Err on the side of surfacing too many rather than too few. False positives are cheap; false negatives are invisible by definition." — as contradicting six ACTIVE premises (003, 070, 095, 106, 119, 121), "and had never been flagged." It underwrites the High batch flag on PRESUMPTION-673/677: a single-authoriser bottleneck with positive feedback and no damping. 14b's definition instructs it to audit the system's premises and contains no instruction to audit itself. **Handling declared, not applied silently:** 14b surfaced its usual number of presumptions and routed 6 of 18 to literature rather than the ~11 the unmodified norm produces, tagged the rest IN-HOUSE, and named the norm conflict as an OPEN candidate without resolving it (ASSUMPTION-730; PRESUMPTION-678, **Critical**, queued).

- **CHANGE-2026-08-05-002 — `assumptions.md` lost a record, and no run reported it.** ASSUMPTION-217 (KSGA sociogram self-contained node panels, 2026-05-22, Status GROUNDED) is present in `assumptions.md.bak.20260804-pre-14eod` and in every backup tested back to 2026-06-05, and was absent from the live file at the start of this run, while still referenced from `presumptions.md`. It was found only because the 08-04 snapshot asserted the strict-block/max-ID offset was "exactly accounted for by the ASSUMPTION-459 gap" and tonight it was 2, not 1. Restored verbatim by this run; post-restore offset 1. The only writers between the two states are 14a's 08-04 append and 15c's status-update pass. Cause unstated (ASSUMPTION-764).

- **CHANGE-2026-08-05-003 — The false all-clear recurred, word for word, the day after it was documented.** `morning-project-status`: "all of this morning's runs fired on time, including the C2A2 wiki daily run… No failures overnight," against `scheduler-health-check`'s **2 FAIL** and `morning-system-health`'s commit failure and 15-day-stale publisher, the same morning. CHANGE-2026-08-04-002 filed this defect into `architecture/`, a directory that instrument does not read. Third consecutive day, and the sharpest evidence yet for OPEN-138 (ASSUMPTION-735; PRESUMPTION-679, High, in-house).

- **CHANGE-2026-08-05-004 — Lit pipeline: 8 items dispositioned, 3 INCORPORATE · 5 REVISE · 0 MONITOR, two batch flags.** D-601..D-608; PREMISE-141 (from P-664), PREMISE-142 (666), PREMISE-143 (668); REVISE-278..282. A **Critical** batch flag on 664/666/668/669 for declaration-substituted-for-measurement with a monitor inside its own failure domain (not re-minted; recorded as an enforcement gap inside PREMISE-141 per PREMISE-096), and a **High** on 673/677, plus a secondary High on 673/675/676. Fifth consecutive day carrying a Critical flag; none of the preceding four has fired an action. Zero MONITOR was a stated choice, not an omission: "The 15d queue has shown zero consumption for 25+ days; a MONITOR entry there would itself be PREMISE-138 clause (1)," with each REVISE instead carrying "an IN-HOUSE FIRST STEP requiring no authorisation, so measurement proceeds without consuming the bottleneck." Yield series: 07-31 0/3 · 08-01 2/6 · 08-02 3/7 · 08-03 0/7 · 08-04 0/11 · **08-05 3/8** (ASSUMPTION-729, 734).

- **CHANGE-2026-08-05-005 — The instrument-doubt caveat was carried, not banked, for a second day.** Today's 3-of-8 INCORPORATE rate is "weak evidence against the 'tilts toward REVISE regardless of input' reading, but it is one run and the three INCORPORATEs all came from the same 15b batch"; REVISE-271's distinguishing study "remains un-run"; MONITOR-498 "remains open against exactly this series" and was reported, not closed. Independence held a second consecutive day — "15a and 15b ran in separate contexts and 15b read nothing in `lit_search_results/for/`." The full UNVERIFIED citation list was disclosed, one fetch was BLOCKED and excluded from provenance, and one search "returned nothing verifiable and was omitted rather than guessed at" (ASSUMPTION-732, 733).

- **CHANGE-2026-08-05-006 — A second pipeline-execution change in three days, again with no DECISION.** "Queue-file and returns-file writes were centralised in the coordinator rather than performed by 15a/15b/15c, to avoid concurrent edits to two shared files." Recorded in a run preamble only, exactly as the 08-04 separate-instance arrangement was. The 08-04 argument stands unanswered: "If the way the pipeline runs can change without a DECISION, the decisions register is not a complete record of how the system got to its current shape" (ASSUMPTION-731).

- **CHANGE-2026-08-05-007 — The 45-second wall became an epistemic instrument.** Four checks were replaced or abandoned on one resource ceiling: `PRAGMA quick_check` skipped ("let torn pages surface as `DatabaseError` — none did"); `extract_agent_node_refs.py` past the cap at 156,930 events / 495 MB and replayed under memoization with "0 cache misses… output identical by construction"; the metabolism builder at `timeout 40` → exit 124 on a 3.7 GB db; `openstory_ingest_lag.py` exiting 2 = "cannot determine," with a direct DB assertion substituted (`MAX(sessions.last_event)` 14 seconds old, against a `.db` mtime 4h stale — "exactly the WAL-mode lie that script exists to avoid"). Every substitution was disclosed in prose; **none was encoded in the artifact the run wrote** (ASSUMPTION-746, 747; PRESUMPTION-680, High, in-house).

- **CHANGE-2026-08-05-008 — The pass-mark defect is 3.8x larger than yesterday's count.** `raw_ASR_word_count` duplicated on **125 of 307** transcripts (41%), disabling the ±25% check on each — "Those 125 days are unmeasured, not clean," a band "four times wider than the recorded 33-day drift band," of which the 33 is a subset. The run declined to clear them: "Nothing was pass-marked… marking them would retire the tripwire." `fidelity_check.py`'s cold-cache `FileNotFoundError` was diagnosed independently by four separate runs today and remains unpatched on the standing rule that the scripts are Tom's, second day (ASSUMPTION-752, 753).

- **CHANGE-2026-08-05-009 — A run nearly reviewed against another session's state, and the error that would have said so was thrown away.** "`/tmp/rep.json` [was] owned by `nobody` and dated Aug 4 16:17 from a *different* session — my redirect was denied, `2>/dev/null` ate the error, and I read yesterday's queue back and nearly reviewed against it." Second consecutive run to hit it. Two presumptions in one line: that `/tmp` is private to a run, and that an error on stderr is an error seen — the latter being the same mechanism that produces `fidelity_check.py`'s false pass-marks (ASSUMPTION-754; PRESUMPTION-681, 682, **Critical**, in-house).

- **CHANGE-2026-08-05-010 — One command, nine answers, and three identical calls that disagreed with each other.** `report --max 99` returned 42 / 31 / 24 / 31 / 25 / 20 / 15 / 15 / 13 / 13 across today's Summa runs; three identical calls inside one run returned **31 / 24 / 31 with disjoint membership**. Every hold, pass and escalation issued today rests on a set that may not be the set. Adjacent: one run reported a sibling's verdict as "the 16:40 run reported all six as passing" against that run's own "0 pass, 6 held, 5 escalations," and McGilchrist PRS-04's repoint target is given two incompatible ways on the same day (ASSUMPTION-755, 756; PRESUMPTION-683, 684, in-house).

- **CHANGE-2026-08-05-011 — The budget rule was named unsatisfiable, not merely exceeded.** "the mandatory contract-and-memory reading alone runs past 4,000 before any review starts, so that ceiling isn't reachable for this task as specified." Ten consecutive days of breach disclosures had presumed the fault lay with the runs. Same day: 6 of 11 Summa runs breached 4k, one reporting ~495k across five parallel subagents; the lit pipeline breached both the 4k and the 30k budget and said so — "Surfaced, not hidden" (ASSUMPTION-757; PRESUMPTION-689, High, queued).

- **CHANGE-2026-08-05-012 — A task established as impossible where it runs is still scheduled there.** "**The regeneration did not run.** I am not marking this successful. Nothing was committed or pushed." Two blockers named: `_DB_CANDIDATES` misses the sandbox path (one line), and `bwrap --unshare-pid` plus a 45 s cap mean "no multi-minute job can survive here at all." The run argued for its own relocation behind a launchd agent, caught its own false positive (`pgrep -f metabolism_monitor` matching the `bash -c` wrapper — "`ps aux` showed no python process at all"), and named why its watchdog cannot see it: "the artifact dates itself and is still under 24 h, so the 'did it produce?' question reads OK today and only turns red this afternoon" (ASSUMPTION-743, 744, 745; PRESUMPTION-686, 690, High, 690 queued).

- **CHANGE-2026-08-05-013 — Four headline counters do not move when their contents do, and this run watched it happen to itself.** DISPOSITION strict block count **568 / max 600** with eight dispositions issued today written only as `(D-601)`…`(D-608)` — surfaced by the evening summary's own verification as "the third instance in three days of a register changing its write format while the counting rule that reads it silently stopped tracking." REVISE strict **118 for a third day** against max 282. `Status: [QUEUED]` **1,527 for a third day** across nineteen dispositions. `^CROSS-NNN:` returning **54 against 90 actual** — "Don't trust that pattern." Tonight's own append moved the loose `[QUEUED]` count 1,803 → 1,810 and left the headline at 1,527; written in the house format rather than worked around, per Rule 11, and recorded (ASSUMPTION-737, 738, 765; PRESUMPTION-687, High, in-house).

- **CHANGE-2026-08-05-014 — The first direct PRS recount in four days, and it retired nothing.** "Counted directly against the live files today: **519 PRS** (513 across 15 traditions + 6 master) and **90 connections** (distinct IDs through CROSS-090). The 07-31 narrative had it right; 08-03 was the outlier." This performs MONITOR-489's settling test for the first time. But 511/165 stands in last night's snapshot, and the master wiki header carries a third set — "35 active findings against 56 actual, 6 pending proposals against 40, and the Network Map PRS table sums to 76 against a stated network total of 222" — reported and, per Rule 7, deliberately not averaged (ASSUMPTION-738, 739; PRESUMPTION-688, in-house).

- **CHANGE-2026-08-05-015 — The review gate, eleventh day.** Pending **40** (approved 254, denied 1, needs_review 1); trajectory 26 → 32 → 34 → 40 over five days. `generate_review_page.py` line 304 unchanged since 2026-05-18 — "**A review pass run now would discard all 34 decisions.** Queue has grown 112% since this was first escalated on 07-29, and the review-pass gap is 13 days — the two facts compound." Two dates circulate for the last decision email, 2026-07-20 and 2026-05-26, a 55-day spread, with `review/archive/` giving 2026-07-23 for the adjacent quantity (ASSUMPTION-740, 741; PRESUMPTION-691, 692).

- **CHANGE-2026-08-05-016 — Auto-open reclassified from flaky to structural.** "The Chrome MCP rewrites `file://` to `https://file://`, and the Linux sandbox has no macOS `open`. Three runs in a row. Worth a real fix rather than a per-run note." Phase 6 git commit BLOCKED for the same class of reason — "sandbox cannot write `.git` objects. Must run on Mac" — with the streak reported as **three days** by one instrument and **four** by two others, unreconciled (ASSUMPTION-736, 742, 761).

- **CHANGE-2026-08-05-017 — The nightly verification disclosed two false results of its own before publishing.** "A part-blind Q-number match returned 17 guardrail flags of which 16 were false — Q.23 means charity in II-II and predestination in I." Then "the corrected sweep used `\bhylomorph\b`, which cannot match *hylomorphism*, and silently dropped Day 129 to report a tidy 0/74. That one was caught only because it disagreed with the 08-04 entry." The remedy: "a self-test that fails loudly instead of returning an empty list." **The register served as the external referent PREMISE-124 asks for, and it worked.** The same run named two spec checks "inconclusive by construction" — mtimes bulk-restamped by the vault sync, and a FLAG/CROSS existence check that "passes only because normalizing `FLAG-05`→`FLAG-005` makes coincidental numeric matches resolve." **This partially RETRACTS ASSUMPTION-728(i):** mtime-based claims in this series, including "`prs_yield_log.csv` modified without gaining a row," are unreliable (ASSUMPTION-748, 749; PRESUMPTION-693, in-house).

- **CHANGE-2026-08-05-018 — A run preferred the prior day's figure to its own, and stated the rule.** "I did not escalate the length section's 57 outside-both-policies days — that count disagrees with the 08-04 pass's stable 24 for measurement-basis reasons, and given my parser already carried one real bug tonight, the prior number is the better one." Applied generally, this makes a figure durable in proportion to how long it has gone unchallenged. The same run's second bug was caught by disagreeing with that same register — the register worked as referent in one direction and as anchor in the other (ASSUMPTION-750; PRESUMPTION-685, High, queued).

- **CHANGE-2026-08-05-019 — The day's best object-level result arrived through its own disclosed blind spot.** Six proposals (PROP-2026-08-05-001..006, queue 34 → 40). "**003** supplies the missing warrant for the accelerator's core premise: if freedom is width of attention, a tradition locked in one mode is *unfree*, not merely limited." "**002** is a recorded, long-form encounter between two registered C2A2 traditions examining each other's first principles… both a content source and a specimen of the interaction protocol the accelerator is meant to instrument." "**004** shows McGilchrist adopting Kastrup's own formulations verbatim… and separately endorsing Smolin/assembly theory against the block universe — a clean, citable contradiction with Carroll." And, disclosed by the same run: "PRS triplets are drawn from publishers' own descriptions and works-cited lists, not from the recordings themselves," one from a paywalled session's host write-up, with each Evidence line saying which, and a vendor 100x figure marked Medium. Two candidates were "seen and rejected" — "Rejecting rather than writing PRS triplets I could not evidence" (ASSUMPTION-758, 759; PRESUMPTION-695, High, queued).

- **CHANGE-2026-08-05-020 — Eighth dark day, one cause, and two correct refusals.** Chrome is signed out of claude.ai; morning and evening both reached `/login?from=logout` — "the first where both sync directions failed on one identical, fully-diagnosed cause." Both runs declined to write an empty artifact: "I wrote a failure note in place of today's summary rather than a silently empty file." Seven MCP servers still need OAuth, which no scheduled run can perform. Agent 16 ran clean on all three intake channels and reported nothing due rather than manufacturing findings; `scheduler-health-check` produced a verdict after vanishing without one on 08-04; Wright and Rohr came on disk (36 and 33 triplets), retiring a standing fallback allowance (ASSUMPTION-751, 761, 762).

## Decision Index

**No new DECISION entries.** `decisions.md` content unchanged since 2026-07-20 (DECISION-078, dated
2026-07-05) — **thirty-first consecutive day.** Its mtime reads 2026-08-05 21:00 with no content change;
recorded as a measurement and not as an event, because vault mtimes are now known to be bulk-restamped
(ASSUMPTION-749). Three candidates named against the day and not logged, per the standing convention
against unilateral promotion:

1. The coordinator write-centralisation arrangement — the second live change to pipeline execution in
   three days recorded only in a run preamble (ASSUMPTION-731).
2. Whether `metabolism-regen-daily` moves behind a launchd agent, now that the environment it is
   scheduled into is established as structurally unable to complete it (ASSUMPTION-743).
3. Both 08-04 candidates, carried unactioned.

## Open Questions

**No new OPEN entries.** `open_questions.md` unchanged since 2026-07-28 (OPEN-139). OPEN-138 and OPEN-139
both **OPEN — awaiting Tom** for a thirteenth day, while five more REVISE flags accrued into the layer
OPEN-138 asks about. Candidates surfaced and not logged:

- **Whether 14b should continue to operate under line 88 of its own definition**, which the pipeline that
  tests 14b's output today found to contradict six ACTIVE premises. This is the first candidate raised
  against the existence of this register at its current production rate, and it is raised by the register
  itself (PRESUMPTION-678).
- Whether the 4,000-token rule is satisfiable as written, after ten days of breach reports that presumed
  it was (PRESUMPTION-689).
- The eternity/timelessness question (FINDING-055), carried unchanged since 07-31.

## Intake Routed

- **[QUEUED] to 15a/15b (6):** PRESUMPTION-678, 685, 689, 690, 691, 695.
- **[IN-HOUSE] (12):** PRESUMPTION-679, 680, 681, 682, 683, 684, 686, 687, 688, 692, 693, 694.
- **[IN-HOUSE] (37):** ASSUMPTION-729..765, with ASSUMPTION-738 and ASSUMPTION-758 flagged
  OBJECT-LEVEL/POSITIVE and ASSUMPTION-749 carrying a partial retraction of ASSUMPTION-728(i).

Routing ratio stated: 6 of 18 to literature rather than the ~11 line 88 would produce. The reduction is
declared here rather than applied silently, and does not resolve the norm conflict.

## Fail-Loud

This run exceeded the 4,000-token per-task budget — **tenth consecutive day**. Two parallel sub-agents
read ~15 session transcripts at tail depth to keep transcript bulk out of context; no multi-MB register
was read whole. Backups written to `*.bak.20260805-pre-14eod`.

**No attended session transcript was read, because none exists** — third consecutive day, confirmed by two
independent methods: a 60-session sweep in which every 2026-08-05 title maps to a recurring scheduled-task
name, and the lit pipeline's own note from inside its run, "Orchestration choices made (no human
present)." The day's entire record is the self-report of scheduled runs, several of which are today
measured to be non-deterministic, cross-contaminated through `/tmp`, or reporting each other's verdicts
incorrectly.

**One repair made inside 14a's write set and declared:** ASSUMPTION-217 was restored verbatim from
`assumptions.md.bak.20260804-pre-14eod` after being measured absent from the live file; post-restore
strict block count 727 (pre-append), max 728, offset 1. Recorded rather than silently repaired.

**Reflexive:** this run's append to `for_lit_search.md` moved the loose `[QUEUED]` occurrence count from
1,803 to 1,810 and left the headline `Status: [QUEUED]` figure at 1,527. It is a live instance of the
presumption it filed tonight (PRESUMPTION-687), produced by writing in the house format rather than
working around it, per Rule 11.

Not done, stated rather than omitted: `agentic-cost-tracker` and `weekly-agent-ecosystem-report` are
unrepresented for a fourth consecutive night; the three owed one-command joins (MONITOR-500 / P-643 /
P-645) were not run here, fourth day; the 26-item legacy retag is unauthorised on its tenth request;
`decisions.md` and `open_questions.md` were deliberately left unmodified; the `revision_flags.md` and
`lit_search_returns.md` block-count breaks were measured but not repaired, both being outside 14a's write
set — third and first day respectively.
