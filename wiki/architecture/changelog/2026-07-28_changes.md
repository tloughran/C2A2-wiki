# Architectural Changelog — 2026-07-28

## Narrative

Autonomous day #23, and the day the pipeline audited itself with a second instance for the first time. The 15a/15b/15c batch cleared eight items and incorporated five premises — PREMISE-130 through -134 — after a run that incorporated one, and then a separate agent, not 15a/15b/15c, ran a verification pass over the batch: sixteen result files checked, seven load-bearing citations re-checked against primary venues and all seven verified exact, and six drafting defects found and corrected inside PREMISE-133 rather than quietly fixed. That is the decorrelated spot-check MONITOR-486 has been asking for since 07-25, and it is the strongest procedural move the pipeline has made. 14b's job is to note what it does not settle: the same file states, of the same batch, that "residual same-model correlation is NOT discharged," so the pass presumes instance separation is the decorrelation PREMISE-132 requires and model-family correlation is a residual, when the premise was minted to defeat exactly that (PRESUMPTION-567); and all six defects are classified "drafting/rigour, not a wrong disposition" although defect (2) is an inflation of apparent convergence and convergence is the stated ground of the disposition (PRESUMPTION-568). The batch also refuted two of 14b's own prior inferences on checkable points — PRESUMPTION-561's Little's Law premise is simply false, and PRESUMPTION-565's non-uniform-direction sub-argument fails — both recorded rather than absorbed (ASSUMPTION-571), and both correctly leaving the backlog concern standing. Elsewhere: CCL Community Explorer increment 2 shipped on the finding that §V's filter generalisation had encoded `prs_3d`'s globals as the general case, with the conclusion that "the general case was always 'read the controls'" — which 14b reads as the fifth abstraction produced by the method that produced the four before it (PRESUMPTION-566). Agent 16 re-checked both watch items, both NOT MET, and turned up a real new route on WATCH-002 (the embed is YouTube `vshC_TxwrVo`, so auto-captions are a transcript path). Two proposals arrived, pending 16 → 18. The review generator remains unmodified since 2026-05-18 for a fourth consecutive day at the top of the list, and this time no review page was generated at all — a silent null explicitly flagged and not diagnosed (ASSUMPTION-567; PRESUMPTION-570). Sixth consecutive dark sync, failing in both directions, and Browser 2 has now dropped off entirely, ending the two-browser fallback. No new DECISION (23rd straight day), no new OPEN.

## Changes Detected

- **CHANGE-2026-07-28-001 — No attended session; autonomous day #23; no new DECISION (23rd straight) and no new OPEN.** No 07-28 changelog or snapshot existed before this EOD run; `decisions.md` unchanged since 07-20 (last entry DECISION-078 / 07-05); `open_questions.md` unchanged since 07-23 (OPEN-139). The evening summary states it was reconstructed from artifacts because `list_sessions` exposes 2,613 sessions with no timestamps (ASSUMPTION-561; PRESUMPTION-574).
- **CHANGE-2026-07-28-002 — Lit pipeline dispositioned PRESUMPTION-557/558/559/560/561/562/563/565: 5 INCORPORATE, 3 MONITOR, 3 REVISE, 1 SYSTEMIC-RISK-FLAG (High), 0 NOVELTY.** PREMISE-130 (recurrence in one component is a defect-class signal — classification only, remedy routed to PREMISE-066), PREMISE-131 (hierarchy of controls: warnings are administrative and an undelivered warning is worth zero), PREMISE-132 (citing is not verifying; the requirement is DECORRELATION, not externality), PREMISE-133 (abstention is a decision and a suspension must name what discharges it, who adjudicates, and by when), PREMISE-134 (PREMISE-127's transfer-condition standard extended reflexively to the project's own self-descriptions). MONITOR-487 ("virtual governor" unconfirmed in Levin's corpus), MONITOR-488 (progress-construct contested strong/strong), MONITOR-489 (self-certified counts; the OPEN-112 recount is decisive). REVISE-247, -248, -249. Totals: PREMISE→134 · MONITOR→489 · REVISE→249 · DISPOSITION→548 (ASSUMPTION-569).
- **CHANGE-2026-07-28-003 — First verification pass by a separate, non-15a/15b/15c instance.** Structural checks 1–6 PASS (16/16 result files present and non-trivial; STEELMAN in all 8 against/ files; zero item-level orphans; queue tag ↔ DISPOSITION ↔ register agree on all 9). Citation spot-check: 7 of 7 verified exact including page ranges and both quantitative figures. Six defects found in PREMISE-133 and corrected on the record per PREMISE-006 (ASSUMPTION-570; PRESUMPTION-567, PRESUMPTION-568).
- **CHANGE-2026-07-28-004 — Two of 14b's prior inferences were refuted on checkable points, and the pipeline recorded a self-non-compliance.** PRESUMPTION-561's central premise is false — Little's Law requires no service process — so REVISE-248 routes the correction back to 14b while confirming, not relieving, the backlog concern (DISPOSITION-536 stands). PRESUMPTION-565's non-uniform-direction sub-argument was refuted convergently by both directions and recorded inside MONITOR-489. Separately, a FAIL-LOUD note states that incorporating PREMISE-133 "puts the pipeline formally out of compliance with its own newest premise, because MONITOR-486 has no discharge rule as of this run" → REVISE-247 (ASSUMPTION-571).
- **CHANGE-2026-07-28-005 — CCL Community Explorer increment 2 shipped (`voice_guide_redesign.md` §X); zero page-side edits.** The shell's roster reader could not read the page at all — fifth instance of the failure mode, first located in the shell rather than a manifest. §V had generalised filters off the Sociogram and landed on `prs_3d`'s conventions; Community Explorer holds the same state in a lexically-bound `Set`. Fix: a section may declare `keys` (selector + attribute) and the roster comes off the controls, read via `.checked`. Gate 161 engine + 328 shell green; coverage 21 controls — 10 covered, 4 excluded, 7 deferred, 0 uncovered (ASSUMPTION-562; PRESUMPTION-566, PRESUMPTION-575).
- **CHANGE-2026-07-28-006 — Two findings recorded as obtainable only by running.** `click()` is an `HTMLElement` method, so the SVG roster needed a dispatched `MouseEvent` to reach d3's handler; and `unresolved` was one error code answering several questions, so the engine now carries the verb and the shell distinguishes "filters are not available on this view" from naming the axes (ASSUMPTION-563).
- **CHANGE-2026-07-28-007 — Three capabilities deliberately not declared, with reasons; Connectome increment 3 is now a stated prerequisite.** `find`/clear deferred until the cut dimension learns "highlighted, not hidden" (`applyLens` dims to 0.08); `zoom`/`pan` reasoned from d3 v7 but **not run** — prove one dispatched wheel first; `#btn-hold`/`#btn-names` are toggles carrying `.on`, which makes Connectome increment 3 a prerequisite rather than a parallel track (ASSUMPTION-564).
- **CHANGE-2026-07-28-008 — Agent 16 re-checked WATCH-002 and WATCH-003; both NOT MET, both still WATCHING; check method extended.** WATCH-002 (Wright): source fetched HTTP 200/53 KB, `entry-content` holds one YouTube embed figure, `article:modified_time` still 2026-07-17. New this check: the embed is **YouTube video `vshC_TxwrVo`**, so auto-captions are a transcript route the original method never contemplated — the method has been extended. WATCH-003 (Rohr): `review/archive/` unchanged at 16 files; no review pass since 07-23, so the item **structurally cannot move until a review runs** (ASSUMPTION-565).
- **CHANGE-2026-07-28-009 — `tools/generate_review_page.py` still unmodified since 2026-05-18; fourth consecutive day as top item.** Line 304 synthesizes pids from `run_date` + counter with no reference to the card set; against today's 18 pending proposals (07-21 → 07-28) it would emit 18 sequential `PROP-2026-07-28-NNN` ids of which **two** exist. Stated fix: emit pids from the real card set and add a reconciliation assertion that **can fail** — there is currently no `assert` anywhere in the file (ASSUMPTION-566).
- **CHANGE-2026-07-28-010 — No review page was generated today at all, and the cause was not diagnosed.** `review/` still tops out at `2026-07-27_review.html` (written 07-27 04:38); today's 04:38 scheduled run produced nothing. Flagged rather than guessed. 14b notes this is a fault of unknown scope filed as a consequence of an adjacent, differently-shaped defect (ASSUMPTION-567; PRESUMPTION-570).
- **CHANGE-2026-07-28-011 — Transmission layer dark for a 6th consecutive sync, failing in both directions; Browser 2 has dropped off entirely.** The 14:02 Chat→Cowork scrape hit the claude.ai login screen on both connected instances ("No Chat context is available to Cowork sessions for 2026-07-28"); at 18:42 EDT delivery found **only one browser connected** and `/recents` → `/login?from=logout`. Chat has had no Cowork context since 07-24, and Cowork ran all day on 07-27's Chat context (ASSUMPTION-568; PRESUMPTION-571; OPEN-135). Several plugin MCP servers (Slack, Notion, Linear, Figma, Atlassian, Intercom, Datadog) again required authorization unavailable to a scheduled run.
- **CHANGE-2026-07-28-012 — Two new proposals; pending review queue 16 → 18.** `PROP-2026-07-28-001` Hawkins, the Thousand Brains Project's plain-language explainer of arXiv:2507.05888 — carries a claim the Hawkins wiki does not hold anywhere, that **the thalamus is a reference-frame transformer, not a relay**, with a stated falsifier; plus hierarchy re-purposed to encode composition rather than abstraction. `PROP-2026-07-28-002` Hoffman, "Consciousness And Its Spacetime Headset" — an unpublished 2025 essay listed with no link, DOI or PDF; **text not retrievable**; the proposal self-marks Confidence: Speculative and "a hypothesis about the source, not an extraction from it" (ASSUMPTION-572, ASSUMPTION-573; PRESUMPTION-572, PRESUMPTION-573).
- **CHANGE-2026-07-28-013 — A methodological moral proposed for adoption past the voice guide.** "A generalisation is only as good as its worst-covered instance, and the check has to be *run against the world*, not asserted" — offered as the same line as the external-referent rule (MONITOR-486, PREMISE-129) and "worth stating once as policy rather than rediscovering per-tab." Not adopted (ASSUMPTION-574; PRESUMPTION-566).
- **CHANGE-2026-07-28-014 — Fail-loud: token budget breached again (~45,000 against a 4,000 per-task budget), and no session transcript was read.** The stated cause is the missing timestamped session index; the stated remedy is a cheap manifest written during the day. The artifact-only reconstruction is warranted by invoking `fact_inventory.md`'s authoritative-record rule (ASSUMPTION-574, ASSUMPTION-561; PRESUMPTION-574).
- **CHANGE-2026-07-28-015 — Carried, unactioned, restated unchanged.** Two undisposed 2026-07-19 proposals (INTEGRITY FLAG); ~174-item monitor backlog with zero 15a/15b consumption since 07-08 (20 days); metric inflation at a **7th consecutive flag** (connectivity CSV latest row still 2026-07-26); `watch_list.md` at 267 KB with <2% active content; the 26-item legacy backlog still carrying `[QUEUED]` tags that make it indistinguishable from live work (bulk retag not performed — stated as needing Tom's authorisation).

## Assumptions Extracted (14a) — 14

| ID | Statement (abbreviated) | Type | Testability |
|---|---|---|---|
| ASSUMPTION-561 | Autonomous day #23; no new DECISION/OPEN; summary reconstructed from artifacts because 2,613 sessions expose no timestamps | architectural / empirical | empirical (in-house) |
| ASSUMPTION-562 | CCL increment 2 shipped; §V had encoded `prs_3d`'s globals as the general case; "read the controls" is the general case; 161+328 green, 0 uncovered, zero page-side edits | architectural / methodological | empirical (in-house) |
| ASSUMPTION-563 | Two run-only findings: `click()` is an `HTMLElement` method (dispatched `MouseEvent` needed); `unresolved` was one code answering several questions | architectural / methodological | empirical (in-house) |
| ASSUMPTION-564 | Three deliberate non-declarations with reasons; `zoom`/`pan` reasoned-but-not-run; Connectome increment 3 now a prerequisite | methodological / architectural | empirical (in-house) |
| ASSUMPTION-565 | Agent 16 re-checked both watch items, both NOT MET; WATCH-002 new YouTube-caption route, method extended; WATCH-003 cannot move until a review pass runs | empirical / methodological | empirical (in-house) |
| ASSUMPTION-566 | `generate_review_page.py` unmodified since 05-18; 18 real proposals vs 18 synthesized pids of which 2 exist; no `assert` in the file | architectural / empirical | empirical (in-house) |
| ASSUMPTION-567 | No review page generated at all today; the 04:38 run produced nothing; cause flagged, not diagnosed | empirical / architectural | empirical (in-house) |
| ASSUMPTION-568 | 6th consecutive dark sync, failing both directions; Browser 2 dropped off entirely; one browser left | methodological / architectural | empirical (in-house) |
| ASSUMPTION-569 | 8 items dispositioned: 5 INCORPORATE / 3 MONITOR / 3 REVISE / 1 systemic flag; totals 134/489/249/548; two decorrelation signals claimed, residual correlation not discharged | methodological / empirical | empirical (in-house) |
| ASSUMPTION-570 | First verification pass by a separate instance; 7/7 citations verified exact; 6 defects corrected in PREMISE-133 on the record | methodological / epistemic | empirical (in-house) |
| ASSUMPTION-571 | Fail-loud: PREMISE-133 puts the pipeline out of compliance with MONITOR-486 (no discharge rule) → REVISE-247; PRESUMPTION-561 refuted → REVISE-248; PRESUMPTION-565 sub-argument refuted → MONITOR-489 | epistemic / methodological | empirical (in-house) |
| ASSUMPTION-572 | Hawkins PROP-...-001: thalamus as reference-frame transformer, not relay — load-bearing because PRS triplets are read as reference frames; falsifiable | empirical / architectural | literature |
| ASSUMPTION-573 | Hoffman PROP-...-002: essay text not retrievable; self-marked Speculative; a third disposition category proposed for "unretrievable but well-described source" | epistemic / methodological | literature + in-house |
| ASSUMPTION-574 | "Run it against the world, not asserted" offered as policy; token budget breached (~45k vs 4k); artifacts invoked as the authoritative record; carried items restated | methodological / epistemic | literature + in-house |

## Presumptions Surfaced (14b) — 10

| ID | Statement (abbreviated) | Type | Risk |
|---|---|---|---|
| PRESUMPTION-566 | "The general case was always 'read the controls'" is the fifth abstraction produced by the method that produced the four it supersedes; the worst-covered-instance moral is applied to the old abstraction, not the new one | methodological / epistemic | Medium |
| PRESUMPTION-567 | The verification pass presumes a separate instance of the same model family is the decorrelation PREMISE-132 requires — while the same file says residual same-model correlation is NOT discharged | epistemic / methodological | High |
| PRESUMPTION-568 | Classifying all six verification defects as "drafting, not judgement" presumes corrected grounds don't move conclusions; defect (2) is an inflation of the convergence the disposition rests on | epistemic / methodological | Medium-High |
| PRESUMPTION-569 | Explaining "5 INCORPORATE vs last run's 1" by the new bar presumes a rate rise is evidence the bar works, not data about its application — on the run judging whether the pipeline can adopt premises about itself | normative / methodological | Medium |
| PRESUMPTION-570 | An undiagnosed silent null (no review page at all) is filed as a consequence of a differently-shaped defect and given the same priority; no existence check on scheduled outputs is proposed | methodological / structural | Medium-High |
| PRESUMPTION-571 | A "five-minute fix" restated unchanged across six failures presumes non-application is an availability problem rather than a signal about the channel's value; Browser 2's loss ends the redundancy design | normative / structural | Medium |
| PRESUMPTION-572 | "PRS triplets are read as reference frames, so a thalamic reference-frame mechanism is load-bearing" transfers through a shared term — the check PREMISE-134 imposed the same day, unapplied | epistemic / architectural | Medium-High |
| PRESUMPTION-573 | A self-assigned "Confidence: Speculative" is endorsed as a control; by PREMISE-132's own structure, labelling is not verifying, and a third-party description is the only evidence the conjunction exists | epistemic / normative | Medium |
| PRESUMPTION-574 | `fact_inventory.md`'s authoritative-record rule (a tie-breaker) is invoked as a sufficiency rule to warrant artifact-only reconstruction; the proposed manifest inherits the same limit | methodological / epistemic | Medium |
| PRESUMPTION-575 | "0 uncovered" is reported as coverage though reachable by classification; the denominator (21) is set by the roster reader, the component today's failure was located in | methodological / structural | Medium |

## Open Questions Raised — 0

No new OPEN-NNN today; the register holds at OPEN-139. Three candidates were surfaced and, per the standing convention of not logging candidate items unilaterally, are not logged as architectural OPENs: (1) should "unretrievable but well-described source" become a third disposition category distinct from APPROVE and DENY (ASSUMPTION-573; PRESUMPTION-573); (2) should "a generalisation is only as good as its worst-covered instance — run the check against the world" be stated once as policy rather than rediscovered per-tab (ASSUMPTION-574; PRESUMPTION-566); (3) REVISE-247's unmet need — who is the external adjudicator for a suspension that binds the pipeline, given that 15b's Tarski argument implies the adjudicator must sit outside the system. The carried CROSS candidates (`friston_hoffman` trace-kernel identity; `friston_wolfram` codebase free energy; virtual-governor ↔ tradition, now gated behind MONITOR-487) remain open at OPEN-136. `open_questions.md` left unchanged.

## Lit-Search Routing (Step 5)

- **[QUEUED] to 15a/15b:** PRESUMPTION-566 (warrant for inductive generalization from small n; when an abstraction is terminal), 567 (correlated errors across instances of one model family; independence assumptions in verification design), 568 (stability of a verdict under revised premises; presentational vs substantive audit findings), 569 (evaluator reactivity; base-rate shifts after a criterion is imposed), 570 (silent-failure detection in scheduled jobs; triage of faults of unknown scope), 572 (transfer back into a domain that borrowed the source vocabulary; metaphor hardening into identity), 573 (calibration of self-reported confidence; unretrievable and secondhand sources), 575 (coverage metrics permitting exclusion; self-determined denominators).
- **REINFORCING (not re-queued):** PRESUMPTION-571 → reinforces PRESUMPTION-541/547/550 (fallback decay; shared preconditions), already queued; the revealed-preference reading is new but the decisive literature is queued under 550. PRESUMPTION-574 → reinforces PRESUMPTION-482 (the auditor has no measure of its own completeness) and PRESUMPTION-533/535/549, already queued.
- **[IN-HOUSE] (deliberately NOT added to the lit queue):** ASSUMPTION-561…574 — decisive test is an in-house query (file/timestamp checks, `node --check` and gate re-runs, a read of line 304 and a grep for `assert`, the scheduled-run record for 04:38, `watch_list.md` 07-28 entries, register counts and disposition reads, a login/extension test), per the standing routing fix (ASSUMPTION-519 / PREMISE-124). Three are split: ASSUMPTION-567 (cause in-house, triage question → PRESUMPTION-570), ASSUMPTION-570 (existence of the pass in-house, decorrelation adequacy → PRESUMPTION-567), ASSUMPTION-572 (the thalamic claim is a review-pipeline matter; the transfer question → PRESUMPTION-572).
