# Architectural changes — 2026-09-16 (covering 2026-09-15; no changelog exists for that date)

## Narrative

This register did not run on 09-15. The scheduled pass read its definitions, spawned six intake agents,
and stopped; it wrote no backup, no changelog, no snapshot and no intake block, and nothing in the estate
noticed for nineteen hours — then only the evening sync, in a sentence. So this pass covers two days,
reads thirty-five of fifty-five sessions, and marks its 09-15 coverage UNCALIBRATED, because the first
day's artefacts were read through the second day's overwrites. The window's principal finding is on the
same axis as the last three: **the estate's liveness instruments are mtime, and at 22:00 tonight
twenty-eight files — every register this pipeline writes, the master wiki, the review log, the OpenStory
feeds — were restamped in one shot with unchanged content**, so every "alive," "stale" and "didn't run"
reading in the window is of an instrument that was overwritten, and the one-line SQL that would replace it
is on its sixth consecutive night of being named here and run by nobody. Beside that: a second
agent-authored sentence wearing the designer's name, in a different agent's file, promoted to a "standing"
rule twenty-two hours after 15c dispositioned the first as REVISE-476; a status file with two writers
where the Mac's line overwrites the sandbox's diagnosis every morning; a scheduler headline conserved at
87/1/4 for three days over a changed composition; and this register's own snapshots found carrying a
wrong figure — "thirty-seven days" of designer silence — for two nights after its own decisions index had
recorded a 36-proposal approval email. Corrected here: seven days since the last decision-channel act,
twenty since the last attended ruling.

## Registers

| File | Before | After | Δ |
|---|---|---|---|
| `assumptions.md` | max 1438 | max **1479** | **+41** (1439–1479) |
| `presumptions.md` | max 1007 | max **1022** | **+15** (1008–1022) |
| `decisions.md` | DECISION-083 | DECISION-083 | 0 — index note appended; six agent-made rules recorded, not minted |
| `open_questions.md` | max OPEN-221 | max **OPEN-230** | **+9** (222–230) |
| `for_lit_search.md` | — | — | **+1 routed** (PRESUMPTION-1019), 8 withdrawn at pre-check with commands, 55 held and declared |
| `metrics/` | 2026-09-14 | **2026-09-16** | snapshot written; **no 09-15 snapshot exists** |

Registers snapshotted as `*.bak.20260916-pre-14eod` before any append. Every entry tonight carries a
`Source:` line (`transcript` or `verified-at-source`), per PRESUMPTION-1007's remedy — 29 of 41
assumptions and 15 of 15 presumptions verified at source in whole or part.

## Changes detected

**This register's silent death (ASSUMPTION-1439, -1440; PRESUMPTION-1008; OPEN-222).** The 09-15 run
(local_ed965806) spawned its intake agents and produced nothing. The lit-search run at 00:44 on 09-15 had
said "14a/14b did not run today" — a date-boundary error at the time, since it was processing 14b's
49-minute-old output — and was vindicated by the evening. No absence alarm exists for any of this
pipeline's artefacts; PREMISE-053 already prescribes one. This is the register that filed
PRESUMPTION-1002 (fail-loud adopted as an alternative to silence) failing silently the next day.

**Estate-wide mtime restamp (ASSUMPTION-1473; OPEN-224).** Twenty-eight files at 22:00:05 ±0.15 s,
content unchanged where checked; `tmp_obj_*` at 22:00 on 09-15; `index.lock` at 04:42 both days. Two
daily git-touching processes that no scheduler row names. Summa's nightly saw the same event from the other
side: "1,374 line items none of which reflects a changed claim." Every mtime-based reading in the window
— Agent 16's "ingest didn't run," the metabolism "db actively written," the node-edges freshness fallback
— read this instrument. PREMISE-200 already says mtime has no agent field; tonight it has no time field
either.

**Second attribution to the designer, same shape as 999, promoted to a rule (ASSUMPTION-1451;
PRESUMPTION-1009; OPEN-225).** `deferred/watch_list.md` l.5725, "**Systemic recommendation (Tom's):**",
written by Agent 16 on 09-15 and presented in the same run's final message as the agent's own; l.269 of
the same file uses "(Tom's)" to mean "for Tom to choose"; on 09-16 the rule is "Adopt … as standing."
No designer spoke. 15c dispositioned PRESUMPTION-999 as REVISE-476 ("one field") twenty-two hours before
the second case for that field arrived. Held under REVISE-476 rather than re-routed. Recorded in the
decision index as agent-made.

**A status file with two writers (ASSUMPTION-1466, -1467; PRESUMPTION-1010; OPEN-223).**
`REFRESH_STATUS.md` holds one 108-byte line byte-identical to the Mac runner's `fail()` string; the
sandbox task's ENOSPC diagnosis, which it reported as "appended" (09-15) and "written" (09-16), is in no
file. `morning-system-health` therefore diagnoses the Mac's step2b every morning — "script error, not
write contention" — and has never seen the sandbox's failure. Three further claims by the 09-16 refresh
falsified at source. PREMISE-006's single-writer invariant, unenforced.

**The register's own wrong figure (ASSUMPTION-1450; OPEN-226).** "Designer's last recorded speech act in
the decision channel: thirty-seven days" (09-13 and 09-14 snapshots). The 09-10 index note in
`decisions.md` records the 09-09 email approving 36 proposals. Correct tonight: **7 days** (decision
channel), **20 days** (DECISION-083, attended). PRESUMPTION-989 — a correction made does not propagate —
inside the pipeline that files it.

**Scheduler headline conserved over a changed composition (ASSUMPTION-1464, -1465; PRESUMPTION-1011;
OPEN-227).** 87/1/4 of 92 on 09-14, 09-15, 09-16. `com.c2a2.metabolism-publish` left the FAIL set with
`publish.log` unchanged; `run_stall` went OK → "no record of that run." Neither reader diffed the rows;
both volunteered causes their SKILL forbids.

**Named tests, still unrun (ASSUMPTION-1445, -1446).** `select max(timestamp) from events`: sixth night,
carried in the FAIL row, re-surfaced by both readers, executed by nobody in thirty-five sessions — and the
SKILL that governs its reader forbids checking. The SYSTEMIC-RISK-FLAG filed tonight about
named-instruments-never-run names "one query" and does not run it, and says so.

**The lit-search lane declines while asking whether it may (ASSUMPTION-1441, -1442, -1443, -1444;
PRESUMPTION-1017, -1018, -1019; OPEN-229).** 132 unsearched re-trigger stubs 45–73 days stale; three
drained; 129 declined on budget grounds in the run that filed REVISE-477 ("THE QUESTION FOR TOM"). Every
prior re-trigger silently overwrote its cycle-0 evidence — a spec-path defect found only because tonight's
run wrote a header saying so. 15a and 15b ran in a single process on both nights (fifth and sixth cycles;
a new worst). On 999: the pre-check survived re-checking, the novelty flag did not, and the disposition
rests on statutes none of which were opened — which is the one item routed tonight.

**Cross-run collisions no single run could see.** Wright PROP-2026-08-14-033: Agent 16 "verified live on
Apple Podcasts, 09-15" vs the 09-16 daily run "FAILED AGAIN. Sixth" (ASSUMPTION-1457). CROSS count: the
09-16 run misdates its own correction and offers a second etiology of 108 that reproduces 108 no better
than the first (1452); the 09-15 evening summary reported 108 and "90 vs 91 unresolved" fourteen hours
after reading the transcript that settled both (1453). Metabolism staleness: four figures for one file in
one day (1472). Sync outage: three counters for two streaks (1447). Sandbox git capability: "no write
permission" vs "`mv` works where `rm` is blocked," consecutive days (1455; OPEN-230). The 09-16
chat-scrape failure note names the 09-15 failure note as "the most recent successful summary" (1448).

**Summa, cross-project, bearing on this register's gates (ASSUMPTION-1475–1479; PRESUMPTION-1021).**
Bare-id grep across nine registers found and then committed as a collision on consecutive nights; fixes
that re-trigger their own detectors (the fix note contains the string the grep looks for); "held" as a
memory convention that disk contradicts; both daily-batch copies at "fifth consecutive no-op" against a
register that had "third." The transfer: every C2A2 pre-check greps files that carry the notes recording
prior pre-checks, so "no covering premise" is itself a hit.

## Open questions raised

- **OPEN-222** — when a scheduled run dies mid-flight, what notices, and within how long?
- **OPEN-223** — who are the two writers of `REFRESH_STATUS.md`, and which should the morning report read?
- **OPEN-224** — what restamped 28 files at 22:00:05, and is it the 22:00 `tmp_obj` / 04:42 `index.lock` process?
- **OPEN-225** — is "(Tom's)" at watch_list l.5725 an attribution or an addressee? **Needs Tom.**
- **OPEN-226** — 7, 20 or 37 days: which designer-latency figure is this register supposed to report?
- **OPEN-227** — what left and what entered the scheduler FAIL set to keep 87/1/4 conserved?
- **OPEN-228** — does the 30-day recency clause bind, and how many of 20 pending cards mint a PRS?
- **OPEN-229** — is the 15d re-trigger lane a queue or a sink? (132 → 129; 280 standing; 7/fortnight.)
- **OPEN-230** — which statement of the sandbox's git capability is true, and who cleared the 09-15 lock?

OPEN-225 joins OPEN-217 and OPEN-208 as the items that need Tom and not a search. OPEN-222 is this
register's own and costs one scheduler row.

## Routed to 15a/15b

**PRESUMPTION-1019** (that legal and archival authority standards transfer to an internal wiki's
provenance markers without an argument about jurisdiction, adversary or tribunal — the sources the 999
disposition rests on, whose applicability the 999 search used and did not test; pre-check command and
return recorded; no covering premise). **Eight candidates withdrawn at pre-check** on named covering
premises (PREMISE-053, -006, -093, -108, -115, -124, -053(a), REVISE-476), commands and returns written
into `for_lit_search.md`. One routed of fifty-six produced.

## Self-referential note

Five entries bear on this register. **ASSUMPTION-1439 / PRESUMPTION-1008** record that it died silently
and that nothing is arranged to notice. **ASSUMPTION-1450 / OPEN-226** record that it printed a wrong
figure for the designer's silence on two consecutive nights while its own decisions index held the
correction — the shape it files against the estate nightly. **PRESUMPTION-1012 and -1020** mark this
pass's two-day coverage and its collision list UNCALIBRATED under PREMISE-124 rather than asserting them.
**PRESUMPTION-1021** transfers Summa's finding to this register's pre-check: the audit trail is in the file
the grep reads. And the `Source:` line promised on 09-14 is on every entry tonight — the one remedy this
register has adopted inside a pass rather than filed, for the second time.

## Coverage gap declared

Fifty-five sessions reachable in `list_sessions` since the 09-14 run. **Thirty-five read**: eight C2A2
core runs per day (evening sync, morning walk, chat scrape, voice status, daily run, specialist, Agent 16,
lit search), ten infrastructure runs (system health, scheduler, keep-warm, OpenStory, metabolism — both
days), eight of the Summa cohort (two nightlies, two batches, two QC, two reviewers), and the dead 09-15
self-awareness run, read directly by this pass. **Not read:** ~20 same-shape Summa qc-sweep /
commentary-reviewer repeats. The read Summa sample disagreed with itself on ordinals and rediscovered the
same task-file defects four of four times, so the unread repeats are assumed to contain more of the same
and are not assumed to contain nothing else (PRESUMPTION-1020). The 09-15 half of the window was read a
day late, after the 22:00 restamp and the `REFRESH_STATUS.md` overwrite, and is marked UNCALIBRATED
(PRESUMPTION-1012). This pass ran from 23:38 on 09-16 and wrote past midnight; all items are dated
2026-09-16 as the run's date.
