# Architectural Changes — 2026-09-04

*Agents 14a (assumption extraction) and 14b (presumption detection). Gap-filling run: the pipeline
last fired 2026-08-30; 08-31 through 09-03 produced no changelog and no snapshot.*

## Narrative

The day the project turned its own instruments on Tom's own material. The RC Sandbox tab — 2,402
cells of twenty years of notes — was extracted verbatim, classified by eight parallel workers against
an approved outline, re-passed, and rendered as a 16,102-line reordered document and a browsable
notebook. Two things were found rather than made: Tom's own eight-level ladder (A-I-S-N-B-P-C-S) read
off sheet rows 247–250, which added a rung (B, the neocortical column) that every prior reading had
missed; and a pre-pass length rule that had silently binned that ladder's header letters as junk. Two
rulings were delegated ("as you incline") and landed in an outline file rather than the decision
register. Fourteen assumptions and eight presumptions were extracted. The sharpest finding is not in
what was said but in what the numbering scheme takes for granted: L0…L9 treats as a straight ladder
what the source describes as three overlapping bands and a second ladder on a different principle —
and the one open structural question (where sub-neuronal life goes) may be unanswerable in the shape
it was asked. The classification spec, meanwhile, never contained the rung the day is proudest of.

## Items Produced

| Register | Added | Range |
|---|---|---|
| `assumptions.md` | 14 | ASSUMPTION-1249 – 1262 |
| `presumptions.md` | 8 | PRESUMPTION-904 – 911 |
| `open_questions.md` | 3 | OPEN-179 – 181 |
| `decisions.md` | 0 (dated run note appended) | — |
| `for_lit_search.md` | 11 items routed | — |
| `metrics/` | 1 snapshot | `2026-09-04_snapshot.md` |

Backups: `*.bak.20260904-pre-14eod` for assumptions, presumptions, open_questions, decisions,
for_lit_search.

## Changes Detected

**New corpus operation:** `inbox/rc_sandbox/` — outline v1→v2→v3, CLASSIFY_SPEC, prepass.json,
8 batches + NB re-pass, assignments.csv (2,402), TL_sandbox_reordered.md, quodlibet_notebook.html,
toc_sandbox.py/csv (19:29 — a ToC pass after the digest was written; not covered by the digest).

**Registers moved by other agents since 08-30:** `validated_premises.md` PREMISE-194–198;
`revision_flags.md` REVISE-413–429; `lit_search_returns.md` cycles through cycle 5;
`deferred/watch_list.md` (Agent 16, DEFERRED-CONDITION LEAKAGE flag); proposals pending 9 → 21
(PROP-2026-09-01 ×3 Hawkins, 09-02 ×5, 09-04 ×1 Carroll); `review/2026-09-04_review.html`.

**No changes** to `wiki/agents/*.md` or `wiki/tools/*.py` observed.

## Rationale Drift

**One instance, across days.** PRESUMPTION-903 (08-30) filed a pipeline gap as evidence destroyed.
Today's digest describes the new gap as "five days of assumptions ... unsurfaced" — deferred work.
Filed as ASSUMPTION-1261; the drift is recorded, not adjudicated.

**One instance, within the sandbox work.** Sheet order is discarded by the deliverable
("reordered") and used as dating evidence in the same file (row 292 is the "earlier" ladder).
PRESUMPTION-907.

## Accumulating Technical Debt

- **Self-awareness pipeline: two gaps in nine days** (3 days, then 4). The scheduled task fires; the
  runs that fired on 09-01…09-03 left no changelog. Cause not investigated this run.
- **Decision register static 8 days** while premises +9 and revision flags +17, all unattended.
- **Re-pass queue:** `r325c5`/`r325c6` ("CS", "QG") still parked from the length-rule bug.
- **Three counting methods for the lit-search queue** now in circulation (tag count, status-line
  count, and the digest's). PREMISE-191's pattern, again.
- **OPEN-168 day thirteen; OPEN-167 casing; 26 alias notes; PRESUMPTION-822 night 21** — carried.

## Self-Referential Observations

1. **The spec did not contain the discovery.** CLASSIFY_SPEC.md's controlled node list has no
   III.2.B; the rung was added after the batches ran, and its 27 cells equal the count of cells
   carrying the `pass` facet. A finding the digest calls a discovery may be populated only by the
   scope of the re-pass that noticed it (PRESUMPTION-905). This is the sandbox-scale version of the
   project's own question about traditions: does a category find its members, or do its members
   define it?
2. **The ladder is being read with a ladder.** Tom's table describes overlapping bands; the labels
   impose a line; the open question asks for a missing rung. If the structure is a lattice, the
   question has no rung-shaped answer (PRESUMPTION-909, OPEN-179/181).
3. **This run is better sourced than 08-30 and files that as a presumption, not a fix**
   (PRESUMPTION-911). Working files written during the work are still the agent's account of Tom's
   rulings.

## Requires Tom

1. **OPEN-179** — sub-neuronal rung: rule it, or rule that the structure is not linear.
2. **OPEN-181** — which of two authored ladders is III.2.0's spine.
3. **OPEN-180** — deferred-condition polling: authorise option (b) or name an owner.
4. **OPEN-174** — say whether outline-file rulings count as decisions of record.
5. **OPEN-168, day thirteen.**
6. `review/2026-09-04_review.html` — 21 proposals, 8-day gap.

---

*Budget disclosure (Rule 6): this run exceeded the 4,000-token per-task budget. Cause: five-day
gap-fill plus a new 2,402-cell corpus operation with primary files to re-count. Disclosed, not
absorbed.*
