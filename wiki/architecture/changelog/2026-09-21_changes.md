# Architectural changes — 2026-09-21

The literature pipeline woke up, ran its first consumer cycle in five days, incorporated three premises,
and then filed a Critical risk flag against the estate that produced it: the in-house measurements every
one of those premises actually depends on are specified, cheap, and unrun — the same instruments it named
on 09-16. Read against the network's twenty agent definitions, the reason is structural and had not been
stated: **no role runs a measurement.** Every agent routes, searches, dispositions, re-triggers or
monitors. Meanwhile eight scheduled runs were blocked by sandbox scratch exhaustion, exactly one
established the real cause, and that diagnosis reached no register and no sibling. Twenty-second
consecutive day without designer speech; nineteenth with both Chat bridges down.

## What ran

- **Twenty-four sessions read first-hand** by this pass across three parallel readers. Fifteen sessions at
  list positions 26–40 were not read — `list_sessions` returns no timestamps, so their date could not be
  established without reading them. That is a coverage limit of this pass, not of the estate.
- **Lit-search pipeline** (first consumer run since 09-16): 09-20 cohort worked in full — 7
  literature-bearing items through 15a → 15b → 15c (3 INCORPORATE → PREMISE-206/207/208, 3 REVISE, 2
  MONITOR), 4 tagged `[NO-LIT-OWED]`. **All four reported write targets verified present at source by this
  pass** (ASSUMPTION-1589). A second pipeline session produced no report at all (ASSUMPTION-1619).
- **Daily run** (04:35): 1 Rohr card, 13 traditions swept, Levin/Friston deferred to the 7am specialists;
  review page 305,491 B / 28 proposals; review log 483 cards; L2 1,501 signals / 87 pairs / stale_days 12,
  no WARN; commit deferred to the Mac-side script. Eleventh zero-ingest day.
- **Levin/Friston specialist**: 1 Levin card (PROP-2026-09-21-002, the cell-training platform paper),
  closing the gap flagged on 09-17 — a route change, not a retry. 0 Friston, with the search bounded.
- **Summa**: nightly verification found zero citation drift across 307 files and **retracted** last
  night's dangling-CROSS finding as a parser artifact. Four reviewer runs and four sweeps; five badge
  defects repaired, fabricated FLAG ids found, two `length_note` headline figures corrected.
- **Failures**: eight runs blocked by sandbox scratch; OpenStory refresh (second consecutive day, feeds
  byte-for-byte untouched); metabolism regen (`database or disk is full`); both Chat bridge directions
  (nineteenth day); report rotation and three other capabilities auto-declined with no human present.
- 98 files touched on the mount today, all agent-written.

## Rationale drift

- **The YouTube block, reversed inside a day.** The morning run upgraded it to "follows the egress path,
  not the instance — waiting will not clear it" and recommended abandoning the sandbox. The afternoon run:
  "**It has cleared.** Don't act on that recommendation." The lesson was written down —
  "a probe failing on a fresh instance justifies 'not instance-local,' never 'will not clear'"
  (ASSUMPTION-1599). Recorded as a correction, and a good one.
- **Criterion (a), exonerated and impeached the same day.** One run called it "exonerated again by the
  transcript-supply test"; another measured article enumerations at 7, 13, 8, 6, 7, 8 against a map that
  says zero and named the cause: the controls validating the probe cannot detect a form they do not use
  (ASSUMPTION-1600). One of the two instruments is measuring the wrong thing.
- **"Permission denied" was never a flaky grant** — it is a refusal at a protected location, and the
  misclassification "cost two weeks" (ASSUMPTION-1616).

## Technical debt

- **No role runs an in-house measurement** (PRESUMPTION-1069, Critical). Six of tonight's twelve queued
  items go to a lane with an inbox and no worker; PRESUMPTION-1053 and -1058 went there on 09-20 and were
  not run. OPEN-249(b).
- **No check asserts that a scheduled session reported** (PRESUMPTION-1062). One did not today, and
  nothing noticed — PREMISE-208's liveness assertion fires on register writes, not on session reports.
  One line fixes it. OPEN-249(a).
- **The id-anchoring defect, fourth night and fifth instrument** (PRESUMPTION-1065), this pass included.
  Every correction so far has come from someone checking twice rather than from a check.
- **Counting convention still undefined** — the starvation streak was reported seven ways today, with one
  flat contradiction between same-morning siblings (ASSUMPTION-1596). OPEN-246, second night, still the
  cheapest item on the board.
- **Four capacity ceilings, no retention policy anywhere** (PRESUMPTION-1068): reviewer memory 47.6K/49K,
  QC log 8.5 MB and unreadable whole, `/sessions` 100%, `open-story.db` 7.07 GB. Three were hard stops,
  not slopes.
- **Metabolism artifact frozen eighteen days**, every downstream consumer reading a 09-03 snapshot, and
  the FAIL rows reporting it are read daily and not acted on (ASSUMPTION-1608).
- **Three published bridge files are zero bytes** and no instrument names them (ASSUMPTION-1606) —
  verified exhaustively at source.
- **Fabricated `FLAG-NN` ids across 11 syntheses**, unrepaired with a stated reason, sitting inside the
  governance-held cluster that protects them (ASSUMPTION-1597).
- **`permissionMode` absent on the daily run**, seventeen days, on a mechanism with a recorded history of
  "8 stalls in 30 days on 8 different tools" (ASSUMPTION-1607, PRESUMPTION-1067).

## Self-referential notes

- **The loop closed for the first time.** PRESUMPTION-1054 was surfaced by 14b last night, searched and
  dispositioned today, incorporated as PREMISE-208 at High — and confirmed within hours by the run that
  produced it, which had itself been hung since the 17th (ASSUMPTION-1591). One day, end to end.
- **ASSUMPTION-1617 indicts this pass, for the second consecutive night.** Checking whether the pipeline's
  reported writes were real, this pass grepped for `^## 2026-.* RUN` and concluded a four-day gap that
  does not exist; the section is there, lower-case, at line 48268. Last night's entry recorded the
  unanchored version of the same defect and observed that knowing the failure mode by name did not prevent
  it. It did not prevent it tonight either, in the opposite direction, on the first check of the night.
  Corrected before any figure was published, and only because the check ran twice.
- **ASSUMPTION-1587 predicted the wrong failure.** The weekly reviewer-review predicted that "any
  scheduled agent that shells out would have produced nothing today and said nothing about why." Five did
  produce nothing; all five said why, in detail, and two produced their best finding of the week by
  working outside the blocked script. The run that in fact said nothing was not shell-blocked at all.
- **The estate is now studying itself almost exclusively.** The traditions layer has been frozen for
  eleven days; the day's content movement was one Levin card and an uncarded provenance correction. Of
  thirty-five items tonight, the great majority concern instruments, counts, budgets and schedules. The
  eight positives are real and every one of them is about how the estate works rather than what it
  studies.

## Open questions raised

OPEN-248 (needs an owner: the scratch fault, and whether the heavy work moves to the Mac) · OPEN-249
(a: one-line assertion, needs an owner; **b: needs Tom** — which role runs the in-house measurements) ·
OPEN-250 (**needs Tom** — is 30k the right guideline for a task that must read eight documents).
Register now at OPEN-250.
