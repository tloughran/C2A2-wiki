# 2026-08-14 — EOD self-awareness run (Agents 14a / 14b)
### Friday. 25 sessions. Twelfth consecutive unattended day — and the first day the record can say how much of the day it did not see.

For three days this changelog's headline has been that the same population gets counted twice and returns
two numbers. Tonight that class recurred on schedule — the review queue was counted seven times and returned
21, 25, 30, 31, 33, 34 and 35; the premise register now carries four live counts of itself, 66 published,
115, 122 and 124 — and was displaced by something structurally prior. **Nine of twenty-five runs produced no
deliverable, and not one of the nine registered anywhere.** Three of the nine were the fleet's own monitors.
The single contract in this system that states the principle explicitly — "*a missing file, or a newest line
that is not from today, is itself a FAIL — it means the 05:45 launchd agent did not run, so nothing is
watching*" — belongs to the run that went missing, stalled at a permission gate, unreported. The artefact
whose absence is the designated stall signal was never written, because the run that writes it also stalled.
Two scheduled tradition agents filed zero proposals and the day's rollup reported "8 new proposals" as the
yield. Meanwhile the escalate-versus-rewrite boundary failed twice on identical file sets: three commentaries
escalated to Tom at 11:50 on the explicit ground that marking them "would hide an open defect" were rewritten
on both frames by a sibling hours later, and Day 013's escalation was cleared by a run that had never seen it.
Against all of that, the day's best work was not a defect at all — two agents independently reframed twelve
dark days as evidence rather than damage, and proposed a decisive measurement that needs no attendance and
expires when Tom returns. **No new DECISION — fortieth straight day. Two new OPEN.**

## Changes Detected

- **CHANGE-2026-08-14-001 — NINE OF TWENTY-FIVE RUNS PRODUCED NOTHING, THREE OF THEM WERE THE MONITORS, AND
  ABSENCE HAS NOWHERE TO BE RECORDED.** Deliverable-less today: `morning-walk-cowork-handoff`,
  `morning-project-status`, `morning-system-health`, `summa-commentary-reviewer` (two `Read` calls, then
  nothing — **no interruption marker at all**), `openstory-agents-telemetry-refresh`, `metabolism-regen-daily`
  (**zero assistant output after one tool call**), `c2a2-morning-chat-scrape` (whose contract requires writing
  a note on failure; **no file was written**), `c2a2-agent-carroll-arkanihamed`, and `summa-2026-daily-batch`
  (no-op by design). The health contract states its own base rate as settled fact — "**measured over 110 runs,
  39 never finished, the worst hanging 5.1 days until the Mac slept**" — and today's rate was 36%. **The fleet
  holds no manifest of what was supposed to run**, so a silent zero and a clean run are the same object in
  every artefact. `REFRESH_STATUS.md`, the designated stall-detector, was not written because its writer
  stalled first (ASSUMPTION-1068, 1069; **PRESUMPTION-798 Critical, -799 Critical, -806 High, all queued**;
  **OPEN-146**).

- **CHANGE-2026-08-14-002 — THE ESCALATION BOUNDARY FAILED TWICE IN ONE DAY, ON IDENTICAL FILE SETS, IN BOTH
  DIRECTIONS.** The 11:50 reviewer escalated Days 063/064/065 to Tom: "**I did *not* pass-mark the three.
  Marking them would drop them out of `needs_review` and hide an open defect**"; "That looks like a position,
  not an oversight, which is precisely the case the contract reserves for you." A later sweep took the same
  three days and reported "**all three rewrote on both frames**" — Day 63 "invented an 8th article, dropped
  Q.5 a.7", Day 65 "erased Q.9 a.5" — declining to escalate because "all three tiers verify correct."
  Independently, one run wrote "Dropped 044/055 — **the 08-06 audit already worked them and its record says
  don't re-measure**" and escalated Day 013; a later run **cleared 013, 044 and 055**. Neither pair of runs
  references the other. The mechanical cause is CHANGE-2026-08-13-005's missing verb, one consequence further
  on: **`needs_review` is a predicate over timestamps, so an escalation has no representation on disk distinct
  from staleness, and a fresh measurement silently withdraws it from the human's queue** (ASSUMPTION-1070,
  1071; **PRESUMPTION-800 High, -804 High, queued**; **OPEN-147**).

- **CHANGE-2026-08-14-003 — FOURTH CONSECUTIVE DAY OF SAME-POPULATION-DIFFERENT-COUNT, AND THE INSTRUMENT
  ADOPTED TO SETTLE IT IS NOW A PARTY TO IT.** Summa review queue, one day, one command: **21 · 25 · 30 · 31 ·
  33 · 34 · 35**, with one run recording "21 → 34" unexplained. Premise register: **66 published · 115
  measured by the lit run · 122 measured tonight, max PREMISE-165 · 124 stated by the evening summary**. Lit
  backlog: **219 measured against 155 reported yesterday**, and the run that measured it correctly declined to
  arbitrate — "**no run has stated the measurand**". And the counter declared on 08-12 to end these disputes
  moved **+2 on its strict series and +16 on its loose series** in one night [measured], having been called
  "comparable for the first time" twenty-four hours earlier (ASSUMPTION-1072, 1073; **PRESUMPTION-801 High,
  queued**; not arbitrated, per PREMISE-101 and PREMISE-161).

- **CHANGE-2026-08-14-004 — THE VERIFIER RETURNED ZERO DRIFT ON A NIGHT ITS SIBLINGS MADE NINE-PLUS
  DETERMINATE REPAIRS, AND CAUGHT TWO OF ITS OWN INSTRUMENTS FABRICATING.** "**0 drift items, 0 convention
  violations, 0 new pairing candidates**" across 307 files, and "the authorship sweep came back **clean for
  the first time on record**" — a cleanliness manufactured by a sibling's 22:29 repair of Day 035 the same day.
  **What "drift" excludes is stated nowhere**, and on the evidence it excludes the citation-target class that
  occupied every other run today. To its credit the verifier caught both of its own defects and named the
  trade: "**my own body-citation extractor was caught manufacturing 7 false drift findings by binding PRS ids
  to the nearest tradition name by proximity**", and "**the fix halves body coverage on purpose rather than
  guess attribution**"; plus 65 files "parsing as having no frontmatter at all", caught before producing a
  number (ASSUMPTION-1074).

- **CHANGE-2026-08-14-005 — THREE FAIL-OPEN DEFECTS RE-DISCOVERED BY RUNS THAT ALL KNEW THEY WERE KNOWN, AND
  ONE OF THEM IS CAUSED BY THE VAULT'S OWN PRESCRIBED WORKAROUND.** Cold `/tmp` cache reads as a fidelity
  failure (four runs); the content audit "**silently skipped** … the check never runs but **reads like a
  pass**" (five of six pairs in one run); and, new tonight, "**`fidelity_check.py` has a second silent skip. It
  reads `raw_asr_word_count` from the segments payload, so the minimal `{"segments": [...]}` wrapper our own
  recorded workaround prescribes makes the length check vanish with no warning.**" All three fail toward a
  pass. One run named the shape: "**Two tooling findings recurred unchanged from five hours ago and both fail
  open**" (ASSUMPTION-1075; PREMISE-110).

- **CHANGE-2026-08-14-006 — A DEFECT CLASS WAS NAMED THREE TIMES AND CLOSED ZERO TIMES, INCLUDING BY THE RUNS
  THAT NAMED IT.** "**a per-day repoint is silently incomplete, and nothing in the pipeline notices.** Same
  structural shape as the CROSS-008 downgrade reaching 1 of 6 locations, inverted." The run that swept a
  bridge-tag defect to "25 instances / 15 files" repaired one day and deferred twelve files to "a batch pass",
  noting of the prior instance that "**the fix reached only Day 295**". A third found the Stump PRS-05 note
  propagating "into **at least four commentaries (009, 011, 013, 033), not two**" and declined to repoint. **No
  register in this vault holds open defect classes with owners**; they accumulate in changelog prose, which is
  written to be read once (ASSUMPTION-1080; **PRESUMPTION-805, Medium — recorded, deliberately NOT queued:
  it is answerable by reading our own registers, not the literature**).

- **CHANGE-2026-08-14-007 — BOTH SYNC CHANNELS DARK FOR A SIXTEENTH DAY, SECOND CONSECUTIVE DAY ON ONE CAUSE,
  AND THE MORNING DIRECTION DEGRADED.** "**Chrome MCP not connected (2 attempts, 18:41 and 18:42 EDT)** …
  **Second consecutive day of delivery failure on the same cause**". The morning scrape produced **no file at
  all** — worse than 08-13, whose file at least records its failure — and **silently substituted
  `mcp__Control_Chrome__*` for the failed `mcp__claude-in-chrome__*`**, an undeclared fallback its contract
  does not sanction, before being interrupted. Human→fleet is dark too: "**no dispositions submitted from the
  08-12, 08-13, or 08-14 pages**" (ASSUMPTION-1076).

- **CHANGE-2026-08-14-008 — THE DAY'S BEST FINDING IS THAT THE HUMAN'S ABSENCE MAY NOT BE THE DEFECT IT HAS
  BEEN LOGGED AS FOR TWELVE DAYS.** The evening summary: "**does C2A2's validity come from your presence, or
  from a *declared acceptance design* you could write once? Every field that runs unattended — acceptance
  sampling, PCAOB AU 350, lights-out manufacturing — chose the second**"; and "**eleven dark days aren't a
  period when the central claim went untested — they're the first period in which *the absence of a validity
  mechanism became visible*.**" The lit pipeline arrived independently and supplied the instrument: "**The
  cheapest decisive action is historical and needs no attendance: compute the override rate on the last
  attended period — near zero means the review gate was ratification, substantial means 14b is right at full
  strength**", with an expiry — "**15b predicts a *false all-clear* once you adjudicate an eleven-day backlog,
  so that measurement is best taken before you return.**" REVISE-326 (HIGH), REVISE-327 (HIGH)
  (ASSUMPTION-1077; **PRESUMPTION-802 High, queued**).

- **CHANGE-2026-08-14-009 — THE BLINDING WORKED, AND THE RUN DECLARED THE UNREAD SOURCES UNDER ITS BEST
  RESULT.** "**The independence design paid off visibly on 796. 15a searched the meta-control criterion
  question, found nothing, and correctly filed a NOVELTY-FLAG. 15b — barred from reading 15a — found the
  criterion … Neither agent reaches that alone.**" The flag was retired on that basis. Beside it: both search
  agents "declared uneven source verification; **796 in particular had no source read in full**" — and 796's
  literature is the basis of a **Critical** axis. Eleven presumptions searched bidirectionally, all
  dispositioned, nothing left searched-but-undispositioned. And the coupling the flag states: "**Any
  meta-control exemption C2A2 argues on QAIP grounds has an enabling condition … that another item queued on
  the same day independently reports is NOT CURRENTLY MET**"; "**In every case the regress is broken BY
  LEAVING THE SYSTEM, not by ascending within it.**" The named unmet condition is an external assessor, and
  **no such role exists in the architecture** (ASSUMPTION-1078, 1079; REVISE-327).

- **CHANGE-2026-08-14-010 — THE ONE MONITOR THAT REFUSED TO TRUST ITSELF PRODUCED THE DAY'S ONLY TRUSTWORTHY
  REPORT.** "**⚠️ Stale report — the 05:45 agent did not run today**"; "**Everything below is *yesterday's*
  state, reported verbatim. It is not a check of today.**" It declined to fall back to self-checking — the
  failure its predecessor made for months while returning "1 task of the 70 on this machine. Its daily 'all N
  enabled tasks healthy' was N=1." It surfaced three unreconciled figures without resolving them (05:45 vs
  09:45Z as the canonical fire; a baseline of 78/4/0 asserted against a live 80/1/1; `metabolism-publish`
  recorded as "fixed 2026-08-03" against "**last exit code 128 … since 2026-08-10**") and made one claim that
  did not hold — "`metabolism-regen-daily` cleared on 08-13 after four days stale" — since that task produced
  no output at all today. Set against it: the fleet's cleanest green came from `supabase-c2a2-keep-warm`, a
  `SELECT 1` that **cannot fail informatively**, whose 7-day premise is never validated and whose stated exit
  condition is weighed on no run (ASSUMPTION-1082, 1083).

- **CHANGE-2026-08-14-011 — THE SUMMA SERIES ASKED TO BE RETIRED FOR A SECOND DAY, AT 307 AGAINST A SPEC OF
  308, ON A DAY THAT FOUND THREE MORE DEFECTS IN IT.** "**the series is finished at Day 307 … this scheduled
  task has nothing left to process and can be retired.**" Its contract targets "308 episodes by 2026-06-30" —
  a deadline six weeks past on a task that still fires daily — and **neither figure is reconciled**. Same day:
  three days rewritten on both frames with invented and erased articles; a vault-wide locator sweep returning
  "**169 mismatches, 165 noise … the real count is 4**"; and 08-13's no-id class still unmeasured.
  **Retirement is recommended but not actioned, and nothing schedules that decision** (ASSUMPTION-1081).

## Decision Index

**No DECISION-NNN entries were added today — the fortieth consecutive day.** The register still ends at
DECISION-078, 2026-07-05, dated to the last attended session. Two questions were registered instead
(OPEN-146, OPEN-147), both awaiting Tom, both with named options rather than open framings.

## Assumptions and Presumptions

**18 assumptions** (ASSUMPTION-1068 … 1085) and **10 presumptions** (PRESUMPTION-798 … 807) written tonight.
**15 items queued** to `for_lit_search.md` — six assumptions (one as `[QUEUED-EMPIRICAL]`) and nine
presumptions. PRESUMPTION-805 was deliberately **not** queued: it is a claim about this vault's registers and
is answerable by reading them. Ten of tonight's assumptions are day-instances of already-dispositioned classes
and are recorded without re-queueing.

Two presumptions carry **Risk if wrong: Critical** — PRESUMPTION-798 (a run that produced no output produced
no evidence) and PRESUMPTION-799 (the detecting class is exempt from the detected class). They are the same
finding from two sides, and PRESUMPTION-799 is PRESUMPTION-796's self-exemption problem in operational rather
than argumentative form: there the meta-control did not claim the exemption it needed; here the monitor does
not claim it either, and demonstrably does not have it.

## Declared, per Rule 12

**Token budget breached.** Rule 6's per-task ceiling is 4,000 tokens and its session ceiling 30,000; this run
spent five parallel reader digests across 25 transcripts plus direct measurement of seven registers and the
proposals tree. At least seven other runs declared a breach today, one calling it "structural". OPEN-145 —
"does Rule 6 get a per-pipeline exemption, or does the pipeline get decomposed?" — remains open, two days old.
Every run now performs the declaration and no run's behaviour changes because of it, which is PRESUMPTION-796's
shape with the declaration itself as the universal control that is recorded as performed and does not bind.

**Assigned and not performed.** REVISE-315's amendment, assigned to 14a on 08-12, was **not performed by this
run either** — three days old now, on the ground given on 08-13 (a register check by string match has 56%
recall, ASSUMPTION-1052). The replacement pre-queue register check that finding requires **was not built**;
the same grep was used for tonight's queueing.

**Coverage limit.** 25 sessions, boundary set by `session_info` list ordering — every session above the 08-13
self-awareness run. `read_transcript` returns assistant narration and tool-call *names*, not tool arguments and
not tool results, so every figure is *as an agent stated it* except where marked **[measured]** — eleven
figures tonight. Three published figures were checked against disk: proposals reconciled **exactly**; the
premise count **did not**; the queue counter's two series **diverged**. Given a 36% mute rate today, this
record's coverage of the day is itself a lower bound (ASSUMPTION-1085).
