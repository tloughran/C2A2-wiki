# 2026-06-25 Changelog

## Agents 14a / 14b — End-of-day self-awareness pass (2026-06-25, autonomous; Tom not present)

### Narrative
A tooling-and-deployment day, mostly attended, on the deployed artifacts rather than the epistemics pipeline. Four interactive threads: (1) a substantial **Heartbeat tool** repair — root-caused the "95 for days" staleness to nothing running the pipeline, rebuilt `refresh_snapshot.sh` self-contained, made the live-refresh button honestly reactive, and scheduled a 6-hour Cowork refresh task (one residual cache-delivery symptom handed off); (2) **Explorer UI fixes** shipped and pushed (commit `1fba4b7`, 12 files) — header unification to brand gold, Sociogram in-iframe title removed, grid/chapter tab fixes, scoped to keep 39 agent-WIP files out; (3) **OpenStory** backend rebuilt, full history backfilled (218,879 events; sessions 1,099→1,332), and restored to a launchd-supervised reboot-safe state; (4) Who's Who / Summa commentary refresh. 14a extracted **12 stated assumptions (361–372)**; 14b surfaced **9 unstated presumptions (398–406)**; **3 decisions (065–067)** and **3 open questions (092–094)** registered; **15 testable items** queued for lit search. Note: the morning chat-scrape and evening chat-delivery both failed — claude.ai is signed out in the connected Chrome — so today's daily-walk Chat context was unavailable; this pass worked from the four Cowork session transcripts and the evening cowork-sync summary. This EOD pass is distinct from the 15a/15b/15c run that fired earlier today on the 06-24 cohort (recorded separately, below).

### 14a — Assumptions (ASSUMPTION-361..372, +12) → `assumptions.md`
- Heartbeat (361–367): staleness root cause = nothing ran the pipeline, ~95 count coincidental (361); self-contained `refresh_snapshot.sh` is the fix (362); a 6-hour Cowork task is adequate cadence, app-open caveat (363); archive-on-change-only = one entry per real update (364); titles, not the count, are the freshness signal (365); the residual symptom is cache-delivery not logic — cache-bust will fix (366); honesty refinement — flash only for new papers, calm "re-checked" otherwise (367).
- Explorer UI (368–370): unify all headers to brand gold `#C9A84C` (368, framework); Heartbeat hero kept as a serif hero, recolored not flattened (369, framework); commits scoped to session files only, never agent-WIP (370).
- OpenStory (371–372): launchd supervision = durable/reboot-safe (371); full no-window backfill recovers the missing pre-72h history (372).

### 14a — Decisions (DECISION-065..067, +3) → `decisions.md`
- **065** Heartbeat repaired + self-contained refresh scheduled every 6h (`c2a2-heartbeat-refresh`) — IMPLEMENTED + SCHEDULED; staged, push pending; residual cache-bust handed off.
- **066** Explorer UI fixes — COMMITTED + PUSHED (`1fba4b7` on `main`, 12 files, +108/−32); the only decision today that reached `origin`; Tom reviewed before push; surfaced a pre-existing repo-wide Dependabot alert (4 vulns, unrelated).
- **067** OpenStory rebuilt, history backfilled, launchd-supervised — IMPLEMENTED on Mac; caveat: seed ended in SIGKILL (trap fired; post-kill DB consistency unverified — PRESUMPTION-405).

### 14a — Open questions (OPEN-092..094, +3) → `open_questions.md`
- **092** Does the app-open 6h Heartbeat schedule actually keep it fresh — or reproduce the OPEN-086 silent-staleness class?
- **093** Should OpenStory get a freshness watchdog — and should it be one shared liveness mechanism with OPEN-086 / OPEN-092?
- **094** How to fix the position-based decision-ID vs stable-`proposal_id` bug, and can the 06-23 archive (7 approvals, 2 matched) be reconciled? (Agent 16 FAIL-LOUD escalation, now OBSERVED.)

### 14b — Presumptions (PRESUMPTION-398..406, +9) → `presumptions.md`
The inferential remainder these sessions did NOT state, concentrated in three places:
- **Liveness / honesty cluster (Heartbeat):** app-gated scheduler as adequate liveness — same class as keystone OPEN-086 (398, HIGH); headless-test passage warrants a "caching not logic" diagnosis, presuming jsdom↔iframe fidelity the symptom itself disproves (399); "looking alive" as a goal, presuming perceived liveness ≈ actual (400).
- **Process-discipline cluster (Explorer/commit):** header uniformity = improvement, consistency over wayfinding (401); a dirty tree reliably hand-partitioned by vigilance, no structural guard against the 39-file sweep-in (402).
- **Durability / integrity cluster (OpenStory + Agent 16):** self-capture of the repair session as artifact-free observation (403); single-Mac launchd durability transferring to the deferred VPS-hub future (404); a SIGKILL'd backfill read as consistent because counts rose (405); the 06-23 approval/proposal mismatch as a reconcilable tooling defect, presuming a recoverable ground truth despite positional IDs (406, MED-HIGH).

### Lit search queue (→ `for_lit_search.md`, +15 QUEUED [cycle 0: 2026-06-25])
6 assumptions (363, 364, 366, 367, 370, 371) + 9 presumptions (398–406). NOT queued: ASSUMPTION-361/362/365 (local diagnostic facts), 368/369 (brand/visual framework), 372 (in-session DB-verified fact). Highest-priority searches: 398 (scheduler liveness / dead-man's-switch — recapitulates the keystone), 406 (positional-vs-stable IDs and decision-record recoverability), 399/366 (headless-test fidelity; cheap to falsify next session), 402/370 (dirty-tree partial-staging error rates), 405 (post-SIGKILL SQLite consistency).

### Registry accounting (this EOD pass)
- Assumptions 360 → 372 (+12). Presumptions 397 → 406 (+9). Self-awareness registry total 757 → 778 (+21). Decisions 64 → 67 (+3). Open questions 91 → 94 (+3). Lit-search queue +15 QUEUED. This 14a/14b EOD pass adds 0 premises/monitors/revisions/dispositions (correct — no 15c in this pass).
- Backups written: `{assumptions,presumptions,decisions,open_questions,for_lit_search}.md.bak.20260625-pre-14eod`.

### Provenance & liveness
All 21 items + 3 decisions + 3 open questions + the metrics snapshot carry PROVENANCE headers (Origin 14a/14b; Chain [14a]/[14b]; Item type ASSUMPTION/PRESUMPTION/DECISION/OPEN-QUESTION/METRICS). OPEN-086 (pipeline liveness) STILL OPEN — this pass fired because the scheduled task fired, not because a watchdog asserted liveness. Today's work in fact *added two more instances* of the same liveness gap (PRESUMPTION-398 Heartbeat scheduler; OPEN-093 OpenStory watchdog), strengthening the case for one shared watchdog.

### Priority order for Tom
Sign in to claude.ai in Chrome (restores the broken walk-conversation loop in both directions — highest leverage) > reconcile the 06-23 approval/proposal mismatch and fix `generate_review_page.py` ~line 304 before the next review pass (OPEN-094 / PRESUMPTION-406) > decide whether OPEN-086 / OPEN-092 / OPEN-093 should be one shared liveness watchdog rather than three > verify the two carried-over Mac commits from 06-24 landed (until the falsifier registration commit lands, DECISION-063 is non-binding) > apply the Heartbeat cache-bust first thing next session to discharge ASSUMPTION-366/PRESUMPTION-399.

---

## Agents 15a / 15b / 15c — Literature-search pipeline (2026-06-25 morning, autonomous; Tom not present)
*Recorded here for completeness; deltas belong to this separate morning run, not the 14a/14b EOD pass above.*

The 15-pipeline processed the full **2026-06-24 cohort** — the 25 items 14a/14b extracted on 06-24 (ASSUMPTION-347..350, 352..357, 359..360; PRESUMPTION-385..397), all `[QUEUED] [cycle 0: 2026-06-24]` at the start. 15a searched supporting and 15b independently searched challenging literature; 15c dispositioned all 25. Validated premises rose **74 → 82 (+8, PREMISE-075..082)**; monitor queue extended through **MONITOR-381**; revision flags through **REVISE-146**; dispositions through **DISPOSITION-325**. Notable dispositions in the cohort tail: ASSUMPTION-359/PRESUMPTION-395 sessionStorage "never on disk" → REVISE-144 / MONITOR-379 (the "never written to disk" claim is false against OWASP guidance); PRESUMPTION-394 owner-self-audit ≠ independence → REVISE-146 (partial REVISE-111); PRESUMPTION-396 single-file 40MB maintainability → MONITOR-380; PRESUMPTION-397 closed nav-keyword set → MONITOR-381. Queue end-state for the 06-24 cohort: searched + dispositioned, provenance chains complete.

### Also autonomous today (not 14a/14b)
- **Agent 16 (deferred-action tracker)** ran clean (0 active watch items) but **escalated a data-integrity FAIL-LOUD flag** — the 06-23 decision archive logged 7 approvals, only 2 with matching proposal files (now OPEN-094 / PRESUMPTION-406).
- **4 new Fredrickson proposals** auto-created (PROP-2026-06-25-001..004): resonance-signifies-love, listening-connects-strangers, interparental-positivity-spillover, positively-in-sync-convergent-validity.
