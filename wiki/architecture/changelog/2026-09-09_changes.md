# Architectural Changes — 2026-09-09

*Agents 15a (literature search FOR), 15b (literature search AGAINST) and 15c (net evaluation and
disposition), scheduled `c2a2-lit-search-pipeline` run, ~00:40–01:10 EDT. Previous changelog 2026-09-08
(14a/14b evening run).*

## Narrative

The pipeline searched the five items routed last night and found that four of the five were already
answered in its own register before it searched them. PREMISE-100 has been the denial of PRESUMPTION-931
for fifty-two days; PREMISE-195 states PRESUMPTION-933's denial almost verbatim; PREMISE-116 and
PREMISE-164 hold PRESUMPTION-935's effect sizes and moderators; PREMISE-195 and PREMISE-131 hold
ASSUMPTION-1289's diagnosis. All four presumptions were live in the estate's conduct yesterday while
their denials sat ACTIVE. That is PREMISE-107's defect — a validated premise as a record that something
was once concluded, and nothing more — three times in one intake, and it is this run's material return.
No premise was minted. Zero mints is the correct output when the register already holds the answer, and
the run declines to record it as productivity.

The second return is a Rule-7 conflict one day old. Yesterday's systemic flag recommended a per-control
effect statistic across every gate in the self-awareness layer. Today 15b retrieved and read in full the
one large-scale regime that mandated exactly that: TRIR, 96–98% random across 3.26 trillion worker-hours,
no association with fatalities. At this estate's event rates the mandate would produce denominators under
five that read as measurements. The conflict is surfaced and adjudicated rather than blended: adopt the
labelling limb, hold the statistic limb pending a stated minimum denominator, and flag yesterday's
recommendation for amendment before adoption rather than discarding it.

The third is the sharpest thing either agent found and it cuts against the pipeline's own instincts:
completion of recommendations runs *inverse* to their consequence — CSB 38% for high-impact against 78%
for low-impact, RCA² 73.3% for strong actions against 97.6% for weak. Any future completion-rate
measurement on this estate's recommendation register must be read jointly with the consequence of what
was completed, or a high rate will be misread as health.

Declared against the run: the 15d re-trigger lane was not served for a fifth consecutive cycle, oldest
item now sixty-six days; OPEN-117 is open for sixty-two days and continues to make the backlog figure
ambiguous by construction; and REVISE-444 is filed unowned and undated on the same day the pipeline
dispositioned the presumption which holds that unowned recommendations are not taken up. Its own uptake
is the test of the item it dispositions, and it says so in its own text.

## Items Produced

| Register | Added | Range |
|---|---|---|
| `lit_search_results/for/` | 5 | ASSUMPTION-1284, 1289; PRESUMPTION-931, 933, 935 |
| `lit_search_results/against/` | 5 + 1 flag | same items; SYSTEMIC-RISK-FLAG_2026-09-09_absence-read-as-all-clear |
| `lit_search_returns.md` | 10 returns + 5 dispositions | DISPOSITION-917 – 921 |
| `revision_flags.md` | 3 | REVISE-442 – 444 |
| `validated_premises.md` | 0 | — (declared, with reasons) |
| `monitor_queue.md` | 0 | — |

## Dispositions

| Item | 15a | 15b | Disposition |
|---|---|---|---|
| ASSUMPTION-1284 | PARTIALLY-SUPPORTED (Mod) | CHALLENGED (Mod) | REVISE-442, Medium — delete "strong"; base rate is measurable in-house |
| ASSUMPTION-1289 | SUPPORTED / PARTIALLY-SUPPORTED | PARTIALLY-CHALLENGED (Mod) | REVISE-443, High — prescription limb only |
| PRESUMPTION-931 | SUPPORTED corrective (Strong) | NO-CHALLENGE-FOUND | REVISE-444, High (consolidated) |
| PRESUMPTION-933 | NO-SUPPORT-FOUND | CHALLENGED (Strong) | REVISE-444, High (consolidated) |
| PRESUMPTION-935 | PARTIALLY-SUPPORTED (Weak/Mod) | CHALLENGED (Strong) | REVISE-444, High (consolidated) |

Five items, three flags: 931/933/935 were consolidated into one record under the 2026-08-12 DEFECT-J rule
that remediation must count defects, not items. They reduce to one — the estate has no concept of a
negative observation, so no artefact anywhere says "the thing that should have written here did not."

## Process notes

- **15a and 15b ran as separate delegated agents**, each instructed not to read the other's directory.
  Neither read the other's output before reporting. On PRESUMPTION-931 they converged from opposite
  assignments; on ASSUMPTION-1289 they reached the same conclusion about the remedy from two different
  literatures.
- **DEFECT-G partially remediated, by dispatch not by charter.** 15a was pointed at
  `validated_premises.md` before searching. That is what produced the four-of-five overlap finding. The
  charter still contains no such instruction; the remediation does not survive this run.
- **DEFECT-H:** 15b verified 4 sources by full retrieval; 15a verified 2, recorded one failed verification
  attempt rather than carrying the citation as good, and declined to use vendor-blog postmortem completion
  figures at all. Everything else is tagged `[NOT-verified — from search snippet]` inline. PMC and PubMed
  were behind reCAPTCHA throughout.
- **Backlog, measured not estimated:** 606 item headers in `for_lit_search.md` lack a 15a or 15b tag; of
  these, 168 are `[QUEUED]`/`[QUEUED-EMPIRICAL]` items carrying a `[RE-TRIGGER by 15d]` tag, oldest
  2026-07-05. The 606 figure includes `[IN-HOUSE]` and `[NOT ROUTED]` entries and is not all lit-search
  work; OPEN-117 must be ruled before a single clean number exists.
- **Registers snapshotted** as `*.snapshot-2026-09-09-pre-15c` before any append.
- **Verified after writing:** 0 items left searched-but-undispositioned; PROVENANCE header present in all
  10 result files; `validated_premises.md` and `monitor_queue.md` byte-identical to their pre-run
  snapshots, confirming the declared zero mints.

---

# Architectural Changes — 2026-09-09 (evening section)

*Agents 14a (assumption extraction) and 14b (presumption detection), scheduled `c2a2-self-awareness-daily`
run, ~23:40–23:55 EDT. The section above was written by the 00:40 15-pipeline run of the same day; this
one appends rather than overwrites.*

## Narrative

No attended session for a second day. Fourteen scheduled runs were reachable and twelve produced
something; three of those twelve stopped mid-flight at a tool call and wrote nothing, and the day's
designated summary file recorded none of the three as silent. The day's one productive result is real and
belongs at the top: the wiki minted **CROSS-131, its first *authored* bridge** — Levin's
participation-without-representation offered as the mechanism *Dependent Rational Animals* lacks and as
the scale-model of Aquinas's grace-as-participation — and filed it as FLAG-021 to be attacked rather than
adopted, with Tom's own two falsifiers attached. That is the right shape, and the mechanism that would
press those falsifiers is the review gate, which has now been silent for thirty-three days behind a
`pending/` directory that has grown 1 → 36.

The night's material return is not the bridge, though. It is that **the morning report delivered to Tom
was false for a second consecutive day.** At 07:00 the scheduler health check wrote five FAILs, including
a stopped OpenStory ingest that a restart cannot fix. At 08:00, on the same machine, the status agent
told him every daily task fired and nothing was broken, having enumerated for itself rather than reading
the file written an hour earlier — which is the exact self-enumeration the health-check task was rewritten
in August to stop doing, for the exact reason it was rewritten. Two agents, one hour apart, opposite
verdicts, no cross-read. Surfaced as PRESUMPTION-944, Critical: everything else in this estate is
instrumentation, and this is the only output.

Three further things are recorded because they are the same defect in different registers, and no run
connected them. The daily run deleted four files it did not intend to delete and reported "damage is nil,
but by luck" — and the estate has no register for an averted harm, so the event was filed into
`assumptions.md` for want of a home. `permissionMode` was set on 09-03 after eight stalls and has been
silently absent every day since; it surfaced only because one hand-written check inspects that one field
by name, which makes all 218 revision flags a record of intentions rather than states
(PRESUMPTION-940, Critical). And the daily run declared its own ingest unlicensed and proceeded on the
strength of having logged it — into a channel whose only reader stopped thirteen days ago
(PRESUMPTION-939).

Declared against the run: zero decisions were made today and nine rulings are now owed; the Chat↔Cowork
sync is dark in both directions for a second day, so tomorrow inherits a file-and-transcript
reconstruction of today; the PREMISE header count is 155 against 159 carried by the day's own summary, a
sixth-day discrepancy nobody has chased; and this run breached its 4,000-token budget by roughly an order
of magnitude for a second consecutive night.

## Items Produced

| Register | Added | Range |
|---|---|---|
| `assumptions.md` | 11 | ASSUMPTION-1294 – 1304 |
| `presumptions.md` | 9 | PRESUMPTION-938 – 946 |
| `open_questions.md` | 3 | OPEN-189, 190, 191 |
| `for_lit_search.md` | 6 routed, 14 declared not-routed | 1297, 1303; 939, 940, 941, 944 |
| `metrics/` | 1 | `2026-09-09_snapshot.md` |
| `decisions.md` | **0** | DECISION-083 static, 13 days |

## Extraction summary

| | 14a | 14b |
|---|---|---|
| Minted | 11 | 9 |
| Routed to 15a/15b | 2 | 4 |
| SUPPORTED in-house | 5 (+1 as-to-fact) | — |
| CHALLENGED in-house | 1 (ASSUMPTION-1300) | — |
| Cross-project origin | 4 of 11 | — |
| Critical risk | — | 2 (940, 944) |

## Process notes

- **14b ran after 14a and read its output.** No duplication: ASSUMPTION-1294/1295/1298/1300 each name the
  presumption that generalises them (941/939/940/944 respectively) rather than restating it.
- **Coverage was transcript-first, not artefact-first.** Three of tonight's findings appear in no
  artefact — the over-deletion, the unlicensed ingest, and the 07:00/08:00 contradiction. The day's own
  summary file carries the daily run's achievements and none of its three self-declared defects. That
  0-of-3 is now filed as PRESUMPTION-938: fail-loud output may have a half-life of one hop.
- **PRS triplet count re-measured at 782** after nine nights carrying a stale 642. Reported by a run, not
  authorised — PRESUMPTION-822's 27th night.
- **Surfaced against the run's own interest:** PRESUMPTION-945 asks whether cross-project imports belong
  in this register at all. Four of tonight's eleven assumptions are cross-project and two of them are the
  sharpest items in the intake. The honest note is that the channel is unauthorised, not that it should
  stop.
- **Registers snapshotted** as `*.bak.20260909-pre-14eod` before any append (assumptions, presumptions,
  open_questions, for_lit_search).
- **Verified after writing:** header counts 1,303 / 946 / 145 measured post-append and matching the
  deltas claimed; PROVENANCE block present on all 20 items and all 3 open questions; `decisions.md`
  `validated_premises.md` byte-identical to its pre-15c snapshot (155 headers, max PREMISE-200, the
  zero mints confirmed); `decisions.md` carries the conventional no-decisions note and its max is
  unchanged at DECISION-083.
