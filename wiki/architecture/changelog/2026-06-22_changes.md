# 2026-06-22 Changelog

## Agents 14a / 14b — EOD self-awareness pass (2026-06-22)

### Narrative
A real-extraction day, and the first successful EOD pass since 06-18 — the pipeline ran on the nights of 06-19/06-20/06-21 only as a retroactive null-snapshot and catch-up cohort, with no 06-20/06-21 changelog or snapshot (OPEN-086 — the pipeline's own silent stall, still open). Today's one attended Cowork session did two substantive things. First, it surfaced two genuinely new, tightly-paired proposals — Levin's "From Development to Cognitive Glue" (PROP-2026-06-22-001, *Bioelectricity*) and Friston's "As One and Many" (PROP-2026-06-22-002, *Entropy*, group-level Markov blanket) — explicitly framed as two vocabularies for one problem, *what binds sub-agents into a super-agent*, i.e. the literal C2A2 individual↔collective transition; the pair forms a clean Levin↔Friston bridge and is the most on-mission content in a now 5-deep, review-bound queue. Second, it resolved OPEN-083: the long-suspected post-Apr-6 "token cliff" was confirmed an artifact of the 2026-04-07 schema migration (`data.token_usage` → `data.agent_payload.token_usage`) zeroing the read path — assistant output is in fact continuous and growing (8.2M→20.4M→33.3M) — clearing PRESUMPTION-352 / MONITOR-349. 14a extracted 5 stated assumptions (ASSUMPTION-333..337), 14b surfaced 6 unstated presumptions (PRESUMPTION-371..376), no new DECISION or OPEN was registered, and OPEN-083 was flipped to RESOLVED. Both daily-sync loops were down again (claude.ai signed out in Chrome, 4th straight day), so the day's transcript surrogate was the Cowork→Chat summary plus the two proposal files.

### 14a (Assumption Extraction)
- Stated assumptions extracted: **5** — ASSUMPTION-333..337.
  - 333 (epistemic/architectural): Levin "cognitive glue" and Friston "group-level Markov blanket" are two framings of one problem → a clean mathematical Levin↔Friston bridge. Routed HIGH.
  - 334 (architectural/methodological): the sub-agent→super-agent binding transition is C2A2's core target ("the literal individual↔collective transition"); a paper's value scales with how explicitly it formalizes it. Part framework commitment.
  - 335 (empirical): the token cliff was a schema-migration read-path artifact, not an output collapse; both-paths read recovers continuous, growing output. Marked **GROUNDED** — validated by C2A2's own live-db probe, not literature.
  - 336 (methodological): correcting the read artifact licenses trust in all downstream yield comparisons ("do not inherit a masked drop").
  - 337 (methodological): the proposal-review queue is review-bound, not search-bound, since 06-16.
- decisions.md: **+0** — no new DECISION (max remains DECISION-060). The two proposals are PENDING review, not adopted; DECISION-054 Round 2 still open.
- open_questions.md: **+0 new**, **1 resolved** — OPEN-083 flipped to RESOLVED (token cliff = artifact); this was a fail-loud correction of a stale `Status: OPEN` field whose body already carried the resolution (flagged in the 06-22 cowork sync). Standing gates carry unchanged: OPEN-084 (PRS construct divergence), OPEN-085 (Summa count gap), OPEN-086 (pipeline liveness watchdog — STILL OPEN).
- Metrics snapshot produced: `metrics/2026-06-22_snapshot.md`.

### 14b (Presumption Detection)
- Unstated presumptions surfaced: **6** — PRESUMPTION-371..376.
  - 371 (epistemic): "same problem, two vocabularies" warrants a formal bridge — cross-vocabulary commensurability. **Twin of ASSUMPTION-333.**
  - 372 (methodological/normative): adding two proposals to a review-bound, 5-deep queue is nonetheless scored as the day's progress — intake-as-progress. **Tension with ASSUMPTION-337.**
  - 373 (methodological): the both-paths fix is presumed complete and durable — no recurrence guard against the next migration that re-zeroes reads. **Twin of ASSUMPTION-335; same silent-read class as PRESUMPTION-369.**
  - 374 (structural): one recent paper by a figurehead author is the right unit for updating a tradition — one-paper-one-thinker intake schema. Recurrence of the "traditions/thinkers are the unit" family.
  - 375 (normative/epistemic): month-over-month token growth is itself reassuring — more = healthier. **Twin of ASSUMPTION-336; metric-direction family with 367.**
  - 376 (methodological/scaling): the papers' formal results transfer intact to a community of inquirers — the proposals name this as "C2A2's extension, not the paper's claim" but never check the transfer conditions. **Twin of ASSUMPTION-334; highest-risk item of the cohort (load-bearing for the accelerator-detector framing).**
- Checked against 14a's output: 371↔333, 372↔337, 373↔335, 375↔336, 376↔334 flagged as twins (stated surface vs unstated substrate), surfaced deliberately under 14b's "false positives are cheap" standard; no true duplications. 373 also tied to the standing silent-read/no-liveness family (369), and 374/375 to standing structural and metric-direction families.

### Lit-search queue
- New items seeded today: **11** (ASSUMPTION-333..337; PRESUMPTION-371..376), all tagged `[QUEUED] [cycle 0: 2026-06-22]` with full provenance headers, under a new "2026-06-22 cohort" section in `for_lit_search.md`. Not yet searched (15a/15b run on their own cadence). No 15c run in this pass.

### Registry accounting (this run)
- Assumptions 332 → 337 (+5). Presumptions 370 → 376 (+6). Self-awareness registry 702 → 713 (+11).
- Decisions 60 → 60 (+0). Open questions 86 → 86 (+0 new; OPEN-083 resolved → 85 open / 1 newly resolved).
- Lit queue: +11 QUEUED (this cohort). Validated premises 65 (+0), monitor queue (+0), revision flags (+0) — no 15c pipeline run in this pass.
- Backups written (pre-run state preserved): `assumptions.md`, `presumptions.md`, `decisions.md`, `open_questions.md`, `for_lit_search.md` → `*.bak.20260622-pre-14eod`.

### Highest-leverage observations (surfaced, not acted on — Rule 3/Rule 12)
- **The pipeline ran tonight only because this scheduled task ran — OPEN-086 is not closed.** There is still no automated missed-run/liveness alert; the auditor still cannot signal its own absence. Tonight's success masks an unfixed mechanism. This remains the keystone meta-risk (PRESUMPTION-369 falsified in production over 06-19→06-21).
- **The day was scored a win on the non-binding axis.** Two on-mission proposals is real value, but the system's own stated bottleneck (review, ASSUMPTION-337) did not move; the queue grew. The cheapest high-value action is to *review* the Levin↔Friston pair (and the carried Rohr/Arkani-Hamed/Carroll items), not to search more (PRESUMPTION-372).
- **OPEN-083 closed without a recurrence guard.** The fix addresses the instance (two payload paths now read) but not the class (the next schema migration can silently re-zero the read). A schema-contract/canary assertion on the token-read path would convert ASSUMPTION-335 from "GROUNDED once" to "guarded going forward" and de-risk PRESUMPTION-373 — the same silent-read pattern as the pipeline's silent miss.
- **The strongest single item to test is PRESUMPTION-376** (paper→tradition transfer conditions): it is load-bearing for the whole accelerator-detector framing and is the unguarded half of the day's most exciting result (the bridge). The proposals' honesty in flagging "our extension" is unusual and good — but the flag is not the check.

PROVENANCE:
  Origin: 14a / 14b (joint EOD note)
  Chain: [14a], [14b]
  Item type: CHANGELOG (daily; self-awareness EOD pass — real extraction, attended day; first successful run since 06-18)
  Transform at each step:
    14a: Read the 2026-06-22 Cowork→Chat sync summary (the attended session's transcript surrogate; both live-sync loops were down — claude.ai signed out) and the two new proposal files (PROP-2026-06-22-001/002); extracted 5 stated assumptions (ASSUMPTION-333..337); resolved OPEN-083 and flipped its stale status field; registered no new DECISION/OPEN; produced the 06-22 metrics snapshot; routed all 5 to for_lit_search.md.
    14b: Read the same surrogate transcript with an interpretive lens; surfaced 6 unstated presumptions (PRESUMPTION-371..376), five flagged as twins of 14a items; routed all 6 to for_lit_search.md.
  Current status: COMPLETE — real-extraction day; 11 items extracted, registered with provenance, and queued; OPEN-083 resolved; registries advanced; backups written. OPEN-086 (pipeline liveness) remains open — pipeline ran because the task ran, not because a watchdog asserted liveness.
