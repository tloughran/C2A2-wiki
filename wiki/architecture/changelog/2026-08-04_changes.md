# 2026-08-04 — EOD self-awareness run (Agents 14a / 14b)

The day the system's assurance layer failed in four places at once and told itself it was fine. Four
scheduled runs — including the watchdog built to catch exactly this — terminated without a verdict of any
kind and were not noticed; the morning status report named the one run that failed as having run on time,
seven hours before the lit pipeline filed a Critical flag describing that precise shape; a QC sweep found
thirty-three artifacts carrying `fidelity_checked: true` written by a check that had crashed before
checking; and a disclosed caveat was measured failing to survive a single hop to its consumer. Against
that, the day's methodology improved where it was watched: 15a and 15b ran as separate instances with
disjoint contexts and stated their residual rather than banking it, three runs retracted their own
findings unprompted, and an unattributed change closed a determinism defect with a test that forces its
own failure path. No new DECISION (30th straight day), no new OPEN, and no attended session exists for
the second consecutive day.

## Changes Detected

- **CHANGE-2026-08-04-001 — Four scheduled runs vanished without a verdict, and the watchdog was one of them.** `c282-wiki-agent-daily-run`, `metabolism-regen-daily`, `scheduler-health-check` and `morning-walk-cowork-handoff` all end at `[user] [Request interrupted by user]` with no report; two died immediately after `mcp__Desktop_Commander__start_process`. None emitted a failure notice — each is absent rather than failed, and `scheduler-health-check` exists to detect this class. PRESUMPTION-661 (dispositioned today as REVISE-277) concerned sessions listed as running that were not advancing; this is the strictly worse case (ASSUMPTION-702; PRESUMPTION-664, **Critical**, queued; PRESUMPTION-665, in-house).

- **CHANGE-2026-08-04-002 — The morning status report named the failed run as successful.** `morning-project-status`: "everything due overnight ran on time — the C2A2 wiki orchestrator…", "No failures to report." `morning-system-health`, three hours earlier and reading the commit record: "The daily wiki run committed nothing again… Second FAIL in three days." Two health instruments, one morning, one subject, contradictory verdicts, no reconciliation anywhere — and the reassuring one is the one Tom reads first (ASSUMPTION-701, ASSUMPTION-713; PRESUMPTION-666, HIGH, queued).

- **CHANGE-2026-08-04-003 — Thirty-three artifacts carry a pass-mark issued by a crash.** 33 transcripts render **32–69%** of raw ASR against a ±25% threshold (worst Day 111 at 0.32, 1,406 body words against 4,283 spoken), and "All 33 currently read `fidelity_checked: true` / `last_qc_outcome: pass`, and none carries a `length_note`." Mechanism: `fidelity_check.py` raises `FileNotFoundError` on a cold `/tmp` cache — "which is every fresh sandbox" — killing the run before the length check, so "the pair gets pass-marked on form alone… a cold cache and a clean pair look identical unless you read `stderr`." Instrument error excluded by refetching 19 days. A second defect in the same file, line 86, produces false *failures* by sampling `## Notes` as transcript prose. Neither patched, on the stated rule that the scripts are Tom's (ASSUMPTION-703, ASSUMPTION-705; PRESUMPTION-667, **Critical**, governed → in-house).

- **CHANGE-2026-08-04-004 — SYSTEMIC-RISK-FLAG 2026-08-04 (Critical), FOURTH consecutive day, and the first to name the shape precisely.** "An instrument emits a reassuring token. The conditions that produced that token are not recorded alongside it. Because the token is reassuring, nothing downstream is motivated to investigate. The error self-conceals." Seven of ten REVISE items are one architectural defect: empty session list (646), green pipeline behind `|| true` (648), a trap firing counted as a save (654), a VERIFIED mark (655), a healthy host section (657), a PASS (660), a "running" status (661). Remedy in three cheap moves: three-valued verdicts (`PASS-DEGRADED`, `VERIFICATION-WITHDRAWN`, `RUNNING-BUT-STALLED`, `NO-SESSIONS-VISIBLE-IN-CHANNEL`), a provenance block on every assurance-bearing artifact, and "no absence claim may rest on one channel." Grounded in Huang et al. on gray failure, Cook/Allspaw on dark debt, Hsiao & Schneider's 5.4% retraction-acknowledgement figure, Dixit et al. on silent data corruption, Fleming & DeMets on surrogate endpoints, the Milbank incident-reporting review, and Dillon & Tinsley on near-misses lowering perceived risk (ASSUMPTION-724).

- **CHANGE-2026-08-04-005 — And three instances of that defect occurred the same day, none of them counted by it.** CHANGE-002 (a status report), CHANGE-003 (33 pass-marks), and a one-hop propagation failure measured end to end: `openstory-agents-telemetry-refresh` disclosed that `PRAGMA quick_check` was replaced by a header-consistency check and torn-copy detection skipped, published "✅ OPENSTORY REFRESH OK … (33 agents)" over a body saying "32/33 roster captured," wrote only the PASS line to `REFRESH_STATUS.md`, and `morning-system-health` read it and reported "OpenStory feeds passed clean." PRESUMPTION-660 was dispositioned as REVISE-276 for precisely this, hours after it recurred (ASSUMPTION-711, ASSUMPTION-712).

- **CHANGE-2026-08-04-006 — 11 items dispositioned: 0 INCORPORATE · 10 REVISE · 1 MONITOR (High) · 1 Critical batch flag.** DISPOSITION-590..600; REVISE-268 (an empty channel is not an empty day), 269 (incident-seeded thresholds need scheduled re-review), 270/273/276 (three independent routes to an unrecoverable assurance token), 271 (self-referential — the register recommends reading it first), 272, 274, 275, 277; MONITOR-501. Yield series: 07-29 1/8 · 07-30 0/4 · 07-31 0/3 · 08-01 2/6 · 08-02 3/7 · 08-03 0/7 · **08-04 0/11** — largest cohort in the series, second consecutive zero. No INCORPORATE was issued and the run said so explicitly rather than letting silence imply a clean check (ASSUMPTION-724).

- **CHANGE-2026-08-04-007 — The independence arrangement recovered and exceeded 08-02's.** 15a and 15b ran as separate agent instances with disjoint contexts, 15b explicitly barred from `lit_search_results/for/`, and 15b's transcript shows no such read. The residual is stated rather than banked: "same model family, shared pretraining and alignment, one coordinator whose framing both inherited. It is *not* independence in REVISE-262's sense." This reverses the 08-03 regression (ASSUMPTION-678) and partially answers REVISE-262 on its third day. **It was recorded only in a disposition preamble — a live change to how the pipeline executes, with no DECISION entry** (ASSUMPTION-724).

- **CHANGE-2026-08-04-008 — Three self-retractions, from three independent runs, none of which changed an instrument.** `summa-2026-nightly-verification` withdrew 9 false PRS-nonexistence hits before publishing — "Had I reported them, you'd have been sent to repoint citations that are already correct." The 20:30 `summa-qc-sweep` discarded a 17-item scripted audit ("Same failure profile as the 07-31 keyword audit") and reverted a pass-mark it had issued hours earlier, preferring the 18:40 run's reasoning to its own. In each case the output was corrected and the producing method was not (ASSUMPTION-706, ASSUMPTION-707; PRESUMPTION-668, HIGH, queued).

- **CHANGE-2026-08-04-009 — A hold with one stated scope was read by two runs as a blanket.** "That hold is *length-only* — the memory says don't flag the 0.31-0.41x ratios as novel, not that the pairs are unreviewable. Their semantic content had never been reviewed. Reviewing them found six sound commentaries and the new criterion-M band." The correction came from a run that read the hold's terms rather than its label. The adjacent case: Agent 16's WATCH-002 caption route is now known to be permanently unexercisable — "That extension, added 07-28, will fail identically every run" — while its check counter advances (ASSUMPTION-719, ASSUMPTION-714; PRESUMPTION-669, HIGH, queued).

- **CHANGE-2026-08-04-010 — The extractor determinism fix: the second remedy in this series shipped with a test that forces its own failure path, and it has no session, no decision and no owner on record.** `list(set(...))` → `sorted()`, closing a defect that "rewrote the whole 40MB wiki_narration.html blob in git on every regen even when no content had changed" — 1,499 differing leaves (1,262 wikilinks + 237 derived edges) between two extractions seconds apart. The test drives two fixed, different `PYTHONHASHSEED`s rather than relying on default variation, and names the principle: "Two default-seed runs are a weak test… force the failure, do not wait for it." Also disclosed: the 08-03 regen pulled in 103 LICENSE/README nodes from `node_modules`, "arrived with an npm install, not with a content change" (ASSUMPTION-720).

- **CHANGE-2026-08-04-011 — And two figures for one quantity, written nineteen seconds apart by one hand, inside the reproducibility fix.** The test script says "1122 node dates moved for that reason alone"; the extractor docstring says "2103 architecture files took their graph date that way." Neither reconciles the other (ASSUMPTION-721).

- **CHANGE-2026-08-04-012 — The delta guard's baseline describes no build, and the race explanation is excluded.** `build_meta.json` records `bytes: 40236674`; `wiki_narration.html` is 40,342,121. On 08-03 this was attributed to the test harness restoring files after the accepted build. Timestamps refute it: the HTML was written 13:31:03 and the metadata 18:09:46 — four hours thirty-eight minutes later, describing a different artifact. The guard's own rule is "the baseline must only ever advance on a build we accepted" (ASSUMPTION-723; PRESUMPTION-674, in-house).

- **CHANGE-2026-08-04-013 — Three surfaces specify the visualization's limits and the shipped artifact is legal on none.** `CLAUDE.md` 2,000 nodes / 3,000 edges; `generate_visualization.py:1322/1328` `MAX_NODES = 20000` / `MAX_SIM_EDGES = 30000`; the build 3,864 nodes / **98,201 links** — 3.3× the code's own edge ceiling, with no limit warning fired. The evening summary saw two surfaces and framed a disjunction ("either the limits were raised or the doc is stale. Flagging rather than assuming") that presumes the other two agree; they do not (ASSUMPTION-722; PRESUMPTION-675, HIGH, queued).

- **CHANGE-2026-08-04-014 — The queue counter did not move when eleven items left the queue.** `Status: [QUEUED]` in `for_lit_search.md` measured 1,527 on 08-03 and 1,527 on 08-04, across eleven dispositions. The evening summary independently surfaced a tenfold magnitude disagreement on the same file — "the run's own post-write check reports 115 − 115 = 0. My direct grep gives 1,347 − 1,347 = 0… Unresolved" — and declined to average it, which is the right behaviour and is recorded as such. But if the headline figure is invariant under disposition, "backlog drain zero for 28 days" is not a finding about the backlog (ASSUMPTION-725; PRESUMPTION-670, HIGH, in-house).

- **CHANGE-2026-08-04-015 — A partial side effect survived an interrupted run.** `c282-wiki-agent-daily-run` fired `unlabel_message` against a Gmail decision email and then died before Phase 0 completed. Measured: `review/archive/` holds 16 files, latest `2026-07-23_decisions.md` — no 08-04 entry. A future run reading Gmail labels will treat that decision as handled. The task's spec reasons about partial failure carefully and entirely inside the filesystem (PRESUMPTION-676, HIGH, queued).

- **CHANGE-2026-08-04-016 — Both sync channels dark for a seventh day, with two different causes for the first time.** Morning: the extension's browser is signed out of claude.ai, and "picking the other one requires interactive confirmation, which an unattended run can't do." Evening: with two extensions connected the tool requires human selection before any action, so **no navigation was attempted** — "Recorded as NOT ATTEMPTED, not as 'would have failed anyway'." That refusal is the three-valued vocabulary the systemic flag recommended inventing, used correctly and unprompted, on the day it was recommended. Seven MCP servers still need OAuth re-authorisation, which no scheduled run can perform (ASSUMPTION-726).

- **CHANGE-2026-08-04-017 — An 08-03 prediction came true on 08-04 and was recorded as confirmation.** "The bottom-frontmatter bug recurred exactly as the 08-03 entry predicted… Two consecutive nightly passes have now each paid this cost from scratch… a regex re-derived each night will keep failing." Independently the same morning: "the sandbox path is now a 7-call dance that I rebuild from scratch each morning." Both name the durable fix; neither is authorised to make it (ASSUMPTION-708; PRESUMPTION-673, queued).

- **CHANGE-2026-08-04-018 — Two runs breached "the budget" and meant different budgets.** "well past the 4k per-task guideline" and "well past the 30k session guideline," same day, same rule number. NINTH consecutive day of disclosure. Yesterday a run proposed a concrete work cap ("three pairs per run"); today a run made the opposite move — "Read with `--max 99`, not the 6-cap" — and breached (ASSUMPTION-727; PRESUMPTION-671, in-house).

- **CHANGE-2026-08-04-019 — Register measurements with no counterpart in any report.** `revision_flags.md` strict block count **118 for a second consecutive day** while ten flags were added — the counting rule broke on 08-03 and stayed broken through the day it was used to count. `metrics/prs_yield_log.csv` modified at 15:05 without gaining a row. `deferred/watch_list.md` at 316,393 bytes, up 10,294 — largest single-day growth on record. The DISPOSITION shortfall of exactly 32 unchanged for a fourth day, cause still unstated. `traditions/stump/prs_triplets.md` still carrying both 25 and 28 on day 5. `validated_premises.md` untouched for a third day; PREMISE-114 and PREMISE-124 unedited on day 11 (ASSUMPTION-728).

## Decision Index

**No new DECISION entries.** `decisions.md` unchanged since 2026-07-20 (DECISION-078) — **thirtieth
consecutive day.** Two candidates named against the day and not logged, per the standing convention
against unilateral promotion:

1. The 15a/15b separate-instance arrangement with 15b barred from `for/` — a live change to how the
   pipeline executes, recorded only in a disposition preamble. The evening summary's argument is quoted
   in full because it is the general case: "If the way the pipeline runs can change without a DECISION,
   the decisions register is not a complete record of how the system got to its current shape."
2. The extractor determinism fix — it changes the supported regeneration path and the git-blob cost of
   every future regen, and it has no session, no decision and no author on record.

## Open Questions

**No new OPEN entries.** `open_questions.md` unchanged since 2026-07-28 (OPEN-139). OPEN-138 and OPEN-139
both **OPEN — awaiting Tom** for a twelfth day, while ten more REVISE flags accrued into the layer
OPEN-138 asks about. Candidates surfaced and not logged:

- Whether a scheduled run that terminates without a verdict is a failure the system is entitled to not
  see (PRESUMPTION-664). This is the first candidate that bears on whether the daily record exists.
- Whether this pipeline's production rate is now reducing rather than raising the probability of action
  (PRESUMPTION-677). It queries PREMISE-138 directly, and four consecutive unfired Critical flags are one
  line of evidence against that premise.
- The eternity/timelessness question (FINDING-055), carried unchanged since 07-31.

## Intake Routed

- **[QUEUED] to 15a/15b (8):** PRESUMPTION-664, 666, 668, 669, 673, 675, 676, 677.
- **[GOVERNED-CANDIDATE → IN-HOUSE] (6):** PRESUMPTION-665, 667, 670, 671, 672, 674.
- **[IN-HOUSE] (28):** ASSUMPTION-701..728, with ASSUMPTION-720 flagged OBJECT-LEVEL/POSITIVE.

## Fail-Loud

This run exceeded the 4,000-token per-task budget — ninth consecutive day, fourth task today. Four
parallel sub-agents read ~21 session transcripts at tail depth to keep transcript bulk out of context;
no multi-MB register was read whole. Backups written to `*.bak.20260804-pre-14eod`.

**No attended session transcript was read, because none exists** — second consecutive day, and a
sub-agent established directly that `list_sessions` omits its own live parent session, which converts
PRESUMPTION-646 from an inference into an observation on the day REVISE-268 was filed against it.

**Self-inflicted and repaired within the run:** this run's append to `for_lit_search.md` incremented the
`Status: [QUEUED]` counter from 1,527 to 1,528, because a line describing the counting pattern matched
it. Detected on post-write measurement, the line was rewritten to break the match, and the count returned
to 1,527. Recorded rather than removed — it is a live instance of PRESUMPTION-670 produced by the run
that filed it.

Not done, stated rather than omitted: the four interrupted runs' work is unrepresented except as
absences; the three owed one-command joins (MONITOR-500 / P-643 / P-645) were not run here, third day;
the 26-item legacy retag is unauthorised on its ninth request; `decisions.md` and `open_questions.md`
were deliberately left unmodified; the `revision_flags.md` block-count break was measured but not
repaired, because that file is outside 14a's write set — second day.
