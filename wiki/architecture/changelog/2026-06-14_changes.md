# 2026-06-14 Changelog

## Narrative
An automated-only Sunday: no interactive Cowork session from Tom was detected, and all activity came from the scheduled Summa pipelines plus the daily-sync agents. The Summa QC sweep completed cleanly — Days 201–205 + 126 refetched and fidelity-checked, all six PASS (ASR segment counts matched exactly; raw-word drift 0.3–0.7%), marked transcript-pass only — and the commentary reviewer re-confirmed its genuine queue is empty. The day's only epistemic yield was self-referential infrastructure: the self-awareness pipeline's own scheduled siblings failed in instructive ways. The morning chat scrape died on a pinned-model error (`claude-fable-5` unavailable), the evening cowork→chat delivery failed on a claude.ai logout (same mode as 06-12), and the 06-13 summaries were missing entirely — a ~3-day sync-channel degradation. 14a extracted one stated assumption (the QC marking/staleness-clock policy); 14b surfaced two unstated presumptions about the durability and observability of the automation substrate. No new decisions, no new open questions.

## Changes
- NEW: ASSUMPTION-317 (stated) — marking a QC item resets its staleness clock, so transcript-only passes are left unmarked to avoid masking the synthesis half's later Layer-4 review. Extracted from the 06-14 QC-sweep logged rationale. Routed QUEUED (cycle 0: 2026-06-14).
- NEW: PRESUMPTION-347 (unstated) — a model identifier pinned in a scheduled-task config stays valid indefinitely; falsified today by the morning scrape's `claude-fable-5` 403. Type methodological/scaling; risk Medium-High. Supplies a concrete cause for the previously-unexplained PRESUMPTION-338 sync lapse. Routed QUEUED.
- NEW: PRESUMPTION-348 (unstated) — a failing scheduled task announces its own failure; falsified by the ~3-day silent channel degradation (no liveness monitor; 06-13 summaries simply missing). Type methodological/structural; risk Medium. Same family as PRESUMPTION-344 (queue-emptiness-as-health). Routed QUEUED.
- CHANGE: Summa QC sweep — Days 201–205 + 126 marked transcript-pass (all six PASS); synthesis halves spot-checked sound but deliberately left unmarked (now captured as ASSUMPTION-317). Summa daily batch regenerated `vault/refs/summa_index.json` (only vault file written).
- FLAG (fail-loud): sync channel degraded ~3 days — morning scrape model error + evening delivery logout + missing 06-13 summaries. Needs Tom: scheduled-task model-config review and claude.ai re-auth in Chrome.
- FLAG (fail-loud): OPEN-082 (parser/linker a/b/c) now blocks BOTH Summa pipelines every run; 65 bottom-frontmatter files reviewed-but-unmarkable; divergence grows daily; ~3 days open. Both pipelines re-confirmed these are false positives from the 06-11 parser regression — a re-confirmation of the trace-vs-substance presumption family; no new ID issued.
- NO new DECISION dated 2026-06-14 (max remains DECISION-056). decisions.md unchanged.
- NO new OPEN dated 2026-06-14 (max remains OPEN-082). open_questions.md unchanged; OPEN-082 staleness noted in metrics.
- CARRY: extraction freeze; proposal queue 10 (review overdue); REVISE-111 (HIGH) pre-registration outstanding; M7/M8 verdicts pending; ANTHROPIC_API_KEY (Ch. IX/X ingest); stale index.lock (Physics Explorer push); AWAITING-REVIEW 65.

## Pipeline accounting (this run)
- Registry deltas: assumptions 316 → 317 (+1); presumptions 346 → 348 (+2); self-awareness total → 665.
- Lit-search queue: +3 QUEUED (cycle 0: 2026-06-14); no 15a/15b/15c run today.
- Backups written (pre-14eod, 20260614): assumptions.md, presumptions.md, for_lit_search.md.
- Source basis: full read of the evening cowork→chat summary (2026-06-14) + failed morning-scrape transcript; Summa QC/commentary fronts summary-sourced. No attended-session transcript existed to read.

PROVENANCE:
  Origin: 14a
  Chain: [14a], [14b]
  Item type: CHANGELOG (daily)
  Transform at each step:
    14a/14b: Produced from the 2026-06-14 evening cowork summary, the failed morning-scrape transcript, and registry/ledger state. Automated-only day — one stated assumption (14a) and two unstated presumptions (14b) extracted; no design-session transcript was available, so no content-tradition changes recorded.
  Current status: COMPLETE
