# Architectural Changelog — 2026-06-05 (Friday)

**Narrative.** The **no-attended streak broke**: today was an attended day with real design work on the Community Explorer (CE). The headline is a ratified architecture decision — **DECISION-050: P1 (federated sub-tabs + shared services) now; P3 (one app, two projections, with a quality-gate promotion pipeline as the membrane between the 855-record directory and the curated graph) as the someday target.** During the P1 build, a prior premise was **falsified in-session**: the "shared `community_id`" sketch is false — the graph's 156 curated communities (`CC-001…`) and the cards directory's 855 records (`C0001…`) are **disjoint id spaces** (0 id / 3 name / 5 url-host matches), so cross-navigation hand-offs were **deferred** to the P3 promotion pipeline and P1 shipped as search box + shared Ask-AI pipeline only. Two long-running deferrals also closed: the **587-change git backlog was committed and pushed** (main at `fda17cd`; CE v2 on the feature branch), so the predicted attended cleanup behind ASSUMPTION-272 / PRESUMPTION-305 actually occurred. Today's extraction is **5 ASSUMPTIONs (273-277) + 6 PRESUMPTIONs (306-311)** — an attended day's signature (assumption-leaning, real design substance), in contrast to the prior three days' presumption-leaning echo batches.

### Agent 14a (stated assumptions)
- **+5 ASSUMPTIONs:**
  - **ASSUMPTION-273** — the 2026-05-29 LOCK search semantics (highlight lens, never filter) transfer correctly and should be inherited *exactly* by the 156-node CE graph. Type: architectural/methodological. (Routed.)
  - **ASSUMPTION-274** — a single shared search/broker module (`lib/c2a2-search.js`, inlined at generation time) is the correct single-source seam for both surfaces. Type: architectural. **Held — framework/engineering commitment, not literature-testable.**
  - **ASSUMPTION-275** — graph and cards are two non-redundant "verbs over one dataset"; neither absorbs the other (P4 rejected). Type: architectural. (Routed, LOW.)
  - **ASSUMPTION-276** — P3 with a Q2 quality-gate promotion pipeline is the correct target architecture; graph membership earned by self-articulation makes the graph a measurement surface. Type: architectural/methodological. (Routed.)
  - **ASSUMPTION-277** — **[GROUNDED this session]** graph (CC-001…, 156) and cards (C0001…, 855) are disjoint id spaces (0/3/5 matches); the "shared `community_id`" premise is false; cross-nav requires a not-yet-existing join. Type: empirical. **Verified directly against the JSON/data.js — not routed.**
- **Decision index (`decisions.md`): +1 — DECISION-050** (CE relationship architecture: P1 now, P3 target; cross-nav deferred to the P3 promotion pipeline). Max now **DECISION-050** (was 049). This is the first numbered decision since 049 — an attended session produced it.
- **Open questions (`open_questions.md`): +1 — OPEN-075** (is the curated↔directory join feasible at useful density, or are these categorically distinct object types? — promoted from the disjoint-id finding; P3's reachability depends on the answer).
- **Carried-context resolution (reported, not a registry status change owned here):** the 587-change working-tree hold (PRESUMPTION-305) and the attended-batch prediction (ASSUMPTION-272) were **discharged** — the backlog was committed and pushed this attended session. The deferral-cost family's predicted "future attended session cleanly commits" actually happened today.
- Metrics snapshot: `metrics/2026-06-05_snapshot.md` written.

### Agent 14b (unstated presumptions)
- **+6 PRESUMPTIONs:**
  - **PRESUMPTION-306** (High) — "one dataset" presumes unifiability despite the same-session disjoint-id finding; P3 rests on a join that may be very sparse. *(Structural; pairs ASSUMPTION-275/277.)*
  - **PRESUMPTION-307** (Medium) — inheriting the 1647-node lock "exactly" into a 156-node graph presumes the rationale survives a ~10x scale drop, though CE's stated need (name lookup) differs. *(Transferred-assumption; pairs ASSUMPTION-273.)*
  - **PRESUMPTION-308** (Medium) — Q2-gated graph visibility smuggles a normative claim (articulate communities deserve visibility; seed records don't). *(Normative smuggling; couples ASSUMPTION-276.)*
  - **PRESUMPTION-309** (Medium-High) — P1 pieces presumed "load-bearing in P3 later" though the central join is unbuilt and one named load-bearing piece (id-keyed hand-off) was already deferred this session; no P3 failure criterion. *(Success-criteria gap.)*
  - **PRESUMPTION-310** (Medium) — accepting "zero Civic↔Scientific cross-links" as honest signal presumes TF-IDF is a valid proxy for relatedness; the verification confirmed the count, not the construct. *(Epistemic; couples PRS-edge-validity family.)*
  - **PRESUMPTION-311** (Medium) — deferring the join to the promotion pipeline presumes curated communities and directory records are the same kind of object that should ever share an id space; the "should never join" alternative was never raised. *(Absent-alternative; distinct from 306 — feasibility vs conceptual appropriateness; pairs OPEN-075.)*
- Dedup check against 14a and prior batches: 273-277 (stated) vs 306-311 (inferred) are non-overlapping. Pairings: 306↔275/277, 307↔273, 308/309/311↔276, 310↔the Civic-Scientific verification. **Not re-surfaced:** the prior 300-305 batch (sync-degradation, PROCESSED_LOG, working-tree) — 305's working-tree concern was instead *discharged* today by the actual commit, noted as carried context, not re-extracted.

### Lit search queue (seeded)
- **9 items routed** to `for_lit_search.md` as `[QUEUED]` at cycle 0: ASSUMPTION-273 (MED), 275 (LOW), 276 (MED); PRESUMPTION-306 (HIGH), 307 (MED), 308 (MED), 309 (MED), 310 (MED), 311 (MED). Each carries a full PROVENANCE block and 15a/15b target-literature lines.
- **Held (not routed):** ASSUMPTION-274 (framework commitment); ASSUMPTION-277 (GROUNDED — empirically settled this session).

### Fail-loud (Rule 12)
1. **P3 reachability now an open empirical question (NEW):** the curated↔directory id spaces are disjoint (0/3/5 matches). The entire P3 target architecture presumes a join that today's measurement suggests may be too sparse to build (OPEN-075; PRESUMPTION-306; risk High). Surfaced, not smoothed over.
2. **TF-IDF construct validity unexamined (NEW):** "zero Civic↔Scientific links" was verified as a *count* and accepted as *signal*; the edge-construction method's validity was not checked (PRESUMPTION-310). The graph may present a methodological artifact as a finding.
3. **P1-as-P3-scaffolding partly already falsified (NEW):** one of the two named "load-bearing in P3" P1 pieces (id-keyed hand-offs) was deferred the same session it was named (PRESUMPTION-309).
4. **CE P1 not yet committed:** the P1 build (generator + regenerated HTML + review-doc CORRECTION) sits on `feature/sociogram-search-integration` awaiting Tom's sign-off; merge to main and pushes remain attended-only.
5. **claude.ai sign-in still LAPSED — 4th consecutive day:** the evening Cowork→Chat delivery failed again (redirect to /login, 22:42 UTC); re-auth remains the single attended fix (ASSUMPTION-263/270; PRESUMPTION-300 → MONITOR-297). Today's localhost work proved the extension itself is fine — only the login is missing.
6. **Human review gate at 9 days:** the new Carroll quantum-cyclic-universe proposal (PROP-2026-06-05) pushed the pending-review queue to **19**; the REVISE backlog and review queue remain gated on a single human.
7. Standing carries: 2026-05-20 lit-batch reconciliation UNDONE; 2026-06-01 / 2026-05-25 dated-artifact gaps not back-filled.

### New ID counters (this 14a/14b pass)
ASSUMPTION-277 · PRESUMPTION-311 · DECISION-050 · OPEN-075.

### Verification
- Registry maxes confirmed post-write: ASSUMPTION max = 277; PRESUMPTION max = 311; DECISION max = 050; OPEN max = 075.
- All 9 routed items carry PROVENANCE headers (Origin / Chain / Original item / Item type / Status); 9/9 tagged `[QUEUED]` at cycle 0; correctly NOT marked searched.
- No new item written without a PROVENANCE block (273-277, 306-311, DECISION-050, OPEN-075, the snapshot, and this changelog each carry one).
- Ground-truth checks (Rules 1/8/12): pending-proposal queue = **19** by file count (Carroll 2026-06-05 on disk); sociogram_feature_review.md confirmed to contain the dated CORRECTION + DECIDED-2026-06-05 lines; ASSUMPTION-277's 0/3/5 figures taken verbatim from that CORRECTION block (the session reported verifying them against the JSON). Git state (587-change commit, main `fda17cd`) read from the evening-sync session report; the repo is not introspectable from this mount, so it is recorded as **reported context**, not independently re-verified here — flagged per Rule 12.
- Backups written pre-run: `*.bak.20260605-pre-14eod` for assumptions / presumptions / for_lit_search / decisions / open_questions.

### Notes / autonomous choices
- **Attended day, assumption-leaning batch (5 ASSUMPTIONs / 6 PRESUMPTIONs ≈ 1:1.2)** — a real-design-substance signature, distinct from the prior three days' presumption-leaning echo batches. Extraction drawn from the attended CE P1 build session and the dated artifacts it produced (sociogram_feature_review.md CORRECTION + DECISION).
- **Did not re-extract** the 300-305 sync/PROCESSED_LOG/working-tree family; noted only that PRESUMPTION-305's working-tree concern was *discharged* by today's actual commit (carried context).
- **Held ASSUMPTION-274 and ASSUMPTION-277 from routing** by type (framework commitment; already-grounded) — surfaced the reason rather than queuing reflexively.
- **Token-budget note (Rule 6):** reading multiple session transcripts + registry tails + format anchors + ground-truth checks exceeds the 4k/task advisory; the scheduled-task success criteria take precedence for this autonomous pipeline; breach surfaced per Rule 12.

---

## Provenance

PROVENANCE:
  Origin: 14a
  Chain: [14a, 14b]
  Original item: CHANGELOG-2026-06-05
  Item type: CHANGELOG
  Transform at each step:
    14a/14b: Compiled from the 2026-06-05 attended Community Explorer P1 build session (read via session_info) and its dated artifacts (sociogram_feature_review.md CORRECTION + DECIDED-2026-06-05 lines), plus the evening Cowork→Chat sync report. Extracted 5 ASSUMPTIONs (273-277) + 6 PRESUMPTIONs (306-311); added DECISION-050 and OPEN-075; routed 9 testable items to for_lit_search.md as QUEUED cycle 0; held ASSUMPTION-274 (framework) and 277 (grounded). Confirmed pending queue = 19 by file count; recorded the 587-change commit / main fda17cd as reported context (repo not introspectable from this mount). Did not re-extract the 300-305 family; noted PRESUMPTION-305's discharge as carried context.
  Current status: COMPLETE

---

*Run by: c2a2-self-awareness-daily scheduled task (autonomous EOD; ATTENDED day — no-attended streak broken)*
*Run timestamp: 2026-06-05 EOD (covers 2026-06-05)*
*Next scheduled run: 2026-06-06 EOD*
