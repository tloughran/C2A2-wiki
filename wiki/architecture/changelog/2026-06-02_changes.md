# Architectural Changelog — 2026-06-02 (Tuesday)

**Narrative.** A no-attended-start, blind-intake day (claude.ai re-auth still owed; no 2026-06-02 chat summary on disk), but — unlike the recent string of pure-echo days — today carried one genuinely new technical finding. The C2A2 wiki daily-run discovered that a stale `.git/index.lock`, left by a crashed process on 2026-05-29, had been **silently blocking all git staging for ~4 days**; it was caught only incidentally during Phase 6. This anchors today's honest self-awareness batch: 1 ASSUMPTION + 3 PRESUMPTIONs + 1 OPEN question, all about the silent-failure and the lengthening human-gated stall — not a re-narration of prior batches. Separately, **2026-06-01 produced no self-awareness changelog/snapshot** (a cadence gap). Tom joined the session near the end to ask for help removing the lock.

### Agent 14a (stated assumptions)
- **+1 ASSUMPTION — ASSUMPTION-265** (the daily-run git phase must verify version-control health each run rather than infer it from no-error; a stale `.git/index.lock` from a crashed process can silently block all staging with no surfaced error). Stated explicitly today by the daily-run agent: "I had to clear a stale `.git/index.lock` left over from a crashed process on 2026-05-29 that was silently blocking all git staging." Type: methodological / architectural.
- Decision index (`decisions.md`): **unchanged on disk** — no attended decision session; the 4 un-numbered candidates carry unchanged. No new decisions.
- Open questions (`open_questions.md`): **+1 — OPEN-071** (should the daily-run git phase include a fail-loud pre-flight integrity check — stale-lock detection + post-stage verification — given the lock silently disabled staging for ~4 days undetected?).
- Metrics snapshot: `metrics/2026-06-02_snapshot.md` written.

### Agent 14b (unstated presumptions)
- **+3 PRESUMPTIONs:**
  - **PRESUMPTION-294** (High) — git silent-success blind spot: the pipeline presumed "git threw no error" == "changes were staged/tracked"; the stale lock disabled staging for ~4 days while runs reported a clean tree, and a rider premise assumes clearing the lock today restores correctness for the lock-window days. *(Concrete instance of the no-interlock claim PRESUMPTION-292; same "appears-to-succeed-but-silently-failed" family as ASSUMPTION-264.)*
  - **PRESUMPTION-295** (Medium-High) — indefinite-deferral-is-cost-free: the 36-file ingest backlog (deferred since 2026-05-26) and the 15-proposal review queue (waiting on a decision email last seen 2026-05-13) are reported as neutral status lines with no accruing-cost accounting or escalation trip-wire. *(Distinct from the intake-visibility presumptions 287/291 — this is the cost of the deferral itself.)*
  - **PRESUMPTION-296** (Medium) — decisions-arrive-only-by-email: Phase 0 reads "no `[C2A2-review-decision]` email" as "no decision," but on a blind-intake day the verbal/chat channel is dark, so a verbally-given decision would be silently dropped. *(Decision-channel-specific instance of intake blindness 287.)*
- Dedup check against 14a: ASSUMPTION-265 (the stated git-health lesson) and PRESUMPTION-294/295/296 (the unstated silent-success blind spot, the deferral-cost premise, and the decision-channel conflation) are non-overlapping. The 2026-05-31 batch (ASSUMPTION-264, PRESUMPTION-291/292/293) was deliberately NOT re-surfaced.

### Lit search queue (seeded)
- 4 items routed to `for_lit_search.md` as `[QUEUED]` at cycle 0: ASSUMPTION-265 (MED-HIGH); PRESUMPTION-294 (HIGH), 295 (MED-HIGH), 296 (MED). Each carries a full PROVENANCE block and 15a/15b target-literature lines.

### Carried context (maintained by the 15-pipeline; reported, not modified here)
- **The 15a/15b/15c/15d lit-pipeline did NOT run today** (no architecture files modified by it on 2026-06-02). REVISE backlog carried at **36 AWAITING-REVIEW** (last recorded 2026-05-31); MONITOR max MONITOR-289; PREMISE max PREMISE-044; DISPOSITION max DISPOSITION-131. The 2026-05-31 batch (264/291-293) remains QUEUED awaiting the next lit-pipeline cycle.

### Fail-loud (Rule 12)
1. **A stale `.git/index.lock` silently blocked all git staging for ~4 days (2026-05-29 → 2026-06-02).** It was caught only incidentally today. The daily runs of 2026-05-29..06-01 reported "changes left staged-clean in the working tree" — **those assurances are unverified for the lock window and may be false** (ASSUMPTION-265 / PRESUMPTION-294 / OPEN-071). NEW headline finding.
2. **claude.ai sign-in still LAPSED** — intake remains dark (no 2026-06-02 chat summary). Re-auth is the single stated fix (ASSUMPTION-263) and remains the top human action item.
3. **2026-06-01 self-awareness artifact GAP** — no 06-01 changelog/snapshot was produced; the scheduled 2026-06-01 EOD run did not deliver dated artifacts. Today's run does not back-fill 06-01.
4. **Human-gated stall lengthening** (PRESUMPTION-295): 36-file ingest backlog deferred ~7 days; review-decision queue waiting ~20 days. No escalation trip-wire.
5. Standing carries: 2026-05-20 lit-batch reconciliation UNDONE; 2026-05-25 dated-artifact gap not back-filled (now joined by the 2026-06-01 gap).

### New ID counters (this 14a/14b pass)
ASSUMPTION-265 · PRESUMPTION-296 · OPEN-071.

### Verification
- Registry maxes confirmed post-write: ASSUMPTION max = 265; PRESUMPTION max = 296; OPEN max = 071.
- All 4 routed items carry PROVENANCE headers (Origin / Chain / Original item / Item type / Status); 4/4 tagged `[QUEUED]` at cycle 0.
- No new item written without a PROVENANCE block (ASSUMPTION-265, PRESUMPTION-294/295/296 each carry one in the registry; the snapshot carries its own).
- Ground-truth checks (fail-loud, Rules 1/8/12): review/2026-06-02_review.html confirmed created 16:21 today (run is today's, not an echo); no 2026-06-02 chat summary (intake dark); no 2026-06-01 changelog (gap day) — all corroborated on disk before extraction.

### Notes / autonomous choices
- **No-attended-start + blind-intake day**, but NOT a pure-echo day: a real new technical finding (the silent git lock) anchored the batch, so the items are about today's substance, not a re-narration of 2026-05-31. Per the 2026-05-30/31 precedent, did NOT re-extract the prior batch as if new.
- **Tom joined near the end** and asked how to find/remove the stale git lock — answered live in the session (not a registry item).
- **Token-budget note (Rule 6):** reading the daily-run transcript + multiple registry tails + ground-truth disambiguation exceeds the 4k/task advisory; the scheduled-task success criteria take precedence for this autonomous pipeline; surfacing the breach per Rule 12.

---

## Provenance

PROVENANCE:
  Origin: 14a
  Chain: [14a, 14b]
  Original item: CHANGELOG-2026-06-02
  Item type: CHANGELOG
  Transform at each step:
    14a/14b: Compiled from the 2026-06-02 C2A2 wiki daily-run transcript (read via session_info), disambiguated against on-disk dates. Extracted 1 ASSUMPTION (265) + 3 PRESUMPTIONs (294-296); promoted OPEN-071; routed all 4 to for_lit_search.md as QUEUED cycle 0. Confirmed the day is genuinely 2026-06-02 (review page created today), intake dark (no chat summary), and 2026-06-01 a self-awareness gap day. The 15-pipeline did not run today; REVISE/MONITOR/PREMISE/DISPOSITION counts carried from 2026-05-31.
  Current status: COMPLETE

---

*Run by: c2a2-self-awareness-daily scheduled task (autonomous EOD; Tom joined the session near the end)*
*Run timestamp: 2026-06-02 16:27 EOD (covers 2026-06-02)*
*Next scheduled run: 2026-06-03 EOD*

---

# Evening Pass — 2026-06-02 (supplementary; interactive Sociogram session)

**Narrative.** The 16:27 pass above ran as a no-attended-start fire and covered the autonomous daily-run. After it, the day produced real attended substance the morning pass could not have seen: (1) the **15-lit-pipeline completed ~16:40**, dispositioning both the carried 05-31 batch and the morning 06-02 batch in one run (2 INCORPORATE → PREMISE-045/046, 3 MONITOR → 290-292, 3 REVISE → 083-085, DISPOSITION-132..139; PREMISE-046 codifies the git-lock "verify the side-effect / fail loud" lesson, now backing OPEN-071); and (2) a substantive **interactive Sociogram engineering session** with Tom that shipped commit `7d56733` — focus-fade synchronous-interrupt opacity fix, `MAX_NODES` 2000→20000, nav 1.6, Summa coverage extended to Day-190 — gated behind a live foreground-tab constitutional review and Tom's sign-off. Notably, the **stale `.git/index.lock` RECURRED** (fresh 0-byte lock at 21:02 UTC) and blocked Tom's attended commit, re-firing the morning's headline failure within hours before he cleared it. This evening pass extracts the new attended material (3 ASSUMPTIONs, 3 PRESUMPTIONs, 1 OPEN) without re-narrating the 16:27 batch.

### Agent 14a (stated assumptions) — evening increment
- **+3 ASSUMPTIONs:**
  - **ASSUMPTION-266** — git staging must use explicit file paths, never `git add -A`, because the wiki tree perpetually carries unrelated modified Summa-vault-sync files. Stated: "explicit paths only — never `git add -A`, since the tree has unrelated modified vault files." Type: methodological/architectural.
  - **ASSUMPTION-267** — raising Sociogram `MAX_NODES` 2000→20000 is correct crash-proofing; 20000 treated as a safe ceiling at current scale (2529 nodes). Shipped in 7d56733. (Supersedes CLAUDE.md node-limit-2000 baseline.) Type: architectural/empirical.
  - **ASSUMPTION-268** — a valid pre-push constitutional review requires live verification in a real foreground browser tab over HTTP (not headless/asserted), with explicit evidence (opacity split, cross-link count, clean console) + Tom's sign-off. Type: methodological.
- Decisions (`decisions.md`): **unchanged on disk.** The Sociogram changes are engineering increments under existing constitutional rules, not new numbered decisions. (The "Sociogram interaction-model lock" remains an un-numbered candidate.)
- Open questions: **+1 — OPEN-072** (should the push/handoff workflow include a cross-repo uncommitted-state check, given Day-190 shipped depending on uncommitted Summa-repo edits?). **OPEN-071 reinforced** with the evening lock recurrence (direct field evidence).

### Agent 14b (unstated presumptions) — evening increment
- **+3 PRESUMPTIONs:**
  - **PRESUMPTION-297** (Medium-High) — cross-repo correctness held by memory + handoff doc, not tooling: the pushed Day-190 viz (wiki repo) depends on UNCOMMITTED edits in the separate Summa 2026 repo (`Days.md`, `summa_index.json`); an Obsidian revert or forgotten commit would silently desync the shipped artifact from its data. *(Same silent-failure family as PRESUMPTION-294 / ASSUMPTION-265; cross-repo instance.)*
  - **PRESUMPTION-298** (Medium) — single live spot-check generalizes: the fade fix was verified on one isolate (`levin`) and one focus (`levin ~ summa`) and signed off as "works" for all. The inductive leap inside the ASSUMPTION-268 review standard.
  - **PRESUMPTION-299** (Medium) — scale-blindness on the 10x cap raise: `MAX_NODES`=20000 validated only against the present 2529-node case; the 2.5k→20k span is untested headroom on an actively growing node set.
- Dedup check against 14a and against the morning batch: 266/267/268 (stated) vs 297/298/299 (inferred) are paired but non-overlapping (explicit-path rule vs cross-repo interlock gap; cap value vs untested span; review standard vs the spot-check generalization inside it). **Not re-surfaced:** the morning 265/294-296. **Noted-not-numbered:** shipping Day-190 with two known unfixed indexer issues (II-II.Q1 mixed-part mis-map; Q36/67/68 gaps) as "goal met" is a concrete instance of PRESUMPTION-295 (deferral-is-cost-free) on data-correctness, folded there rather than given a new ID (Rule 2 — avoid over-production).

### Lit search queue (seeded) — evening increment
- 6 items routed to `for_lit_search.md` as `[QUEUED]` at cycle 0: ASSUMPTION-266/267/268, PRESUMPTION-297/298/299. Each carries a PROVENANCE block + 15a/15b target-literature lines. These are QUEUED only — the 16:40 lit-pipeline run had already completed before they were written, so they await the next c2a2-lit-search-pipeline cycle.

### Fail-loud (Rule 12) — evening additions
1. **The stale `.git/index.lock` RECURRED the same day** (fresh 0-byte lock, 21:02 UTC) and blocked Tom's attended evening commit — the morning headline failure re-fired within hours. Cleared via `rm -f`; push then landed (7d56733). Confirms OPEN-071's pre-flight check is warranted (recurring, not one-off).
2. **Cross-repo silent-desync risk shipped to prod** (PRESUMPTION-297 / OPEN-072): Day-190 viz pushed while its enabling Summa-repo data sits uncommitted; integrity rests on Tom remembering to commit + Obsidian not reverting `Days.md`. No interlock.
3. **Two known indexer correctness gaps shipped under "goal met"** (II-II.Q1 mixed-part mis-map; Q36/67/68 gaps; Day-35 "Genesis" duplicate suspected) — documented in `INDEXER_FINDINGS_2026-06-02.md`, deferred to Tom (instance of PRESUMPTION-295).
4. Carries from the 16:27 pass stand: claude.ai re-auth still owed (browser delivery to Chat failed again this evening, same logout); 2026-06-01 self-awareness gap not back-filled; 2026-05-20 lit-batch reconciliation undone.

### New ID counters (evening 14a/14b pass)
ASSUMPTION-268 · PRESUMPTION-299 · OPEN-072.

### Verification (evening pass)
- Registry maxes confirmed post-write: ASSUMPTION max = 268; PRESUMPTION max = 299; OPEN max = 072.
- All 6 routed items carry PROVENANCE headers; 6/6 tagged `[QUEUED]` at cycle 0; correctly NOT marked searched (the 16:40 lit-pipeline had already finished).
- No new item written without a PROVENANCE block (266/267/268, 297/298/299 each carry one in the registries).
- Ground-truth: evening Sociogram session read via session_info (commit 7d56733 confirmed in transcript; lock recurrence at 21:02 UTC confirmed in transcript). 15-pipeline completion (PREMISE-045/046 etc.) read from the evening-sync session, reported as carried context (maintained by the 15-pipeline, not modified here).

### Notes / autonomous choices (evening pass)
- **Appended, did not overwrite.** The 16:27 dated artifacts were preserved; this is a clearly-marked supplementary evening section (Rule 3 — surgical). Rationale: the day had two genuinely distinct phases (no-attended autonomous AM; attended interactive PM) and the PM substance post-dates the morning pass.
- **Token-budget note (Rule 6):** reading the evening transcript + registry tails + format anchors exceeds the 4k/task advisory; scheduled-task success criteria take precedence for this autonomous pipeline; breach surfaced per Rule 12.

---

## Provenance (evening pass)

PROVENANCE:
  Origin: 14a
  Chain: [14a, 14b]
  Original item: CHANGELOG-2026-06-02-EVENING
  Item type: CHANGELOG
  Transform at each step:
    14a/14b: Compiled from the post-16:27 interactive Sociogram session (read via session_info) plus the evening-sync session reporting the 16:40 lit-pipeline completion. Extracted 3 ASSUMPTIONs (266-268) + 3 PRESUMPTIONs (297-299); promoted OPEN-072 and reinforced OPEN-071 with the evening lock recurrence; routed all 6 to for_lit_search.md as QUEUED cycle 0. Did NOT re-extract the morning batch (265/294-296). Appended (non-destructive) to the existing 2026-06-02 dated artifacts.
  Current status: COMPLETE

---

*Run by: c2a2-self-awareness-daily scheduled task (autonomous EOD supplementary pass; covers the attended evening Sociogram session)*
*Run timestamp: 2026-06-02 ~23:00 EOD-evening (covers 2026-06-02 PM)*
*Next scheduled run: 2026-06-03 EOD*
