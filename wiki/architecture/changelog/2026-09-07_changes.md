# Architectural Changes — 2026-09-07

*Agents 14a (assumption extraction) and 14b (presumption detection). Gap-filling run: the 2026-09-06
run did not occur; this run executed mid-morning 09-07 (10:53 local), not end-of-day. Previous
changelog 2026-09-05.*

## Narrative

Two unattended days, filed together. The material is the lit-search pipeline's 09-06 cycle on the 09-05
intake — three items, two REVISE flags, one MONITOR — plus this morning's Monday tradition agents
(Levin, Friston: two proposals) and the walk handoff (no walk notes; 25 proposals pending; master wiki
unwritten since 09-02). The 09-06 cycle's central fact is that its 15b delegation produced nothing and
the orchestrator wrote every AGAINST file after reading the FOR files, then filed a High systemic-risk
flag recommending that this not be done (PRESUMPTION-918). Six assumptions and five presumptions were
extracted, all from agent output, for the eighth consecutive day. One agent claim was checked and half
of it was wrong (ASSUMPTION-1275). One new question: what a missed 14 run should leave behind (OPEN-183).

## Items Produced

| Register | Added | Range |
|---|---|---|
| `assumptions.md` | 6 | ASSUMPTION-1270 – 1275 |
| `presumptions.md` | 5 | PRESUMPTION-918 – 922 |
| `open_questions.md` | 1 | OPEN-183 |
| `decisions.md` | 0 (dated run note appended) | — |
| `for_lit_search.md` | 3 items routed; 8 declared not-routed | — |
| `metrics/` | 1 snapshot | `2026-09-07_snapshot.md` |

Backups: `*.bak.20260907-pre-14eod` for assumptions, presumptions, open_questions, decisions,
for_lit_search.

## Changes Detected

**Registers moved by other agents since 09-05:** `lit_search_returns.md` 09-06 section
(DISPOSITION-907–909, SYSTEMIC-RISK-FLAG); `revision_flags.md` REVISE-435–436; `monitor_queue.md`
MONITOR-597; `lit_search_results/{for,against}/` 7 files (4 orchestrator-written); `for_lit_search.md`
09-06 15c run note and inline tags on the 09-05 intake; `inbox/proposals/pending/` +4 (Rohr ×2, Wright ×2
on 09-06; Levin, Friston on 09-07); `daily_sync/chat_to_cowork/2026-09-06_chat_summary.md` (FAILED);
`agents/openstory/*` and `agents_tab.html` (09-07 06:23–08:23); `inbox/rc_sandbox/TL_sandbox_reordered.md`
and `tl_sandbox_verbatim.md` mtime 09-07 10:53 (two minutes before this run — content not diffed; may be a
sync touch, as the 09-06 22:09 mass-mtime batch appears to be).

**No changes** to `wiki/agents/*.md` (latest 06-23) or `wiki/tools/*.py` (latest 08-12).

**Task-file path errors, two:** OPEN-167 (`Wiki`/`wiki`, ninth run) and the Monday agents' task file
missing the `Projects/` segment (worked around in-run, reported in transcript).

## Rationale Drift

**DISPOSITION-908 grounds the fallback practice in PRESUMPTION-914** ("the load-bearing justification")
— but 914 was surfaced by 14b on 09-05 *from* the fallback. The practice's stated justification is
younger than the practice (ASSUMPTION-1272). Recorded, not judged.

## Accumulating Technical Debt

- **Multi-agent execution degraded two cycles running**: 8 of 22 files on 09-05, 4 of 7 on 09-06, with
  search independence 10/11 then 0/3. REVISE-426/427's question is being answered by the harness.
- **Decision register eleven days static**; REVISE-436's binary ruling is the third such request queued.
- **Review pass**: master wiki and findings unwritten since 09-02 while pending grew 21 → 25.
  ASSUMPTION-1231 window closes in three days.
- **Missed 14 runs** now two gaps in eight days; no marker convention (OPEN-183).
- **Premise count** 153 measured twice now; 157 carried by the digest. Digest should be corrected or
  the four found.
- OPEN-168 day sixteen; three channels absent on the same two days.

## Self-Referential Observations

1. **The flag recommends against its own authorship.** The SYSTEMIC-RISK-FLAG's recommendation (3) —
   fail loud rather than orchestrator-write — was written by the orchestrator that had just written
   three AGAINST files. Declaration was treated as correction (PRESUMPTION-918). This is PREMISE-124
   (self-measurement) at the audit layer.
2. **The cheap test may not have an input.** DISPOSITION-909's linear-extension test presumes relational
   triples in a corpus that REVISE-434 says cannot be read without a criterion (PRESUMPTION-920).
3. **A tradition of one.** The Friston sweep excluded active-inference work "by other groups." A network
   built on MacIntyre's traditions is, at the agent level, tracking authors (PRESUMPTION-921).
4. **This run is the gap it flagged.** Filing 09-06 under 09-07 is the date-advance-without-observation
   pattern 15d recorded on 08-02, reproduced by the self-awareness layer (PRESUMPTION-922, OPEN-183).

## Requires Tom

1. **REVISE-436** — binary: (a) FAIL LOUD when 15b delegation fails, or (b) permit fallback recorded as
   one reading, AGAINST before FOR. Two cycles have run under neither.
2. **REVISE-435** — run notes to report read-channel and execution-channel independence separately.
   Cheap; can be adopted by 15c without a ruling if you say so.
3. **OPEN-183** — missed 14 runs: gap-fill under the run date, or DARK marker plus back-dated filing?
4. **OPEN-182** — still unruled; practice is running ahead of it (11 more agent-sourced items tonight).
5. **Review pass** before 09-10 (ASSUMPTION-1231); 25 pending.
6. OPEN-179/181 (ladder) — PRESUMPTION-920 first: do the III.2 triples exist as data?

---

*Budget disclosure (Rule 6): this run exceeded the 4,000-token per-task budget — two agent definitions,
the prior changelog and snapshot, the 09-06 pipeline section, two transcripts, and four register tails
before writing to seven files. Disclosed, not absorbed. Comparable to 09-05.*

---

# Architectural Changes — 2026-09-07, EVENING RUN (second run of this date)

*Agents 14a and 14b, ~23:40 EDT. The 10:53 section above was a gap-fill for the missed 09-06 and closed
at its own run time; this section covers **10:53 → 23:40** and does not overlap it. Filed as a second
section of the same dated file rather than a new file, because the agent spec says one changelog per day.
This is a de facto answer to half of OPEN-183 (two runs on one date → one file, two sections) and is
offered as a proposal, not a ruling.*

## Narrative

Five and a half hours of activity and eleven of quiet. The lit-search pipeline ran clean for the first
time since the fallback practice began — 2 delegated launches, 2 completed, 0 orchestrator-written,
independence 3/3 on both channels — and its three dispositions all went REVISE (437, 438, 439), with a
second High SYSTEMIC-RISK-FLAG in two days. The sewing bootstrap fired for the twelfth time, declined its
own Phase 3 for the eleventh, and reported the first decline in the connected-pages bucket in the series
along with a `.git/index.lock` written at 10:31 by a sandboxed process that no sandbox can remove. The
Openstory telemetry step hit a hard 6.1 GB / 4.2 GB disk ceiling. Both sync channels failed again. Seven
assumptions and six presumptions were extracted; **one of the thirteen came from an attended session** —
the first non-agent-sourced item since 08-30, and it is the only one in the day whose stated method
includes its own falsifier (ASSUMPTION-1277). Two new questions (OPEN-184, -185).

## Items Produced

| Register | Added | Range |
|---|---|---|
| `assumptions.md` | 7 | ASSUMPTION-1276 – 1282 |
| `presumptions.md` | 6 | PRESUMPTION-923 – 928 |
| `open_questions.md` | 2 | OPEN-184, OPEN-185 |
| `decisions.md` | 0 (dated run note appended) | — |
| `for_lit_search.md` | 4 routed; 9 declared not-routed | — |
| `metrics/` | 1 snapshot section appended | `2026-09-07_snapshot.md` |

Backups: `*.bak.20260907-pre-14eod2` for assumptions, presumptions, open_questions, decisions,
for_lit_search (the `-pre-14eod` names were taken by the 10:53 run).

## Changes Detected — 10:53 → 23:40

**Real work, in order.** Attended session ~10:50–11:00 (repo hygiene: `commit_daily_run.sh`,
`janitor.py`, `.gitignore`, `gen_sandbox.py`, `COMMIT_ME_2026-09-07.sh`, `inbox/rc_tome/
TOC_v2_proposal.md`; nothing committed — the sandbox cannot write `.git`). Lit-search cycle 11:06–11:16
(7 result files, DISPOSITION-910–912, REVISE-437–439, SYSTEMIC-RISK-FLAG 2026-09-07). Chat→Cowork scrape
11:17 FAILED (Chrome extension not connected — fifth consecutive day). Sewing bootstrap 11:58–12:03.
Openstory telemetry 12:03 FAILED. Cowork→Chat 18:39–18:40 written, delivery FAILED (fourth consecutive).
Agent 16: idle run, 0 due.

**A mass-mtime batch at 22:00** touched ~60 files with no visible content change, including all five
self-awareness registers, `synthesis/*_bridge.md` ×12, `heartbeat/data/*`, `inbox/proposals/pending/*`
and 20+ `vault/synthesis/Day-*` pages. This is the second such batch in two days (09-06 22:09) and it is
the reason this run reconstructed the day from a `find -newermt` sweep **plus** content, not from mtimes
alone. Recorded as evidence under PRESUMPTION-925, and as the third same-day instance supporting
ASSUMPTION-1276.

**No changes** to `wiki/agents/*.md` (latest 06-23) or `wiki/tools/*.py` (latest 08-12).

**Task-file path error:** OPEN-167 (`Wiki`/`wiki`) reproduced for the tenth run. Worked around.

## Rationale Drift

**The falsifier moved indoors and nobody noticed.** ASSUMPTION-1277 — a guard rule must pass a fixture
*and* fail a control with the rule neutralised — is stated only in a commit message. It is the strongest
verification standard anywhere in today's output, it was produced in the one attended hour, and it is not
in any register, SKILL, or CLAUDE.md. Meanwhile the day's other twelve items were produced by agents
whose own verification is declaration (the thing today's SYSTEMIC-RISK-FLAG is about). Recorded, not
judged.

## Accumulating Technical Debt

- **`.git/index.lock`, 0 bytes, 10:31, orphan.** Second sandbox-created lock in eight days; `rm -f`
  refused (`Operation not permitted`). Blocks every `git add`/`git commit`. The evening summary flagged a
  ~19:55 deadline; it is now past 23:00, so tonight's daily-run commit has very likely already failed and
  the day's work is uncommitted. **One line from the Mac fixes it** (see the evening summary, item 0).
- **Recommendation 6 of 08-30** — forbid index-writing git calls from sandboxed agents — is still in no
  SKILL or CLAUDE.md, and the 11:58 run disclosed making one such call itself before noticing the lock.
- **Openstory step 2 is now permanently failing**, not intermittently: 6.1 GB DB, 4.2 GB sandbox disk.
- **Decision register: eleven days.** REVISE now at 439; the escalation counter is at fourteen.
- **Connected pages fell 69 → 65**, first decline in the series, and the run that found it could not name
  the four pages.
- **Review pass:** 27 pending, master wiki unwritten since 09-02, ASSUMPTION-1231 window closes 09-10.
- Four sewing recommendations remain NOT DONE at 4, 3 and 2 weeks; all four are file-writes with a
  generator already supplied (PRESUMPTION-923, OPEN-184).

## Self-Referential Observations

1. **The layer's two claims about mtime are inconsistent, on the same day.** 14a extracted "the mtime
   authorship window sees a day-old file as run output" as a diagnosis of a real data loss; eight hours
   later the same layer reconstructed the whole day from mtimes and called it "the same thing" as reading
   transcripts. A 22:00 sync batch then touched sixty files. PRESUMPTION-925.
2. **The audit apparatus is the majority of the audited corpus.** `architecture/lit_search_results` is
   2,661 of 4,879 vault pages — **54.5%** — every page an orphan by construction, all counted in the
   census, the connectivity series and the graph's node population. No register counts pages written.
   PRESUMPTION-927, OPEN-185.
3. **Four cycles have run under an unanswered binary, and all four chose limb (b).** REVISE-438 arrives
   with the literature's answer to a question the practice settled a fortnight ago. Whether "no ruling"
   is a state or a choice is PRESUMPTION-928 — the same shape as PRESUMPTION-918 one level up.
4. **The one attended hour produced the one item with a falsifier.** Thirteen items, twelve
   agent-sourced. The exception is not a coincidence worth asserting on n=1, but it is the first datum
   OPEN-182 has had that is not agent-generated.
5. **The system's fastest-growing decision class has no register.** Twelve-times-fired "ONE-TIME" task,
   a hard disk ceiling, two missed 14 runs — all classified by the evening summary as "scheduler
   decisions, not project decisions," a category with no numbering and no escalation path.
   PRESUMPTION-924.

## Requires Tom

1. **Tonight/first thing: `rm -f ".../RC Karpathy Wiki Project/.git/index.lock"`** then
   `COMMIT_ME_2026-09-07.sh`. Today's work is uncommitted.
2. **Reconnect the Claude in Chrome extension.** Both sync directions dark since 09-05; five consecutive
   scrape failures.
3. **REVISE-436/438 — the FAIL LOUD binary.** Over-determined: the literature says (a) and today's 3/3
   run shows (a) is achievable. One line rules it. Fifth cycle otherwise runs under no ruling.
4. **Openstory** — prune the DB or move step 2 Mac-side. It will fail every night until then.
5. **OPEN-184** — can a deterministic gate (fixture + neutralised control + reversibility) discharge the
   no-blind-push rule for claim-free, reversible changes? Four stalled recommendations turn on this.
6. **OPEN-185** — is the audit layer's output in-corpus for connectivity metrics? Cheap ruling, large
   effect on every figure in that series.
7. **OPEN-183** (missed-run filing) and **OPEN-182** (agent-sourced intake) — both carried, both still
   unruled; this run's two-sections-one-file choice is a proposal for the first.
8. Review pass before 09-10; 27 pending.

---

*Budget disclosure (Rule 6): this run exceeded the 4,000-token per-task budget. Two agent definitions,
the morning changelog and snapshot, both sync summaries, the 09-07 risk flag, REVISE-437–439, the sewing
report, one session transcript and five register tails were read before writing to six files. Disclosed,
not absorbed. Comparable to the 10:53 run.*

*Coverage limits, declared: the attended session's transcript was not located — it does not appear in the
45 most recent `list_sessions` entries — so that hour is reconstructed from the artefacts it wrote
(`COMMIT_ME_2026-09-07.sh` and its commit message, `TOC_v2_proposal.md`) and from the 18:40 summary.
ASSUMPTION-1276/1277 are quoted from those artefacts, which were authored by an agent with the designer
present; no designer speech was read this run. The `.git/index.lock` sits above the connected-folder
mount and was not independently verified from this session.*
