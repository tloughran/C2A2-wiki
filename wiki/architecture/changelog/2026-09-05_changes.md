# Architectural Changes — 2026-09-05

*Agents 14a (assumption extraction) and 14b (presumption detection). Consecutive-day run; previous
changelog 2026-09-04.*

## Narrative

A Saturday with no attended session and a dead sync channel in both directions, so the day's material
is entirely what the scheduled agents said to each other. The lit-search pipeline ran cycle 6 on the
09-04 cohort and dispositioned all eleven items — five REVISE flags that it reduces to three rulings
for Tom — while declaring, at length, that nine of twelve subagent launches were interrupted and the
orchestrator wrote a third of the result files itself. Seven assumptions and six presumptions were
extracted, every one from agent output. Two findings are worth the walk: the 50-cell "stratified"
gold sample that three flags depend on works out to about two cells per stratum (PRESUMPTION-916,
challenged by arithmetic), and this pipeline has no way to say whether an item on its register was
something Tom believed or something an agent wrote down in his absence (PRESUMPTION-912 → OPEN-182).

## Items Produced

| Register | Added | Range |
|---|---|---|
| `assumptions.md` | 7 | ASSUMPTION-1263 – 1269 |
| `presumptions.md` | 6 | PRESUMPTION-912 – 917 |
| `open_questions.md` | 1 | OPEN-182 |
| `decisions.md` | 0 (dated run note appended) | — |
| `for_lit_search.md` | 3 items routed; 10 declared not-routed | — |
| `metrics/` | 1 snapshot | `2026-09-05_snapshot.md` |

Backups: `*.bak.20260905-pre-14eod` for assumptions, presumptions, open_questions, decisions,
for_lit_search.

## Changes Detected

**Registers moved by other agents today:** `revision_flags.md` REVISE-430–434; `monitor_queue.md`
MONITOR-591–596; `lit_search_returns.md` cycle-6 section (DISPOSITION-896–906, SYSTEMIC-RISK-FLAG);
`lit_search_results/{for,against}/` 22 files; `for_lit_search.md` 15c run note and inline tags on the
09-04 intake; `deferred/watch_list.md` (Agent 16, idle); `daily_sync/cowork_to_chat/2026-09-05_cowork_summary.md`
(delivery FAILED).

**No changes** to `wiki/agents/*.md` or `wiki/tools/*.py` (checked by mtime against the 09-04 changelog).

**Process change recommended, not made:** 15c asks that the next run use one subagent per direction
across all items rather than per item group (ASSUMPTION-1264). Recorded; not 14a's to adopt.

## Rationale Drift

None new. ASSUMPTION-1261's drift (gap as deferred vs. destroyed) went to MONITOR-594 with both search
directions written by one context and discounted to one reading.

## Accumulating Technical Debt

- **Decision register nine days static** while REVISE +5, MONITOR +6 today. The agent-fed registers
  grow; the designer-fed one does not (PRESUMPTION-912).
- **Premise count disagreement:** 153 `^PREMISE-NNN:` headers measured tonight vs. 157 carried by the
  09-04 snapshot and today's digest. Uninvestigated. PREMISE-191's pattern.
- **Multi-agent execution degraded** to one context for 8 of 22 files; the file names do not show it,
  only the PROVENANCE lines do (ASSUMPTION-1265; REVISE-426/427 data point).
- **Cycle-6 load-bearing citations unresolved** by anyone (ASSUMPTION-1268).
- **DARK tagging of 08-31…09-03** requested by MONITOR-594; not done tonight — folded into OPEN-182
  rather than acted on unilaterally.
- OPEN-168 day fourteen; OPEN-167 casing (eighth run); 15d lane 132 items (inherited count); carried.

## Self-Referential Observations

1. **The pipeline generated data about REVISE-426 by failing at it.** Whether adding agents adds
   epistemic value is an open revision flag; tonight the multi-agent layout collapsed to one context
   and the run filed that as a data point against itself (ASSUMPTION-1265).
2. **"Stratified" was a quality word, not a design.** Three flags rest on a measurement whose
   arithmetic no one ran. 14b ran it: 24 strata, ~2 cells each (PRESUMPTION-916). This is the
   sandbox-scale version of PREMISE-124 — a self-measurement whose design was itself unmeasured.
3. **A dead channel and a quiet day are indistinguishable from here** (PRESUMPTION-917). The digest
   wrote "no attended session ... the vault moved anyway." From Cowork's side that is all that can be
   seen. Fourteen days of it now.
4. **This run's own input is the question it raises.** Everything extracted tonight was said by an
   agent. The register has no field for that (PRESUMPTION-912). OPEN-182 asks whether it should.

## Requires Tom

1. **REVISE-433 → OPEN-179/181** — the ladder: total order / lattice / per-tradition. 15c says the
   table already answers (b) or (c); PRESUMPTION-915 asks whether this is a ruling or a reading.
2. **REVISE-432 sample design** — approve, but not as written: 50 cells over 24 strata cannot report
   per-stratum accuracy (PRESUMPTION-916). Coarser stratification or larger n.
3. **REVISE-434** — one paragraph of misplacement fault classes before the 16k-line read.
4. **OPEN-182** — on unattended days: extract from agent output, or file DARK and stop? Authorship tag?
5. **OPEN-168, day fourteen.** OPEN-174, OPEN-180 carried.
6. Premise count: 153 vs 157 — someone should count once and say which.

---

*Budget disclosure (Rule 6): this run exceeded the 4,000-token per-task budget — reading two agent
definitions, the prior changelog/snapshot, and four register tails before writing to six files.
Disclosed, not absorbed. Smaller than 09-04's overrun.*
