# 2026-08-12 — EOD self-awareness run (Agents 14a / 14b)
### Wednesday. 24 sessions. Tenth consecutive unattended day — but not quite: Tom was here for twenty-six minutes.

The day's one attended act left no transcript and no decision: three Start-Here tabs went into the voice
guide at T1 between 14:20 and 14:46, splitting the project's front door into a Doing lens and a Saying lens
— an argument about how C2A2 introduces itself, shipped as three manifest entries. Underneath it, the lit
pipeline turned on its own output and found the sharpest thing this register has produced: in six of
fourteen items the literature challenged not the hazard but the *remedy*, and each of those remedies adds an
instrument that, on the day it ships, is a check that has never failed. The remediation programme this
register implies is self-amplifying — and PREMISE-107 said so on 20 July and stopped nothing, because the
agents that mint register items do not read the register. Four extractor defects were found in one nightly
run, all returning plausible numbers, three of them making the vault look worse than it is — the exact
mirror of yesterday's four checks that could not fail, which means the tooling layer errs in both
directions inside twenty-four hours and no metric movement can currently be attributed to the corpus. The
same defect population was counted four times today and returned 166, 52, 45 and 102, and no run treated
the spread as the finding. A wrong `summa_ref` on thirteen contiguous files made frontmatter, commentary
and every static check mutually consistent and jointly wrong, which is why four prior reviews passed them.
Six reviewer queues stood 100% held, on their fifth through eleventh consecutive runs, none re-audited, on
a day when three separate discoveries would have invalidated holds. Three runs reported one file as both
fresh and four days stale and none reconciled it. Both sync channels were dark for a fourteenth day, in
opposite ways. **No new DECISION — thirty-eighth straight day. Two new OPEN, the first since 23 July.**

## Changes Detected

- **CHANGE-2026-08-12-001 — Tom was here for twenty-six minutes and the register cannot see it except by mtime.** `voice_guide/knowledge/{start_here,what_is_c2a2,what_is_saying}.default.md` at **14:46**, 3,017 / 3,686 / 4,077 B; `manifests.json` now **nine tabs, sociogram at T3 and eight at T1** [measured]. The documented path — "**one manifest entry + one knowledge file per tab**" — worked with no new tool, prompt or schema. **No DECISION logged**, and the evening run said so: "**if the T1 tab set is a commitment rather than an experiment, that's a gap to close**" (ASSUMPTION-998, 999; **PRESUMPTION-784, Medium, queued**).

- **CHANGE-2026-08-12-002 — THE REMEDY IS THE HAZARD. The day's material finding, and its object is this register.** SYSTEMIC-RISK-FLAG-1: "**in six of fourteen items the literature challenged not the hazard but the *implied remedy***, and each of those remedies adds an instrument that … **is a check that has never failed** — which is PRESUMPTION-768 restated one level up. **The remediation programme this batch implies is self-amplifying.**" Proposed fix, which changes the schema: "**a mandatory remedy-cost field, and no item grades above Moderate unless it can state how its instrument would be shown capable of failing**" (ASSUMPTION-1001; **OPEN-140**).

- **CHANGE-2026-08-12-003 — A VALIDATED PREMISE SAT IN THE REGISTER AND BOUND NOTHING, AND THE AGENTS THAT WRITE THE REGISTER DO NOT READ IT.** "**PREMISE-107 already said most of this — and did not prevent six such remedies in one batch.**" Two of the day's three process defects are instances: **DEFECT-F** — "**14a/14b do not read `validated_premises.md` before queueing**", six of fourteen items affected, PREMISE-110 having prescribed the exact remedy "**twenty-three days before four fail-open instruments were found in one day**" — and **DEFECT-G** — "**15a's charter never points it at the register**, so it reported PREMISE-140's content as potentially novel. **A novelty flag from an agent that can't see the validated premises isn't a novelty finding**" (ASSUMPTION-1002, 1003, 1004; **PRESUMPTION-781, Critical, queued**). **DEFECT-F is remediated for tonight only**: this run grepped the premise register before queueing, found four overlaps, narrowed four searches and withheld one item entirely.

- **CHANGE-2026-08-12-004 — The pipeline incorporated for the first time in four days, and declined half of what it incorporated.** DISPOSITION **666 → 680**; **1 INCORPORATE · 7 MONITOR · 6 REVISE (REVISE-317..322)**; **PREMISE-153** sourced to "**PRESUMPTION-776 (clause P1 only; clause P2 NOT incorporated)**" [measured]. "**Zero left searched-but-undispositioned.**" **103 files under `lit_search_results/` carry today's mtime (51 for, 52 against)** [measured] against the day's own claim of 28 — re-tagging, not searching, and unremarked (ASSUMPTION-1000). Also: **DEFECT-H**, "15a marked **65 citations `[unverified — from search snippet]`**; 15b marked 11 of 34", and **DEFECT-J**, "**the four Critical items of this intake reduce to fewer than four defects … Remediation must count one**" (ASSUMPTION-1005, 1006).

- **CHANGE-2026-08-12-005 — RULE 7 TWICE, AND ONE OF THEM NEEDS A SENTENCE, NOT A SEARCH.** "**ASSUMPTION-966's incompatibility claim is dispositioned CONDITIONAL on a review model nobody has stated; REVISE-315 asserts it as established**" — "**Serial → the incompatibility is arithmetic and INCORPORATEs. Sampled or exception-based → it dissolves. No further search will settle it**" (now **OPEN-141**). And: "**ASSUMPTION-968's +80%/−70% figures are misquoted … Read correctly the source argues FOR C2A2's current centralised topology. REVISE-315 needs amendment by 14a**" — **assigned to this agent and not performed tonight, declared at ASSUMPTION-1035** (ASSUMPTION-1008).

- **CHANGE-2026-08-12-006 — FOUR EXTRACTOR DEFECTS, ALL PLAUSIBLE, THREE MAKING THE VAULT LOOK WORSE THAN IT IS.** "**All four returned plausible numbers; three were caught only by reading source lines by hand.**" One "read disclaimer clauses as citations"; another was "**structurally unable to see the only dangling path**" it existed to find. Yesterday's four checks failed open; today's four fail closed. **The instrument layer errs in both directions inside one day, and each day's proposed remedy is directional** (ASSUMPTION-1011; **PRESUMPTION-786, High, queued**).

- **CHANGE-2026-08-12-007 — The headline metric improved by two thirds and the run refused to call it repair.** "**The A1 list shrank from 8 days to 1, and that is a measurement correction, not repairs**", on the premise that "**citing a restated id is not the same as asserting its withdrawn content**." Measured: **307 files, 12 drift items (34 on 08-11), 25 violations, 107 pairing candidates.** Also, second night running: "**The task spec's step-3 mtime rule is not executable against this mount**" — a unilateral method substitution, disclosed, not escalated (ASSUMPTION-1009, 1010).

- **CHANGE-2026-08-12-008 — THE SAME POPULATION WAS COUNTED FOUR TIMES TODAY AND RETURNED 166, 52, 45 AND 102.** Each run corrected the last; none reported that a defect population had moved by a factor of three in a day. The 10th sweep put it plainly — "**The band list we've been working is the wrong queue**" — and the 8th generalised only as far as "**Any prior vault-wide census built that way is suspect**" (ASSUMPTION-1012; **PRESUMPTION-778, High, queued**).

- **CHANGE-2026-08-12-009 — The hypothesis the corpus had been repairing against was killed: "Header absence isn't the cause."** **245/307 (80%) transcripts lack `**Article N**` headers; 151 of those still anchor articles** — "**the ASR announces every article in prose regardless.**" So "**A numbers-only sweep across the 102 would leave those gaps in place**", and from the other direction, "**a header count returns 0 and falsely clears the commentary**" (ASSUMPTION-1013).

- **CHANGE-2026-08-12-010 — A WRONG UPSTREAM KEY MADE THIRTEEN FILES SELF-CONSISTENTLY WRONG, AND THAT IS WHY FOUR REVIEWS PASSED THEM.** "**A wrong `summa_ref` makes everything downstream self-consistent.**" Days 241–257 plus 118 and 282; two failure directions in one band, hence "**No sweep is safe. The direction alternates.**" Blocked on a decision with a stated blast radius: "**correcting 13 files unattended changes what the Summa explorer publishes … It needs one decision covering both halves**" (ASSUMPTION-1014; **PRESUMPTION-782, High, queued**).

- **CHANGE-2026-08-12-011 — Every fidelity check in the fleet failed today for the same non-reason, and four runs diagnosed it independently.** "**All 23 pairs reported `fidelity: fail`, but the reason was a cold `/tmp` cache, not a transcript problem.**" Two runs refetched live rather than declining the frame — "**live fetch confirms the recorded numbers within 1%**" — and one hand-computed 5-gram overlap at **0.89–0.96**. A check that returns FAIL for every input carries no information; nobody filed it (ASSUMPTION-1015).

- **CHANGE-2026-08-12-012 — A vault-wide census returned 121 false positives and was caught only by coincidence.** The extractor "split on first `---`"; "caught only because **Day 208 was known-good**" — repaired two hours earlier by a sibling. Corrected: **242 of 307 top frontmatter, 65 footer** — the reverse of what `Summa.md` documents. Self-corrected mid-run: "**My note claimed 7 ids, but the body also cites Wright PRS-04**" (ASSUMPTION-1016).

- **CHANGE-2026-08-12-013 — A stored memory was found false and corrected; a second was found stale and could not be.** "**A recorded memory is wrong and I've corrected it** … **False here.**" And: "**The recorded rule that MacIntyre citations are form-check-only pending you is stale**" — `traditions/macintyre/` now exists — so "**every prior day whose MacIntyre bullet was left form-checked is now upgradable**", logged only, because the agent has no write path to the rule (ASSUMPTION-1017).

- **CHANGE-2026-08-12-014 — The id-level sweep was declined five times by five runs that do not read one another.** "**This must not be swept at id level**"; "**sweeping the bundle would have broken a correct citation to fix a wrong one**"; "**A sweep would have corrupted half the band**"; "**a cheaply-audited sixth day is how a wrong sweep gets authorized**"; "**I stopped at 2 of a permitted 6 rather than degrade the body-level ID checking that is the only thing these reviews do that the static checks don't**" (ASSUMPTION-1018; **PRESUMPTION-783, Medium, queued**).

- **CHANGE-2026-08-12-015 — The two-id-system conflict was routed around per-citation for a second day and still not escalated.** "'reference frames' is **PRS-05 in `wiki.md` and PRS-04 in `prs_triplets.md`**"; "**Master layer and tradition layer disagree**"; "`cross_program_index.md` **stops at CROSS-054**". Stated blast radius: **29 syntheses** (ASSUMPTION-1019).

- **CHANGE-2026-08-12-016 — A third of one run's budget went on pairs a sibling pass-marked yesterday, and the run named the trap.** "**Reading them before checking the log for those specific day numbers is the duplicate-runs trap.**" Second consecutive day of fleet contention; the mitigation is still a convention, not a lock (ASSUMPTION-1020).

- **CHANGE-2026-08-12-017 — SIX QUEUES 100% HELD, ON THEIR FIFTH THROUGH ELEVENTH RUNS, NONE RE-AUDITED — ON A DAY THREE FINDINGS WOULD HAVE INVALIDATED HOLDS.** "**None re-audited, none pass-marked to clear**"; "**all previously escalated, not re-audited**"; "**11 of the 19 are open escalations already sitting with you … and re-queue by construction**." The `held` outcome is a decision candidate for a **fifth** day (ASSUMPTION-1021; **PRESUMPTION-779, High, queued**).

- **CHANGE-2026-08-12-018 — The morning handoff named the binding constraint and briefed into silence for a fourteenth day.** "**The review gate is the binding constraint** … **Nothing ingests until that moves.**" Walk notes **NO**; decisions **0**; **pending 10 → 26 overnight**; **no `[C2A2-review-decision]` email since 08-07**; `Status:` unpopulated since **2026-05-17**; `queue.md` untouched since **2026-05-13**. Measured tonight: **pending 26 · approved 301 · denied 1** [measured] (ASSUMPTION-1022).

- **CHANGE-2026-08-12-019 — Fourteenth dark day, both directions, and today the two failures had different causes.** Inbound 08:53: "`/login?from=logout`", "**Signing in is a credential action I won't perform autonomously**", **1,077 B**. Outbound 18:41: "**the extension was not reachable on three consecutive attempts**", **8,589 B** [measured]. Both wrote dated failure artefacts; the consumer downstream saw only "**Walk notes: NO**" (ASSUMPTION-1023; **PRESUMPTION-785, Medium, queued**).

- **CHANGE-2026-08-12-020 — ONE FILE WAS REPORTED AS FRESH AND AS FOUR DAYS STALE ON THE SAME MORNING BY THREE RUNS, AND NONE RECONCILED IT.** "**snapshot age 0.00h; lag −22.84h**" beside "`metabolism_data.json` **generated 2026-08-08 09:59Z, 4.0 days ago**" and "**the snapshot hasn't actually been written in four days even though the task reports success**." The economical reading — stated by nobody — is that the gate asserts on the temp snapshot, not the published file (ASSUMPTION-1024; **PRESUMPTION-780, High, queued**).

- **CHANGE-2026-08-12-021 — Scheduler 79 OK / 1 WARN / 2 FAIL, third consecutive day, and no threshold exists at which that changes anything.** `com.c2a2.metabolism-publish` "**exit 128**". The check's own charter on itself: "**blind three ways for months while reporting 'all clear' … N=1**." Alongside: "**Daily-run commits failed 08-09 through 08-11. Today's run committed fine**" against "**Phase 6 (Commit/push): BLOCKED — sandbox cannot write `.git` objects**" — two different commit paths, unreconciled by either run (ASSUMPTION-1025).

- **CHANGE-2026-08-12-022 — Telemetry frontier jumped to 23.3h after four days at ~1.5h; the run published and supplied the condition under which it should not be trusted.** "**Inside the 36h guard, so I refreshed per the task spec — but this is the first day in five that the frontier isn't ~1.5h old**"; "**the runtime stopped writing around 2026-08-11T12:26Z while a process still holds the DB open**"; "**If tomorrow's frontier is again 2026-08-11, the runtime is down and the feeds are republishing near-duplicate telemetry.**" **33 agents · 27 agent_nodes · DB 4.20 GiB · +4,409 events.** Disclosed: "**the sandbox can't delete files from the outputs mount, so I truncated the 4.5GB snapshot to 0 bytes**" (ASSUMPTION-1026).

- **CHANGE-2026-08-12-023 — A backlog of 177 evaporated under a corrected key, for the second time in five days.** "**A naive basename diff reported 177 unprocessed files; matching on the stem the log actually keys on returned 0.**" "**The 2026-08-11 fix is holding.**" Also: "**Three traditions produced zero proposals, correctly**"; "**two topically-plausible arXiv IDs were deliberately left unwritten as leads**"; and, unfixed, "**~474 orphaned `tmp_obj_*` files**" left by its own `git add`, arXiv **HTTP 429** on every attempt. `review/2026-08-12_review.html` **342,911 B at 04:47**, against 141,901 B yesterday — **2.4× for 26 cards against 10** [measured] (ASSUMPTION-1027).

- **CHANGE-2026-08-12-024 — The Summa series was declared finished and recommended for retirement a sixth time, on existence evidence, on a day six runs measured the contents.** "**307/307 pairs on disk**", "**0 gaps**", "**this scheduled task can be retired**" — against a wrong `summa_ref` on thirteen files, a live defect population of 102, 12 drift items, and 80% of transcripts without article headers. **Plan target 308 unreconciled; deadline six weeks past** (ASSUMPTION-1028).

- **CHANGE-2026-08-12-025 — A predicted loss did not happen, and Agent 16 gave the reason instead of claiming the save.** "**The predicted silent loss of the 08-08/08-09 dispositions did not happen — no decisions file was ever written from the defective page**", and on the older case, "**incidental loss remains the better-fitting reading.**" Still on disk: `review/2026-08-10_review.html` **121,696 B**; `deferred/watch_list.md` **389,173 B at 21:00**, up from 380 KB, unarchived; **WATCH-002/003 both at check count 4, next due 08-18** [measured] (ASSUMPTION-1029).

- **CHANGE-2026-08-12-026 — The Wednesday agents left a solution line UNKNOWN on purpose.** "**the topic list attests that Kastrup discussed *the limits of Analytic Idealism* but says nothing about what he said**"; the cross-tradition reading offered and hedged in one breath — "**the attribution is the session author's reading, not a quotation of either man**"; "**neither wiki should adjudicate it alone**"; "**a placeholder proposed a third time is still a placeholder**." Disclosed: the skill file's **wiki root path is wrong (missing `Projects/`)**; budget **3× over** (ASSUMPTION-1030).

- **CHANGE-2026-08-12-027 — REFLEXIVE: the uncommitted-path count could not be measured tonight, and this run recorded a gap rather than a zero.** `git rev-parse` from this sandbox returns "**fatal: not a git repository (or any parent up to mount point)**" [measured]. 08-11's figure of 191 paths could be neither confirmed nor updated. **Any number reported would have been fabricated** (ASSUMPTION-1031).

- **CHANGE-2026-08-12-028 — REFLEXIVE: two narrated figures were checked against disk and did not match.** The evening summary's "28 result files" against **103 with today's mtime**, and its placement of `SYSTEMIC-RISK-FLAG_2026-08-12.md` at the tree root against its actual home in `against/` [measured]. Neither is material. Both are recorded because the checking is the point, and because PRESUMPTION-777 — dispositioned REVISE this morning — says the checking is what this layer cannot generally do (ASSUMPTION-1034).

## Decisions

**No new DECISION. Thirty-eighth consecutive day.** `decisions.md` unchanged at max ID **78 (2026-07-05)**
[measured]. **Fourteen candidates stand, two new today**: the **`summa_ref` two-half repair** — thirteen
contiguous files whose correction changes what the explorer publishes and which "needs one decision covering
both halves" — and the **remedy-cost field**, 15b's proposed schema change, now filed as **OPEN-140**.
Carried: the **`held` outcome for `qc_sweep.py mark`** (fifth day, now the named cause of six simultaneous
100%-held queues); the **lock or lease for concurrent vault writers** (second day, second instance today);
**Request A**; the **Mac-side git unblock**; the **Levin↔Friston bridge rephrasing**; the `.days == 7` → `>=`
fix; the **`length_tier` call** (nine weeks); the **canonical-file / two-id-system decision**, now with a
measured blast radius of 29 syntheses; **retire-or-repurpose `summa-2026-daily-batch`** (sixth
recommendation) and the bootstrap audit; the 14 alias files; the control arm; the in-house measurement
authorisation. Enumerated at ASSUMPTION-1032.

## Open Questions

**TWO NEW OPEN — the first since 2026-07-23.** **OPEN-140**: should the register carry a mandatory
remedy-cost field, with no item grading above Moderate unless it can state how its own instrument would be
shown capable of failing? **OPEN-141**: in ASSUMPTION-017, does "humans validate everything" mean every
artefact or every category at least once, and is review serial, sampled, or exception-based? One sentence
closes 141 and it is the only thing that can — the pipeline has stated that no further search will settle
it. **OPEN-138 and OPEN-139 remain OPEN — awaiting Tom for a twentieth day.**

## Positives, recorded neutrally

The lit pipeline **turned its instrument on its own output** and found that six of its own remedies were
the thing the literature challenged; **incorporated one clause of a premise and explicitly refused the
other**; **named three defects in its own charter, two of them in the charter of the agents that feed it**;
and **declared a 144-item backlog it was scoped away from rather than letting the count imply coverage**.
The nightly verifier **found four defects in its own extractors, said three of them had made the vault look
worse than it is, and refused to call the resulting improvement a repair**. Five reviewers **declined an
id-level sweep for five different correct reasons**, two of them **stopping short of their permitted quota
to protect the checking that only they do**. One run **corrected a stored memory it found false**; another
**reported a stale rule it had no power to fix rather than working around it silently**; another **declined
to report a drift it could not distinguish from its own tooling**. One **caught its own false-positive
census only because a coincidence supplied a known-good day, and said so**. Agent 16 **reported a
prediction of its own as falsified, with the mechanism**, and **declined to delete a file outside its
charter**. Telemetry **published inside its guard and supplied tomorrow's disconfirming test in the same
paragraph**. Both sync runs **refused to fabricate**, and one **refused a credential action for a
fourteenth day**. This run **applied DEFECT-F's fix to itself before queueing, withheld one item because a
validated premise already covered it, recorded a measurement gap rather than a zero, and declared an
assigned amendment as not performed**. At least seven runs surfaced budget breaches, **including this one**.
