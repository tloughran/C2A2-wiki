# Architectural changes — 2026-09-17

## Narrative

One day, fifteen sessions read of about twenty-eight, no designer speech — the eighteenth such day. The
window's principal finding is small and exact: **the daily run wrote a wrong ordinal into the decision
draft, saw it, and could not correct it**, because the permission tier that allows an unattended run to
create a draft does not allow it to edit one; the disclosure went to a log nobody reads and the error goes
to Tom. Nobody argued the tier; it is the one item routed tonight. Beside it: **two mtime restamps in one
day** (59 files at 15:55, 9 at 22:00) on top of 09-16's one — and this pass, for once, ran the diff it had
been presuming: the five registers are byte-identical to their 09-16 backups over the backup's length.
Phase 6 unblocked and the commit landed at 05:45, an hour before the artefacts it was meant to carry. The
scheduler headline moved for the first time in four days and no reader saw it move. The chat scrape counted
"third day" for a channel whose every file since June is a failure note. The lit-search lane was still in
flight at midnight with one 15a file, no 15b file, and two `Agent` spawns as its last entries — either the
first structurally separate 15a/15b in seven cycles or the second silent death in three days. And this
register's own arithmetic was wrong twice on 09-16 (the lane is 139, not 129; the line is 5728, not 5725)
and is corrected here.

## Registers

| File | Before | After | Δ |
|---|---|---|---|
| `assumptions.md` | max 1479 | max **1508** | **+29** (1480–1508) |
| `presumptions.md` | max 1022 | max **1033** | **+11** (1023–1033) |
| `decisions.md` | DECISION-083 | DECISION-083 | 0 — index note appended; five agent-made rules recorded, not minted |
| `open_questions.md` | max OPEN-230 | max **OPEN-236** | **+6** (231–236) |
| `for_lit_search.md` | — | — | **+1 routed** (PRESUMPTION-1024), 5 withdrawn at pre-check with commands, 5 held with tests, 29 assumptions held in-house |
| `metrics/` | 2026-09-16 | **2026-09-17** | snapshot written |

Registers snapshotted as `*.bak.20260917-pre-14eod` before any append; the 09-16 backups were then diffed
against the live files' prefixes (identical — PRESUMPTION-1031). Every entry carries a `Source:` line;
26 of 29 assumptions verified at source in whole or part; all 11 presumptions pre-checked with command and
return.

## Changes detected

**Creation permitted, correction denied (ASSUMPTION-1485; PRESUMPTION-1024; OPEN-233).** "Not
re-attempted this run — recorded as such rather than as a seventh failure" in PROCESSED_LOG; "7th failed
attempt" in the Gmail draft; `update_draft` auto-declined; "Noting here so the draft's error does not
propagate into the trail." The first draft is the delivered draft. Routed: is the gate-modify-not-create
pattern recognised, and what are its remedies? Policy question for Tom: leave, discard, or supersede?

**Two restamps a day, registers verified intact (ASSUMPTION-1480; PRESUMPTION-1031; OPEN-231).** 59 files
at 15:55:36–37 (birthtime = mtime on the Mac), including every register this pipeline writes, the 15a
result file and the day's proposal; 9 at 22:00:07 including the master wiki. Both processes unnamed. The
five registers' prefixes `cmp` identical to the 09-16 backups — the 15:55 process touches, for those five.
The remaining 54 files were not diffed and are marked UNCALIBRATED. Every mtime-based reading today
(OpenStory's "still the 09-16 22:00 copy," Summa's "0 drift," the scheduler's "newest mtime names the
slot," metabolism's "alive, mtime 05:17") read an instrument overwritten twice.

**The commit raced the artefacts and won (ASSUMPTION-1486, -1489; PRESUMPTION-1030; OPEN-234).** Phase 6
"deferred to `commit_daily_run.sh` (05:45, Mac-side)"; scheduler "committed 09:45Z"; review page 06:39
EDT, L2 stream 06:40, draft 10:40Z after a ~32-call Phase 2. Same shape at the specialist boundary: the
SKILL says "7am before this task"; the specialist fired 03:04, the daily run 04:34, the Stump card landed
05:19, and "Stump skipped" held only because Phase 2 was slow. Two clocks where the design assumes an order.

**Scheduler headline moved, unseen (ASSUMPTION-1496, -1497).** 87/1/4 → 87/2/3; OK conserved; one FAIL
became a WARN (git debris, lock cleared, 6 `tmp_obj_*` remain); no row diff — the reader's SKILL forbids
it. `select max(timestamp) from events`: seventh night named, run by none of fifteen sessions; two runs
answered the liveness question from mtime instead.

**Four counters, one streak (ASSUMPTION-1492, -1488, -1506; PRESUMPTION-1026, -1033; OPEN-236).** The
scrape: "third day running," "last successful sync is 09-15" — that file's header is FAILED; every
`chat_to_cowork` file since 06-20 is a failure note (114 of 125 carry the string; the other 17 are
differently-worded failures; last non-failure 06-19). Agent 16: fourteenth; evening sync: fifteenth.
Zero-ingest "seventh" rests on a master-wiki line that no longer exists. Summa's ordinal went 3, 5, 5, 7.
The metabolism run gave none; this register supplies "eleventh" — one more counter with one more owner.

**"No issues to report" (ASSUMPTION-1493).** The morning project status, in the paragraph that reports a
heartbeat disabled since June, a wrong path in its own SKILL, an unreadable folder, "thirty-one" enabled
tasks (34), and "all daily ones fired" (the scrape fired and produced nothing). PREMISE-109 names this
summary by name. CHALLENGED under a premise INCORPORATED for it.

**The 30-day window has no home (ASSUMPTION-1501; OPEN-232).** Stump: a 2018 argument reprinted 2025,
"fails the 30-day window and is proposed under the 'significant work not yet captured' clause." Third
redefinition in three specialist runs. No file under the mount states the clause; the SKILL says 60.

**OpenStory, day three (ASSUMPTION-1499; PRESUMPTION-1029).** Diagnosis confirmed at the line (`.strip()`
on a list, `resolve_ref` l.134); one-line fix named, not applied. Two claims about `REFRESH_STATUS.md`
("written with the FAIL line"; "the Mac-side run did not update it") both false at source: the file holds
the Mac wrapper's line, day three, never the sandbox's. The run did not read the file it reasoned about.

**Metabolism, eleventh (ASSUMPTION-1498).** Both prior blockers hit in sequence; 6.31 GiB against 6.0 GB
free; `tempfile` silently falls back to `/tmp`; "structurally unable to succeed from the sandbox … a
method decision for you." The first run in the series to state the constraint as a constraint and to
decline its own workaround for a reason. DB 6.57 → 6.63 → 6.70 → 6.78 GB.

**The lit-search lane at midnight (ASSUMPTION-1481, -1482, -1483, -1484; PRESUMPTION-1023; OPEN-235).** One
15a file ("PARTIALLY-SUPPORTED," novelty flag not raised), two 09-13 re-trigger cycle-1 files (not the
items due today), no 15b file, no run note, two `Agent` spawns as the last entries. Cycle-suffixed paths —
"proposed, not applied" on 09-16 — applied; and `_cycle5`/`_cycle7` files pre-existed, so 1442's "every
prior re-trigger overwrote" was partly false. The untagged lane is 139 (129 + 2 of 08-30 + 8 of 09-13),
not 129. The subagents' "Independence attestation" is self-written, same-family — PREMISE-004 already
says what that is worth.

**Content drift caught by a specialist (ASSUMPTION-1502; PRESUMPTION-1032).** "Recommend CROSS-008 be
restated as form/levels rather than final causality … three sources deep without Stump saying it." A
proposal-only agent auditing the wiki against its primary source — this register's job, done for content
— and filing it as a card because the channel has no RESTATE verb. Recorded, not routed (out of scope).

**Known, unread, uncarded (ASSUMPTION-1490; PRESUMPTION-1025).** The Levin bioRxiv: fetch refused, browser
declined, "a title-only card is the Wright mistake," no card, no watch, no ledger row. The rule is right;
what it leaves has no channel.

## Open questions raised

- **OPEN-231** — what restamped 59 files at 15:55 and 9 at 22:00, and are they one process or two? **Needs the Mac.**
- **OPEN-232** — where is "the 30-day window" written?
- **OPEN-233** — when a permitted write errs and the correction is unpermitted: leave, discard, or supersede? **Needs Tom.**
- **OPEN-234** — is the 05:45 commit ordered after the daily run's artefacts, or does it race them?
- **OPEN-235** — did the 09-17 lit-search run complete?
- **OPEN-236** — which figure is the Chat→Cowork streak: 3, 14, 15, or ~90 — and what does it count?

OPEN-233 joins OPEN-225, OPEN-217 and OPEN-208 as items that need Tom and not a search. Four of the six are
one command each and the command is written in the entry.

## Routed to 15a/15b

**PRESUMPTION-1024** (creation permitted, correction denied — the tier nobody argued; pre-check command
and return recorded; no covering premise). **Five withdrawn at pre-check** on PREMISE-004, -109, -107,
-006/OPEN-223 and -124 (the last then tested in-house and discharged). **Five held** with no covering
premise and a named test. **No 14a item routed** — all twenty-nine are in-house measurements. One routed
of forty produced.

## Self-referential note

Four entries bear on this register. **ASSUMPTION-1483 and -1484** correct 09-16's lane arithmetic (139,
not 129; `_cycleN` files pre-existed). **ASSUMPTION-1503** corrects a line citation (5728, not 5725).
**PRESUMPTION-1031** was surfaced against this register's own restamp entries and then tested in the same
pass — the one presumption tonight that did not survive to the queue. **ASSUMPTION-1507 / metrics note 9**
record that this pass breached Rule 6 by a wide margin, declared it, and — like every prior pass —
proposed no number; and surface why: 14a's definition forbids design recommendations, Rule 6 asks for one.
That is a conflict between two governing texts, reported as such (Rule 7) rather than resolved by this
agent. The cost is stated so the number can be set against it.

## Coverage gap declared

About twenty-eight sessions reachable in `list_sessions` since the 09-16 run. **Fifteen read:** eight C2A2
core (walk handoff, chat scrape, project status, daily run, Stump/Fredrickson, Agent 16, evening sync,
lit-search pipeline — in flight), five infrastructure (scheduler, system health, metabolism, OpenStory,
Supabase), two Summa (nightly verification, daily batch). **Not read:** ~11 Summa qc-sweep /
commentary-reviewer repeats; the evening sync's digest of them was read and is not treated as a
substitute. Transcripts expose assistant prose and tool-call names only — tool inputs and outputs are
elided — so every "verified" is a check on the mount, not on the call. This pass ran from 23:38 on 09-17
and wrote past midnight; all items are dated 2026-09-17. Three intake readers were spawned in parallel;
all three returned.
