# 2026-08-08 — EOD self-awareness run (Agents 14a / 14b)
### Covers a two-day block: 2026-08-07 and 2026-08-08

The day the series broke, and the break was found by accident. The 08-07 EOD run fired, produced one
line of text and six tool calls, and died at `[Request interrupted by user]` having written nothing —
the first missing day in 118. No alarm fired on the absence; the 08-08 lit pipeline noticed a missing
intake *header* while counting a queue and correctly reported that the cause "can't be determined from
inside the vault." It could be, and tonight it was, by reading a session transcript — an artefact
outside the producing agents' write set. That is REVISE-283's evaluator condition satisfied in
practice, for the first time, unnamed by anyone. Against that: two agents gated ingestion on unread
primary sources, a detector criterion was retracted as invalid rather than merely wrong, a cohort
hypothesis arrived with the range it predicts, and a self-correction arrived with its own blast-radius
check attached. No new DECISION (thirty-fourth straight day), no new OPEN, and no attended session for
the sixth consecutive day.

## Changes Detected

- **CHANGE-2026-08-08-001 — The daily series broke and nothing fired on the absence.** Session `local_095f1c55` in full: one text line, six tool calls, `[Request interrupted by user]`. No `2026-08-07_changes.md`, no `2026-08-07_snapshot.md`, no `## 2026-08-07` intake header. Third stall in five days to end at that marker, at a third distinct point in the run. The morning instruments the next day reported the pipeline healthy. There is no signal in this system that fires when a scheduled self-audit produces no record — which is exactly the shape of 15b's 08-06 Critical flag ("no signal that fires on the *absence* of verification"), now landed on the self-awareness pipeline itself (ASSUMPTION-808; PRESUMPTION-717, **High**, queued).

- **CHANGE-2026-08-08-002 — A limit was stated correctly and treated as terminal.** "**There was no 14a/14b intake**... Whether the upstream pipeline didn't fire, fired empty, or fired and failed to write **can't be determined from inside the vault.**" Recorded as a POSITIVE: the agent named the boundary of its evidence base rather than guessing. The presumption underneath is not that the vault is incomplete but that **the vault is the world** — when it runs out, the question closes rather than routing to a different evidence type. Tonight's run crossed the boundary in one call (ASSUMPTION-809; PRESUMPTION-718, **High**, queued).

- **CHANGE-2026-08-08-003 — The lit pipeline ran on backlog and reported that the backlog is mis-routed.** "**I did not exit null, because the queue was not empty** — 260 items stood `[QUEUED]` unsearched, the oldest starved 34 days." 5 drawn, 3 REVISE (292/293/294), 2 MONITOR, **0 INCORPORATE**; backlog 260 -> **255**; "a fill rate that has exceeded drain for 31 days." Its own headline: "**all five items name a *measurement on C2A2's own output* as their disposition-changing condition — none needs literature, none has been run in 117 days. Weekly re-triggering cannot move them.**" If that generalises, thirty-one days of drain-rate reporting has been measuring the wrong constraint (ASSUMPTION-810, 812; PRESUMPTION-733, **High**, queued).

- **CHANGE-2026-08-08-004 — Independence was challenged from inside, unprompted, for the first time.** "**15a and 15b ran in one model context on a batch about independence — this run is an instance of the defect it reports.**" On 08-06 this challenge came from 15c after the fact; today the run made it against itself before anyone asked. Also self-caught: "my first queue-depth count was wrong (234/229; actually 260/255): **a single-format parser missed 26 inline-style entries**" — the fifth PRESUMPTION-687-class failure in four days, with a sixth later the same day (ASSUMPTION-811, 813, 851).

- **CHANGE-2026-08-08-005 — One bug, two runs, opposite blast radius, neither citing the other.** Monitor: "Explicit set intersection this run against the 47 real pending proposal_ids: **zero matches**. **A review pass run today discards all 47 decisions.**" Wiki run: "Today's 47 were **saved only by being uniformly APPROVE**... **A mixed approve/deny batch would mis-route silently.**" Both are same-day, same agent family, same `generate_review_page.py` line 304, thirteenth-plus day named, blast radius now 47. Reported without reconciliation, per Rule 7; nothing in the system is charged with resolving it (ASSUMPTION-816, 817; PRESUMPTION-719).

- **CHANGE-2026-08-08-006 — The backlog metric had been reading clean because its input pipe was disconnected.** "**prior runs were reporting it wrong.** 172 inbox files are unprocessed, some from April. **Earlier runs said \"inbox clear — 0 unprocessed,\" which was true only because the approvals hadn't been copied in yet.**" Phase 1 ingested 4 of 47; "at 4 ingests/day this does not converge." Same failure shape as the morning all-clear class, one layer down (ASSUMPTION-818, 819).

- **CHANGE-2026-08-08-007 — Fabricated dates were caught, generalised correctly, and left without an instrument.** "**Two search results carried fabricated dates** — a 2023 Hoffman podcast returned as June 2026, a 2021 Hawkins episode likewise. Both caught by verification before anything was written. **Worth distrusting search date attributions structurally, not just this once.**" The generalisation is right; nothing counts how many prior date attributions were never spot-checked, and the recency filter that gates ingestion keys on exactly the fabricated field (ASSUMPTION-820; PRESUMPTION-720, **High**, queued).

- **CHANGE-2026-08-08-008 — Two agents gated ingestion on unread primary sources, and one refused an item that passed its own filter.** Wolfram: "the YouTube page returned no description or transcript to me"; "**no claim Wolfram actually made in the talk is verified**"; both candidates Speculative "with an explicit warning not to let those labels drift up without someone watching the video." And the refusal: a qualifying item declined because it is "a personal memoir with no PRS content — extracting triplets would be **empty and in poor taste**." Second consecutive day PRESUMPTION-695's class has been answered by behaviour, by a second agent, neither citing it (ASSUMPTION-824, 826).

- **CHANGE-2026-08-08-009 — The first external venue at which three tracked traditions are co-present.** "**MC0001** (first machine-consciousness conference, CIMC/Berkeley, May 29-31 2026) put **THREE C2A2 traditions on one program**: Wolfram, Friston, and Levin." CROSS-049 flagged as highest-value because it "would be **the first source where those two programs are in even indirect contact rather than joined by our own inference**" — the sharpest statement yet of what the cross-connection register is and is not evidence of, and a candidate criterion for auditing all ninety (ASSUMPTION-823, 825 — 825 queued).

- **CHANGE-2026-08-08-010 — A watchdog and a task both reported correctly and contradicted each other.** Regen: "**Result: SUCCESS**... snapshot age 0.01h" — with "the exact command as written **FAILS** in the sandbox," root-caused to `_DB_CANDIDATES` line 69, and "**That is a workaround, not a fix.**" Scheduler: "failed its output check for **three consecutive mornings** (1.6 -> 2.6 -> 3.6 days) — **reporting runs but not writing** `metabolism_data.json`... **a real stall, not a one-off.**" Both true. The joint reading — succeeded via manual override, deliberately no commit — lives in a clause no watchdog can read (ASSUMPTION-827, 828; PRESUMPTION-724).

- **CHANGE-2026-08-08-011 — Two blockers named as hard two days ago dissolved with no fix logged.** The 4.1 GB backup "now fits because `TMPDIR` is `/sessions/.../tmp` (7.1 GB free)"; whole-file `quick_check` completed on the 3.83 GB DB — the same check reported on 08-06 as exceeding the 45-second wall and explicitly not run. On 08-06 a run corrected its own stored root cause to name that wall. The system has no representation for "currently not biting" as distinct from "repaired" (ASSUMPTION-829; PRESUMPTION-721, queued).

- **CHANGE-2026-08-08-012 — The all-clear and its refutation appeared in the same report.** "The C2A2 wiki pipeline is **healthy**: last night's overnight agents all completed" — and, same summary: "The metabolism data refresh **reports success every morning but hasn't actually written a new file since August fourth**... **it's failing silently.**" Sixth day of this class and the first in which the correction travels with the claim. Unreconciled; the headline is what a reader carries away. Separately, two morning instruments reported the janitor in incommensurable terms — "2 `reindexer_freshness` + 7 `src_pub_refs_drift`" against "**a hundred twenty-six broken wikilinks**" (ASSUMPTION-832, 833).

- **CHANGE-2026-08-08-013 — A detector criterion was retracted, not just a verdict.** "**A prior escalation was wrong, and I've corrected it.** The 2026-08-01 run flagged Day 106's transcript for a fabricated citation... **That test was invalid.** It's a **licensed proper-noun restoration, not fabrication.**" The retraction names the *test*. Every prior escalation produced by the zero-occurrence-in-ASR criterion inherits it; no run sweeps for them. Same shape as 08-06's 88 phantom drift items, also never retracted from wherever they landed (ASSUMPTION-840; PRESUMPTION-725, **High**, queued).

- **CHANGE-2026-08-08-014 — Third consecutive day a determinate repair arrived paired with a warning against generalising it.** "Days 213/214/215 cite Stump as `P5` and `P14-P15` — in the frontmatter *and* the body. Those aren't ids; they're the **`Label:` field prefix**" — and immediately: "**Do not sweep this vault-wide**: `P4` is also Friston's PRS-04 label and `P1` is Hoffman's PRS-01." Elsewhere: "**Friston PRS-04 is correct on Day 252** — the third sighting this week, after two were repaired as mis-glossed. **A rename table built from those two failures would have corrupted this one.**" Three independent warnings in three days is not three cautions; it is a measurement that **the id space is not a namespace** — while a vault-wide sweep stands authorised-pending (ASSUMPTION-841, 846; PRESUMPTION-726, **High**, queued).

- **CHANGE-2026-08-08-015 — A falsifiable prediction, with its test range named.** "It's live on **231-235 — five consecutive days, seven traditions**." "**The defect tracks when a pair was written, not which tradition it cites** — which **predicts 236-240**." Repair: "**24 tokens renamed, zero arguments moved.**" Recorded as a POSITIVE: in this series causal hypotheses have usually been offered as "the cheapest place to look first." Days 236-240 are separately recorded as un-worked and unclaimed. A near-miss in the same run would have defeated its own detector: "`grep -v \"PRS-\"`... **hid 12 of 24 tokens** and would have shipped **a half-repair that greps clean under the old detector**" (ASSUMPTION-842, 843).

- **CHANGE-2026-08-08-016 — The corpus moved under the citations, measured twice independently.** Nightly verification: commit `712beee` "added **16 PRS ids**... **purely additive, so no existing citation was invalidated, but 128 days now predate a tradition file they cite.**" QC tier, same night: "**Levin PRS-01 was carrying a turnover claim it doesn't state** — that claim's real home is **PRS-52, added 2026-07-21**... **A correctly-unanchored claim became anchorable because the wiki grew.**" No register entry carries the corpus state it was judged against, so no verdict can be replayed and no clean pass expires (ASSUMPTION-838, 845; PRESUMPTION-727, **High**, queued).

- **CHANGE-2026-08-08-017 — The highest fidelity scores in the log came from the batch with four undetectable defects.** "Overlap came back at **97.9/98.2/99.0% 3-gram — the highest in this log**"; and "**all four defects found are invisible to every detector currently in use.**" Among them: "**Friston PRS-14 had been quietly de-clinicalised** — \"mental states\" where the entry says \"symptoms\"" and "**a criterion-M leak that eight passes missed.**" If the detectors cannot see these classes, every clean pass in the series is uninformative about them, and the number that reads best certifies least (ASSUMPTION-847; PRESUMPTION-729, **Critical**, queued).

- **CHANGE-2026-08-08-018 — A repair pushed a file out of the band it is judged against, and said so.** "**I inflated the file and am flagging it rather than burying it.** Anchoring added ~370 words... moving the ratio **from 1.154 to 1.401** — **the repair pushed the file from inside tolerance to outside it.**" A second run independently: "from 0.94 to 1.042 (+213 words)." Tonight's verification carries 21 unexplained length deviations with no field distinguishing drifted from repaired-into-deviation (ASSUMPTION-848; PRESUMPTION-728, queued). Adjacent: "the copy defect... **the built-in length check was comparing a number against itself**," affecting **125 of 307 transcripts** (ASSUMPTION-849).

- **CHANGE-2026-08-08-019 — The nightly verification found two defects in itself; only one carries a retroactive scope.** "the metaphysical guardrail was **matching bare `Q.NN` numbers without checking which Part** the day sits in, inflating tonight's first count **from 0 to 13** — **earlier entries reporting double-digit guardrail counts should be distrusted**"; and "the tradition-binder **required a literal `traditions/` prefix**, so **nine ids in Days 091-094 were silently going unverified rather than being checked**." The second is the graver class — a non-check reported as a check — and it is the one without a retroactive statement. The same run made an mtime claim ("All 614... mtime-verified unchanged") against this register's standing convention; 14a measured five registers carrying an identical mtime to within 36 ms tonight, at the same 21:00 as the commit (ASSUMPTION-837, 839).

- **CHANGE-2026-08-08-020 — A defect shape was re-scoped from scattered to contiguous, for the second time.** "The \"sixth shape\"... is a **contiguous band at Days 179-185**, **7 of 7 consecutive days, not the four scattered instances logged**"; "the **~422-bullet backlog should be re-scoped by band**." The logging granularity, not the defect, produced the scatter — the same discovery as Days 036-055 on 08-06 (ASSUMPTION-852; PRESUMPTION-730).

- **CHANGE-2026-08-08-021 — Fifth consecutive day of shared-`/tmp` contamination, and the sixth 687-class miscount.** "**A stale-artifact trap fired.** Staging the report through `/tmp` hit a permission error, and **the follow-up read silently returned *another session's* cached report from yesterday.**" The cold-`/tmp` fidelity trap fired independently in four sweeps. And: "the log writes bare `| 261 |`, not `| Day 261 |`, grepping the latter made a fully-reviewed pair look never-reviewed, and **I nearly acted on that**." The `/tmp` namespacing decision named on 08-06 remains unmade (ASSUMPTION-851, 853).

- **CHANGE-2026-08-08-022 — The budget rule acquired its second measurement, and it is arithmetic.** "**Budget breach, surfaced.** This run **blew the 30k session guidance on a queue that had no work in it.** **The contracts preamble is ~18k of fixed cost before a single pair opens.**" PREMISE-146 already holds the 4,000-token ceiling unsatisfiable as specified; today supplies the fixed cost that makes the 30k ceiling near-unmeetable too. Thirteenth consecutive day, six runs, two ceilings, zero changes. **This run is in breach and declares it** (ASSUMPTION-854; PRESUMPTION-731, queued, REFLEXIVE).

- **CHANGE-2026-08-08-023 — A self-correction arrived with its own blast-radius check.** "**Correction logged**: prior runs projected the 6-check stale threshold at 2026-09-01. That's **one check late** — it's **2026-08-25**." And: "Corrected in the entry; **no flag was suppressed by the error.**" The second clause is the part usually missing. Recorded as a POSITIVE — and explicitly *not* a satisfaction of REVISE-283's evaluator condition, since the agent checked itself (ASSUMPTION-856).

- **CHANGE-2026-08-08-024 — A highest-priority cross-connection was downgraded for ill-posedness, with a procedure supplied.** "**FPD-009** (Kastrup dissociative boundary equiv Friston Markov blanket) **downgraded from \"HIGHEST\" as not evaluable as posed**, with a decision procedure supplied and dispatched." First downgrade in the series on grounds of form rather than evidence (ASSUMPTION-857).

- **CHANGE-2026-08-08-025 — The completed task asked to be retired for a second day while performing the third option.** "**Wrote 0 transcripts and 0 syntheses**... all 307 pairs are on disk, **contiguous with no gaps**"; "this scheduled task **can be retired**." The 08-06 changelog named the unoffered option — repurpose as the only daily check that 307/307 is still 307 — and today the run **performed that check** and still framed the choice as retire-or-continue (ASSUMPTION-855; PRESUMPTION-732).

- **CHANGE-2026-08-08-026 — Tenth dark day, both directions, both refusals correct.** "**Scrape failed — claude.ai is not logged in**... **I don't sign in on your behalf.**" "**Delivery to Chat failed — the summary file is the only carrier.**" The two-browser ambiguity resolved; `/recents` still redirects to `/logout`. One sign-in resolves both; seven plugin MCP servers additionally need OAuth no scheduled run can perform (ASSUMPTION-836).

- **CHANGE-2026-08-08-027 — Register decay is now monotone on two of four registers, measured by 14a and reported by no one.** Eleven REVISE ids minted across two days with **zero** new strict blocks; DISPOSITION shortfall **32 -> 46**; premises max-minus-blocks **43 -> 44**; monitor blocks recovered +2. `for_lit_search.md` strict `[QUEUED]` **1,527 for a sixth consecutive day** while loose moved 1,824 -> 1,844 on tonight's eighteen items — measured within this run for a third consecutive night. And `prs_yield_log.csv` has a row again: **`2026-08-08,16,530,4`**, first since 2026-07-31; **seven days are missing and not backfilled** (ASSUMPTION-859, 860; PRESUMPTION-734, **High**, queued).

## Decision Index

**No new DECISION entries.** `decisions.md` content unchanged since 2026-07-20 (DECISION-078, dated
2026-07-05) — **thirty-fourth consecutive day.** No mtime claim is made, per ASSUMPTION-749. Five
candidates named across the block and not logged, per the standing convention against unilateral
promotion:

1. **The canonical-file decision**, now blocking work in **four traditions**: Hawkins is "a two-id-systems tradition — the reference-frame id is 04 in `prs_triplets.md` and 05 in `wiki.md`," and `arkanihamed/prs_triplets.md` carries "a pre-existing duplicate PRS-10 and two conflicting totals — nothing renumbered, it needs your call" (ASSUMPTION-822, 844).
2. Whether `generate_review_page.py` line 304 is patched — "I did not patch it — code change, your push gate" — now with two same-day readings of its blast radius that no one has reconciled (ASSUMPTION-816, 817).
3. Whether `summa-2026-daily-batch` is retired, **or repurposed as the corpus-completeness sentinel it already is** (ASSUMPTION-855) — carried from 08-06, unactioned.
4. Whether `/tmp` is namespaced per session — fifth consecutive day of contamination (ASSUMPTION-853) — carried from 08-06, unactioned.
5. Whether the Carroll PRS-14/15 vault-wide sweep is authorised, now with a **third** independent same-week demonstration that the "one determinate target" criterion is unreliable (PRESUMPTION-726).

## Open Questions

**No new OPEN entries.** `open_questions.md` unchanged since 2026-07-28 (OPEN-139). OPEN-138 and
OPEN-139 both **OPEN — awaiting Tom** for a **sixteenth day**, and the evening run supplied the first
measurement of the downstream cost: "OPEN-138 is sixteen days old and **today generated three more
findings that are structurally inert without an answer to it**" (ASSUMPTION-858). Candidates surfaced
and not logged:

- **Whether the evaluator condition is satisfied by reading outside the vault.** REVISE-283 says the
  detector tier must be checked by a component that did not produce the artefact, "which no existing
  pipeline agent satisfies." Tonight it was satisfied — by reading a session transcript. No agent's
  charter says the transcript layer is an evidence source (ASSUMPTION-809; PRESUMPTION-718).
- **Whether the `for_lit_search` queue has a consumer at all.** If the run's headline generalises, 255
  items are queued to agents that cannot dispose of them, and the drain rate is not the constraint
  (ASSUMPTION-812; PRESUMPTION-733).
- **Whether a clean QC pass means anything**, given four defect classes invisible to every detector in
  use and the highest fidelity scores in the log arriving on the batch that carried them
  (ASSUMPTION-847; PRESUMPTION-729, Critical).
- **Whether six unattended days is a scheduling fact or a change in what the system is.** At least
  seven distinct human-dependency terminations in this block, each reported locally, none aggregated
  (ASSUMPTION-860; PRESUMPTION-735).
