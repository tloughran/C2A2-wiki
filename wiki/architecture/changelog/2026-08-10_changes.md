# 2026-08-10 — EOD self-awareness run (Agents 14a / 14b)
### Monday. 29 sessions. Eighth consecutive unattended day.

The lit-search pipeline ran, and the thing it returned was a retraction of the network's own story about
itself: 15a and 15b, in twelve isolated contexts, established that `[Request interrupted by user]` fires
with no user action, and that four session deaths on 08-09 were attributed to a user who wasn't there.
That is the first time in this pipeline's history that an item 14b surfaced as a headline was queued,
searched and returned inside twenty-four hours, and the first time the return contradicted what the
network had been carrying. The same run drained twenty-six items after thirty-three days of zero,
achieved the architectural independence flagged on 08-08 and lost on 08-09 — and then minted PREMISE-152,
the literature saying homogeneous debate stops paying, which undercuts it, because 15a and 15b share one
model. Elsewhere: Agent 16 retracted its own previous-day advice on the grounds that following it would
have produced a false pass; the wiki daily run found that 08-09 had written eighty triplets and logged
none of them, and that eight of thirteen tradition agents caught the duplication risk unaided; two
four-month-old questions were closed negatively; and the nightly verifier declined to mint a Rohr × Stump
convergence because "if the term was imported rather than found, the network manufactured the convergence
it detected." Against that: yesterday's twelfth headline was refuted by measurement today, two more runs
died at the interrupt marker, a peer converted one of those deaths into a five-month diagnosis, and the
morning brief told Tom nothing looks broken on a morning with two FAILs and a self-declared failure.
No new DECISION — thirty-sixth straight day. No new OPEN.

## Changes Detected

- **CHANGE-2026-08-10-001 — The network retracted its own attribution for four deaths, and the retraction came from the pipeline in under a day.** "15a and 15b independently landed on the same primary sources and agreed: `[Request interrupted by user]` **fires with no user action**"; "Four session deaths on 08-09 were **attributed to a user who wasn't there**." REVISE-306, HIGH. First closed loop from 14b headline to literature return (ASSUMPTION-922; **PRESUMPTION-751, Critical, queued**).

- **CHANGE-2026-08-10-002 — The backlog drained for the first time in thirty-four days.** Measured: **26 `SEARCHED-15a`, 26 `SEARCHED-15b`, 26 `DISPOSITIONED-15c` stamped 2026-08-10**, against zero of each on 08-09; 25 result files each direction; DISPOSITION-630..654; PREMISE-150..152; MONITOR-505..513; REVISE-295..309 (ASSUMPTION-923) [measured].

- **CHANGE-2026-08-10-003 — The queue was not what its owner thought it was, and half of it is mis-routed.** "The queue was **not one day's intake**. It held **50 unsearched items across three cohorts**, including the entire 2026-08-08 batch that the 08-09 run never took"; "exactly **50% of the lit-search queue is mis-routed**" (MONITOR-505). Self-caught parser miss on ASSUMPTION-825 corrected rather than carried (ASSUMPTION-924; **PRESUMPTION-761, High, queued**).

- **CHANGE-2026-08-10-004 — Independence was achieved, and the run wrote down the finding that undercuts it.** "twelve isolated subagents that could not read each other's directories, not one context with two hats. **But they share one model**" (MONITOR-512), and PREMISE-152 set to **Moderate** because "several citations behind PREMISE-152 are unresolved search snippets" (ASSUMPTION-925, 926).

- **CHANGE-2026-08-10-005 — A general epistemic rule was proposed and not logged.** "**Request A (one rule, closes four flags): no status may be set to resolved/handled/healthy on the absence of a negative signal alone.**" It is the general form of the 5/5 stale detector, the phantom QC pass, the advanced-without-searching due dates and the seven identical connector weeks. **DECISION candidate; not logged** (ASSUMPTION-927).

- **CHANGE-2026-08-10-006 — Agent 16 retracted its own previous-day advice, and named the failure mode it would have caused.** "Yesterday's summary told you to verify the line-304 fix against the then-current 2-item queue. **That would have produced a false pass.**" Mechanism: "the export loop is **key-based, not positional**… the payload survives exactly when the synthetic key set happens to equal the real `proposal_id` set" (ASSUMPTION-928).

- **CHANGE-2026-08-10-007 — The predicted failure fired, and partially, which is worse.** `review/2026-08-10_review.html`, **121,696 bytes, 05:02** [measured], generated over an **8-card mixed-date queue**: "**four collide, four don't**… It will export four of your eight decisions and **silently drop the other four with their notes**." A partial failure presents as a successful export. Line 304 named for a **fifteenth-plus day** (ASSUMPTION-929; **PRESUMPTION-754, Critical, in-house**).

- **CHANGE-2026-08-10-008 — The 08-09 mystery resolved: the work existed, only its log did not.** "**The 2026-08-09 run wrote 80 triplets across 12 traditions and never wrote a line to `PROCESSED_LOG.md`.**" This closes ASSUMPTION-864, which recorded that whether the eight in-flight subagents wrote anything "is recorded nowhere." "**Eight of thirteen tradition agents caught it independently and refused to duplicate**" — five did not. "**The root cause is still live:** ingested proposals keep `status: pending` in their front matter" (ASSUMPTION-930, 931).

- **CHANGE-2026-08-10-009 — Two four-month-old open questions closed, both negatively.** FINDING-062/068: "**Kastrup's dissociative boundary is phenomenal inaccessibility, not conditional independence — it is not a Markov blanket**"; FPD-009, at "Priority: HIGHEST" since April, **retired**. FINDING-061: "the restrictions run in **opposite informational directions**," the Arkani-Hamed/Wolfram homology is an equivocation. Both **withdraw a premise rather than answer a question** (ASSUMPTION-933; **PRESUMPTION-753, High, queued**).

- **CHANGE-2026-08-10-010 — A tracked tradition corrected itself onto one of C2A2's own metrics.** "**Fredrickson's own N=73 cohort declines to privilege positive valence, so C2A2's community-health metric may be drawn at the wrong boundary**" (FLAG-019). "If the boundary moves from *positive shared affect* to **shared affect with mutual care**, grief-holding and lament become a measurable civic good rather than the absence of one." Propagation to four wiki files verified [measured] (ASSUMPTION-934, **queued — strongest literature item tonight**; PRESUMPTION-757, Medium, queued).

- **CHANGE-2026-08-10-011 — A new defect class: the correctly-cited id whose upstream meaning changed.** "**Sixteen syntheses cite CROSS-008; I touched one**… **Nothing there is false; it's over-warranted**"; and, from a different register the same day, "**34 files carry claims the wiki retired today**… **None of these is a bad id; each is a live claim the source no longer makes. All flagged, nothing edited.**" Invisible to every id-checking instrument the network owns. **The two counts (16 vs 8 for CROSS-008) are unreconciled** (ASSUMPTION-935, 936; **PRESUMPTION-756, High, queued**).

- **CHANGE-2026-08-10-012 — A convergence was declined on the grounds that the network may have manufactured it.** "**Rohr × Stump was deliberately not minted**… two of the three Rohr sources use 'second-personal' **in the proposal's voice, not Rohr's**… **If the term was imported rather than found, the network manufactured the convergence it detected.**" Arrives one day after PRESUMPTION-747 asked what a cross-connection is evidence *of*, and this time the refusal was executed (ASSUMPTION-937, **queued**).

- **CHANGE-2026-08-10-013 — A thinker's own instrumentalism cut through a wiki bridge, and was escalated rather than filed.** "**Friston explicitly deflates the literalist reading** — FEP-conformity is 'a useful explanatory fiction… that may or may not be entertained by the object itself'… This cuts through how this wiki currently phrases the Levin↔Friston bridge" (ASSUMPTION-939, **queued**).

- **CHANGE-2026-08-10-014 — Three independent queue-starvation mechanisms, three reviewers, one day, none citing another.** (a) "The report keys freshness off the **newest** of a pair's two stamps"; (b) "That queue was **not empty — it was rounding down.** The effective threshold is **8 days, not the 7 it documents**"; (c) "the queue is now **100% holds** — decisions waiting on you, not work a reviewer may do." Corrected stale counts: **144 / 151 / 161 of 307**, against a report queue of 3-5 (ASSUMPTION-940).

- **CHANGE-2026-08-10-015 — A reviewer reproduced, in its own first scan, the exact blind spot it exists to route around.** "My first staleness scan used `min()`… i.e. **I reproduced the exact report blind spot this run exists to route around**." Buggy scan **28**; corrected **151**. Disclosed with the wrong number named, before picking work (ASSUMPTION-941).

- **CHANGE-2026-08-10-016 — The seven-week `length_actual_words` diagnosis became a measurement.** "**212/242 declaring counts disagree**" (87.6%); median drift 33 words; **28 pairs change policy band**; 6 false breaches; 9 out of band, 2 genuine and unflagged. Day 073 1620→1939; Day 074 2280→3157. Deliberately unpatched: "**fixing one day by hand would make it the only accurate row in 307 and would hide the systemic problem**" (ASSUMPTION-942, 943).

- **CHANGE-2026-08-10-017 — 31 syntheses were written without a constitutionally-specified input, and nobody had noticed.** "**31 transcripts… have a TODO placeholder where `## Notes` should be**" (Days 006-011, 043-065, 071-072) — **counted, not assessed** (ASSUMPTION-944; **PRESUMPTION-763, High, queued**).

- **CHANGE-2026-08-10-018 — The held set was declined by every reviewer that met it, for the sixth through ninth consecutive run, each for a different stated reason.** "pass-marking them to clear the queue would **quietly convert your open decisions into closed ones**"; "I surveyed each against the log, **re-measured nothing, and pass-marked nothing**." Escalated: `qc_sweep.py mark` **lacks a `held` outcome** — one field, unwritable by every agent that needs it (ASSUMPTION-945; **PRESUMPTION-762, Medium, queued**).

- **CHANGE-2026-08-10-019 — One boundary produced three failures in three registers, and only one run named it.** "**FAILED — do not mark this run successful.** Root cause (**diagnosed, not guessed**): `prs_yield.py` **dates each triplet from git history, so uncommitted triplets read as 'absent'**" — **110 PRS ids on disk absent from git; 160 uncommitted paths**. Same day: Phase 6 **blocked**, sandbox cannot write `.git`; `com.c2a2.metabolism-publish` **exit 128**. **The measurement layer reads git as ground truth and the write layer cannot reach it** (ASSUMPTION-946, 947; **PRESUMPTION-755, High, in-house**).

- **CHANGE-2026-08-10-020 — A latent truncation bug became visible only because its input stopped being aligned.** "**A MiB-only chunk loop silently truncated the last 212 KiB — caught only by the size-equality check**… it would have produced a **plausible-looking but short snapshot**"; "the DB is no longer an integer number of MiB (it was on 08-08 and 08-09)" (ASSUMPTION-948).

- **CHANGE-2026-08-10-021 — Yesterday's twelfth headline was refuted by measurement today.** "The sewing bootstrap **refuted itself**… **Those pages aren't orphan-equivalent; they're the vault's dominant source of hub backlinks**" — **218 of 307 carrying 836 outbound wikilinks**. "**Its recommended fix survives, its severity framing doesn't.**" ASSUMPTION-913 measured *inbound*; today measures *outbound*; **both may be true and the register recorded only the first**. Also: "**last week's '+7 progress' was resolver oscillation**" — 57→64 then 64→57 (ASSUMPTION-949, 950).

- **CHANGE-2026-08-10-022 — Four numeric contradictions between morning instruments, one morning, reported not averaged.** "third time in **four** days" vs "third time in **five** days"; metabolism generated **05:59:13** vs **09:59Z** on the field the freshness gate reads; scheduler baseline **78/4/0** vs today's **79/1/2** with three WARNs vanishing unexplained; `held_paths.md` a FAIL in one report and absent from the other's failure surface. And an unreconciled adjacency: one instrument reported the publish outcome "**unverified**" while its sibling held "**exit code 128**" (ASSUMPTION-951, 952).

- **CHANGE-2026-08-10-023 — Two more deaths at the marker, on the day the marker was cleared of user causation.** `local_93a2c33b` (Summa qc sweep) died **immediately after two `Edit` calls with a file-delete permission pending** — two edits landed with no verification, no log row, no summary, and an unknown delete target; `local_27ae1e1d` (cost tracker) died mid-`Write` (ASSUMPTION-953) [measured].

- **CHANGE-2026-08-10-024 — A peer converted a sibling's death into a five-month diagnosis.** The reviewer-review: the cost tracker "fired at 09:34 and **again wrote nothing — five months**." Its transcript shows an interrupt mid-`Write`. **The same misattribution shape the network retired at the top level today, recurring one level down, the same day** (ASSUMPTION-954; **PRESUMPTION-760, Critical, in-house**).

- **CHANGE-2026-08-10-025 — The human-facing morning brief said nothing looks broken.** "the C2A2 wiki run, the Summa daily batch, the metabolism refresh, and the lit-search pipeline **all completed. Nothing looks broken.**" Same morning: the metabolism refresh **declared its own failure**, the scheduler reported **2 FAIL**, the cost tracker died. The brief hedged its numbers and did not hedge its verdict (ASSUMPTION-955).

- **CHANGE-2026-08-10-026 — Twelfth dark day, both directions, third consecutive delivery failure, and both runs again refused to fabricate.** "**Chrome MCP itself was working; the session cookie is gone**… wrote a failure note **rather than a bogus summary**"; "**I screenshotted to confirm rather than infer it.**" Still no consecutive-failure counter anywhere (ASSUMPTION-956).

- **CHANGE-2026-08-10-027 — A null result was reported as the headline, and the escape clause carried 100% of the output.** "**Both agents found nothing inside the 30-day window. That is the headline, not a caveat**" — and "**All four proposals are filed under the filter's second clause.**" Three fetches refused as out-of-provenance; two proposals rest on abstract-level retrieval, marked Medium/Speculative (ASSUMPTION-957; **PRESUMPTION-758, High, queued; PRESUMPTION-759, Medium, in-house**).

- **CHANGE-2026-08-10-028 — A task declared a corpus complete by existence and proposed its own retirement, on a day four runs measured content defects in the same files.** "**307 transcripts, 307 syntheses, zero gaps**… this scheduled task can be retired" — fourth such recommendation — against a plan target of **308**, unreconciled (ASSUMPTION-958; **PRESUMPTION-763, High, queued**).

- **CHANGE-2026-08-10-029 — The lit pipeline's own count does not reconcile to its own artefacts.** Measured tonight: run reports **25** processed; **25 result files each direction**; **25 dispositions**; but only **24 inline stamps**, and the run's closing line reads "Processed 24 items." The twenty-fifth pair, **PRESUMPTION-618**, is timestamped **21:00** — twenty hours after the other twenty-four — **with no queue stamp at all**. Tenth instance in six days of the PRESUMPTION-687 class (ASSUMPTION-959) [measured].

- **CHANGE-2026-08-10-030 — REFLEXIVE: this run committed the same class inside the paragraph explaining it.** The first draft of tonight's intake note spelled the strict counter's pattern out twice in prose, **raising the strict counter by 2 with no items queued**. Caught by post-write measurement, corrected in-run, and reported in `for_lit_search.md` rather than silently fixed. **Eleventh instance in six days** (ASSUMPTION-961) [measured].

## Decisions

**No new DECISION. Thirty-sixth consecutive day.** Thirteen candidates stand, three of them new today:
**Request A** (the resolved-on-absence rule — one rule, four flags); the **Mac-side git unblock** for Phase 6;
the **Levin↔Friston bridge rephrasing**, escalated to the master agent. Carried: line 304 (fifteenth-plus
day, now with a measured partial-loss page standing ready as the test bed); the `held` outcome for
`qc_sweep.py mark`; the `.days == 7` → `>=` one-character threshold fix; the `length_tier` call (**eight
weeks**); the canonical-file decision; retire-or-repurpose `summa-2026-daily-batch` (fourth
recommendation) and the bootstrap audit; the 14 alias files; the control arm; the in-house measurement
authorisation. Enumerated at ASSUMPTION-960.

## Open Questions

**No new OPEN.** OPEN-138 and OPEN-139 remain OPEN — awaiting Tom for an **eighteenth day**.

## Positives, recorded neutrally

The lit pipeline **corrected its own parser miss rather than carrying it**, **refused to overwrite
superseded counts**, **downgraded PREMISE-152 for its own citation quality**, **refused to call
ASSUMPTION-909 tested** for want of a control arm, and **minted the premise that undercuts its own
method**. Agent 16 **retracted its own prior-day recommendation** and stated the next failure's shape in
advance. The wiki daily run **disclosed a 35x budget breach unprompted**, **reported its root cause as
still live**, **refused to claim Phase 6 succeeded**, and **closed two questions negatively**. The nightly
verifier **fixed two extractor bugs before reporting** — averting fabricated findings on Days 075, 092
and 094 — **proved read-only compliance with a byte-identical before/after snapshot of 614 files**, and
**declined a convergence it suspected the network had manufactured**. Six Summa reviewers **declined the
held set**, one **disclosed its own `min()` bug and the wrong count it produced before picking work**, one
**refused to patch a stale field because the repair would hide the systemic defect**, one **refused to
guess an unrecoverable discount code**, one **refused to repoint a load-bearing id unilaterally**, and one
**logged a counterintuitive n-gram drop specifically so a future run would not revert good work**. The
metabolism run **refused to report success** and distinguished diagnosis from guess. Telemetry
**disclosed a truncation bug it had itself introduced**. The reviewer-review **overturned its own prior
week's reported progress** and **disclosed a 2x word-budget breach rather than trimming evidence**. Both
sync runs **refused to fabricate**. The Levin/Friston hunt **reported zero as the headline**, **named its
rejections**, and **downgraded confidence to match retrieval depth**. At least seven runs surfaced budget
breaches, **including this one**.
