# Architectural Changes — 2026-09-10

## Narrative

One act defined the day and everything else is its wake. A batch APPROVE covering all 36 pending
proposals — emailed 2026-09-09, parsed at Phase 0 this morning, 36/36 matched by prop_id with zero
warnings — closed a thirty-three-day silence in the proposal decision channel and produced the largest
single ingest in the network's history: 85 PRS triplets across ten traditions, 782 → 867, with
CROSS-132..135, FINDING-086..090 and FLAG-023/024. The same ingest exposed a five-register append defect
by misfiring on one of them, re-opened 147 absence-declinations in the sibling Summa project as
unverified, and left six cards of uncertain warrant inside a 99.8% approval rate on the run's own
account. Meanwhile the morning's lit-search pipeline turned on itself twice — measuring that 9 of 11
items across two cycles were pre-answered by the premise register before either search agent looked, and
surfacing Nemeth's finding that *assigned* devil's advocacy produces cognitive bolstering rather than
genuine challenge, which is a construction question about Agent 15b and therefore about every CHALLENGED
status in this estate. No attended session; Chat↔Cowork dark in both directions for a third day; eleventh
consecutive day on which every item extracted here is agent-stated rather than designer-stated.

## Changes Detected

- **CHANGE-2026-09-10-01 — `pending/` reached zero for the first time since 2026-08-27.** 36 → 0;
  `approved/` 378 → 414. Review-pass gap 14 days → 0. Decision channel silence 33 days → answered.
- **CHANGE-2026-09-10-02 — network state 782 → 867 PRS triplets, 131 → 135 cross-connections, 85 → 90
  findings.** Per-tradition deltas: hawkins +16, kastrup +13, rohr +13, friston +8, carroll/levin/wright
  +7 each, mcgilchrist +6, hoffman/wolfram +4.
- **CHANGE-2026-09-10-03 — in-run repair of `traditions/hoffman/prs_triplets.md`.** Duplicate
  `*Total PRS triplets:*` anchor caused four triplets to land mid-file; caught by a max-id-vs-stated-total
  check and repaired in place. Four further registers (`arkanihamed`, `stump`, `loughran`, `macintyre`)
  carry the same defect, logged as "needing a human" on 2026-08-11 and unrepaired for **thirty days**.
  Raised as OPEN-193.
- **CHANGE-2026-09-10-04 — `validated_premises.md` +3 (PREMISE-201, 202, 203)** after two cycles of zero
  minting. PREMISE-201 clause (3) rests on an AJR quotation that failed retrieval and is flagged
  unverified — the register's first premise minted with a known-unverified load-bearing clause.
- **CHANGE-2026-09-10-05 — second consecutive 15b systemic-risk flag** (`no-second-look`, High): four
  unrelated control types, all terminating at production, none verified at T+n. The flag states its own
  falsification counter and was not dispositioned today.
- **CHANGE-2026-09-10-06 — Phase 2 (hunt) skipped by explicit judgement**, not omission. First recorded
  deliberate suppression of proposal supply. No negative record written; OPEN-194 asks whether one should
  be.
- **CHANGE-2026-09-10-07 — Phase 6 (commit/push) not performed.** 152 dirty paths including another
  session's changes to `.gitignore`, `scripts/janitor.py`, `scripts/commit_daily_run.sh`. Per the
  no-blind-push rule this awaits local HTTP review and sign-off.
- **CHANGE-2026-09-10-08 — third consecutive outbound delivery failure, second consecutive day dark in
  both directions.** Chrome extension not connected; browser-pane claude.ai grant unanswerable in an
  unattended run. Inbound scrape failed for an eighth day.
- **CHANGE-2026-09-10-09 — `wiki/metabolism/metabolism_data.json` is 7 days cold** (generated
  2026-09-03) and is being read as current by `morning-system-health`. Regeneration blocked by a 6.3 GB
  byte-copy into a 4.1 GB sandbox; the OpenStory telemetry refresh met the identical constraint
  independently the same day.
- **CHANGE-2026-09-10-10 — Agent 16 found a live card held against a retired target.**
  PROP-2026-08-14-033 is correctly held open, but against "the Admirato / KSBJ *Between Beliefs*
  archive," a publisher surface WATCH-002 retired on 09-08. One line fixes it; the fix is outside the
  agent's write scope and was not made.

## Decisions

No new entries in `decisions.md`; it remains at DECISION-083 (2026-08-27), **fourteen days**. The day's
governing decision — the 36-card batch APPROVE — was made outside the register and is recorded in the
register's 09-10 note. **Twelve rulings are now owed and named** (REVISE-436's seventh unruled cycle, the
WATCH-002/003 audit question, OPEN-174, OPEN-185 through OPEN-194), plus three further requests addressed
to Tom outside this register: push sign-off, Kastrup member access before FLAG-023's window closes, and a
claude.ai `scope: site` grant.

## Assumptions Extracted (14a)

Thirteen: **ASSUMPTION-1305 – 1317**. Four SUPPORTED in-house (1311, 1313, 1316, 1317), eight routed to
15a/15b, one UNTESTED. All agent-stated; two cross-project. The sharpest are **1307** (a batch APPROVE
cannot carry item-level warrant — six of thirty-six cards affected), **1309** (82% of routed items
pre-answered by the register), and **1310** (assigned devil's advocacy produces bolstering, not
challenge — a construction defect in Agent 15b, filed at High with a cheap test and no owner).

## Presumptions Surfaced (14b)

Ten: **PRESUMPTION-947 – 956**. Structural 3, epistemic 2, methodological 2, normative 2, scaling 1. One
Critical (**953** — that the estate's reports are read soon enough to matter; today supplied both the
strongest positive datum in that direction and a fourteen-day measurement of the latency), four High
(**948** — that a premise answering an item should exempt it from search, which would make a 158-item
register monotonic by construction; **951** — that "needs a human" is a queue rather than a dead-letter;
**954** — that a caveat neutralises a number's load-bearing role; **956** — that an agent's edit boundary
is its responsibility boundary). Nine of the ten name an in-house measurement available before any
literature search.

## Open Questions Raised

- **OPEN-192** — should an item already answered by an ACTIVE premise be excluded from search, or
  searched precisely because that is the only occasion the premise is re-exposed?
- **OPEN-193** — at what age does an item logged "needing a human" become declined? The instance is
  thirty days old and misfired today.
- **OPEN-194** — should a skipped hunt phase leave a negative record, so that "surveyed, found nothing"
  and "not surveyed" remain distinguishable in the archive?

## Lit Search Queue

Twenty-three items appended to `for_lit_search.md`, twenty `[QUEUED]` and three `[NOT-QUEUED]` with
reasons (arithmetic, or settled in-house). Two marked `[QUEUED — PRIORITY]`: ASSUMPTION-1310 and
PRESUMPTION-948. The 15d re-trigger lane is unserved for a sixth cycle — 170 items, oldest 2026-07-05.

## Provenance

All items written this run carry PROVENANCE headers per
`wiki/architecture/provenance_protocol.md`. Registers snapshotted as `*.bak.20260910-pre-14eod` before
any append: `assumptions.md`, `presumptions.md`, `decisions.md`, `open_questions.md`,
`for_lit_search.md`.

## This Run's Own Limits, Stated

- **Coverage is partial and declared.** Ten of roughly sixteen reachable runs were read; Summa commentary
  reviewer / qc sweep repetitions, the Summa daily batch, morning project status, the walk handoff,
  scheduler health and the Supabase keep-warm were not. Items may exist there that this run did not see.
- **No designer speech was available.** Eleventh consecutive day. Every item tonight is extracted from an
  agent's report about its own work, which is the condition PRESUMPTION-912 tracks.
- **This run is inside PRESUMPTION-949.** Tonight's output is speech, it names twelve measurements, and it
  attaches an owner to none of them. That is recorded rather than exempted.
