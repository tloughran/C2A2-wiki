# Architectural Changelog — 2026-07-05

## Narrative

Today (Sunday) was an attended day centered on the **Review Log PRS-triple modal** ("Explorer Bugs PRS triplets review" session, Tom present), flanked by two scheduled runs (the Wolfram specialist agent and the C2A2 wiki daily run). The attended session built the clickable pop-up over all 447 PRS-triple rows (header-pinned Prev/Next, arrow keys, X/outside/Esc close), moved the nav from footer to header to stop the buttons jumping, failed to fully kill a residual vertical header jump, parked that tweak with a written handoff, and then — reversing the parking minutes later — Tom chose to push the current build to GitHub for ISME, modal and known defect included, via a surgical two-file commit. That commit blocked on `.git/index.lock` and then a fresh `.git/HEAD.lock` (candidate live writers: Sunday janitor, heartbeat cron), and the transcript ends without recording whether the push landed — **verify on the Mac before treating the modal as live**. The Wolfram run filed one proposal (PROP-2026-07-05-001) from a partially-fetchable source; the daily run was orchestrator-only (0 new proposals, correct expected zero) and left its commit 5b7e68a push-pending on the Mac.

## Changes

CHANGE-2026-07-05-001 (Decision): DECISION-076 — Ship the Review Log PRS-triple pop-up for ISME now via a surgical two-file commit (`scripts/assemble_review_log.py` + `wiki/review_log.html`); private-first files stay unpushed. Attended; ADOPTED, execution unresolved at transcript end (git locks).

CHANGE-2026-07-05-002 (Decision): DECISION-077 — Park the modal vertical-jump fix (handoff: `handoffs/review-log-triplet-modal.md`, next fix = fixed `.tbox` height) and the architecture-registry drift commit until after ISME. Attended; ADOPTED.

CHANGE-2026-07-05-003 (Assumptions): Extracted ASSUMPTION-411..416 (6) — footer-nav jump diagnosis; vertical-centering diagnosis (in-session evidence against, unconfirmed); modal-in-place as valid reading of "hyperlink to associated file"; ship-with-known-defect acceptability; stale-vs-live git-lock reasoning; Wolfram Q&A segment novelty at downgraded confidence. All UNTESTED.

CHANGE-2026-07-05-004 (Presumptions): Surfaced PRESUMPTION-443..447 (5) — un-owned Mac-side convergence step presumed reliable (structural, High); deadline-driven release gating (normative, Low-Medium); human-mediated compile loop as debugging substrate (methodological, Medium); uncoordinated multi-writer git repo (structural/scaling, Critical); snippet-level novelty screening when primary source unfetchable (epistemic, Low-Medium). All UNTESTED.

CHANGE-2026-07-05-005 (Open questions): Raised OPEN-109 (residual modal jump cause), OPEN-110 (git-lock provenance and multi-writer coordination), OPEN-111 (architecture-content convergence policy; growing uncommitted drift), OPEN-112 (447 vs 300 vs 260 PRS-count discrepancy).

CHANGE-2026-07-05-006 (Registry sync): Synced the 2026-07-03 lit-search returns into the registries — assumptions.md: ASSUMPTION-403 CONTESTED (MONITOR-412), 406 GROUNDED (PREMISE-094), 407 CONTESTED (MONITOR-413), 408 CONTESTED (MONITOR-414); presumptions.md: PRESUMPTION-436 CHALLENGED (REVISE-171), 437 CHALLENGED (REVISE-172), 439 CONTESTED (MONITOR-415), 440 CHALLENGED (REVISE-173), 442 CHALLENGED (REVISE-174). Chains extended [14a/14b → 15a, 15b → 15c]. (Note: statuses had been current in for_lit_search.md since 07-03 but the registries were not updated — a 2-day sync lag; no 14a/14b runs are on record for 07-03 or 07-04, and those changelogs are absent.)

CHANGE-2026-07-05-007 (Lit-search queue): Seeded the 2026-07-05 EOD cohort in for_lit_search.md — ASSUMPTION-412, 414, 415, 416; PRESUMPTION-443..447 — all tagged [QUEUED] or [QUEUED-EMPIRICAL].

CHANGE-2026-07-05-008 (Metrics): Produced 2026-07-05 snapshot. Registries now: Assumptions 416, Presumptions 447, Decisions 77, Open questions 112, Validated premises 94.

## Carry-over blockers (status update)

- Compute sandbox: WORKING again this run and in the daily run (the 06-30..07-03 disk-full condition was not observed today). Watch for recurrence.
- claude.ai Chrome login: last confirmed logged-out 07-03 (morning scrape); not re-tested today.
- Git convergence: daily-run commit 5b7e68a push-pending; ISME two-file commit outcome UNRECORDED (locks); architecture drift uncommitted since 07-01 (OPEN-111).
- 4 review proposals pending (2 McGilchrist, 1 Kastrup, 1 Carroll) per today's review page, plus PROP-2026-07-05-001 (Wolfram).

## Dating note

Session-list ordering places the attended Explorer session's last activity after this morning's scheduled runs, so it is recorded as a 2026-07-05 session; its git-status paste is consistent with 07-03 or later. [inferred]

## Late-evening addendum (second 14a/14b pass)

The attended session continued past the first EOD run, and the git-lock cliffhanger resolved on camera. Narrative: `ps` checks showed no live git/janitor/heartbeat (the grep hits were Claude session command lines), so all three locks were cleared and the commit raced through — c543afa landed, but with only the generator: the daily wiki agent's commit 5b7e68a had already captured `review_log.html`, regenerated (by luck of pipeline ordering) from Tom's updated generator, confirmed by marker greps (tnavwrap=2, 6vh pin=1). The push then FAILED: origin/main had advanced b1d0692 → 511b3b2 by a writer no local check identified, the rebase aborted on the uncommitted architecture drift (OPEN-111 biting in real time), and the transcript ends with main unpushed. **The ISME modal is committed locally but NOT live on GitHub Pages.**

CHANGE-2026-07-05-009 (Decision): DECISION-078 — Treat the locks as stale and race the live agents to the commit rather than pausing them. Attended; ADOPTED; executed. Commit landed; push failed; convergence unresolved.

CHANGE-2026-07-05-010 (Assumptions): Extracted ASSUMPTION-417..420 (4) — stale-lock/false-positive diagnosis; daily-agent attribution of the page commit; marker-based build verification (with a rationale-drift flag: expected grep count silently reinterpreted 1 → 2); fetch+rebase+push as sufficient convergence recipe (in-session counter-evidence recorded). All UNTESTED.

CHANGE-2026-07-05-011 (Presumptions): Surfaced PRESUMPTION-448..450 (3) — racing concurrent writers as acceptable coordination (methodological/structural, High); local process table as complete writer enumeration (epistemic/structural, Medium-High); marker greps as substitute for the no-blind-push visual sign-off (normative/methodological, Medium). All UNTESTED.

CHANGE-2026-07-05-012 (Open questions): Raised OPEN-113 — local/remote convergence after the failed push: who pushed 511b3b2, and how to rebase past the agent drift safely. Blocking the ISME deliverable going live.

CHANGE-2026-07-05-013 (Lit-search queue): Seeded the late-evening addendum cohort — ASSUMPTION-420 [QUEUED-EMPIRICAL, HIGH]; PRESUMPTION-448 [HIGH], 449, 450 [QUEUED].

CHANGE-2026-07-05-014 (Metrics): Snapshot addendum — registries now: Assumptions 420, Presumptions 450, Decisions 78, Open questions 113.

### Carry-over blockers (superseding update)

- Git convergence: NOW THE TOP BLOCKER. Local main = c543afa (+ 5b7e68a); remote = 511b3b2 (unattributed writer); rebase blocked by uncommitted architecture drift; push rejected. The modal is not live. First Mac-side step: `git stash` (or commit the drift per OPEN-111), `git rebase origin/main`, `git push`, then check the reflog/authors for 511b3b2 (OPEN-113).
- The earlier "verify on the Mac before treating the modal as live" note is superseded: outcome now recorded — committed locally, push failed.

Self-referential note [inferred]: the first EOD self-awareness run (PID 11867) appears in the session's own `ps` paste as one of the "live wiki agents" being raced — the observer was among the concurrent writers it was documenting.
