# Architectural Changelog — 2026-06-10 (Wednesday)

**Narrative.** An **attended build-and-verify day on four fronts**, with instrumentation as the headline. **DECISION-056: the Pathway 29 Agent Metabolism view shipped live** — design doc, prototype generator, three-view visualization with a git-derived yield axis, clean 7-file commit (`3080a23`) verified on the public GitHub Pages site. **DECISION-055** was Tom-ratified mid-verification: his counter forensics ("2374/2400 nodes · 30000 edges in view" vs "2500 shown / 30000 pass") exposed both 30000s as artifacts of the legacy MAX_EDGES DOM cap, and he picked the architectural fix — render only budgeted edges. DECISION-053's sociogram passed all four verification checks (iframe wiring closed), then Tom's three observations opened real findings: a time-slider date-cut bug, the Summa zero-substrate fact, and H-Admin's centrality living entirely in the hidden substrate layer (→ OPEN-080, parked). DECISION-054's task one was DELIVERED (ladder tools embedded as Physics Explorer + RC Document Explorer; ISME paper re-uploaded). The education-tab session GROUNDED the Back-button cure live. And the sync loop half-recovered: morning scrape failed (8th day) but **evening delivery succeeded — first in 8 days**, unexplained in both directions. Today's extraction is **9 ASSUMPTIONs (301–309, one GROUNDED/held) + 6 PRESUMPTIONs (334–339)**.

### Agent 14a (stated assumptions)
- **+9 ASSUMPTIONs:**
  - **ASSUMPTION-301** — substrate reveal is correct because it shares the proven admission code path (claimed after direct observation stalled the renderer). Methodological. (Routed, MED.)
  - **ASSUMPTION-302** — the heavy-toggle stall is the synchronous 30k-line DOM build, "not the force sim"; budgeted rendering will clear it. Architectural/empirical. (Routed, MED.)
  - **ASSUMPTION-303** — actors are "cumulative telemetry, not single-dated events" → exempt from the date cut (fixes the slider bug, acknowledged "Real bug, my fault"). (Routed, LOW.)
  - **ASSUMPTION-304** — embedding the two ladder tools in the Community Education Tab fulfills task one's scaffold provisioning ("That's task 1."). (Routed, LOW-MED.)
  - **ASSUMPTION-305** — Back-button desync = iframe src history pollution; replace() + pageshow-resync cures it. **GROUNDED in-session** (live post-Back probe). (HELD.)
  - **ASSUMPTION-306** — on history restore the iframe is the source of truth; shell resyncs to content. (Routed, LOW.)
  - **ASSUMPTION-307** — git-derived yield is a valid metabolism axis (token cost verified in-DB same session). (Routed, MED.)
  - **ASSUMPTION-308** — deterministic scheduler before any bandit layer. (Routed, LOW-MED.)
  - **ASSUMPTION-309** — progress/peace KPI split: MacIntyre number for progress; endorsed-steelman, not agreement, for peace. (Routed, MED.)
- **Decision index (`decisions.md`): +2 — DECISION-055** (budgeted-edge DOM rendering + counter-label truth; refactor in progress, paused at OPEN-080) and **DECISION-056** (Pathway 29 Metabolism sub-tab ADOPTED & SHIPPED to the public site). DECISION-053 ADVANCED (verification complete); DECISION-054 task one DELIVERED. Max now **DECISION-056** (was 054).
- **Open questions (`open_questions.md`): +2 — OPEN-080** (should the Agent Explorer preset reveal substrate, where H-Admin's 601-edge centrality lives? parked AskUserQuestion) and **OPEN-081** (count reconciliation: PRS 269-vs-260, proposals 3-vs-7, findings 35-vs-41 between registries and master-wiki/briefing figures).
- Metrics snapshot: `metrics/2026-06-10_snapshot.md` written.

### Agent 14b (unstated presumptions)
- **+6 PRESUMPTIONs:**
  - **PRESUMPTION-334** (Medium) — verification-without-observation: code-path identity silently substituted for behavioral observation when the renderer stalled. Third member of the verification-transfer family (324 → 328 → 334) — flagged as a candidate systemic pattern for Agent 13. *(Epistemic/methodological; pairs ASSUMPTION-301.)*
  - **PRESUMPTION-335** (Medium) — the house validator's scope presumed to define correctness; display-level invariants unchecked, Tom as de-facto anomaly detector (validator passed the artifact whose counters contradicted). *(Methodological.)*
  - **PRESUMPTION-336** (Medium-High) — captured file-write telemetry presumed representative of agent activity; Summa's zero substrate read as data fact, capture bias unexamined despite the adjacent parked capture-bias flag. *(Epistemic; generalizes PRESUMPTION-326. The day's sharpest find — the sociogram, metabolism view, and yield metrics all sit on this layer, now publicly.)*
  - **PRESUMPTION-337** (Medium) — the single-human commit gate presumed to scale: three queues accumulated today, one landed; ~45-file messy tree surfaced. *(Scaling; pairs PRESUMPTION-332.)*
  - **PRESUMPTION-338** (Medium) — sync restoration presumed durable after one success following 8 failure days; lapse and recovery both unexplained, no root-cause item opened. *(Epistemic/methodological.)*
  - **PRESUMPTION-339** (Medium) — exhaust-tracks-aim: live public "yield" = git commits, before PRS/peace dimensions exist in the metric — a value judgment shipped as an instrument default. *(Normative/epistemic; pairs ASSUMPTION-307/309.)*
- Dedup check: no overlap with 14a's items (334/335/339 are the unstated complements of stated 301/302/307-309 — pairings noted inline); the 06-09 family (328–333) not re-surfaced; 336 generalizes 326 rather than repeating it.

### Lit search queue (seeded)
- **14 items routed** to `for_lit_search.md` as `[QUEUED]` at cycle 0 (2026-06-10): ASSUMPTION-301 (MED), 302 (MED), 303 (LOW), 304 (LOW-MED), 306 (LOW), 307 (MED), 308 (LOW-MED), 309 (MED); PRESUMPTION-334 (MED), 335 (MED), 336 (MED-HIGH), 337 (MED), 338 (MED), 339 (MED). Each carries a full PROVENANCE block and a search-strategy line.
- **Held (not routed): 1** — ASSUMPTION-305 (GROUNDED by live in-session browser verification).

### Fail-loud (Rule 12)
1. **Sync loop half-restored, unexplained:** morning scrape failed at /login (8th consecutive day); evening delivery then SUCCEEDED — first in 8 days. No root cause for lapse or recovery (PRESUMPTION-338). Watch the morning scrape.
2. **Two attended sessions parked on open questions at EOD:** sociogram mid-refactor on the OPEN-080 substrate design call; education-tab at the wrap/commit choice. Both carry resume cues.
3. **Commit queue at 2 + messy tree:** sociogram refactor files and the education-tab two-file commit are local-only; the Mac working tree carries ~45 modified files and untracked agent folders ("a minefield for the next push") — triage offered, pending.
4. **Count reconciliation (OPEN-081):** registry vs master-wiki/briefing divergence on PRS (269/260), proposals (3/7), findings (35/41). Carry-forwards kept from registries; figures flagged, not averaged (Rule 7).
5. **Human review gate:** pending proposals awaiting Tom (3 per 06-09 registry; 7 per briefing — reconciliation in OPEN-081); REVISE backlog 091–094 carries.
6. **Pipeline note:** no 15-pipeline disposition of the 06-09 batch observed within this pass's window; the 14 new items join the queue behind it.

PROVENANCE:
  Origin: 14a (changelog) with 14b section co-produced
  Chain: [14a], [14b]
  Item type: CHANGELOG (daily)
  Transform at each step:
    14a/14b: Produced from today's session transcripts (sociogram verification, measurement-prototype pickup, education-tab fixes, Pathway 29 metabolism, morning briefing, morning scrape FAIL, evening delivery SUCCESS) + registry state.
  Current status: COMPLETE
