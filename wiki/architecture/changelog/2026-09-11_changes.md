# Architectural Changes — 2026-09-11

## Narrative

A quiet day by every throughput measure and a loud one by every epistemic measure. Zero ingests, zero
proposals hunted, one card written, no decisions arrived — and in that stillness the estate produced its
first three-way numeric divergence (107 / 131 / 135 cross-connections, each stated as fact by a run that
could not see the other two), its first self-invalidated remedy (a deferred-condition fix keyed on a field
present in zero of 414 files), and its first same-day pair of runs reaching opposite conclusions about the
same blocker — one establishing from sqlite.org that no byte-copy is needed to read the live database, the
other declaring the sandbox structurally unable on the strength of that copy and recommending the task be
re-architected around it. The lit-search pipeline emptied its queue and reported, for a third cycle and
now at 100%, that every routed item was pre-answered by an ACTIVE premise the originating run never cited;
it rejected the efficiency remedy this register proposed yesterday on the ground that it would make 158
premises unfalsifiable by construction, minted one premise from twenty items, and declined three more
because the sources were paywalled. No attended session; outbound sync dark a fourth day, inbound a ninth;
twelfth consecutive day on which every item extracted here is agent-stated.

## Changes Detected

- **CHANGE-2026-09-11-01 — cross-connection count is disputed three ways.** 107 (daily run banner) vs 131
  (master wiki, declared stale by the morning walk run) vs 135 (09-10 ingest, restated by this register
  last night). First three-way divergence on record. Connection density is not computable tonight.
  ASSUMPTION-1319, OPEN-195.
- **CHANGE-2026-09-11-02 — network otherwise flat: 867 triplets (0 ingested), 93 findings (+3), 1501 L2
  signals (+1202).** The signal jump is a 5× rise admitted by a guard that only rejects a >10% *drop*;
  its explanation is labelled inference by its own author. ASSUMPTION-1318, PRESUMPTION-957.
- **CHANGE-2026-09-11-03 — `pending/` 0 → 1.** PROP-2026-09-11-001 (Carroll, Mindscape 367, Jared Diamond
  2026-09-07), **verification-gated**: episode identity, date, guest, book and description confirmed;
  transcript exceeded retrieval budget and was not read; all three PRS candidates marked Speculative with
  the specific check named. Arkani-Hamed returned a real zero, with the four searches named.
- **CHANGE-2026-09-11-04 — lit-search queue emptied.** 20 items, both directions, fully dispositioned:
  40 result files, 3 systemic-risk flags, DISPOSITION-928–947, **PREMISE-204**, MONITOR-600–605,
  REVISE-447–458. **One mint from twenty — the lowest yield per cohort on record**, with three further
  mints declined and two of those declined for paywalled, unretrieved sources.
- **CHANGE-2026-09-11-05 — pre-answering measured at 20/20, seven verbatim.** Third measurement of the
  series (prior two at 82%). The proposed grep remedy was dispositioned **REVISE, not INCORPORATE**
  (REVISE-449): suppressing search on pre-answered items would render all 158 ACTIVE premises
  unfalsifiable. 15b relocated the defect upstream to **premise propagation, not routing** (Critical).
  OPEN-192 unruled; OPEN-199 raised tonight as the question upstream of it.
- **CHANGE-2026-09-11-06 — DEFERRED-CONDITION LEAKAGE FLAG escalated open → urgent**, with a calendar
  deadline: PROP-2026-09-02-002's condition ("queue a retrieval check after 2026-09-24") is held by
  nothing. **On 09-24 nothing will fire.** The pattern detector reached the same conclusion independently
  from the ingestion end (FINDING-089). Channel 4 remains unopened; the abstention's cost is recorded.
- **CHANGE-2026-09-11-07 — the recommended systemic fix for that flag is invalid as worded.** It keys on a
  `content_verified` frontmatter field appearing in **zero of 414** approved files. First self-invalidated
  remedy on record. ASSUMPTION-1328.
- **CHANGE-2026-09-11-08 — the 09-10 batch pass was machine-verified and closes exactly.** 36 cards / 36
  dispositions, approved 378 → 414, pending → 0; all ids unique, every date-prefix matching its filename,
  every file containing its own `proposal_id`. **First auditable pass since the INTEGRITY FLAG was
  raised.** Separately: decisions now arrive by **Gmail** carrying id and filename, `review/` root holds
  zero HTML pages, so the position-based-ID bug is out of the path — *if the route is standing* (OPEN-196).
- **CHANGE-2026-09-11-09 — two same-day runs contradicted each other on the same blocker and neither could
  know.** Lit pipeline: `sqlite3 'file:…?mode=ro'` needs no byte-copy, VERIFIED at sqlite.org (REVISE-455,
  "answerable today"). Metabolism regen: the sandbox is "structurally unable" because of that copy, and the
  task should move to launchd. Collateral: the database's size is given as ~3 GB, 6 GB and 6.3 GB in three
  places. ASSUMPTION-1325, PRESUMPTION-961.
- **CHANGE-2026-09-11-10 — metabolism regeneration failed for a fifth recorded time** (08-01, 08-06, 09-06,
  09-10, 09-11); Desktop Commander auto-declined with nobody present. `metabolism_data.json` is now **8
  days stale** and the publisher's own 36 h freshness gate — breached at 65 h on 09-06 — has not been
  evaluated since. The run states plainly that **"a sentence in CLAUDE.md cannot grant"** the permission
  needed, and that the task needs no model in the loop at all. OPEN-197.
- **CHANGE-2026-09-11-11 — a health report's green contradicts a daily run's own phase output.** Morning
  system health listed "today's daily run both finished and committed" as green; the 09-11 daily run reports
  **Phase 6 BLOCKED — sandbox cannot write .git objects**. The benign reading (the health run read 09-10's
  artefact as current) cannot be confirmed, because no green line carries the timestamp of its evidence —
  which three ACTIVE premises (086, 100, 141) already require. Filed at **Critical**: PRESUMPTION-962.
- **CHANGE-2026-09-11-12 — 264 lines of script changes will never be committed by any job.**
  `check_scheduler_health.py` (+113), `test_check_scheduler_health.py` (+84), `commit_daily_run.sh` (+27),
  `metabolism_monitor.py` (+24), `janitor.py` (+9), `.gitignore` (+7) sit outside the daily-run allowlist by
  design; 167 paths dirty overall; `Claude outputs/` and `outreach/` untracked at top level.
- **CHANGE-2026-09-11-13 — the 09-10 run never wrote its Run Narrative entry, though that file's header
  asserts it did.** Today's run recorded the gap rather than backfilling an entry it did not witness. The
  master wiki carries the same contradiction from the other side and the morning walk run declined to pick
  a side, flagging that tomorrow's 08:00 run will overwrite the trail.
- **CHANGE-2026-09-11-14 — a second search-layer date fabrication.** The Cornell "Shut up and Calculate!"
  page surfaced twice as a September 2026 event and is bylined **9/19/2017** — nine years old. Caught by
  reading the byline, a check no task file requires. Same shape as the Fridman×Hoffman trap.
  ASSUMPTION-1331, PRESUMPTION-968.
- **CHANGE-2026-09-11-15 — the Friday specialist task file names a wiki root that does not exist** (missing
  the `Projects/` segment). The run used the real path and completed; the SKILL.md repair is outside its
  write scope and was not made — PRESUMPTION-956's pattern recurring the day after it was surfaced.
  ASSUMPTION-1332.
- **CHANGE-2026-09-11-16 — PROP-2026-08-14-033 burned a fourth run.** Correct refusal each time, nothing
  ingested, nothing yielded; it is the sole occupant of the compile queue, so the queue looks alive.
  Cumulative retrieval cost recorded nowhere. OPEN-198.
- **CHANGE-2026-09-11-17 — OPEN-194's requested convention was practised without being adopted.** The daily
  run named its four direct searches in the log for exactly the reason OPEN-194 gave, did not cite it, and
  the convention entered no task file. OPEN-194 stays OPEN; tomorrow's run inherits nothing.
  ASSUMPTION-1330, PRESUMPTION-963.
- **CHANGE-2026-09-11-18 — the execution queue is four months stalled.** 11 open items, nothing added since
  2026-05-13, item one still the March scaffolding placeholder, three email tasks (Gallagher, Wokorach,
  ACPA) four months old and possibly moot. Not pruned; read each morning as live state. PRESUMPTION-966.
- **CHANGE-2026-09-11-19 — fourth consecutive outbound sync failure, third consecutive day dark in both
  directions, ninth consecutive inbound failure.** Chrome extension not connected; the browser pane's
  claude.ai grant is unanswerable in an unattended run. OPEN-191 asked on 09-09 whether such a task should
  remain scheduled; unruled, and it ran and failed again.
- **CHANGE-2026-09-11-20 — 15d re-trigger lane unserved for a seventh consecutive cycle**, with six new
  MONITOR items added to it today by a run that cannot serve it.

## Decisions

No new entries; the register remains at **DECISION-083 (2026-08-27) — fifteen days**. Phase 0 was a
verified no-op. **Seventeen rulings are now owed and named** (see the 09-11 note appended to
`decisions.md`), one of them — the deferred-condition leakage ruling — now carrying a **2026-09-24**
deadline.

## Assumptions Extracted (14a)

Fifteen: **ASSUMPTION-1318 – 1332.** Twelve SUPPORTED in-house, one routed (1321), two UNTESTED
(1324, 1327). All agent-stated; zero cross-project, which is a coverage artefact of the unread Summa
cohort rather than a real fall. The sharpest are **1319** (three cross-connection counts, none measured),
**1325** (two runs, one blocker, opposite conclusions, no mechanism for either to learn of the other),
**1328** (a remedy keyed on a field in zero of 414 files), **1329** (a condition that fails visibly on
2026-09-24), and **1326** (that a capability cannot be granted by prose in a governance file — a claim
about the relation between this estate's written rules and its enforcement surface, and therefore wider
than the task that produced it).

## Presumptions Surfaced (14b)

Twelve: **PRESUMPTION-957 – 968.** Structural 3, epistemic 3, methodological 3, normative 2, scaling 1.
One **Critical** (**962** — that "green" means checked today; the green in question is the commit path).
Five High: **958** (that a number reported by the run which produced it is a measurement — the general form
of the day's two numeric divergences), **960** (that 20/20 pre-answering is a routing fault rather than an
observation whose reading — saturation or unfalsifiability — nobody has selected, while both candidate
remedies presume a third reading neither supports), **961** (that two runs on the same day cannot
contradict each other), **967** (that a verification gate written inside a card substitutes for
verification), **968** (that a search layer's date errors are incidents rather than a base rate). Eleven of
the twelve name an in-house measurement available before any search.

## Lit Search Queue Seeded

**Four items routed** — ASSUMPTION-1321, PRESUMPTION-959, PRESUMPTION-960, PRESUMPTION-968 — each with a
search strategy and, where one exists, the cheaper in-house test named alongside it. **Twenty-three of
twenty-seven items were held in-house and the holding was declared rather than performed silently**, on the
stated ground that the queue was emptied hours ago and the premise register is measured at 100% likely to
pre-answer. **Declared against interest:** this is the behaviour ASSUMPTION-1309 proposed and REVISE-449
rejected, executed by judgement instead of by grep. Whether those differ is OPEN-199, and this register
cannot adjudicate it.

## Provenance

All twenty-seven items carry PROVENANCE headers per `provenance_protocol.md` v1.0, with Origin, Chain,
Original item, Item type, Transform and Current status. Registers snapshotted as
`*.bak.20260911-pre-14eod` before any append. Every item tonight is marked `[stated]` from an agent run
report (14a) or `[inferred]` (14b); **no item derives from designer speech** — twelfth consecutive day.
