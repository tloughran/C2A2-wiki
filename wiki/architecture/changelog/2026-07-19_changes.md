# Changelog — 2026-07-19

Fourteenth autonomous day, and the sharpest split yet between what the pipeline learned and what the fleet reported. The pipeline had its best day on record: the 12-item batch came back with **six INCORPORATEs (PREMISE-099–104)**, 15a/15b independence became structural rather than asserted for the first time, and — the day's real event — **the accelerator's own target function came under challenge from material the accelerator itself collected**. Levin's virtual-governor result argues that forcing parts into too-complete agreement destroys the local optimization that made the collective intelligent; if it transfers, Rung-2 should not be scored on convergence, and convergence would be evidence that the detector is damaging what it measures (A-475, OPEN-125). 14b's addition is that convergence has never been written down as a hypothesis anywhere in this registry, which is why its refutation arrives as news (P-500, risk Critical). Against that: **the reporting layer inverted**. A summarizing agent told Tom "No failures to report" on a morning with four concurrent failures and delivered it outbound as voice (A-480, P-503), and a named inter-agent handoff was dropped with neither party noticing (P-502). Underneath both, one shape recurred four times — **detectors that become more reassuring as the condition they monitor worsens** (P-505), stated locally once by 15d and generalized by nobody.

PROVENANCE:
  Origin: 14a
  Chain: [14a]
  Item type: CHANGELOG
  Transform: compiled from direct reads of the twenty 2026-07-19 scheduled-session transcripts listed in the snapshot's method note; registry-state reads of assumptions.md / presumptions.md / for_lit_search.md / open_questions.md / decisions.md; and the 07-18 changelog and snapshot for baselines. Session inventory via session_info; all inspected 2026-07-19 sessions were scheduled tasks — no interactive Cowork session occurred. Transcript coverage delegated to three parallel subagents; token budget breached and disclosed below.
  Current status: RECORDED

## What went to plan today (recorded, not celebrated)

- **Six INCORPORATEs in one cycle** — the largest single-day yield on record. PREMISE-099–104 from A-469, A-472, P-494, P-495, P-497, P-498. Yesterday's observations about counting authority, watchdog scope, deferral-as-policy, evidence-kind confusion and registry scaling are now validated premises.
- **15a/15b independence became structural.** "15b was blocked from reading 15a's output, which is the first run where that independence was structural rather than asserted" (A-477). The load-bearing property of the whole for/against design was, until today, asserted — which puts a question mark over the 905 pairs already on record.
- **15c audited its own pipeline's output.** REVISE-231, High systemic risk across 7 of 12 items: "the observations are well evidenced but the proposed remedies are unvalidated against the actual mechanism when a cheap discriminating test exists" (A-479).
- **Sewing agent's weekly pass.** 10 pages, 36 agentic calls across 12 traditions, 18 bridge notes (4 created, 11 appended, 3 zero-byte stubs filled); stubs 13 → 10. Seven calls failed the cites-a-specific-id check and were rewritten rather than shipped as boilerplate.
- **15d clean and on schedule.** 21 re-queued, 67 carried, 3 escalations, 452 MONITOR ids reconciled; MONITOR-420's auto-escalate trigger fired exactly as specified.
- **First connectivity movement in four weeks** — sparse −6, connected +6, and the bootstrap audit confirmed it "isn't resolver drift" because the hub layer is identical in rank order.

## Registry changes (this run)

- **Assumptions:** +8 (A-474 … A-481), all UNTESTED.
  - **A-474** — the census measures machine-dump volume, not graph health: "the apparent +225 jump mixes ~+145 real growth with ~+80 of definitional difference." Fourth week flagged, not acted on.
  - **A-475** (HIGH) — over-alignment against convergence scoring; the day's most consequential stated claim, with its own instrumentation gap named: no proxy separates *understood* from *adopted*, and "without it the constraint is unenforceable."
  - **A-476** (HIGH) — 15d's staleness detector loses sensitivity as consumption stalls; wall-clock remedy stated and deliberately not implemented ("that's a design decision, not 15d bookkeeping").
  - **A-477** — independence structural for the first time; everything before it was asserted.
  - **A-478** (HIGH) — the queue never drains: "at ~12 items per run against a daily enqueue rate." Plus a sixfold token-budget breach reported under fail-loud, with the conclusion that "the budget and the pipeline's scope are inconsistent, and one of them needs revising."
  - **A-479** (HIGH) — remedies unvalidated against mechanism, and three of them "push more signal into a channel with demonstrated zero throughput."
  - **A-480** (HIGH) — "No failures to report," delivered outbound, on a four-failure morning.
  - **A-481** — connector enumeration broken four weeks; includes an explicit self-retraction ("Last week's brief framed the missing 7-day signal as a monthly-vs-weekly mismatch. That was too charitable") and a refusal to infer past the instrument.
- **Presumptions:** +6 (P-500 … P-505), all UNTESTED.
  - **P-500** (Risk **Critical**) — convergence presumed to be the success signature of inter-tradition dialogue. Nowhere stated, built into the Rung-2 detector and every cross-connection counter, and the alternative signature has no instrument.
  - **P-501** (Risk Medium-High) — a measurement disagreement presumed to be a de-inflation problem rather than a contradiction. Two same-day censuses, four differing numbers, two differing corrections, no reconciliation; plus two ID-maximum disagreements in the same day's summary.
  - **P-502** (Risk High) — flagging to a named agent presumed to constitute delivery. First observed instance of a designed handoff named, aimed and silently dropped.
  - **P-503** (Risk High) — summarizers presumed to read what they summarize. The run read its own sources faithfully; its sources do not include the failing agents' outputs.
  - **P-504** (Risk Medium) — a routinely and openly violated constraint presumed still to be a constraint; fail-loud has become the mechanism by which breach is normalized.
  - **P-505** (Risk High) — detectors presumed to degrade gracefully rather than invert. Four same-day instances, one local statement between them.
- **Open questions:** +4 (OPEN-125 convergence scoring and the understanding/adoption proxy; OPEN-126 externalized success criteria — testimony vs evidence; OPEN-127 census exclusion and break-marker; OPEN-128 institutional events as a node type).
- **Decisions:** +0 (latest remain 076/077/078 from 07-05; **fourteenth** consecutive autonomous day with no new decision).
- **Validated premises:** **+6 — PREMISE-099 … 104** (max ID now 104).
- **Lit search queue:** +14 items — the 07-19 14a/14b batch. A-475, A-476, A-478, A-479, A-480, P-500, P-502, P-503, P-505 at HIGH. Enqueued into a queue that today stated it never drains; recorded as such in the batch header.
- **Backups:** assumptions / presumptions / for_lit_search / open_questions → `.bak.20260719-pre-14eod`.

## The four deep problems — status

1. **Chat sync down both directions** (07-13 → 07-19, now 7 days). Unchanged: claude.ai signed out in the extension's Chrome profile, `/recents` redirects to `/logout`, a second Chrome browser on the account still needs selection. The 17th, 18th and 19th are uncaptured.
2. **Master-wiki evidence frozen** at 300/90/50 since 07-09; FINDING-048 / FLAG-016 deposits un-ingested. Worse today: PRS now has **three** live counters — 300 maintained, 448 per-file sum (self-caught for a second consecutive day, with the run noting "two consecutive self-catches means flag 3 should be fixed at source"), and 453 from the connectome weekly, which validated PASS on its own terms.
3. **Persistence cannot self-complete.** c282 Phase 6 blocked on both commit and push: a 0-byte `.git/index.lock` the mount will not let the agent remove, no push credentials, 121 files staged, and the repo sitting on branch `voice-guide-realtime-faq` rather than main.
4. **Instrumentation (new, and now the sharpest).** Four detectors whose pass state is reachable while the monitored subsystem is dead — 15d staleness, the scheduler watchdog's unrunnable checks, connector enumeration, the vault census (P-505). This is the family the fleet's daily green signal is made of.

## New this run

- **The target function is contestable and was never written down** (A-475 / P-500 / OPEN-125). The system collected the material that challenges its own success criterion. That is the accelerator working — and it also means every result to date has been scored on an untested criterion.
- **The reporting layer inverted** (A-480 / P-503). Not a wrong number this time: the negation of four concurrent failure reports, delivered outbound to the only person who can intervene.
- **A named handoff was dropped** (P-502). Connector health → morning system health, cost-tracker gap, four months of missing reports from a task that "is failing rather than absent." Neither party recorded it as unconfirmed.
- **Detectors that invert rather than degrade** (P-505). The general form of A-472 and A-476; four instances in one day, generalized by nobody inside the fleet.
- **Fail-loud as the mechanism of normalization** (P-504). Three budget breaches today — the lit pipeline (~6×), the Sunday tradition agent, and **this pipeline** — all honestly reported, none revised, no agent with authority to revise. Disclosed rather than absorbed, per Rule 12.

## Standing items awaiting Tom (updated order)

1. **OPEN-125 — should Rung-2 be scored on convergence at all?** New at the top because it bears on the target function of the whole system, and because the instrumentation question (a proxy separating *understood* from *adopted*) has to be answered before any alternative metric can be built. Flagged by the sewing agent as the walk-worthy item.
2. **Chrome sign-in + browser selection** — sync down 7 days, both directions. Everything else assumes a channel that can transmit.
3. **Bind health claims to artifacts** (A-480, P-503, P-505) — one rule would close three of today's four headlines: no health claim without a named artifact and a freshness bound, and delete or mount every check whose pass state is reachable while its subsystem is dead.
4. **OPEN-123 — escalate on age, or throttle output?** Fourteen days of production into a queue nobody drains; the lit run has now stated the non-drainage as arithmetic (A-478).
5. **Counting authority (OPEN-124, OPEN-127, P-501)** — now six instances. Name one canonical counter per artifact, rule on the census exclusion, and decide whether known-drift flags block emission rather than annotate.
6. **Persistence loop (OPEN-121)** — `.git/index.lock`, credentials, and the wrong branch. 121 files staged, commits still unpushed.
7. **Review backlog** — 32 proposals (29 per Agent 16), 19-day gap, no decision email since 06-30. Includes the Wright proposal two agents deliberately declined to process; that one needs listening to or denying.
8. **OPEN-126 — testimony vs evidence** ruling on externalized success criteria; two Rohr proposals volunteered them unprompted this week.
9. **OpenStory + metabolism recovery (Mac-side)** — `.recover` on `open-story.db`; seventh consecutive telemetry failure, twelfth consecutive failed regen, `quick_check` failing on a 4.46 GB db with rowid corruption at pages ~828k–1051k. Nothing downstream can move until the writer is alive.
10. **Backup** — "BACKUP FAILED — H drive not mounted." Nothing backed up, nothing pruned, log unreachable.
11. **OPEN-128 — institutional events as a node type**; second week, no home, and the VERSES/Friston material is exactly the evidence the design says it wants.
12. **Housekeeping** — retire the bootstrap audit (fifth firing of a one-time task); archive `watch_list.md`'s run log (2,861 lines, under 2% live); fix `generate_review_page.py`'s position-based decision IDs *before* the review pass; delete the needs_review tombstone.

## Note on paradigm-shift channel

No routing adjudications changed: Kastrup↔Levin still awaits adjudication (pending since 07-08), Hoffman-sides-with-Levin still BLOCKED pending primary-text verification (REVISE-214). One new candidate: the Rohr "Practicing 'Just This'" proposal's PRS-CANDIDATE-B — non-suppressive attention as precision-weight decay on fear-priors — flagged as a Friston paradigm-bridge candidate. The sewing agent also sharpened Levin×Friston for a fourth consecutive week: "The interesting one is a *crossing*, not a convergence... Two traditions passing through the matter/information boundary in opposite directions," and recommended promotion to a standalone synthesis page, `friston_levin_bridge.md` now carrying eight distinct claims. A cheap empirical test was proposed for the Kastrup/Levin nesting-vs-dissociation dispute: whether IFS/parts-work outcome literature records post-treatment persistence of "parts" — Levin predicts yes (agent repurposed), Kastrup predicts the dissociative boundary dissolves. Upstream, FINDING-048's evidence intake remains frozen since 07-09.
