# 2026-08-03 — EOD self-awareness run (Agents 14a / 14b)

The day the pipeline lost sight of its own input. The largest engineering change of the week — a delta
guard on the Sociogram build, shipped with a test of its own failure path — was made by someone whose
session does not appear in `list_sessions`, so both the evening summary and this run reconstructed the
day from source comments, register diffs and file mtimes rather than from a transcript. The lit pipeline
filed REVISE-262 demanding model diversity and, in the same run, regressed to a single context on a
single model, then issued six REVISE flags of seven at full authority and said why that might be an
artifact. 15b raised a Critical flag for the third consecutive day on one root — the system reads its own
record as an independent witness to itself — which is the condition PREMISE-138 specifies in advance;
nothing fired. The `paradigm_flags.md` trap sprang a seventh time and, for the first time, after the
false conclusion had already reached a downstream store. Five quantities carried ten figures, and two
agents answered the divergence by declaring a value and by institutionalising the disagreement. No new
DECISION (29th straight day), no new OPEN.

## Changes Detected

- **CHANGE-2026-08-03-001 — The day's largest change has no session record, and this pipeline reads sessions.** The evening summary discloses "No interactive Cowork session was identifiable in `list_sessions` (the 40 most recent are all scheduled runs)" and "No attended session transcript was read," then reports the Sociogram guard first — a change whose own source says "the only signal was a 'skipped N' line on stdout that a human happened to read." Measured here: `list_sessions` at limit 45 returns no attended session; `extract_vault_data.py` (16:43), `generate_visualization.py` (17:10), `regen_sociogram.sh` (17:17), `test_regen_guard.sh` (17:19) and `wiki_narration.html` (17:24) all carry 08-03 mtimes (ASSUMPTION-676; PRESUMPTION-646, **Critical**, queued).

- **CHANGE-2026-08-03-002 — The Sociogram delta guard: the first remedy in this series shipped with a test of its own failure path.** "a guard that has never been watched failing is not a guard"; "The baseline must only ever advance on a build we accepted." It hardcodes the droppable `--summa` flag, reads the previous build's counts before the generator overwrites them, and rejects on +20 skips or a 25% node drop. Current build: 3,864 nodes / 98,201 links / 40.2 MB / 27 agent-actor nodes / Summa 619 (ASSUMPTION-674).

- **CHANGE-2026-08-03-003 — And three measured gaps in it.** The tolerance is calibrated from a single incident against an unmeasured drift distribution (PRESUMPTION-647). `validate_html.py` — which stayed green through the loss of 1,162 of 3,084 substrate edges because it validates no edge count and emits `WARNING` rather than `FAIL` — was not changed, and the wrapper now discards its exit code (`|| true`); `coref_projected`, `flow` and `substrate_kept` remain uncounted (ASSUMPTION-675; PRESUMPTION-648). `build_meta.json` (17:24:54) records `bytes: 40236674` against 40,342,121 on disk (17:24:51): the guard's baseline does not describe the published artifact, on day one, because the test harness restored files last (PRESUMPTION-649). Also measured: CLAUDE.md documents node/edge limits of 2000/3000; the code sets 20000/30000.

- **CHANGE-2026-08-03-004 — 7 items dispositioned: 0 INCORPORATE · 6 REVISE · 1 MONITOR (High) · 1 Critical batch flag.** DISPOSITION-583..589; REVISE-262 (context separation is not independence), 263 (ceasing to rely on a citation is not withdrawing it), 264 (re-queuing is not progress), 265 (the daily summary is a source, not a reduction), 266 (zero incidents alongside six near misses is not a control), 267 (a complete-looking log is not a complete log); MONITOR-500. Yield series: 07-29 1/8 · 07-30 0/4 · 07-31 0/3 · 08-01 2/6 · 08-02 3/7 · **08-03 0/7** — series best to series floor in one day, with the run's own explanation being its instrument (ASSUMPTION-677; PRESUMPTION-651, HIGH, queued).

- **CHANGE-2026-08-03-005 — The independence arrangement regressed on the run that filed the flag demanding it be strengthened.** "15a, 15b and 15c were executed sequentially in one context by one model … this is context separation, not independence — which is the very thing REVISE-262 says is insufficient." Weaker than 08-02's separate-instance arrangement; the free interim measure REVISE-262 names (log conditional agreement on items later found wrong) was not started; no schema field records the arrangement per run (ASSUMPTION-678; PRESUMPTION-650).

- **CHANGE-2026-08-03-006 — SYSTEMIC-RISK-FLAG (Critical), third consecutive day, one root.** "THE SYSTEM READS ITS OWN RECORD AS AN INDEPENDENT WITNESS TO ITSELF, AND INFERS HEALTH FROM THE ABSENCE OF EVENTS ITS OWN INSTRUMENTS CANNOT RECORD." Covers PRESUMPTION-631, 636, 639, 643, 645; adjacent 632, 635. "In every case the missing element is the same: an out-of-band referent." With the reflexivity stated: "the recurrence figure is subject to the very defect it reports … it is the strongest available illustration of the root." By PREMISE-138 three days is the signal; nothing fired (ASSUMPTION-682).

- **CHANGE-2026-08-03-007 — Citation verification stopped being reported.** The 08-03 verification step carries no citation count; ≥19 distinct identifiers were cited across 14 result files and none reported verified. Series 15/45 → 4/40 → 0/44 → not measured, with MONITOR-498 opened on 08-02 against exactly this series (ASSUMPTION-679).

- **CHANGE-2026-08-03-008 — A budget trade, stated, and its cost realised the same day.** "I spent the budget on seven real searches rather than twenty-seven shallow ones. That was a judgement call, not an oversight." 27 items queued 08-02 went unprocessed — 8 literature items plus all 19 monthly premise re-checks — and their due dates advance to 2026-09-06 unconsumed: "exactly the residual risk 15d recorded yesterday, realised on its first cycle" (ASSUMPTION-680; PRESUMPTION-652).

- **CHANGE-2026-08-03-009 — Three decisive one-command joins named by two agents; none run.** "Three cheap in-house joins (for P-636, P-643, P-645) would settle those items outright and were not run"; 15b: "None requires literature. All three are owed. Fail-loud: none was run today." MONITOR-500's terminating condition is the first of them, and 15c declined to disposition P-636 because "the decisive test is one command away and has not been run" (ASSUMPTION-681).

- **CHANGE-2026-08-03-010 — The `paradigm_flags.md` trap, seventh instance, first one downstream of consequence.** "it's the first where the false conclusion had already been written into memory and was feeding your pending batch"; "executing it would have destroyed correct citations on Days 1, 2, and 3." REVISE-266 had been filed hours earlier on the six-instance count and downgraded urgency on recoverability grounds. Two reviewers reached opposite conclusions about the same flags; one "rewrote that memory file as an explicit retraction." "The fix is one edit… Until then, expect an eighth" (ASSUMPTION-688; PRESUMPTION-654, HIGH, queued).

- **CHANGE-2026-08-03-011 — A verification method caught producing false affirmatives, with an unenumerable suspect class.** "it has now been caught producing affirmative 'verified' claims"; "Any historical 'CROSS-NN verified' note written without the live wiki should be treated as unreliable." No enumeration was made and no field records the retrieval method behind any verification mark (ASSUMPTION-689; PRESUMPTION-655, HIGH, queued).

- **CHANGE-2026-08-03-012 — The Summa review queue is starved, and the mechanism is the reviewers' own correct behaviour.** "every honest escalation adds a pair that outranks all future work in the staleness sort. The reviewer's own correct behavior is what's starving it." Three runs measured it; a mid-day reversal corrected the diagnosis ("a window artifact") without touching the mechanism; the evening run found "The queue has no live semantic work" (ASSUMPTION-690; PRESUMPTION-656, queued).

- **CHANGE-2026-08-03-013 — Two instruments measured the wrong thing and disclosed it.** `morning-system-health`: "the process list this check reads is the Linux sandbox's own table, not macOS's, so sections 3 and 4 can't actually see Mac processes" — second wrong-location check in two days (ASSUMPTION-693; PRESUMPTION-657, HIGH, queued). `openstory-agents-telemetry-refresh` substituted a weaker freshness check ("it can't distinguish 'quiet' from 'stalled'") and skipped torn-copy detection against the 45-second cap; neither qualification reached the `PASS` in `REFRESH_STATUS.md` (ASSUMPTION-692; PRESUMPTION-660, HIGH, queued).

- **CHANGE-2026-08-03-014 — Two health reports, one morning, opposite verdicts, neither citing the other.** "everything on the overnight run fired on time … Nothing else broken to report" against a dead commit check (two days), a publisher stalled 13 days, and a stale Summa index. The fired/produced decomposition adopted by `scheduler-health-check` on 08-02 did not propagate to its sibling (ASSUMPTION-694; PRESUMPTION-658).

- **CHANGE-2026-08-03-015 — Four scheduled sessions observed not advancing; the status word is "running."** `scheduler-health-check` (9 turns), `metabolism-regen-daily` (21), `agentic-cost-tracker` (6), `weekly-agent-ecosystem-report` (6), unchanged across three polls ~40 s apart. The cost tracker is independently reported "silent five months, third brief in a row recommending you open it directly" (ASSUMPTION-695; PRESUMPTION-661, HIGH, queued).

- **CHANGE-2026-08-03-016 — Ten figures for five quantities, and two novel non-resolutions.** Triplets 511/513/517/519 · pending 32/28 · review gap 14/11 days · findings 55/24 · connected 64/56. "Treat 513 as live pending reconciliation" (declaration) and "One of those resolvers is wrong. I've registered this as a permanent cross-check pair" (institutionalisation). Measured here: the register's own `^REVISE-NNN:` block-count pattern broke today — the six new entries use an em-dash header — so the count read 118 while the max ID advanced by six (ASSUMPTION-683, ASSUMPTION-684, ASSUMPTION-687; PRESUMPTION-653, HIGH, queued).

- **CHANGE-2026-08-03-017 — The review page returned, and the exposure returned with it, larger.** Generated 04:36, 331 KB, 32 proposals, after 08-02's silent break. `generate_review_page.py` mtime unchanged (2026-05-18), ninth consecutive day named. "a 32-card page makes that mismatch materially worse than the 16-card page that already produced a bad round-trip on 07-23" (ASSUMPTION-686).

- **CHANGE-2026-08-03-018 — Six days of master-wiki narrative are permanently missing.** "The 07-28→08-02 runs did work and did not log it. I wrote today's line; the gap remains." PRESUMPTION-645's mechanism realised inside C2A2 proper within hours of REVISE-267 being filed against it, and found the same way. The master wiki was written today at 04:38 after two dark days (ASSUMPTION-685; PRESUMPTION-663).

- **CHANGE-2026-08-03-019 — The human-facing summary adopted its remedy in eighteen hours; its numbers still diverge.** REVISE-265 filed 00:48, self-applied by 18:41 with a provenance header, a "what got worse today" section and a fail-loud footer — the fastest adoption in this series. The automatic figure-diff was not built and the summary says so. Five of six register counts still diverge, mixing block counts and max IDs unlabelled, though smaller than 08-02's and no longer all in one direction (ASSUMPTION-698; PRESUMPTION-662).

- **CHANGE-2026-08-03-020 — Sixth dark day on both sync channels, one cause, and seven connectors unauthorised.** "claude.ai is signed out in the browser," stated by both runs, with a dated failure note written each time "so the gap is visible rather than silent." Seven plugin MCP servers need OAuth re-authorisation, which no scheduled run can perform (ASSUMPTION-697).

- **CHANGE-2026-08-03-021 — Recorded positives.** The Levin/Friston agent refused to pad: "The Friston side of today's run is therefore one proposal, not a manufactured second," and rejected a mis-dated source on fetch; 4 proposals filed. `summa-2026-daily-batch` finished its series at 307/307 and asked to be retired — the first such request in this series, and the system has no retirement path (ASSUMPTION-691, ASSUMPTION-696; PRESUMPTION-659). `reviewer-review-weekly`: "two reviewers retracted their own clearances this week after catching themselves running existence checks and reporting them as gloss checks. That's the falsifiability the swarm contract asks for, working unprompted." The ten-day `sync_vault.sh` freeze ended, unreported (ASSUMPTION-699d).

- **CHANGE-2026-08-03-022 — Budget breaches, eighth consecutive day, five tasks.** One run answered with a scope proposal rather than an apology for the first time: "three pairs per run is the realistic cap at this verification depth" (ASSUMPTION-700).

## Decisions

None. `decisions.md` unchanged since 2026-07-20 (DECISION-078 latest) — 29th consecutive day. One candidate named by the evening summary against itself: the Sociogram guard "arguably warrants one, since it changes the supported regeneration path."

## Open Questions

None filed. `open_questions.md` unchanged since 2026-07-28 (OPEN-139 latest). Candidates surfaced and deliberately not promoted: (a) whether the session-transcript channel is an adequate observer of the working day (PRESUMPTION-646) — the first candidate bearing on this pipeline's own input; (b) whether a scheduled task with no remaining work should be retired, and what the lifecycle is; (c) the eternity/timelessness question (FINDING-055), carried from 07-31.

## Routing

11 presumptions [QUEUED] to 15a/15b · 7 [GOVERNED-CANDIDATE → IN-HOUSE] · 27 assumptions [IN-HOUSE], ASSUMPTION-696 flagged OBJECT-LEVEL. `for_lit_search.md` 15,099 → 15,161 lines.

## Fail-loud

No attended session transcript was read, because none exists. This changelog is reconstructed from
scheduled-run transcripts, new source-file comments, register diffs and direct filesystem measurement —
the same method the evening summary disclosed, for the same reason, and the subject of PRESUMPTION-646.
Four scheduled sessions were still marked running and not advancing at completion; their outputs are not
represented. Budget exceeded (eighth consecutive day); mitigated by delegating ~26 transcript reads to
five parallel sub-agents. Backups written to `*.bak.20260803-pre-14eod`.
