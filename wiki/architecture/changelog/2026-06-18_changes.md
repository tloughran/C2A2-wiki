# 2026-06-18 Changelog

## Agents 14a / 14b — EOD self-awareness pass (2026-06-18)

### Narrative
A real-extraction day, unlike the 06-17 null. One attended build session — "Thinker summaries for sociogram" — designed and shipped a Sociogram feature: a yellow `?` summary marker on all 15 thinkers, on Summa (Habash folded in), and on the Traditions header, opening a click-to-open / click-away-to-close brief pop-up, with the marker deliberately NOT toggling the node's filter checkbox. The data path is single-source-of-truth: the pop-up reads each thinker's `wiki.md` `**Summary**` block (non-thinker briefs from `traditions/_extra_summaries.json`), so future edits flow in on the next regen with no second copy to drift. After a one-time seed via `apply_summaries.py`, `wiki.md` becomes the source of truth and the bios are hand-edited there directly ("living bios") — with a loud caveat never to rerun the seed, which would clobber those edits. The feature was locally verified on localhost:8080 and pushed to `main` as commit `0fdc8ea` (per the constitutional rule, the localhost review + push stays with Tom). Both sync sessions correctly note there was no attended *planning* session (the 06-17 PRS-yield remedies remain a proposed agenda), but this build was attended and design-bearing: 14a extracted 5 stated assumptions, 14b surfaced 8 unstated presumptions, and DECISION-060 + OPEN-085 were registered.

### 14a (Assumption Extraction)
- Stated assumptions extracted: **5** — ASSUMPTION-328..332.
  - 328 (architectural): single-source-of-truth bios — read from the same `wiki.md` the agents maintain, "no second copy to drift."
  - 329 (methodological/architectural): the one-time seed `apply_summaries.py` must never be rerun (it clobbers hand-edits). Flagged as accumulating technical debt — destructive tooling left in-repo, guarded by a note.
  - 330 (architectural/methodological): `regen_sociogram.sh` is the only supported regen path (hardcodes `--summa`, guards Summa-less builds); direct `generate_visualization.py` forbidden.
  - 331 (methodological): manual local visual verify "satisfies the constitutional check" → clear to push.
  - 332 (architectural): the `?` feature is independent of Summa node counts, so the unexplained ~256-vs-379 gap doesn't block the push.
- decisions.md: **+1** — DECISION-060 (Sociogram summary-popup feature + "living bios" maintenance workflow; ADOPTED — built, verified, shipped as commit `0fdc8ea`).
- open_questions.md: **+1** — OPEN-085 (Summa ~256-vs-379 commentary-node gap in the regenerated `wiki_narration.html`: real drop or sandbox-measurement artifact? — parked as a Summa-pipeline item). Standing gates carry unchanged: OPEN-082 (parser/linker), OPEN-083 (run `probe_openstory.py`), OPEN-084 (269/264/262 reconcile-vs-report).
- Metrics snapshot produced: `metrics/2026-06-18_snapshot.md`.

### 14b (Presumption Detection)
- Unstated presumptions surfaced: **8** — PRESUMPTION-361..368.
  - 361 (structural): one short Summary block is the right granularity to represent a tradition.
  - 362 (structural): summaries belong on nodes (thinkers + Summa + header), not edges/sub-traditions.
  - 363 (epistemic/normative): a model-drafted, human-approved mini-bio adequately represents a living tradition (approval = adequacy).
  - 364 (methodological): local visual verify certifies correctness — render-correct ⇒ content-correct. **Twin of ASSUMPTION-331** (its deeper unstated form), not a duplicate.
  - 365 (methodological/scaling): the SSOT payoff presumes agents/Tom will actively maintain the Summary blocks going forward.
  - 366 (scaling/methodological): a documentation-only caveat is a sufficient guard for an armed destructive script. **Twin of ASSUMPTION-329**, not a duplicate.
  - 367 (normative): adding pop-ups improves the Sociogram (more on-demand info = better) — normative smuggling.
  - 368 (epistemic): the ~256 and ~379 Summa counts are commensurable, so the gap reads as a "drop." Recurrence of the 269/264/262 construct-divergence pattern (PRESUMPTION-357) on the Summa axis.
- Checked against 14a's output: 364↔331 and 366↔329 are flagged as twins (stated surface vs unstated substrate), surfaced deliberately under 14b's "false positives are cheap" standard; no true duplications.

### Lit-search queue
- New items seeded today: **13** (ASSUMPTION-328..332; PRESUMPTION-361..368), all tagged `[QUEUED] [cycle 0: 2026-06-18]` with full provenance headers, under a new "2026-06-18 cohort" section in `for_lit_search.md`. Not yet searched (15a/15b run on their own cadence). No 15c run today.

### Registry accounting (this run)
- Assumptions 327 → 332 (+5). Presumptions 360 → 368 (+8). Self-awareness registry 687 → 700 (+13).
- Decisions 59 → 60 (+1). Open questions 84 → 85 (+1).
- Lit queue: +13 QUEUED (this cohort). Validated premises 65 (+0), monitor queue (+0), revision flags (+0) — no 15c pipeline run today.
- Backups written (pre-run state preserved): `assumptions.md`, `presumptions.md`, `decisions.md`, `open_questions.md`, `for_lit_search.md` → `*.bak.20260617-pre-14eod`.

### Highest-leverage observations (surfaced, not acted on — Rule 3/Rule 12)
- The cheapest high-value fix from today's extraction is converting the never-rerun caveat from documentation into a code guard (make `apply_summaries.py` refuse when `wiki.md` already carries edits, or retire/rename it post-seed) — this closes the PRESUMPTION-366 risk and de-arms the named technical debt of ASSUMPTION-329.
- The single-source-of-truth architecture (ASSUMPTION-328) is sound but banks a payoff that depends on a maintenance loop not yet demonstrated (PRESUMPTION-365); worth watching whether the bios actually get edited.
- OPEN-085 belongs to the Summa pipeline, not the sociogram feature; the canonical PRS three-count divergence (OPEN-084) is now noisier with 279/222 orchestrator figures added, reinforcing REVISE-121's disambiguate-don't-reconcile posture.

PROVENANCE:
  Origin: 14a / 14b (joint EOD note)
  Chain: [14a], [14b]
  Item type: CHANGELOG (daily; self-awareness EOD pass — real extraction)
  Transform at each step:
    14a: Read the 2026-06-18 attended "Thinker summaries for sociogram" transcript (session_info MCP), cross-checked against the morning chat-scrape and evening Cowork→Chat sync; extracted 5 stated assumptions (ASSUMPTION-328..332); registered DECISION-060 and OPEN-085; produced the 06-18 metrics snapshot; routed all 5 to for_lit_search.md.
    14b: Read the same transcript with an interpretive lens; surfaced 8 unstated presumptions (PRESUMPTION-361..368), two flagged as twins of 14a items; routed all 8 to for_lit_search.md.
  Current status: COMPLETE — real-extraction day; 13 items extracted, registered with provenance, and queued; registries advanced; backups written.
