SEARCH-AGAINST-PRESUMPTION-368:
  Date searched: 2026-06-19
  Original item: PRESUMPTION-368
  Original statement: "[inferred] The two Summa counts (~256 vs ~379) are commensurable, so the difference reads as a 'drop' not a definitional mismatch."

  PROVENANCE:
    Origin: 14b
    Chain: [14b → 15b]
    Original item: PRESUMPTION-368
    Item type: PRESUMPTION (unstated — surfaced by inference)
    Transform at each step:
      14b: Surfaced as the unstated commensurability assumption
      15b: Searched for challenging literature
    Current status: CHALLENGED

  Challenging evidence found: Yes

  Sources:
    1. Construct validity / multiple operationalism (Cronbach & Meehl; Campbell & Fiske) — two measures are comparable only after establishing they operationalize the SAME construct on the same scale; counts from different sources/definitions are not commensurable by default, so their difference is not automatically a "drop."
    2. Definitional artifacts in measurement — a numeric difference between differently-defined counts can be entirely an artifact of what each counts (which entities qualify as a "Summa node"), not a change in the underlying quantity; reading it as a "drop" presupposes the very commensurability in question.
    3. Recurrence with 269/264/262 (PRESUMPTION-357 family) — this cohort has already surfaced the same category error for the PRS-yield counts; treating differently-sourced counts as one reconcilable quantity is a repeating systemic pattern, raising the prior that 256-vs-379 is definitional too.

  Strength of challenge: Strong

  Summary: Treating ~256 and ~379 as commensurable is strongly challenged: measurement theory requires demonstrating shared construct and scale before a difference is interpretable, and a gap between differently-defined counts is often a definitional artifact rather than a real drop. Reading it as a "drop" smuggles in the commensurability that has not been established — the same construct-validity error already flagged for 269/264/262 (PRESUMPTION-357).

  Specific risks: The system narrates a definitional mismatch as a real loss (or vice versa), misdiagnosing its own state; downstream decisions (incl. ASSUMPTION-332's "doesn't block push") inherit the misframing.

  Mitigations available: Define precisely what each count counts (source, inclusion rule) BEFORE comparing; if the definitions differ, label the gap a definitional mismatch, not a drop; if a single quantity is genuinely intended, reconcile the operationalizations or pick one canonical definition (mirror the 357 construct-disambiguation fix).

  STEELMAN:
    Strongest counterargument: If both numbers are honestly attempts to count "Summa nodes in the graph," then a large gap genuinely signals something went wrong (a real drop or bug), and dismissing it as "definitional" risks waving away a true defect — sometimes a discrepancy IS a discrepancy.
    What would need to be true for C2A2 to be safe: The two counts' definitions are written down and compared; only then can the gap be correctly classified as definitional-artifact vs real-change.
    How to test: Write the inclusion rule behind 256 and behind 379; if they differ, the gap is (at least partly) definitional; if identical, it is a real discrepancy to debug.

  Search scope: construct validity/multiple operationalism; definitional artifacts; commensurability of differently-sourced counts. Comprehensive.

  Recommendation: CHALLENGED
