SEARCH-AGAINST-PRESUMPTION-551:
  Date searched: 2026-07-27
  Original item: PRESUMPTION-551
  Original statement: [inferred] Auto-generated bridges are presumed genuine structural homologies by being generated and filed; auto-bridges enter synthesis/ without a transfer-condition check (the PREMISE-127 gap at generation scale).

  PROVENANCE:
    Origin: 14b
    Chain: [14b -> 15b]
    Original item: PRESUMPTION-551
    Item type: PRESUMPTION (unstated - surfaced by inference)
    Transform at each step:
      14b: surfaced from a batch of auto-generated bridges filed without carrying over the PREMISE-127 check
      15b: Searched for challenging literature
    Current status: PARTIALLY-CHALLENGED

  Challenging evidence found: Partial

  Sources:
    1. Generate-and-test / evolutionary discovery (Campbell 1960, "Blind Variation and Selective Retention"; Simonton on scientific creativity; genetic-programming symbolic regression, Koza). — Prolific candidate generation followed by a selection stage is a legitimate and productive epistemic architecture; the generation stage is SUPPOSED to over-produce. Filing candidates is not a truth claim if a selection/validation stage follows. Challenges reading "filed = presumed valid."
    2. Provisional-adoption norm (the PRESUMPTION-545 -> PREMISE-127 15b steelman). — The already-accepted remedy is provisional adoption with a "transfer condition: unverified" tag, not pre-filing proof. If synthesis/ is explicitly a candidate pool rather than a validated register, no new anti-pattern exists.
    3. Cost-of-validation / triage (software static-analysis triage literature). — Requiring a full transfer-condition proof on every one of a growing set of auto-bridges may be infeasible; risk-ranked sampling of the batch may be the rational control rather than exhaustive per-bridge proof.

  Strength of challenge: Moderate

  Summary: The challenge is that batch generation of candidate bridges is not per se an error: generate-and-test is a standard discovery architecture, and the accepted remedy from the PREMISE-127 case is provisional-flagging, not pre-filing proof. The presumption bites only if (a) synthesis/ is treated as a validated register rather than a candidate pool, or (b) downstream consumers use unflagged auto-bridges as settled. It does not show the bridges are invalid, only that their status must be labeled.

  Specific risks: Over-applying the presumption gates the exact high-volume synthesis C2A2 exists to produce behind an expensive per-bridge proof, killing throughput.

  Mitigations available: Tag auto-bridges "transfer-condition: unchecked" on entry to synthesis/ (PREMISE-127 applied as a cheap label, not a proof); risk-rank the batch and validate top candidates first.

  STEELMAN:
    Item: PRESUMPTION-551
    Strongest counterargument: A generation log that over-produces candidates is doing its job; conflating "filed" with "claimed true" imports a validated-register reading that synthesis/ need not have. The productive fix is a status label plus a downstream filter, which is PREMISE-127 operationalized, not a new problem.
    What would need to be true for C2A2 to be safe: synthesis/ entries carry an explicit validation-status field and no downstream consumer treats unflagged auto-bridges as validated.
    How to test: in-house - count today's new bridges carrying a recorded transfer-condition check, and check whether any downstream step consumes an unflagged auto-bridge as settled.

  Recommendation: PARTIALLY-CHALLENGED
