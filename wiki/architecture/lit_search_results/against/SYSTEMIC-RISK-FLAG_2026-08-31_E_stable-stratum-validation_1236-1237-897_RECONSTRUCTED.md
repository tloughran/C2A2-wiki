SYSTEMIC-RISK-FLAG_2026-08-31_E  (originally filed as _B; DESTROYED and RECONSTRUCTED)

  !! RECONSTRUCTION NOTICE — READ BEFORE USING THIS FILE !!
    This flag was originally written by the Agent 15b context handling the permissions/measurement
    cluster as `SYSTEMIC-RISK-FLAG_2026-08-31_B.md` at approximately 00:50 on 2026-08-31. A
    different concurrent Agent 15b context wrote its own `_B.md` at 00:52 and OVERWROTE it. That
    agent self-reported the overwrite at the top of the surviving `_B.md`. The original file was
    never read by anyone. It is gone.

    THIS FILE IS NOT A RECOVERY. It was reconstructed by Agent 15c from the authoring agent's own
    end-of-run return summary — a report of the file, not the file. See the identical notice in
    `SYSTEMIC-RISK-FLAG_2026-08-31_D_..._RECONSTRUCTED.md` for why this pipeline, on the evidence
    of this same run, does not count that as an independent copy. Marked LOSSY and DERIVATIVE.

    Fields known to be lost: literature basis, the author's stated risk level, and the author's
    recommendation wording. Risk level below is 15c's inference.

  Date: 2026-08-31
  Filed by: Agent 15b (Literature Search AGAINST), 6-item permissions/measurement assignment
  Reconstructed by: Agent 15c, 2026-08-31, same run
  Cohort: 2026-08-30 intake by Agents 14a/14b
  Affected items: ASSUMPTION-1236, ASSUMPTION-1237, PRESUMPTION-897

  COMMON VULNERABILITY:
    The pipeline validates where nothing changes, while all reported movement is elsewhere.

      - ASSUMPTION-1237 licenses the whole week-over-week delta programme on the ground that five
        top-hub counts reproduced exactly. Top hubs are, by construction, the stratum least
        sensitive to peripheral change. Per PRESUMPTION-897's own eight-week series, roughly 97%
        of the growth in that period was orphans — pages with no inbound links, which top-hub
        counts cannot register. Orphan detection is a distinct code path. The control therefore
        validates only the parser prefix the two paths share, not the measurement whose deltas are
        being claimed. A control that cannot fail is not a control.

      - ASSUMPTION-1236 and ASSUMPTION-1237 contradict each other inside the same run. 1237 treats
        exact reproduction as evidentially valuable — it is the entire warrant for the deltas.
        1236 treats exact reproduction as clutter and grounds discarding the artifact. Reproduction
        is being counted as informative when it yields assurance and as uninformative when it costs
        storage. Both cannot be right as stated; at most one distinction rescues them (the FACT of
        reproduction is cheap and informative, the RETAINED ARTIFACT is not), and neither item
        draws it.

      - PRESUMPTION-897 supplies the population the other two are reasoning about and asserts its
        growth is benign. It is the item with the weakest evidence in the cluster — no peer-reviewed
        information-retrieval or wiki-orphan literature was reached within budget — so the stratum
        where all the change is happening is also the stratum this run knows least about.

  RISK LEVEL: High  (15c inference — the authoring agent's own rating was lost in the overwrite)

  RECOMMENDATION (as summarised by the authoring agent):
    Re-select controls from the stratum where change is occurring — sample from the new
    machine-generated trees, not from the five most stable hubs — and validate on more than one
    subset. Quantify representativeness rather than asserting it. Until then, week-over-week deltas
    should be reported with the control's scope stated: validated on the shared parser prefix only.

  KEY SOURCES CARRIED FROM THE PER-ITEM FILES (the flag's own citation list was lost):
    J Immunol Methods 2024, PMID 38479453 — positive-control selection determines apparent assay
    sensitivity. SEP, "Reproducibility of Scientific Results" — the identical re-run IS the
    reproducibility check. See `ASSUMPTION-1237_against.md` and `ASSUMPTION-1236_against.md`,
    which survived intact.

  STATUS: Reconstructed, lossy. Re-running the original 15b context over these three items is the
  only way to restore full fidelity, and is recommended if this flag is to ground any decision.
