SEARCH-FOR-ASSUMPTION-452:
  Date searched: 2026-07-16
  Original item: ASSUMPTION-452
  Original statement: ONE convention, three layers - NO SELF-PRODUCED ARTIFACT MAY CERTIFY ITSELF (tooling by replay, denominators by independent corroboration, captures by primary-text verification). The three flagged instances share one generative cause and one rule settles them.

  PROVENANCE:
    Origin: 14a
    Chain: [14a -> 15a]
    Original item: ASSUMPTION-452
    Item type: ASSUMPTION (stated)
    Transform at each step:
      14a: Extracted/inferred to intake queue (for_lit_search.md)
      15a: Searched for supporting literature; result SUPPORTED (strength Strong)
    Current status: SUPPORTED

  Supporting evidence found: Yes

  Sources:
    1. SLSA v1.1 Specification & FAQ (slsa.dev, 2024). The framework's central design principle is that provenance must not be self-generated by the entity it certifies: at L1 self-attested/unsigned provenance carries no integrity guarantee, while L2+ requires provenance produced by the build platform independently of the build script. Directly instantiates 'no artifact may certify itself.'
    2. Systems Hardening, 'SLSA Build Provenance' (2024). A compromised build can emit false L1 provenance; independence of the attestor from the attested is what closes the gap - the general form of C2A2's rule across heterogeneous artifact classes.
    3. R.I. Cook, 'How Complex Systems Fail' (1998), pt. on defenses: independent verification layers are the standard defense-in-depth posture; a single generative cause settled by a single convention matches the 'shared latent condition' framing.

  Strength of support: Strong

  Summary: The attestation/provenance literature independently arrived at exactly C2A2's rule: the SLSA level ladder is built on the distinction between self-generated (untrustworthy) and independently-generated (trustworthy) attestations, and treats self-certification as the defining weakness of its lowest level. The convention generalizes cleanly across artifact classes (tooling, denominators, captures) because the underlying principle - separate the certifier from the certified - is class-independent. This is a mature, widely-deployed doctrine, not a novel claim.

  Caveats: SLSA governs build artifacts; C2A2's captures/denominators are epistemic artifacts, so transfer is analogical. The literature supports the principle but not the specific three-layer partition, which is C2A2's own design.

  Recommendation: SUPPORTED
