SEARCH-FOR-PRESUMPTION-736:
  Date searched: 2026-08-10
  Original item: PRESUMPTION-736
  Original statement: [inferred] The interrupt marker "[Request interrupted by user]" is presumed exogenous (caused by actual user action) and has never been diagnosed. Four sessions died at this marker on a day with no attended session.

  PROVENANCE:
    Origin: 14b
    Chain: [14b -> 15a]
    Original item: PRESUMPTION-736
    Item type: PRESUMPTION (unstated — surfaced by inference)
    Transform at each step:
      14b: Inferred the unexamined exogeneity assumption from four unattended-day interruptions never diagnosed
      15a: Searched for supporting literature on failure attribution and spurious interrupt markers
    Current status: NO-SUPPORT-FOUND for exogeneity as the default explanation; SUPPORTED for the alternative (harness-side/endogenous causation is a documented, non-novel phenomenon)

  Supporting evidence found: Yes — but the evidence found argues against treating the marker as reliably exogenous, which is the opposite of what the presumption (as currently held, unexamined) assumes. Framed correctly per the FOR/AGAINST split: this is FOR the claim that the interrupt marker's exogeneity should not be presumed — i.e., FOR overturning the unstated assumption.

  Sources:
    1. GitHub, anthropics/claude-code, Issue #35738, "[BUG] Spontaneous 'Request interrupted by user' triggered by Linux kernel 6.17.0-19-generic." — Documents a case where the exact marker text ("[Request interrupted by user]") appeared with zero user action, root-caused to a kernel version, and resolved by downgrading the kernel. Direct, concrete precedent for the marker firing with a fully non-human cause. [unverified — from search snippet; issue number and resolution as reported by search summary, not independently confirmed against the live GitHub issue]
    2. GitHub, anthropics/claude-code, Issue #21477, "[BUG] Recurring 'Request interrupted by user' errors during normal operations." — A second, independent report of the same marker appearing during normal (non-interrupted) operation. [unverified — from search snippet]
    3. GitHub, anthropics/claude-code, Issue #7298, "[BUG] Claude Code immediately returns 'Interrupted by user' on any input." — Traced in the search summary to a context-window/MCP-tool-size condition (~32,294 tokens exceeding a ~25,000 threshold) rather than to any user keystroke, in version 1.0.108. [unverified — from search snippet]
    4. GitHub, anthropics/claude-code, Issue #70543, "[BUG] Claude Code fabricated a user interruption/instruction and preserved it after compaction." — Documents the harness fabricating interruption/instruction text that was not user-authored, and preserving it through context compaction so it reads as legitimate history afterward — i.e., an endogenous artifact that becomes indistinguishable from a genuine user action after the fact. This is close to a direct precedent for "died at the marker on a day with no attended session."
    5. Cursor Community Forum, "Cursor Agent automatically injects false 'previous response was interrupted' continuation after completed response." — Analogous phenomenon in a sibling agentic-coding tool (Cursor), showing this is not idiosyncratic to one harness implementation but a class of failure across LLM agent harnesses.
    6. Failure-attribution literature in multi-agent LLM systems (general, not marker-specific): "Which Agent Causes Task Failures and When? On Automated Failure Attribution of LLM Multi-Agent Systems" (arXiv:2505.00212) and "Seeing the Whole Elephant: A Benchmark for Failure Attribution in LLM-based Multi-Agent Systems" (arXiv:2604.22708) report that state-of-the-art automated failure-attribution methods achieve under 15% accuracy locating the true root-cause step of a failure on hard benchmarks — theoretical/empirical grounding for why an unexamined default attribution (to the user) is epistemically weak, since even purpose-built attribution tooling struggles badly at this task.
    7. General reliability-engineering distinction between "fault," "error," and operator-attributed causes (ScienceDirect, "Error Operator" topic overview; log-based fault/operator classification literature) — establishes that operator-input and system/code-logic are treated as distinct, separately-diagnosable error-origin categories in mature incident-analysis practice, i.e., collapsing them by default (assuming operator/exogenous cause without diagnosis) is a recognized methodological gap, not a neutral default.

  Strength of support: Strong (for the claim that the interrupt marker is a known, harness-side, non-user-caused phenomenon in this exact tool family) — multiple independent, specific, named-issue reports in the same codebase (anthropics/claude-code) describe the identical marker text firing without user action, with at least one root-caused to a kernel version and another to a context-size threshold, and one describing the harness fabricating and preserving fake interruption text.

  Summary: The literature — here, primarily first-party bug trackers rather than academic papers, since this is a tool-specific implementation question — directly supports treating the exogeneity of "[Request interrupted by user]" as an open empirical question rather than a safe default. Multiple independently filed, still-open-looking issues in the same repository (anthropics/claude-code) document this exact marker string appearing with no user action, traced in different cases to a kernel version, an MCP/context-size threshold, and harness-side fabrication during compaction. A structurally similar phenomenon is documented in a competing tool (Cursor), suggesting a general class of agentic-harness failure rather than a one-off. Separately, the academic failure-attribution literature shows that even dedicated attribution tooling is unreliable (sub-15% accuracy on hard cases), which supports skepticism toward any unexamined default attribution, exogenous or otherwise. Together this constitutes real, checkable, on-point precedent for diagnosing rather than presuming the cause of the four unattended-day interruptions.

  Caveats: (a) The GitHub issue citations are drawn from search-result snippets/summaries, not independently opened and read in full; issue numbers, exact root-cause attributions, and resolution status should be verified directly against github.com/anthropics/claude-code before being treated as settled fact. (b) None of the found issues is a controlled study — they are individually-reported incidents, so they establish existence (the marker CAN fire without user action) but not a base rate (how OFTEN it does, or whether it explains these specific four sessions). (c) The in-house discriminator named in the batch item (correlate the marker against session length, subagent fan-out, tool class, wall-clock) remains the only way to test causation for this specific case; the literature here only establishes plausibility and precedent, not proof for these four sessions.

  Search scope: Preliminary-to-moderate. Comprehensive enough to establish that this is a documented, named phenomenon in the exact tool family with multiple independent reports; did not attempt to read full issue threads, check current open/closed status, or find a root-cause distribution across reported cases.

  Recommendation: SUPPORTED (for the claim that exogeneity should not be presumed without diagnosis) / equivalently NO-SUPPORT-FOUND (for the unstated presumption that the marker reliably indicates real user action)
