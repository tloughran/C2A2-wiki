# Handoff 4 — OpenStory → C2A2 Agent Explorer

_Session 4, 2026-06-08. Supersedes HANDOFF_openstory_session3.md for current state. Resume cue: "resume the OpenStory work"._

## Decisions locked this session
1. **Ship subtabs 1+3 now; build subtab 2 (sociogram) fresh next session.** The switcher + interrogative explorer are well-specified and were landed + validated this session. The sociogram is a full D3 force-graph port + a new extractor + visual verification — deferred to its own session to keep "caution over speed" and stay within budget. (User decision.)
2. **Sociogram edge model = shared wiki-node references — feasibility CONFIRMED.** Probed the DB directly: scanning 41.5k events, wiki-node refs are dense (tool calls 26k, prompts 10.7k, assistant text 4k). Among scheduled-agent sessions, **4,205 agent-pairs share ≥1 wiki node; 953 pairs share ≥3.** Strong signal for a force graph.
3. **Cleaner agent join discovered (use this for the new extractor).** For scheduled agents, `sessions.label` is the literal task header `<scheduled-task name="c2a2-lit-search-pipeline" ...>`. Pull the taskId from the `name="…"` attribute — more robust than the truncated-prefix match used in `agent_telemetry.json`. (The 50-char truncation still applies to the raw label; the `name=` value sits early in the string, so it survives — verify per-agent when building.)

## Done & verified this session
1. **Sub-tab switcher in `agents_tab.html`.** New `#subtab-bar` (Schedule | Sociogram | Explorer) styled to match `explorer.html`'s `.tab-btn` UX (gold `#C9A84C` active, dark theme). Existing schedule UI (`#content-area` + `#narration-strip`) wrapped as `#view-schedule.view-pane` with no logic changes. Playback controls wrapped in `#schedule-controls` and hidden on non-Schedule views. Switch JS is a self-contained IIFE appended to the single existing `<script>` (same scope → reads `TELEMETRY`, `AGENTS`, `CAT`, `fname`, `showDetail`).
2. **Subtab 3 — interrogative explorer.** Sortable/filterable table over `TELEMETRY.agents` (34): Agent, Category (color chip), Sessions, Events (+inline bar), Eval+Apply, E/A ratio, Errors, Tool-cov %, Last run. Text filter + category dropdown; click any column header to sort; click a row → existing detail panel via `taskId → AGENTS` lookup (with reverse-alias for `c282`/`c2a2`). Graceful "No agents match" / missing-telemetry fallback.
3. **Sociogram pane** is a styled "coming soon" placeholder (`#view-sociogram`) so the tab reads as intentional, not broken.
4. **Validation green:** `node --check` on the full extracted script (single block, 91 KB) PASS; `validate_html.py` (JS syntax / double-brace / CSS brace-balance, 118 pairs) ALL PASS.

## NOT verified
- **Visual render in a browser.** Attempted via the connected Chrome, but the automation tooling rewrites `file://` URLs (prepends `https://`) and the alternate bridge wasn't running, so no screenshot was captured. Structural/syntax validation is green and the layout change is additive (`view-schedule.active` is flex-column with the same `content-area`(flex:1)+`narration-strip` stack as before), so risk is low. **Quick manual check:** open `agents_tab.html`, click each subtab — Schedule should still animate (controls visible), Explorer should show a 34-row sortable table, Sociogram shows the placeholder; controls hide off Schedule.

## Next steps (resume here)
1. **Subtab 2 — agent sociogram (the meaty piece).**
   - New extractor `agents/openstory/extract_agent_node_refs.py`: open DB read-only (`mode=ro`), iterate events (`subtype IN message.assistant.tool_use / message.user.prompt / message.assistant.text / message.assistant.thinking`), pull wiki-node refs (regex on `.md` paths + `[[wikilinks]]`, normalize to vault basenames; ideally intersect with the canonical 1647-node set from `extract_vault_data.py`). Group sessions by **canonical agent taskId** (from the `name=` attr in the label) → per-agent node set. Emit edges = shared-node count (weight). Write `agent_node_edges.json` (sibling) or fold into `agent_telemetry.json`.
   - Port the D3 v7 force-graph UX from `wiki_narration.html` / `c2a2-wiki-narration/scripts/generate_visualization.py` for consistency (dark theme, node click → panel). Render into `#view-sociogram`; nodes = agents (size by sessions/events, color by category via `CAT`), edges = shared-node weight. Reuse `showDetail` on node click. Inject data between markers (file:// can't fetch — house rule).
   - House rules: regular strings not f-strings in any generator, single braces, `node --check` + `validate_html.py` after every edit, graceful fallback, **and actually open it in a browser this time.**
2. **Phase B — seed (#8) + schedule (#7).** Agent-only bridge mode (`AGENTS_ONLY=1` → `~/openstory-watch-agents`) + one-time seed (`OPEN_STORY_WATCH_BACKFILL_HOURS=0`, Terminal clipboard-relay on the real machine; 64 MB cap holds). Then routine watch→bridge so new runs ingest within the 72h window. Scheduled task: re-run extractor + injector (+ node-ref extractor) for incremental refresh; cadence TBD with Tom.
3. **Separate track Tom flagged:** "a next session forking agents."

## Key paths / facts
- **Event schema (for the node-ref extractor):** `events(id, session_id, subtype, timestamp, agent_id, parent_uuid, payload)`. Tool calls are `subtype='message.assistant.tool_use'`; tool name + args at `payload.data.raw.message.content[].name` / `.input` (e.g. `input.file_path`). Note: the older `tool_coverage≈0` agents predate the tool field — node refs for them come from prompt/text events, not tool calls.
- **DB:** `~/Documents/Non-Claude Projects/OpenStory/data/open-story.db` (~1 GB, live; open `mode=ro` only). Tables: events, sessions, patterns, turns, plans. Mount path in sandbox: `/sessions/<id>/mnt/Documents/Non-Claude Projects/OpenStory/data/open-story.db`.
- **Vault:** `~/Documents/Claude/Projects/RC Karpathy Wiki Project/wiki/`. Tab: `agents_tab.html` (now ~1,775 lines after subtab work). Scripts/JSON/maps: `wiki/agents/openstory/`. Validator: `wiki/c2a2-wiki-narration/scripts/validate_html.py` (hardcodes `/tmp`; in a sandbox with no `/tmp` write, run a copy with the temp path sed-replaced into a writable dir).
- **Parent shell:** `explorer.html` hosts chapter tabs in an iframe — Sociogram=`wiki_narration.html`, Narrative Connectome=`prs_3d.html`, Agent Map=`agents_tab.html`, Curriculum Tools=`summa_explorer.html`. `.tab-btn.active` = `#C9A84C`. The canonical "Sociogram" force-graph UX to mirror lives in `wiki_narration.html` (regen via `wiki/c2a2-wiki-narration/regen_sociogram.sh`).
- **Git:** branch `feature/sociogram-search-integration`. The whole `wiki/agents/openstory/` tree + the `agents_tab.html` edits were committed + pushed this session (feature files only; the repo's many other uncommitted agent-generated changes were deliberately left untouched).
