# Narrative (PRS) Connectome — change batch, 2026-05-20

**Status: PROMOTED to live 2026-05-20** (Tom consented after visual review of `prs_3d_review.html` + the full explorer). `wiki/prs_3d.html` now equals the reviewed file byte-for-byte; `wiki/explorer.html` tab renamed. **Not yet pushed to GitHub** — the push is Tom's from the host shell (the sandbox can't push). A late add in this batch beyond the four phases: a **brightness filter slider** and a **"Year ≥" time slider** (schema mirrored from the Sociogram), plus the legend/layer fix so the vis rides over the key.

**Validation:** `node --check` clean; data counts + content-population + layer-wiring all PASS (`validate_prs_3d.py`).

All viz changes were made through the `c2a2-prs-3d` generator from the pristine `template_prs_3d.html` (reproducible; the generator is not idempotent — always feed it the template).

## What changed

- **Phase 0 — Rename.** Tab "3D PRS" → **"Narrative Connectome"** (`explorer.html`); page `<title>`/`<h1>` → "C2A2 — Narrative (PRS) Connectome"; the explorer description rewritten to the connectome framing.
- **Phase 1 — Coil altitude.** Coils now sit at their **discovery-year (~2026) band** instead of being pinned to each tradition's founding-era node — fixing the "no post-2020 coils" surprise. A `year` field was added per cross-connection/coil in the extractor (latest year cited in the entry; all current coils resolve to 2026).
- **Phase 2 — Convergence hubs.** Resource nodes whose resource is shared across **≥2 traditions** get a gold glow + a legend count. *Finding:* only **3** literal cross-tradition hubs exist (max 2 traditions on one resource) — empirical confirmation that traditions converge **analogically, not verbatim**; the coil layer is the real convergence instrument.
- **Phase 3 — Generative coils (directed).** **17** high-precision **solution→resource** handoffs across traditions (≥3 shared significant tokens, Jaccard ≥0.15), drawn as direction-coloured lines (dim source → bright target), with a "Generative" toggle + legend. This is the "spiral" — the literal PRS chain. v1 is **lexical**; semantic (embedding-based) detection is the v2.

## Layer counts now rendered

231 triplets · 90 cross-connections · 32 coils · **17 generative chains** · 35 findings.

## Awaiting consent

Visual review of `prs_3d_review.html` → promote to live `prs_3d.html` → (Tom's) git push.

## Follow-on additions (same day, promoted to live)

- **Legend/layer fix** — transparent canvas so the vis rides over the legend.
- **Brightness slider** (true filter, default 1.35×) and **"Year ≥" time slider** (Sociogram `setDateThreshold` schema).
- **Edge-picking** — click a coil / cross-link / generative edge → its info in the panel (Three.js Line raycast; nodes keep click priority).
- **"?" pop-ups** on the title (view overview) and legend (each item spelled out), click-on/click-off toggle (`prsTogglePop`).
- **explorer.html cache-buster** — tab switches append `?v=<ts>` so promotions always load fresh (prevents the stale-iframe confusion).

## Deferred / next

Rise-from-roots coil arcs; the **verdict/outcome** beat in the narrative unit (a data-model change); semantic generative detection; the broader perspective set; per-node info-panel detail for convergence and generative links.
