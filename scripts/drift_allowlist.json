{
  "_meta": {
    "purpose": "Machine-readable allowlist of INTENTIONAL variance for the janitor's drift checks. See wiki/architecture/fact_inventory.md, recommendation R5 and hazard H1. An entry here declares that a specific difference is DELIBERATE, so the drift detector suppresses it and the intentional difference never reads as a bug.",
    "why_this_file_exists": "H1: 'Intentional variance must be encoded as machine-readable data next to the code, never as prose in an audit. Until that encoding exists, [the drift table] is an attractive nuisance.' This file is that encoding. Prose comments (e.g. build_bundle.py's 'MacIntyre is the book's author') explain intent to humans but are invisible to the detector; this file makes the same intent executable.",
    "rules": [
      "Add an entry ONLY for deliberate variance that has a recorded rationale. NEVER silence a real drift bug here just to quiet the report.",
      "Every entry needs 'reason' (why the difference is intentional) and 'source' (where the intent is authoritatively recorded in code or docs).",
      "A hex MISMATCH -- the same tradition assigned two different colors by two pipelines -- is never allowlistable. That is always a hand-copy bug. Only ABSENCES (a tradition deliberately omitted from a pipeline) belong here."
    ],
    "updated": "2026-07-21"
  },

  "roster_display_only": [
    {
      "taskId": "com.tomloughran.openstory.watchdog",
      "reason": "launchd job (reverse-DNS id, runs every 5 min) that watches the OpenStory backend. It produces no OpenStory *sessions*, so it is correctly absent from agent_map.json (the telemetry-join roster) while still belonging in the AGENTS display roster. Display-only by construction, not drift.",
      "source": "wiki/agents_tab.html AGENTS entry (category:infrastructure, schedule:'every 5 min'); the two-scheduling-worlds finding in wiki/architecture/fact_inventory.md Family 1"
    },
    {
      "taskId": "collaboration-history",
      "reason": "Display-only agent (runs outside OpenStory, so it produces no sessions for the telemetry join and is correctly absent from agent_map.json) that still belongs in the AGENTS display roster. Classified display-only by Tom 2026-07-21.",
      "source": "wiki/agents_tab.html AGENTS entry (category:project, schedule:'Every 2 days at 2:00 AM'); the two-scheduling-worlds finding in wiki/architecture/fact_inventory.md Family 1"
    },
    {
      "taskId": "korbyt-tasks",
      "reason": "Display-only agent (manual-only, runs outside OpenStory, so no sessions to join and correctly absent from agent_map.json) that still belongs in the AGENTS display roster. Classified display-only by Tom 2026-07-21.",
      "source": "wiki/agents_tab.html AGENTS entry (category:project, schedule:'Manual only'); the two-scheduling-worlds finding in wiki/architecture/fact_inventory.md Family 1"
    }
  ],

  "palette_absence": [
    {
      "tradition": "macintyre",
      "absent_from": "bundle",
      "reason": "MacIntyre is the book's AUTHOR, not a satellite tradition. commentary-explorer/build_bundle.py deliberately excludes him because he has no satellite page (the text being annotated is his); his absence from that pipeline's palette is correct, not drift.",
      "source": "wiki/commentary-explorer/scripts/build_bundle.py (exclusion comment near the THINKERS table); wiki/architecture/fact_inventory.md hazard H1"
    }
  ]
}
